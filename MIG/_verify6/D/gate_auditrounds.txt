============================================================================================
전 라운드 회귀 감사 — 대조 0건
============================================================================================

### ★커버리지 — 보고서는 있는데 `patch.json` 이 없는 배치 11건
   이 배치들의 결론은 **기계로 확인할 수 없다.** 아래 「유실 0」은 그만큼 공허하다.
   [3차 A] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [3차 B] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [3차 C] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [3차 D] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [4차 A] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [4차 B] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [4차 C] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [4차 D] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [5차 A] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [5차 B] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다
   [5차 C] 보고서만 있고 patch.json 없음 — 결론을 기계로 못 본다

### 반영 대기 — patch.json 은 냈는데 아직 안 붙인 배치 5건
   [5차 D] `applypatch.py 5 --only D` 를 돌려라
   [6차 A] `applypatch.py 6 --only A` 를 돌려라
   [6차 B] `applypatch.py 6 --only B` 를 돌려라
   [6차 C] `applypatch.py 6 --only C` 를 돌려라
   [6차 D] `applypatch.py 6 --only D` 를 돌려라

### 유실(정정이 정본에 없다) — 0건

### STALE(옛 값이 취소선 밖에 살아있다) — 0건

### ev 되돌아감 — 0건

============================================================================================
⚠대조할 `patch.json` 이 없다 — 배치가 기계 판독 패치를 내지 않았다는 뜻이다.
   (5차까지가 그랬고, 그래서 ev 상향 492행 중 317행이 유실됐다.)
커버리지 결손 11 · 유실 0 · STALE 0 · ev되돌아감 0  → ★조치 필요
============================================================================================
