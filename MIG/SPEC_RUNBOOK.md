# 함수 명세 배치 런북 — 20개를 어떻게 닫았고, 다음 N개를 어떻게 똑같이 닫는가

> 2026-09-11 작성. 게임 0.5.8. 근거 = `_spec/r1`~`r6` 실측 + `resolved_2026-09-10.json`.

## 이 문서의 자리 (4종 세트)

| 문서 | 누가 읽나 | 무엇 |
|---|---|---|
| **`METHOD_MAP.md`** | **무엇부터 할지 정하는 사람** | 상황 → 방법 라우팅, 각 방법의 한계·신뢰도 서열 |
| `IR_TOOLKIT.md` | **재료를 찾는 사람** | rlib → IR/rmeta 추출, 어떤 크레이트에 뭐가 있나, 도구 목록 |
| `SPEC_GUIDE.md` | **명세를 쓰는 사람**(작성 에이전트) | JSON 규격, `constants`/`reads` 경계, 함정, 자체검사 |
| **`SPEC_RUNBOOK.md`(이 문서)** | **배치를 굴리는 사람**(메인 세션) | 몇 라운드를 어떻게, 언제 멈추고, 어떻게 병합·저장하나 |

작성 에이전트에게는 `SPEC_GUIDE.md` 만 읽히면 된다. 이 문서는 **오케스트레이션용**이다.

---

# 1. 한 줄로 본 파이프라인

```
S0 표본 선정 ─→ S1 사전 미리 채우기 ─→ S2 독립 재작성 ×N라운드 ─→ S3 QC ─→ S4 교차대조
                                              ↑                              │
                                              └────── 수렴 안 하면 재투입 ─────┘
                                                                             ↓
      S7 병합·투영·저장 ←─ S6-b 기계 검증(tcx·오라클) ←─ S6 미확정 해소(재료 확장) ←─ S5 수렴 판정
```

**핵심 발상 하나만 기억하면 된다: "한 번 잘 읽기"가 아니라 "독립적으로 여러 번 읽고 어긋난 곳만 사람이 본다".**
IR 독해는 지어내기(hallucination)보다 **빠뜨리기**가 훨씬 많은데, 검사기는 지어낸 것만 잡는다. 빠뜨린 것은 **독립 2회 대조**로만 드러난다.

---

# 2. 왜 다회 재작성인가 — 실측 수렴 곡선

같은 20개 함수를 서로 안 보여주고 독립 재작성한 라운드 간 일치도(`speccmp.py`):

| 대조 | 판정상수 | 호출 | 구조체필드 | 의미 |
|---|---|---|---|---|
| **r1 ↔ r2** | 87.0% | 74.7% | 84.2% | 1차만 믿었으면 **판정상수 8개 중 1개가 틀렸을 것** |
| **r3 ↔ r4** | 96.6% | 96.2% | 95.7% | 사전 도구(`distruct`/`dienum`) 투입 후 |
| **r5 ↔ r6** | **100.0%** | 97.9% | 93.0% | 잔여 8개만 재투입 → **판정상수 완전 일치** |

읽는 법:
- **판정상수(constants)가 수렴의 주지표다.** 이게 100%면 "AI가 무엇을 기준으로 판단하는가"가 확정된 것이다.
- **`호출`과 `필드`의 잔여 불일치는 대부분 표기 차이**다. r5↔r6의 필드 93%는 사실 충돌이 아니라 한쪽이 중첩 구조체를 절대 오프셋으로, 다른 쪽이 상대 오프셋으로 적은 것이었다(전수 확인함).
- ⚠**일치는 하한이다.** 둘 다 같은 걸 놓치면 100%로 나온다. 그래서 S6(재료 확장)이 따로 필요하다.

**r1↔r2의 74.7%(호출)가 이 파이프라인의 존재 이유다.** 1회 독해를 정본으로 삼으면 안 된다.

---

# 3. 단계별 절차

## S0. 표본/대상 선정 — `pick20.py`

시범 배치라면 **대표성**이 전부다. 쉬운 것만 고르면 일정 추정이 통째로 틀어진다.

`pick20.py` 의 표본 규칙(그대로 재사용 가능):
- IR **100~500줄** 구간 — 함수 개수의 36.7%로 가장 대표적
- `game-ai\src\**` 소스만 (core/std 제네릭 인스턴스 제외)
- **클로저 제외** — 부모의 일부라 단독 명세 단위가 아니다
- **한 소스 파일당 최대 2개** — 몰리면 문맥이 공유돼 속도가 부풀려진다
- AI 판단 계층 위주 정렬(`plan_legacy` / `*_score` / `*_eval` / `fight_*`)

출력 = `spec_batch1.json`(함수명 · 소스 `파일:줄` · **IR 파일 + 본체 줄범위**).

⚠**`sym` 필드는 믿지 마라** — 메타데이터 id 가 들어간 버그가 있다. 심볼은 `ir_from` 줄의 `define` 에서 직접 읽어라.

**실전 대상 선정이라면** 표본 규칙 대신 목적으로 고른다: 바꾸고 싶은 행동 → 그 행동을 내는 플랜/술어 → 그 함수. 단 **"한 파일당 2개" 규칙만은 유지**하라(문맥 공유로 인한 착시 방지).

## S1. ★사전 미리 채우기 — 여기서 병목의 60~70%가 사라진다

작성 에이전트가 DWARF를 손으로 타게 두면 안 된다. **배치를 던지기 전에** 사전을 만들어 둔다.

| 도구 | 만드는 것 | 규모(실측) |
|---|---|---|
| ★**`tcxdict.py`** | 구조체·열거형 **정본 사전**(컴파일러 `tcx` 출처) | 게임 타입 **4,586** / 필드 15,138 / variant 13,294 |
| ~~`distruct.py`~~ | (2차 폴백) DWARF 역산 구조체 사전 — **키가 leaf 이름 1개뿐이라 동명끼리 덮어쓴다** | 26,416키 중 91.4%가 제네릭 인스턴스 |
| ~~`dienum.py`~~ | (2차 폴백) DWARF 역산 열거형 사전 — tcx 와 **모순 0**이나 커버리지 39.7% 결손 | 7,016 |
| `divtable.py` | `dyn Trait` vtable 슬롯 이름 | — |
| `fnparts.py` | 함수 본문 조각 위치(**본문 읽기 전에** 먼저) | `_gcbc` 자동 폴백 |
| `knobscan.py` | 매직 상수 전수 + 함수명 + `파일:줄` | 노브 후보 인벤토리 |
| `rmetadocs.py` | 개발자 한국어 주석 → `_docs\*.txt` | **2,925개 · 7초** |

★**두 개의 규칙만 지침에 박아두면 배치 품질이 확 오른다:**
1. **IR 을 읽기 전에 `MIG\_docs` 를 먼저 grep 하라.** 우리가 역추적하려던 코드표가 개발자 주석에 그대로 적혀 있는 경우가 많다.
2. **`_gaibc` 에 `declare` 만 있으면 `_gcbc`/`_gvbc` 에서 `define` 을 찾아라.** 1~6차에서 17건이 "본문 없음"으로 포기됐는데 전부 `_gcbc` 에 있었다.
3. ★**사전은 `tcxdict` 를 1차로 쓰라.** 구사전과 충돌하면 **`tcx` 가 정본**이다(컴파일러가 직접 말한다). 구사전은 **tcx 에 없을 때만** 2차 폴백으로. 근거·수치 = `IR_TOOLKIT.md §6-b`.

⚠**성능 사고 주의**: 사전 도구가 느리면 그 자체가 신호다. `dienum.py` 가 40분 걸린 원인은 정의만 해두고 안 쓴 정규식 대신 열거자마다 70MB 파일을 다시 `re.search` 한 것이었다. 파일당 1회 dict 로 바꿔 **40분 → 57초**. **자매 도구(`distruct` 58초)와의 시간 격차 자체가 버그 신호였다.**

## S2. 라운드 N — 독립 재작성

한 라운드 = 함수 하나당 에이전트 하나, **동시 투입**. 산출물은 `_spec/r<N>/<이름>.json` 하나씩.

**의뢰 문안은 §7-A 를 그대로 복사해 쓴다.** 반드시 들어가야 하는 것:
- `SPEC_GUIDE.md` 를 **끝까지** 읽으라는 지시
- 담당 함수 · 소스 `파일:줄` · **IR 파일 + 줄범위**
- 출력 경로 + `qcspec.py` 자체검사가 `[PASS]` 날 때까지
- ★**이전 라운드 디렉터리를 절대 열지 마라** — 독립성이 깨지면 대조가 무의미해진다

**실측 속도(r1)**: 20개 동시 · 벽시계 **11분** · 개당 평균 **8분 26초**.

## S3. QC — `qcspec.py`

**지어낸 것**을 잡는다: 본문에 없는 상수, 없는 호출, 규격 위반.

```bash
cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python qcspec.py _spec/r3
```

⚠**검사기가 사실을 지우도록 강요하는 결함이 4개 있었다**(전부 수정). 새 결함이 보이면 **관대한 쪽으로 고쳐라** — 검사기 오탐은 작성자가 진짜 사실을 빼게 만든다. 이게 QC의 가장 위험한 실패 모드다.

## S4. 교차대조 — `speccmp.py`

```bash
PYTHONIOENCODING=utf-8 python speccmp.py _spec/r3 _spec/r4
PYTHONIOENCODING=utf-8 python speccmp.py _spec/r3 _spec/r4 --detail <명세이름>
```

**불일치 = 사람이 볼 곳을 정확히 찍어준 목록**이다. 결과물이 아니라 **좌표**로 쓴다.
「분류차」로 표시되는 것은 사실 차이가 아니라 양식 차이(구조체 오프셋을 `constants` 에도 적은 것)이니 무시한다.

## S5. 수렴 판정 — 언제 멈추나

**멈춤 조건: 판정상수(`constants`) 일치 100%.**
- 100% 미만이면 **불일치가 남은 함수만** 다음 라운드에 재투입한다(전량 재투입 금지 — 낭비다).
- 실제 진행: r1~r4는 20개 전량, **r5·r6은 잔여 8개만**. 그 8개에서 판정상수 100% 달성.
- `호출`/`필드`는 100%까지 갈 필요 없다. 잔여를 **표기 차이인지 사실 차이인지 눈으로 한 번 확인**하고 넘어간다(이번엔 전부 표기 차이였다).

## S5-b. ★★완결 조건 게이트 — `specgate.py` (2026-09-11 신설, 4차에 G7~G10 추가)

**왜 만들었나.** 유저 지적이 정확했다 — *"제대로 분석한거라면 여러번 분석해도 같은 결과가 나오는거 아니야?"*
라운드마다 결과가 흔들린 이유는 실력이 아니라 **완결 조건이 없어서**였다. 그리고 3·4차에 실제로 세어 보니
**내용은 수렴하는데(4차 오프셋·값 오류 0) 내 파이프라인이 샜다.** 게이트는 그 누출구를 하나씩 막은 것이다.

```bash
python -X utf8 specgate.py            # 전량
python -X utf8 specgate.py --only 14  # 한 함수
python -X utf8 specgate.py --gate G7  # 한 게이트
```

| 게이트 | 막는 것 | 실제로 무엇을 놓쳐서 만들었나 |
|---|---|---|
| **G1** 자기모순 | 같은 명세 안 두 곳이 다른 말 | 17 `flee_die` 가 `logic` 엔 `i64::MAX`, note 엔 `usize::MAX` |
| **G2** 호출부 전수 | "유일 호출처" 류 무근거 주장 | 09 「유일 호출처」 — 실제 **8곳**. grep 을 안 했을 뿐 |
| **G3** 형제 함수 | 같은 플랜의 다른 진입점 누락 | 14 가 `v30` 만 보고 `sub_plan` 이 쓰는 `v41` 을 통째로 놓쳤다 |
| **G4** 술어 시그니처 | 인자 받는 술어의 접힌 상수 오독 | 01 이 `icmp ult x,2` 를 소스 임계로 읽었다(`is_jungle(&self,usize)`) |
| **G5** sig 정본 대조 | `&` ↔ `&mut` 어긋남 | 08 `params[6] &mut TeamPlan` → 실제 `&TeamPlan` |
| **G6** logic 미반영 | 표는 고쳐졌는데 `logic` 에 옛 값 | 3차 실오류 5건 중 3건이 이 형태였다 |
| ★**G7** 과열림 | 이미 해소된 항목이 `open` 에 남음 | 4차 배치B 8건. **신설 직후 첫 실행에서 사람이 못 본 1건을 또 잡았다**(18) |
| ★**G8** 표에 옛 값 | **G6 의 역방향** — `logic` 은 고쳤는데 표에 옛 값 | 4차 D-E3 `ty.Tower.0`(3차 P-4 가 `logic` 만 고쳤다) |
| ★**G9** callees 오염 | `logic` 이 메서드를 필드로 적어 자동수집이 놓침 | 4차 D-E2 `Entity` 4메서드 누락 |
| ★**G10** class 오분류 | 확정된 사실을 `미탐색` 으로 앉혀 둠 | 4차 배치A 제안. 4건 적발 → `notes[]` 로 분리 |
| ★**G11** ev 지시 미반영 | `history` 가 「ev 4→2」를 지시했는데 표가 안 따라감 | 5차 배치A. 같은 형태가 3차 §7 에서도 8건 샜다 |
| ★★**G12** `src_line` 대조 | `consts[].src_line` 이 IR `!dbg` 사슬과 어긋남 | 6차 배치A. **5라운드간 무검사 축** — 붙이자 즉시 56건. ⚠**7차에 47→13 으로 교정**(아래) |
| ★★**G13** `knobs.where` 대조 | IR 줄번호·**인용한 명령**이 실제 `.ll` 과 어긋남 | 6차. 같은 축 계열 — 8건 |
| ★★**G14** `mem.dir` 방향 | `mem[].dir`(r/w)이 IR 의 load/store 와 모순 | 8차 배치A. ⛔**「부재」는 결함이 아니다**(콜리 안·레지스터 승격·접힘) — 후보 하나가 그래서 46건을 냈다. 검출력 = **변이 시험 53.1%** |
| ★★**G15** `consts.kind` 관측 | 상수 종류가 **IR 소비 오프코드**와 모순 | 8차 배치B. ★판정식이 **「분류」가 아니라 「반증」** — 주장을 지지하는 관측이 0건일 때만 불일치라 **관측이 늘면 오탐이 준다** |
| ★★**G16** `sig.params.role` | 인자 역할·IR 속성 주장이 `define` 과 모순 | 8차 배치C 설계 → **9차 배치B 승격**. 8차 적발 `P4` 6·`P6` 14 가 **전수 오탐**이었고 원인이 오프셋 귀속이 아니라 **인용 처리**였다(아래) |
| ★**G18** `logic`↔표 상호참조 | `logic` 이 인용한 필드·오프셋이 표에 **아예 없다** | 9차 배치D. `G6`/`G8` 의 빈틈 — 그 둘은 *값이 어긋났나*만 보고 *빠졌나*는 안 본다. ⚠검출력 **22.1%**(logic 이 자유 서술) |
| ★**G19** `knobs.value` | 노브의 **값**이 IR 과 모순 | 9차 배치C. `G13` 은 `where` 만 본다. ⚠검출력 **34.3%** — 대조 가능한 행이 **221 중 104(47%)** 뿐 |
| ★**G17** `history` 전파 | `history` 가 닫은 결론이 표에 안 실림 | 8차 배치D. **`G8`(logic↔표)의 짝**. ⚠느슨한 앵커는 **폐기 대상의 인접 항목**을 집는다(오탐률 90%) |
| ★★**G20** `shared` 명세간 공유사실 | **두 명세가 같은 사실을 다르게 적는다** | 11차 배치D 제안 → 11차 후속 신설(`sharedchk.py`). ★**유일한 cross-spec 게이트** — G1~G19 는 전부 「명세 1개 안」을 보므로, 두 명세가 갈려도 각자 무모순이라 아무도 안 잡았다. 규칙 R1 (base,offset)→필드명 · **R2 (base,필드명)→offset**(표기로 설명 안 됨) · R3 `dir` 미기재 · R4 노브값. ⚠**억제가 적발보다 많다**(적발 8 · 억제 9) |

> ★**게이트 건수를 이 문서에 적지 마라.** 명세가 자라면 변하고, 실제로 7차엔 `G12` 수치가
> 이 표 · `srclinecheck` docstring · 도시에에서 **셋 다 달랐다**(13 / 10 / 8 — 8차 배치A 적발).
> **기준선은 숫자가 아니라 명령이다**: `python -X utf8 specgate.py`.
> 위 표의 수치는 **그 게이트를 왜 만들었는지 말하는 이력**이지 현재값이 아니다.

### ★G12 교정 (7차) — **새 게이트는 오탐부터 검증하라**

7차 배치A 가 담당 G12 8건을 **줄 원문으로 전수 반증**했다 — **8/8 전부 오탐**이었다.
`srclinecheck.py` 가 새는 곳이 셋이었다:

| # | 샌 곳 | 왜 |
|---|---|---|
| ① | `switch` case 라벨 줄 | 그 줄에 `!dbg` 가 없다(종결자에 붙는다) → **정답을 못 본다** |
| ② | `phi` 의 상수 인입 | 마찬가지로 `!dbg` 가 없어 `line 0` 으로 버려진다 |
| ③ | 리터럴 정규식 | `%2`(SSA)·`!2`(메타데이터)·`align 2`·`gep .. i64 16`(필드 오프셋)을 **전부 상수로 착각** |

★**「지시대로 고쳤으면 8건 전부 판정 반전 오류」** — 게이트가 명세를 오염시킬 뻔했다.

조치: ③ 잡음 제거(`denoise`) + ①② **귀속 추론**(case 라벨 → arm 블록 종결자, phi 인입 → 인입 블록 종결자).
**47 → 13건.**

⚠**그 과정에서 한 번 더 틀렸다.** ①②의 귀속을 그냥 후보에 더했더니 **16 → 22 로 늘었다.**
G12 의 판정식이 「후보가 있는데 claim 이 없으면 불일치」라서 **후보를 늘리는 것이 곧 오탐을 늘린다.**
배치A 가 본 8건에 맞춰 규칙을 넣다 **과대적합**한 것이다.

⟹ 후보를 **두 등급**으로 갈랐다:
- **강한 후보** = 그 줄 자체에 `!dbg` 가 붙은 명령 → **불일치 판정의 근거**
- **약한 후보** = 귀속 추론 → *claim 이 맞는지 확인하는 데만* 쓴다

> ★★**귀속은 명세를 구제할 수는 있어도 기각할 수는 없다.**
> 추론으로 얻은 근거로 남의 주장을 뒤집지 마라 — 그건 게이트가 아니라 또 하나의 추측이다.

**교훈**: 새 게이트가 대량 적발을 내면 **그게 성과라고 믿기 전에 표본을 원문으로 반증**하라.
G12 는 붙자마자 56건을 냈고 그게 「검사받지 않는 축」의 증거로 쓰였는데, 실제로는 **상당수가 오탐**이었다.
적발 건수는 게이트의 성능이 아니라 **가설**이다.

## S5-e. ★★7차 결과 — **「검사받던 축에서 새로 나온 오류 = 0건」**

7차는 6차 결론을 **수치로 확증한 라운드**다. 네 배치가 정정 **70건**(실오류 42 · 분류오류 7 ·
판정반전 1 · 보강 6 · **오탐 14**)과 ev 하향 21건을 냈다.

### 실오류 42건이 나온 자리 — 한쪽으로 쏠려 있다

| 축 | 건수 | 그 라운드에 그 축이 어땠나 |
|---|---|---|
| `knobs.where` | **17** | G13 은 **백틱 IR 인용이 있는 행만** 문면을 대조했다. 인용 없는 행은 「줄이 존재하나」만 봤는데 `.ll` 이 1,500만 줄이라 **아무 줄번호나 존재한다** = 사실상 무검사 |
| `mem.*`(note/name/base/offset) | **14** | `mem.dir` 검사기를 **이번에 처음** 만들었다 |
| `consts.meaning` | **6** | `consts.kind` 검사기를 **이번에 처음** 만들었다 |
| `consts.src_line` | 2 | G12(6차 신설) |
| 기타 | 3 | |

**42건 중 37건(88%)이 「그 라운드에 처음 검사한 축」에서 나왔다.**
발견 경위도 `new` 30 · `reused` 7 · `inherited` 5.

### ★그리고 `reused`·`inherited` 12건을 전수로 열어 보면

| 분류 | 건수 | 실체 |
|---|---|---|
| 6차 게이트가 **이미 지적했는데 안 고쳐진 것** | 10 | evidence 에 「G13 지적 그대로 실오류」라고 적혀 있다 |
| `history` 가 이미 닫은 결론이 **표에 전파 안 된 것** | 2 | 「G8 형, 게이트가 못 잡음 — G8 은 `logic`↔표만 본다」 |

⟹ **「6차까지 검사받던 축에서 새로 발견된 오류」는 0건이다.**
**검사받은 자리는 더 이상 오류를 안 낸다.** 명세가 흔들려서 오류가 계속 나오는 게 아니다.

### 「봤다」와 「검사했다」는 다르다

배치들은 매 라운드 명세 전문을 **읽었다.** 그러니 42건 전부가 '봤던 곳'이다. 그런데도 못 잡았다.

표본: `[03] knobs[1]/[3]/[4]` 는 `icmp ugt %76, 2` 라고 적혀 있었고 **여섯 라운드 동안 아무도
이상하게 안 봤다.** 실제 IR 은 `icmp ugt i64 %76, 2` — 타입이 빠진 인용은 G13 의 문면 대조를
통과 못 한다. **눈으로는 맞아 보이고 기계로는 틀린 것** — 이런 건 계측기밖에 못 잡는다.

### ⚠`behavior_change` 의 정의가 흔들렸다

네 배치가 **전건 `false`** 로 냈는데, 배치A 는 같은 건에 대해 산문에는
*"노브를 바꿔 쓰려는 사람이 이 표를 믿으면 20 과 40 을 반대로 넣는다 — 실사용 영향이 있는 오류다"*
라고 썼다. **필드 정의를 「지금 재현 코드가 바뀌나」로 읽었기 때문**이다.

이 프로젝트의 목표는 「AI 판단함수 **완전 재구현**」(CLAUDE.md §3)이다.
⟹ 정의를 **「이 명세대로 재구현하면 틀린 동작이 나오나」**로 고쳤다. 그 기준으로 세면 **8건**:
노브 arm 방향 뒤집힘 2 · 쓰기 대상 오기 2(소유 구조체 오프셋 ↔ 힙 원소) ·
**존재하지 않는 노브** 1 · TLS 메모 미기재 1 · 태그를 임계로 분류 다수.

### ⟹ 척도는 라운드 수가 아니라 **축 커버리지**다

8차부터 배치를 **함수가 아니라 축으로** 나눈다(`mkdossier.py --axis`).
남은 무검사 축 = `mem.offset` 표기 규약 · `sig.params` 의 sret 규약 · `knobs.value` ·
`callees` 동명 다중 · `logic`↔`mem`/`consts` 상호참조.

## S5-f. ★8차 — **축으로 나눈 첫 라운드**, 그리고 「적발은 가설이다」의 확증

7차 결론(「검사받던 축에서 새 오류 0건」)에 따라 배치를 **함수가 아니라 축으로** 나눴다
(`mkdossier.py --axis`). 네 배치가 각각 축 하나를 끝까지 닫는다.

### 결과

| 축 | 후보 적발 | **반증 후** | 승격 |
|---|---|---|---|
| A `mem.dir` 451행 | 4벌 7 / 3 / 46 / 7 | **0** (중간판 3건 전수 오탐) | **G14** |
| B `consts.kind` 186행 | 3벌 52 / 68 / 102 | 23 (자기 패치 26행도 **스스로 철회**) | **G15** |
| C `sig.params.role` 124행 | 4벌 44 | **2** (오탐 42) | ⬜보류 |
| D `history`→표 188행 | 10 | **1** (오탐 9) | **G17** |

정정 **38건**(실오류 32·보강 6) + ev 하향 1. `behavior_change` **24건**
(7차엔 0이었다 — 정의를 「재구현하면 틀리나」로 고친 효과).

★**후보 총 적발 100여 건 중 진짜는 26건.** 네 축 전부에서 오탐이 압도했다.

### ★★이 라운드가 낳은 두 가지 방법

**① 변이 시험(배치A)** — 명세를 **일부러 뒤집어 넣고** 게이트가 몇 %를 잡는지 잰다.
`memdir` 실측 **238/448 = 53.1%**. 「적발 0 인데 게이트가 무능한 것 아닌가」에 답하는 **유일한 수단**이다.
⟹ 새 게이트를 만들면 **적발 수와 함께 검출력을 같이 보고**하라.

**② 판정식을 「분류」에서 「반증」으로(배치B)** — 이 파이프라인에서 가장 값나가는 설계 전환이다.

| | 분류식 | **반증식** |
|---|---|---|
| 규칙 | 후보가 있는데 주장이 없으면 불일치 | **주장을 지지하는 관측이 0건일 때만** 불일치 |
| 관측을 늘리면 | **오탐이 는다** | **오탐이 준다** |

7차에 내가 `G12` 를 고치며 추론을 후보에 더했다가 16→22 로 늘린 그 함정을,
**규칙이 아니라 판정식의 성질로** 막는다. 「귀속은 구제에만」이 저절로 성립한다.

### `kind` 축은 실제로 무너져 있었다

`kind=임계` 107행 중 **순서비교가 관측되는 건 25행(23%)뿐**이었다. 나머지는 갈 곳이 없어
기본값 `임계` 로 떨어진 것 — **「임계 111행」은 분류가 아니라 나머지**였다(무검사가 아니라 **무측정**).

어휘를 6종(`계수`·`산출값` 추가)으로 넓히고 파생을 IR 관측 우선으로 바꾸자:

| | 전 | 후 |
|---|---|---|
| 임계 | 88 | **26** |
| 계수 | – | **16** |
| 산출값 | – | **33** |

배치B 가 남긴 `강 4 · NEG 2` 도 **예고대로 자동으로 닫혔다**.

### ★「고칠 수 있는 것」과 「구조적으로 그런 것」을 같은 통에 넣지 마라

이 라운드에 같은 성질의 문제를 **세 곳**에서 고쳤다:

| 어디 | 무엇을 결함으로 세고 있었나 | 실체 |
|---|---|---|
| `specgate` G9 | 「손확인」 2건 | 동명 필드가 실재하면 **정상**이다 |
| `auditrounds` 커버리지 | 결손 11건 | 전부 `patch.json` **계약 이전**(≤5차) 라운드 |
| `auditrounds` STALE | 4건 | 전부 **나중 라운드가 정당하게 덮어쓴 것** |

⟹ `specgate.note()` · `auditrounds` 의 `legacy`/`later` 로 분리했다.
**0 을 목표로 삼을 수 없는 지표는 죽는다.** 빨간불이 상수가 되면 아무도 안 보고,
안 보면 진짜 결함도 같이 샌다(`§S5-b` 의 「게이트가 오탐을 내면 게이트를 고쳐라」와 같은 이유).

## S5-g. ★★9차 — **게이트를 믿을 수 있게 만든 라운드**. 정적 분석의 종점

8차 뒤 열려 있던 `G12` 8건과 「아직 게이트가 없는 축」 넷을 배치가 하나씩 맡았다.

| 축 | 적발 | **반증 후** | 검출력(변이 시험) |
|---|---|---|---|
| `consts.src_line`(G12 재작성) | 8 | **0** (8/8 오탐) | **100%** · 검사범위 145→**153** |
| `sig.params.role`(G16) | 20 | **0** (20/20 오탐) | **100%** (599/599, **음성 대조 129 포함**) |
| `knobs.value`(G19 신설) | 26 | **1** | 34.3% (대조 가능 104/221) |
| `mem.offset`+`logic`↔표(G18 신설) | 20 | **1** | 22.1% |

정정 56 + ev 2. **74건 중 70건을 배치가 스스로 오탐으로 판정했다.**
⟹ **게이트 19개 전부 0 · 회귀 전건 유지됨.**

### ★검출력을 처음으로 수치화했다 — 그리고 **음성 대조**가 나왔다

8차 배치A 의 변이 시험은 「일부러 틀리게 바꾸면 잡는가」만 봤다. 9차 배치B 가 거기에
**음성 대조 129건**(무해한 변경은 **잡으면 안 된다**)을 더해 599/599 를 냈다.
⟹ **「무조건 적발하는 소음 게이트가 아님」까지 같은 시험으로 증명된다.**

낮은 검출력도 그대로 적었다(G18 22.1% · G19 34.3%). **숨기면 「통과 = 정합」으로 오독된다.**
`knobs.value` 는 대조 가능한 행이 **221 중 104(47%)** 뿐이고, 「전량 대조」를 목표로 잡으면
나머지 117행에 **억지 판정**을 내게 된다.

### ★★내가 만든 사고 — **거짓 초록**

배치A 의 게이트를 승격하며 `srclinecheck.py` 를 순환 import 가 되게 덮어썼는데,
`specgate` 가 게이트마다 `try: import X / except: return` 이라 **예외를 삼키고 `G12=0`** 을 찍었다.
**「8건 전부 해소」로 읽을 뻔했다.** 검사는 한 번도 안 돌았다.

공교롭게 **배치B 가 같은 결함을 독립적으로 지적**하며 G16 승격의 조건으로 걸었다 —
*"후보 `C_rolechk.py` 가 `AttributeError` 로 돌지도 않는데 아무도 몰랐다."*

⟹ `broken()` 신설. 검사기가 죽으면 **`0` 이 아니라 `!`** 로 찍고 이유를 출력한다.
이 프로젝트가 반복해 당한 「조용한 no-op」의 **다섯 번째**이고, 이번엔 내가 만들었다.

### 배치가 뒤집은 내 지시

| 무엇 | 실제 |
|---|---|
| 「`0x860[len]` 같은 값이 **있다**」 | `mem[].offset` **451행 전량이 이미 순수 16진수**(8차에 사라졌다). 혼선은 `offset` 이 아니라 **`base` 칸**에 있었다 |
| 8차 `A=7 vs D=7` 갈림 = offset 파싱 탓 | `memdir.py` docstring 자신이 「A·D 둘 다 그 행을 제외」라 적는다. 원인은 **`base` 동정의 부재** |
| 7차에 넣은 phi 구제 | **죽은 코드였다** — `litpat` 화이트리스트가 phi 인입(`[ 1, %33 ]`)을 애초에 매치 못 했다 |
| 7차 「gep 잡음 제거」 | 실제로 안 됐다 — gep 오프셋은 `i64 16` 처럼 **타입 접두를 달고** 통과한다 |
| 「`G13` 통과를 전제로」 | 깨졌다. 유일한 실오류가 있는 세 행은 `G13`=0 인데도 `where` 가 **117줄**까지 어긋나 있었다 |

### ★`mem[].offset` 표기 규약 (D9-OFF)

1. `offset` = `base` 가 이름 붙인 **좌표 원점에서의 바이트 변위**, 순수 16진수 하나.
   판정 기준 = 「`offset_of!`/tcx 로 그 값 **하나**를 대조할 수 있는가」.
2. 원점이 **컨테이너의 원소**면 `base` 를 원소 타입으로 내리고 `offset` 을 원소 기준으로 다시 센다.
3. `base` 문법 = `<타입>[.<필드경로>][(<한정어>)]`. 같은 함수 안에서 **같은 원점 = 같은 문자열**.

> ★**갈림의 기준은 「힙이냐」가 아니라 「상수 변위가 있느냐」다.**
> 힙 Vec 원소엔 소유 구조체로부터의 상수 변위가 **없다**(`load(+0x860) + idx×184`).
> 그래서 `0x860[len]` 은 **바이트변위+역참조+첨자 3좌표를 한 칸에** 넣은 것이라
> 어떤 검사기로도 파싱 불가였다. 반대로 인라인 배열은 상수 변위가 있으니 컨테이너 기준이 정상.

⚠부수 교훈: 규약을 **문법만 보고** 적용하다 새 철자 분열을 만들 뻔했다 —
**문법 정규화와 좌표계 동일성은 별개 조건**이고, 전자만 보면 후자를 깬다.

### ⚠`op:insert` 는 인덱스 도장을 깨뜨린다

배치D 가 `/specs[15]/mem[12]` 를 삽입하자 5·7차가 찍어 둔 **같은 경로의 도장이 다른 행을 가리켰다**.
내용은 멀쩡한데 `auditrounds` 가 STALE 1 · ev되돌아감 1 을 냈다.
⟹ 삽입 시 **경고 + `--restamp` 필수**, `auditrounds` 는 「인덱스 이동」을 판정 보류로 분리.
**인덱스 경로는 삽입에 취약하다** — 키 기반 경로가 근본 해결이지만 계약 변경이라 미뤘다.

## S5-h. ★★10차 전수 감사 — **「IR→명세」 방향이 답이었다**

유저 지시: *"한번 모든 검사 한번 20개 함수에 대해서 하자. 필요해 보이는 것만 하는 거 말고 싹 다."*
9라운드 동안 **게이트가 지목한 것**만 봤고, 그 시점 게이트 19개가 전부 0 이었다.

### 결과 — 정정 **82건**, 그중 **행 추가 17건**

| | |
|---|---|
| 실오류 27 · 보강 53 · 판정반전 2 | **`behavior_change` 5** |
| ★**행 추가 17건** | 「값이 틀린 것」이 아니라 **「아예 없던 것」** |
| 지시 오류 보고 | **30건** (라운드 최다) |

### ★★방향을 뒤집은 것이 핵심이었다

배치C 관측(그대로 인용):
> 8·9차의 오탐률 70~95% 보다 낮은 이유는 이번이 **게이트 후보를 검증한 게 아니라
> IR→명세 방향으로 직접 읽었기** 때문이다. 「게이트가 준 후보」는 오탐이 지배하지만
> **「IR 원문에서 역으로 찾은 것」은 대부분 진짜였다.**

⟹ **게이트는 「있는 칸이 틀렸나」만 본다. 「빠졌나」는 전수로만 나온다.**

### ★`closed[]` 가 썩어 있었다 — 22건(D 15 · A 7)

닫을 당시의 **도구 사정이 화석으로 보존**돼 있었다. 그 사이 도구가 좋아졌는데 아무도 안 봤다:

| 화석 문구 | 지금은 |
|---|---|
| 「`_gaibc` 에 `declare` 만 있어 확인 불가」 5건 | 전부 **`_gcbc` 에 `define` 이 있다** |
| 「`!dbg` 소실로 복원 불가」 2건 | **`dloc.py`** 로 풀린다 |
| 「`distruct` 가 `array$<?>` 라 T 미확정」 | **`tcxdict` 한 줄**로 확정 |

> ★★**바뀐 것은 사실이 아니라 도구였다.** `closed` 는 **도구가 바뀔 때마다 재검토 대상**이다.

배치A 는 `04 closed[0]` 이 같은 파일 `history[3]` 과 **정반대**를 말하는 것도 찾았다(판정반전).

### ★`mem` 누락이 **체계적 형태**였다

`07`·`08`·`14`·`18` 에서 같은 결손이 반복 — **「Vec 의 `ptr`/`len` 은 싣고 원소 역참조는 안 싣는다」**.
그 포인터 + `len×stride` 가 원소를 쓰는 **실제 주소**라, 없으면 재구현이 불가능하다.
게다가 **같은 Vec 을 두 명세가 다른 규격으로** 적고 있었다(`12` 는 cap/ptr/len 세 행, `14` 는 0행).

### ★`12 handle_chat` — 「외연으로는 맞고 의미로는 틀렸다」

8차 오라클이 `Chat +0x1 u8` 을 `0=Top∧Mid∧Bottom / 1=Mid∧Bottom / 2=Top …` 이라 적었는데,
IR 원문은 `rule_scope::play_code_allowed` 의 **술어**다:
`0→morgard_exists · 1→serpen_exists · 2~4→line_exists(Top/Mid/Bottom) · ≥5→무조건`.

`spawn_epic={0,7,8}` 이 **우연히** `Top∩Mid∩Bottom` 과 같아 **오라클(외연)로는 절대 안 갈린다**
— `METHOD_MAP ⑥` 한계 3의 교과서적 사례다. `TutorialType` variant 가 9개뿐이라 외연은 전 입력에서
일치하므로 `behavior_change=false` 지만, **3중 AND 로 재구현하면 튜토리얼 종류가 늘어나는 순간 갈라진다.**

### ★★내 도시에가 **세 축을 물리적으로 숨기고 있었다**

배치B·C 가 독립적으로 적발. 고정 컬럼 이름이 실제 키와 어긋나 **전 라운드·전 배치에서 공백**이었다:

| 칸 | 렌더가 찾던 키 | 실제 키 | 결과 |
|---|---|---|---|
| `sig.params` | `ty` | **`type`** | 인자표 타입 전부 공란 |
| `siblings.entries` | `name`/`sym`/`note` | **`path`/`vis`/`at`/…** | 형제 표 전 행 공란 |
| `closed` | `a`/`ev` | **`why`** | **닫은 근거가 전 함수에서 공백** |

**10차는 바로 그 축들을 검사하라고 시킨 라운드**였다. 게다가 `history` 는 **함수마다 키가 다른 자유 키**라
고정 컬럼으로는 원리적으로 못 찍는다.
⟹ `table()` 이 **행에 있는 키를 빠짐없이** 찍도록 고쳤다(`cols` 는 이제 「앞에 세울 순서」일 뿐).

### ⟹ `selftest.py` 신설 — **「고쳤다」를 기계가 판정한다**

오늘 **「고쳤다」가 네 번 거짓**이었다: 7차 phi 구제가 죽은 코드 · 7차 gep 제거 미작동 ·
8차 `kind` 꼬리 절단 효과 0 · 9차 거짓 초록. 전부 **고친 뒤 다시 재지 않아서** 생겼다.
**코드를 바꾸는 것과 동작이 바뀌는 것은 다른 사건이다.**

유저 지시(*"몇 번 정도 테스트 돌리고 수정, 또 같은 테스트 돌리고 수정 반복해봐"*)대로 6회 루프를 돌렸고,
**매 회차에서 실제로 결함이 나왔다**:

| 회차 | 그때 고친 것 |
|---|---|
| 2 | `insert` 가 `mem`/`knobs` 에서 **항상 앞 배열에만** 꽂힘(배치D 적발) |
| 3 | `ev_up` 이 `guard` 없이 **엉뚱한 행에 도장** · 거짓 `from` 이 조용히 통과 |
| 4 | KB 표기가 한글에서 **~30% 과소**(문자↔바이트) · `exe=None` 이 「조인 실패」와 구별 안 됨 |

`selftest` 는 게이트 8개를 **직접 import 해서 호출**한다 — 9차처럼 순환 import 로 죽어도
`specgate` 가 예외를 삼키기 전에 여기서 드러난다.

## S5-i. ★★11차 통제 실험 — **「정정 파이프라인 자체」가 오류원이 됐다**

유저 지시: *"똑같은 테스트 11차로 해봐."* 10차와 **같은 임무·같은 담당**으로 재측정했다.
달라진 것은 하나 — 10차에 렌더러가 숨기던 세 축(`closed.why`·`sig.params.type`·`siblings`)이 **이제 보인다.**

### 결과 — 82 → 61건. 수렴하지 않았지만 **성격이 완전히 바뀌었다**

| | 10차 | 11차 |
|---|---|---|
| 정정 | 82 | **61**(실오류 35 · 보강 26) |
| 행 추가 | 17 | 12 |
| `behavior_change` | 5 | **8** |

### ★값 축은 닫혔다 — 네 배치가 따로 확인

`mem` 오프셋·방향 · `consts.src_line` · `sig.params` · `siblings` 를 **전수로 다시 대조해 불일치 0**
(A 108/40/34 · B 107/31 · C 전건 · D 115/32/30). `01`·`02` 는 **아예 0건**.

배치A 의 결론이 중요하다:
> 「10차에 렌더러가 세 축을 숨긴 게 원인이었나」의 답은 **아니다** — **보이게 하고 다시 재도 그 축들은 깨끗하다.**

### ★★남은 것 — 셋 다 「정정 파이프라인」

**① 10차가 고친 것이 다시 틀렸다**(배치B 2건). `05 knobs[1]` 은 10차 판단의 **전제**(들여쓰기 4)가
실측(6)과 달랐고, `07 mem[22]` 는 **잘못된 배열에 꽂혀** 방향이 `w` 로 뒤바뀌었다.

**② ★10차가 「완료」라고 보고한 행이 실제로는 없었다**(`08 live_list[0]`).
원인 = `applypatch.resolve` 의 `spec.get("writes") or []` — **빈 배열이 falsy 라 새 객체**를 돌려준다.
거기 `insert` 하면 정본은 그대로인데 **`--dry` 는 성공, 게이트는 0, 보고서엔 「완료」**.
**조용한 no-op 여섯 번째.** ⟹ `create=True` 로 정본의 키에 실제 리스트를 만들고,
**삽입 후 정본을 다시 읽어 확인**하며, **어느 v2 배열에 들어갔는지**를 찍는다.

> 배치B: **「`selftest` 가 도구에 한 일(「고쳤다」를 기계가 판정)을 명세 정정 자체에 해야 한다.」**

**③ `closed[].why` 가 늘 가장 약한 근거를 고른다**(A·B·C·D **전원** 적발).
`closed_reason` 이 **첫 매치**를 돌려주는데 `CLOSE` 는 라운드순 `+=` 이라 **언제나 가장 이른 라운드가 이긴다.**
실측: `closed` 151행 중 **22행**에 가려진 근거가 있었고, 중복 `(i,needle)` 쌍이 **6개**.
⟹ **최장 needle + 같으면 나중 라운드** 채택 · `audit()` 에 **`shadowed`·`dupkey`** 신설.
효과: `12` 가 2차 「45칸」(다른 함수 결과) → **3차 「297칸 전수 진리표」**로 바뀌었다.

⚠`closelist.py` **자신이 4차에 「needle 은 그 항목의 고유 문면으로 잡는다」는 교훈을 적어 뒀는데**
그 형태가 살아 있었다 — **규칙을 적는 것과 기계가 강제하는 것은 다르다.**

### 실제 발견

- ★**`09` 의 최종 판정이 3단이 아니라 4단**이었다 — 소스 `L1326`(`flank_allies >= 2`)이 **통째로** 빠져 있었고
  줄 길이로 4/4 ±0 복원. 10차가 막혔던 `2·len(flank)=3` 모순의 정체는 **「1321 이 한 줄이다」라는 가정**이었다.
  ⟹ ★**「재료 부재」에 범위를 성실히 열거해도 「전제」가 틀리면 범위 열거가 구제해 주지 않는다.**
- `10` 의 `is_recent_visible` 에 **즉시통과 2단**이 앞에 있다 — 명세대로면 「눈앞의 적」을 놓친다.
- `17` 의 **Chat 페이로드 쓰기 3행**이 없어 빈 Chat 이 나간다.
- **시야 기억창 = 120틱**(2초)이 06·08 의 필터를 지배하는데 명세 어디에도 없었다.

### ⟹ 12차를 같은 형식으로 도는 것은 답이 아니다

네 배치가 일치해서 권고했다. 남은 것은 **도구 결함**이다:
`closelist` 앵커(**고침**) · `applypatch` 사후검증(**고침**) · `consts.kind` CALLARG · `callees` 경로 우선 조회 ·
**명세 간 `shared` 사실 대조 게이트**(D: 22건의 27%를 단독으로 덮는다).

## S5-j. 11차 후속 — 남은 도구 결함 3건을 닫았다

★이 절의 형태가 이 프로젝트의 표준이다: **고치기 전에 센다.**
세 과제 모두 「하면 좋겠다」가 아니라 **실측이 규모와 방향을 바꿨다.**

### ⓐ `consts.kind` — 「CALLARG 관측자 추가」는 **1행짜리**였고, 진짜 문제는 어휘였다

과제 이름대로 CALLARG 를 파 보니 20함수 통틀어 CALLARG 관측이 붙은 행은 **5행**뿐이었고,
그중 오분류는 **1행**이다. 그 1행의 정체는 콜리 인자가 아니라 **산술 intrinsic** —
`llvm.usub.sat.i64(%x, 150000)`. 소스의 `saturating_sub` 가 내려온 것이라 `sub` 와 의미가 같은데,
LLVM 에선 `call` 이라 `CALLARG`(약)로만 잡혀 **기각에도 구제에도 못 쓰이고** 있었다.
⟹ `kindchk._ops` 에 산술 intrinsic 패턴을 `ARITH` 로 추가(1줄 규칙, 1행 해소).

★★그런데 `미상` 7행을 분해하니 **CALLARG 는 1행뿐이고 나머지는 어휘 결손**이었다
(DBGSTR 3 = 문자열 바이트 길이 · 관측0 3 = 접힘·바이어스). `kindchk` 자신이 docstring 에
「이 축을 닫으려면 `mkspec3` 의 파생 규칙을 고쳐야 한다」고 적어 둔 그 자리다.
⟹ 어휘에 **`오프셋가감`**(가산·감산 바이어스)·**`길이`** 를 신설.

**낱말을 넣기 전에 「기존 행을 몇 개나 가져가는지」를 셌고, 그 측정이 설계를 바꿨다:**

| 후보 낱말 | 닿는 행 | 측정이 말한 것 |
|---|---|---|
| `여유분`·`여유치`·`바이어스` → `오프셋가감` | 4 | 3행이 `미상`. 안전 |
| `길이` | **15** | ⚠6행이 정상 `임계`(「배열 길이 2 = 비교 상한」)다. **낱말만 보면 빼앗는다** |
| `하드컷`·`상한`·`하한` → `임계` | 12 | 10행이 이미 `임계`, 2행은 앞 체인이 먼저 잡는다. 사실상 1행 |

`길이` 를 그냥 넣었으면 정상 6행을 망가뜨렸을 것이다. 두 가지로 막았다 —
**⑴ 체인에서 `임계` 뒤에 둔다**(`길이 … 상한` 은 임계로 간다)
**⑵ `길이` 의 지지 관측을 전부 WEAK 로 둔다**(`DBGSTR`·`CALLARG`·`CMP_EQ`).
⑵ 때문에 `CMP_ORD` 가 관측되면 규칙③에서 낱말이 **진다** — 즉 `길이` 는 **구제만 되고 기각은 안 된다.**
7차의 「귀속·추론은 구제에만 쓰고 기각에는 쓰지 마라」가 어휘 수준에서 강제된다.

**결과**: `kind=미상` **7 → 0**. 나머지 186행은 **전부 불변**(전후 대조 확인).

### ⓑ `callees` — 조용히 자르던 `rs[:3]` 을 **IR 호출 심볼 대조**로 바꿨다

구판은 `harvest_callees` 가 `split("::")[-1]` 로 경로를 버리고, `fnlookup(leaf)` 결과를
**`rs[:3]` 으로 조용히 잘랐다.** 실측 — 조회에 성공한 184개 이름 중 **36개가 잘렸고**,
`02 default` 는 후보가 **988개**였다. 그 행의 `path`·`sig`·`at` 은 「임의의 세 개」인데
**`ev:3`(tcx 정본 대조) 도장이 찍혀** 있었다. 조용한 no-op 의 친척 — **조용한 거짓 확증**이다.

⟹ 명세의 `ir` 범위에서 `call`/`invoke` 의 **망글링 심볼을 정답지로** 쓴다. v0 망글링은
경로 성분을 길이접두와 함께 그대로 담으므로(`…6effect…12range_adjust`) 부분문자열로 대조된다.
⚠`leaf` 만으로 대조하면 안 된다 — `tick`·`default` 같은 흔한 이름이 아무 심볼에나 걸린다.
**경로의 모든 성분이 한 심볼 안에 있을 때만** 앵커로 인정한다.

**결과**: 262행 중 **IR 앵커 56행만 `ev3`**, 나머지 206행은 `ev4` + 「⚠미확정 — 같은 leaf 후보 N개」.
`00` 을 보면 `ev3` 12행이 IR 호출 심볼 12개와 **정확히 일치**하고,
`is_visible`×3 · `level`×3 · `team`×3 같은 **동명이인**만 내려갔다.
★**ev 가 대량으로 내려간 것이 이 수정의 성과다.** 전에는 206행이 근거 없이 정본 등급이었다.

### ⓒ `G20 shared` — 유일한 cross-spec 게이트. **억제가 적발보다 많다**

G1~G19 는 전부 「명세 1개 안」을 본다. 두 명세가 같은 사실을 다르게 적어도
**각자 자기 안에서는 무모순**이라 아무도 안 잡는다. 20개 명세는 같은 구조체를 반복해 읽으므로
(`PlayerState+0x930` 을 15개 명세가 적는다) 이 축은 **IR 을 다시 안 보고도**
「최소한 하나는 틀렸다」를 말할 수 있다.

**초판 49건 → 8건.** 줄어든 것이 성과다. 오탐 원인을 넷 찾아 전부 규칙으로 막았다:

| 오탐 원인 | 실례 | 억제 |
|---|---|---|
| ★**enum 페이로드 중첩** | `TeamPlan+0x420` 을 `08`=`objective.Morgard.phase` · `18`=`objective.Serpen.phase` — **둘 다 맞다** | variant(대문자 성분)에서 갈리면 양립 |
| 정밀도 차이 | `team` / `team.tag` · `Tower.ty` / `ty.Tower.ty` | 경로 접두 **또는 접미** |
| 표기 접미 | `game.data` / `game.data_ptr` · `player_champion` / `player_champion[][]` | `_ptr`·`[]` 벗긴 뼈대가 같음 |
| `dir` 의 `r` vs `w` | 한 명세는 읽고 다른 명세는 쓴다 | **정상** — `-`(미기재)일 때만 결함 |

**신설 직후 자기적발 3건** — 이 게이트를 만들면서 내가 낸 버그가 셋이다:
1. `norm_name` 이 소문자로 낮춰 **variant 판정(`^[A-Z]`)이 죽었다** → 중첩 억제가 통째로 안 먹음
2. 타입 주석 `:` 를 자르는 정규식이 **경로 구분자 `::`** 까지 먹어 `Chat::Battle.__0` → `Chat`
3. 참여 명세마다 flag 해서 불일치 **8건이 19건으로** 찍혔다
   (`AbstractGameWithCache game` 한 건이 명세 6개에 걸쳐 있다)
   ⟹ 「적발 건수는 게이트의 성능이 아니라 가설」(7차). **가장 낮은 참여 index 에서 한 번만** 센다.

★그리고 억제된 9건은 **지우지 않고 `중첩`/`정밀도`/`표기` 로 보이게** 남긴다(7차 규칙).

**남은 8건**(= 다음 라운드의 재료, 아직 정정 안 함):
`Entity+0x128` 을 `13`=`Tower.ty` · `14`=`ty.Tower.ty` · `15`=`ty.Tower.info.ty` (**셋 중 최소 둘이 틀렸다**) ·
`Entity+0xb8/0xc0/0xc8` 의 `.0` 유무(`03` vs `16`) · `Entity+0x70` ·
`AbstractGameWithCache game` 이 `0x0` 과 `0x8` 양쪽에(fat pointer 두 절반을 같은 이름으로) ·
`13` 이 `PlayerState+0x930/0x9c0` 의 `dir` 미기재(다른 14·10개 명세는 `r`).

### 회귀
`selftest` **30/30** · `auditrounds` 전건 유지됨 · 기존 게이트 3건(G11/G16/G17 = 확인된 오탐) 불변 ·
`TOOLS.md` 126개 미분류 0 · 정본 md 768KB 재생성.

## S5-k. ★★12차 — **게이트 20개가 처음으로 전부 0 이 됐다.** 그리고 그 0 을 의심해야 했다

유저 지시: *"같은 검증 마지막으로 한번 더해."* 11차와 **같은 담당·같은 형식**이되,
그 사이에 도구 셋이 바뀌었다(`kind` 어휘 · `callees` IR 앵커 · G20 신설).
⟹ 「같은 테스트」가 아니라 **「바뀐 도구로 같은 자리를 다시 재는 것」**이었고, 그래서 유효했다.

### 결과

| | 10차 | 11차 | 12차 |
|---|---|---|---|
| 정정 | 82 | 61 | **70**(실오류 24 · 보강 26 · 행추가 8 · ev상향 12) |
| `behavior_change` | 5 | 8 | **5** |
| 게이트 | — | 3(전부 오탐) | **11 → 0** |
| 지시 오류 보고 | — | 28 | **27** |

적용 후 `selftest` **40/40**(12건 신설) · `auditrounds` 전건 유지 · ev2 289 / ev3 493 / ev4 112.

### ★★이 라운드의 본체는 명세가 아니라 **11차 후속에 내가 만든 도구**였다

네 배치가 **독립적으로** 같은 결함을 짚었다. 11차 후속에서 「조용한 거짓 확증」을 없애려고
`callees` 앵커를 만들었는데, **그 앵커 자체가 세 가지로 틀려 있었다.**

| 결함 | 실측 | 원인 |
|---|---|---|
| **거짓 `ev3` 3행** | `game_core::Effect::range` 가 `00`·`16` 에서 `ev3` 인데 **그 호출이 IR 에 없다** | `s in sym` 이라 `5range` 가 `12range_adjust` 에 부분문자열로 걸린다. docstring 은 「길이접두와 함께 담으므로」라 적어 놓고 코드는 경계를 안 셌다 |
| **놓친 `ev3` 9행** | `08` 의 `v24_objective_setup…` 은 직접 호출인데 `ev4` | `<impl …>`·`::<'a, 'b>` 를 `::` 로 쪼개 망글링에 없는 조각이 생긴다 |
| **자기호출 날조 2행** | `03 defensive_crisis` 가 자기를 부른다고 | `from_iter_in::<…, defensive_crisis::{closure#0}>` 의 **타입 인자**를 호출로 읽었다 |
| ★**「후보 1개 = 정답」** | 배치D 담당 **58행 중 16행(28%)** 이 「그 함수가 아님이 IR 로 증명되는」 경로를 정의처까지 달고 실려 있었다 | `fnlookup` 이 **tcx 3크레이트만** 본다. std·bumpalo 구현은 사전에 없으니 동명이 하나뿐이면 무관한 게임 함수가 실린다(`15 drop` → `ProfTimer::drop`, 실제는 `Vec<Chat>::drop`) |
| **원리적 불가를 「미확정」과 뭉갬** | `tick`·`is_visible`·`get_entity_by_id` | **vtable 간접호출이라 심볼이 애초에 없다.** 그런데 `pick` 은 「IR 범위에 이 호출이 없다」고 **명백한 거짓**을 적고 있었다 |

⟹ 고친 뒤 `ev3` 는 56 → **57**(거짓 3 제거 · 회수 9 · 판별력 없는 행 제외). 숫자는 거의 안 변했는데
**내용은 전부 갈렸다.** 「수치가 그대로면 안 바뀐 것」이라는 직관이 여기서 틀린다.

### ★수정 중에 내가 두 번 더 같은 함정을 밟았다 (이게 이 절의 교훈이다)

1. **첫 수정판이 `ev3` 를 56 → 206 으로 부풀렸다.** `::` 로 쪼갠 성분 중 식별자만 남기니
   `<A as B>::update` 는 **`update` 하나만** 남아 그 이름이 든 아무 심볼에나 걸렸다
   (`update` 119행 · `strategy` 21행이 한꺼번에 거짓 `ev3`). ⟹ 경로 **문자열 전체**에서
   식별자를 긁고 문법어를 빼야 판별 성분(`CombineEffect`·`EffectType`)이 살아난다.
   **적발이 늘면 일단 의심하라** — 11차 G20 이 49→8 로 준 것이 성과였던 것과 같은 이유다.
2. ★★**부정문 가드를 넣자마자 진짜 지시 3개를 삼켰다.** G17 이 내가 쓴 주의 문구
   (「현재 `consts[2]` 는 이 지시의 대상이 아니다」)를 지시로 읽길래 「슬롯 뒤 90자에 부정어가
   있으면 억제」를 넣었더니 **슬롯 7개 중 5개가 억제**됐다 — 지시 뒤에 붙은 해설의 부정어가
   지시까지 덮었다. 그 상태로 게이트는 **0 을 찍었고, 그건 깨끗해서가 아니라 눈을 가려서**였다.
   ⟹ 부정이 **`ev N→M` 마커보다 앞설 때만** 억제하도록 좁혔다(억제 5 → 2).

> ★**그래서 「게이트 전부 0」은 결론이 아니라 검사 대상이다.**
> 9차의 거짓 초록은 검사기가 **깨져서** 0 이었고, 12차의 위험은 검사기가 **눈을 가려서** 0 이었다.
> 두 번 다 「0 이 떴다」와 「깨끗하다」를 잇는 자리에서 났다.
> ⟹ 이번엔 `selftest` 에 **억제가 진짜 적발을 삼키지 않는지**를 검사하는 절을 신설했다(`t_guards`).
> ⚠그 신설 검사 중 하나도 처음엔 `… or True` 라 **언제나 통과**하는 가짜였다(고침).

### ★내 지시가 틀렸고 배치가 IR 로 뒤집었다 — G20 `R3` 폐기

도시에에 「`13` 의 `PlayerState+0x930/0x9c0` `dir` 미기재는 **기계적으로 확실한 누락**」이라 적어 보냈다.
배치C 가 반증했다 — `13 target_bush_v30` 은 **fastcc 인자승격**이라 본문에 gep 가 **0건**이고,
읽기는 **호출부**(`LineGankCoverPlan::sub_plan`, m10.ll:11762~11791)에 있다. 복제본 `14` 는 본문에
gep 가 있어 `r` 이 맞다. ⟹ **`dir` 은 cross-spec 사실이 아니라 함수마다 다른 사실**이고,
시키는 대로 채웠으면 **IR 에 없는 로드가 있다고 주장**하게 된다. `R3` 규칙 자체를 걷어냈다.

★교훈: **cross-spec 게이트를 만들 때 「이 축이 명세 사이에 공유되는 사실인가」를 먼저 물어라.**
공유되는 것 = 레이아웃(오프셋·필드명·타입). 공유되지 않는 것 = **그 함수가 무엇을 하는가**(`dir`·`src_line`).

### 실제 게임 사실

- ★**시야 기억창 120틱**의 본문을 찾았다 — `_gcbc/g07.ll:157005~157047`.
  `is_recent_visible` = ①`is_visible_cell` 참이면 **즉시 true** ②플레이어 null 이면 **즉시 false**
  ③`last_seen + 120 >= game.tick()`. **`06` 에는 실렸는데 `08` 에는 없었고**, `08 closed[2]` 는
  그 물음을 **무관한 근거**(「오라클 13/13, 임계 900」 = L194 의 15초 리스폰)로 닫아 두고 있었다.
  ⟹ 배치B 가 `_gaibc` 만 뒤지다 못 찾았던 것이고, `METHOD_MAP §1-①`(「`declare` 만 있으면
  `_gcbc`/`_gvbc` 에서 `define` 을 찾아라」)이 그대로 적용되는 자리였다.
- `Entity+0x128` 을 `13`·`14`·`15` 가 **세 이름으로** 부르고 있었다. tcx 산술이 닫는다 —
  `Entity.ty@0x68` + 페이로드 `0x8` + `Tower.ty@0xb8` = `0x128` ⟹ **`ty.Tower.info.ty`**(`15` 가 맞다).
- `Entity+0x70` 은 **포인터가 아니라 인라인 값**이다(`13` 의 `&Tower` 가 오류 · 유일한 `behavior_change`).
- `EntityType` 14 variant 중 **`Champion` 만 튜플 variant** 라 페이로드 필드명이 `0` 이다
  (`ty.Champion.0.skill_cooldown`). 나머지 13개는 `info`.
- `05` 의 패턴 바이트 길이 9행은 **전부 `길이`** 다. `c14~c19` 가 `산출값` 이었던 건
  end_plan 코드값 4·5·6·8 과 **숫자가 겹쳐서**이고, `c11~c13` 만 면한 이유는 **겹칠 상대가 없어서**였다.

### 회귀
게이트 **G1~G20 전부 0** · `selftest` **40/40** · `auditrounds` 전건 유지 · `TOOLS.md` 126개 미분류 0 ·
정본 md **788KB** · `open` 8건 · **`ev1` 여전히 0행**.

## S5-c. ★★「검사받지 않는 축」 — 라운드보다 이걸 물어라

6차 통제 실험의 결론이고, **이 파이프라인에서 가장 값비싼 교훈**이다.

배치 A 는 **새 계측기를 하나도 만들지 않고**(`found_by:new`=0) 실오류 8건을 찾았다.
원인은 명세 불안정도, 새 계측기도 아니었다 — `consts[].src_line` 이 **5라운드 동안 어떤 게이트도
안 보는 축**이었고, 그 오류들은 **1차부터 그대로** 있었다.

> **오류는 검사받지 않는 축에 고인다. 라운드를 돌려도 그 축은 안 줄어든다.**

반대 증거도 같은 라운드에서 나왔다 — 새로 붙인 `offset_of!` 계측기는 **오류를 0건** 찾았다.
이미 `tcxaudit` 이 덮던 영역이라 새 정보가 없었다.
⟹ **계측기를 붙였다고 오류가 나오는 게 아니라, 그 계측기가 「한 번도 검사 안 된 영역」을
처음 밟을 때 나온다.**

### 축별 커버리지를 세어라 (2026-09-11 실측)
| 축 | 행수 | 상태 |
|---|---|---|
| `consts.src_line` | 186 | **G12 신설** → 붙이자 56건 |
| `knobs.where` | 221 | **G13 신설** → 8건 |
| `mem.dir` | 451 | ⬜아직 무검사 |
| `consts.kind` | 186 | ⬜아직 무검사 |
| `sig.params.role` | 124 | ⬜아직 무검사 |

★**"몇 차를 더 돌릴까"가 아니라 "어떤 축이 아직 검사 안 되나"를 물어라.**
라운드는 끝이 없지만 **축은 유한하고 열거 가능하다.** 세는 법:
```bash
# 명세가 쓰는 축(필드) 목록 ↔ 도구 소스에 그 키가 등장하는지
python -X utf8 -c "…"   # 6차에 쓴 스크립트는 03_시행착오.md 절에 기록
```

**판정 기준**: **G1~G8·G10 은 0 이어야 한다.** G9 는 두 등급이다 —
`★강함`(enum 필드를 관통하는 경로 = 필드일 수 없다)은 **실오류**이고, `손확인`은 동명 필드가 있어
기계로 못 가르는 목록이라 **눈으로 한 번 보고 넘기는 것이 정상**이다.
⚠**G9 를 순진하게 "필드 표기인데 동명 함수 있음"으로 만들면 오탐이 지배한다** — Rust 는 필드 `line` 과
메서드 `line()` 이 공존하고 `nearest_enemy`·`chats`·`pos` 가 실제로 그렇다(초판이 이 4건을 전부 올렸다).

★**게이트가 오탐을 내면 게이트를 고쳐라, 손으로 넘기지 마라.** 한 번 넘기기 시작하면
그 다음부터 **진짜 결함도 같이 넘긴다**(`tcxaudit --prose` 의 `~~취소선~~` 오탐이 그 실례).

## S5-d. ★★라운드 파이프라인 4계약 — **경계마다 사람 손을 뺀다**

6라운드 실측: 오류의 상당수가 **분석이 아니라 「사람이 손으로 옮겨 적는 경계」**에서 났다.

| 라운드 | 유실 | 발견자 |
|---|---|---|
| 1차 | 정정 13건 중 10건이 표에 안 들어감 | 나중에 손으로 |
| 3차 | 정정이 **항목 이동만** 되고 본문은 옛 결론 | 4차 배치B |
| 3차 | §7 ev 상향 8건 전부 미반영 | 4차 |
| 4차 | `patch4.py` 가 **배치 A 를 통째로 누락** | 5차 직전 |
| 5차 | `history` 가 ev 상향을 지시했는데 표 미반영 | 5차 배치A |
| 5차 | **ev 상향 492행 중 317행 유실**(집계표만 받음) | 6차 배치A·B |

⟹ **경계마다 계약을 두고 기계가 넘긴다. 사람은 판단과 승인만 한다.**

| 경계 | 계약 | 도구 |
|---|---|---|
| **지시** | 정본을 **자르지 않고 통째로** 넘긴다. 손으로 쓰는 건 라운드 고유 지시뿐 | ★`mkdossier.py` — 담당 함수 명세 **전문** + 정본 문서 경로·해시 + 게이트 **자기 몫** + 보고 스키마를 한 파일로. 신선도·정본 실재 실패 시 **생성 거부** |
| **보고** | 배치는 **기계 판독 패치**를 낸다. 산문은 부가 | `_verify<N>/<B>/patch.json` — 배치가 `--dry` 로 **적용 가능 여부 사전 검증** |
| **반영** | **기계가** 붙인다. `old` 가 없으면 **실패로 보고**(조용한 no-op 금지) | `applypatch.py` — 정수 필드 지원 · **재실행 안전**(이미 적용 구분) · `mem` ev3 상한 강제 |
| **검증** | 다음 라운드가 **전 라운드를 자동 회귀** | `auditrounds.py` — **커버리지 결손**(보고서만 있고 patch 없음)을 실패로 센다 |
| **위생** | 정본에 **지금 참인 것만**. 출처 표기·취소선 금지 | `cleanspec.py` — `ev` 분포 불변식(나빠지면 적용 거부) |

★**규칙 하나로 줄이면: 「브리핑에만 있는 사실은 버그다.」** 새 사실이면 **먼저 정본에 넣고**,
지시문은 그걸 가리킨다. 정본 = 명세(`_spec`) · 방법(`METHOD_MAP`/`SPEC_GUIDE`/이 문서/`TEMPLATE.rs`) · 도구.

### ★7차 변경 — 지시를 **요약에서 무손실 조립로** 바꿨다

6차까지 지시는 `mkbrief.py` 가 만든 **요약 브리핑**이었다. 수치는 정본에서 세고 심볼은 실재 확인을
통과했는데도 **매 라운드 지시 오류가 나왔다.** 이력을 모아 보니 원인이 하나였다 —
**오류가 난 자리는 예외 없이 「줄여 적은 자리」**다:

| 라운드 | 지시 오류 | 무엇을 줄였나 |
|---|---|---|
| 3차 | 「불가능」 3건이 4차에 **전부 뒤집힘** | 근거를 빼고 **결론만** 옮겼다 |
| 4차 | `tcxaudit` 기준선 수치를 **네 배치 전부** 지적 | 인자가 다른 실행값을 **수치만** 옮겼다 |
| 5차 | `game_ai::is_ignored_well_enemy`(rustc E0425) | 모듈 **경로를 잘랐다** |
| 5차 | ev 집계 **스코프 미기재** → 배치B 오적발 | 분모의 **정의를 생략**했다 |
| 5차 | ev 상향 492행 중 **317행 유실** | 보고를 **집계표로** 받았다 |
| 6차 | `BRIEF_FACTS.md` 가 정본보다 **낡음** | 생성물을 한 번 만들고 **재사용**했다 |

⟹ 유저 지시(2026-09-11): *"명세·분석방법·분석도구를 결과 받을 때마다 수정하고, **이걸 통째로** 넘겨라."*

`mkdossier.py` 는 `mkbrief` 가 `[:110]` 로 썰던 칸을 **전부 전문으로** 싣는다. 길면 길게 둔다 —
파일이라 **토큰이 아니라 디스크**를 쓴다. 배치당 110~150KB. 담는 것:

| 절 | 내용 | 왜 |
|---|---|---|
| §0 | 정본 sha256 스탬프 + **읽는 쪽이 직접 돌릴 확인 명령** | 6차 stale 브리핑 사고 |
| §1 | 담당 함수 표 + **`sig.vis`** | `pub` 아니면 오라클 진입이 막힌다(5차 `18` 이 47행 방치) |
| §2 | 정본 문서 **경로 + 해시 + 줄 수**(요약 없음) | 「요약본이 아니라 원문을 읽어라」 |
| §3 | 담당 함수 명세 **전문**(무손실) | 줄인 자리가 곧 오류 난 자리 |
| §4 | 게이트 미해소 **자기 몫만** + §4-b **아직 게이트 없는 축** | S5-c — 오류는 검사받지 않는 축에 고인다 |
| §5 | `patch.json` 스키마 + 경로 문법 + 산문 4항목 | 보고 유실 봉쇄 |
| §6 | 배치별 출력 디렉터리 | 동시 실행 경합 방지 |

★**검증**: 게이트 총 59건이 배치별 15+18+15+11 로 갈렸다 — **유실 0**. 분할 함수는
들여쓰기 연속행(「실제 후보」·「인용」)까지 끌고 온다. 그걸 빼면 배치가 **어디로 고칠지를 잃는데**,
그게 바로 요약 사고의 축소판이다.

### ⚠첫 시도에서 배치 둘이 죽었다 — **「통째로」를 파일 단위로 착각했다**

7차 첫 투입에서 **배치 B·C 가 600초 무진전으로 사망**했다. 마지막 흔적이 각각
「도시에를 통째로 읽겠다」·「해시 일치 확인, 이제 정본 문서」였다. 한 파일 110~152KB 를
한 번의 `Read` 로 삼키려다 스트림이 멎은 것이다.

★**원인은 내가 「통째로」를 잘못 읽은 것이다.** 유저가 말한 「통째로 넘겨라」는
***정보가 잘리지 않는다***는 뜻이지 ***한 파일이어야 한다***는 뜻이 아니다.
요약을 없애려다 **읽을 수조차 없는 파일**을 만들었다 — 정보량은 맞는데 **전달이 0**이 됐다.

⟹ `mkdossier --split`(기본값): §3 을 **함수별 파일로 뺀다.**
- `DOSSIER_<B>.md` = 지시·게이트·보고형식 (**7KB**)
- `_verify<N>/<B>/spec_<NN>_<name>.md` = 함수 하나 전문 (15~37KB)
- 생성 시 **60KB 초과 파일을 경고**한다

**교훈(버전 무관)**: 무손실과 단일 파일은 다른 축이다. **자르지 않되 나눌 수는 있다.**
지시문에는 「한 번에 하나씩 읽어라」와 **읽는 순서**를 명시한다 — 파일만 나누고 순서를 안 주면
배치가 또 통째로 읽으려 든다.

⚠**결과가 오면 그 자리에서 해당 정본에 반영한다.** "다음 브리핑에 적어야지" 하고 산문으로 들고 있으면
그게 유실이다 — 위 표의 6건이 전부 그렇게 샜다.

## S6. ★미확정 해소 — 여기서 재료를 바꾼다

S5까지는 **정해진 IR 줄범위 안**만 읽는다. 그래서 `unknown` 이 쌓인다(20함수 기준 **157건**).
S6은 **범위와 재료를 넓히는 별도 배치**다. 이게 이번 작업에서 가장 수확이 컸다.

넓히는 축 4가지:
1. **aux 범위** — 담당 범위 밖의 클로저/헬퍼. `fnparts` 로 찾아 `aux: [{ir_file, ir_from, ir_to}]` 로 명시하고 읽는다
2. **다른 크레이트** — `_gcbc`(game_core 본문·정적 vtable 전역) / `_gvbc`(game_view = UI·렌더러)
3. **rmeta** — 개발자 주석(`_docs`), 식별자 테이블, (미탐색) MIR/Span
4. **아웃오브라인 본체** — 인라인돼 사라진 줄 알았던 함수의 `define` 이 따로 있는 경우(`fnparts`)

산출물 = **오버레이 JSON** (`resolved_<날짜>.json`): 함수별로 `resolved`(was/now 쌍) · `new_knobs` · `still_unknown`, 그리고 여러 함수가 공유하는 사실은 `_shared` 에 한 번만.

**실측 성과**: `unknown` **157 → 22**, `knobs` **99 → 208**, `resolved` **104건**.
그리고 **"원리적 불가" 판정이 11건 뒤집혔다**(~~7건~~ → 4차 반증검증에서 4건 추가) — 전부 원인이 같았다: **가진 재료의 한계를 문제의 한계로 착각.**

| 뒤집힌 판정 | 무엇으로 |
|---|---|
| game_core 함수 본문 9건 "별도 크레이트라 없음" | `_gcbc` |
| `Arc<dyn>` vtable 슬롯 8건 "정적 vtable 없음" | `_gcbc` 의 정적 vtable 전역 |
| `objective_entity_id_...` 의 `_ => None` "LLVM이 제거" | `fnparts` 로 아웃오브라인 본체 발견 |
| `epic.rs` 소스 줄 다수 "`!dbg` 소실" | **`inlinedAt` 루트까지 타기** |
| `\C0` 옵코드 "rustc 내부 인코딩이라 명세 없음" | 템플릿 상수 6,127개 전수 디코드로 문법 역산 |
| `is_minion` 이름 "IR 3크레이트에 심볼 0건" | rmeta 식별자 테이블 |
| "Top = 화면 위쪽" "렌더러 소관이라 IR에 없다" | `_gvbc` 의 `render_minimap` |

## S6-b. ★검증 단계 — 오라클과 `tcx` (2026-09-11 신설)

S6 까지는 **읽어서 만든 표**다. 이제 그 표를 **기계로 검증하는 두 경로**가 생겼다.
전문 = `IR_TOOLKIT.md` **§6**(tcx 사전·감사) / **§7**(SDK 실행 오라클).

| 경로 | 무엇을 검증 | 한계 |
|---|---|---|
| **`tcxdict` / `tcxaudit`** | 구조체 오프셋·열거형 태그·필드 이름 = **컴파일러 정본** | ADT 아닌 것(vtable 슬롯), `&dyn` 팻포인터 뒤 절반 |
| **SDK 실행 오라클**(`-C lto=fat`) | `pub` 함수의 **입출력 진리표** | `pub` 인 것만. **외연이 같은 두 표현은 못 갈린다** |

**언제 쓰나**
- 명세에 오프셋을 하나라도 적었으면 → `python tcxaudit.py --prose <문서.md>` 를 **제출 전에** 돌려라.
  실측: 발표분 778건 감사에서 **오귀속 4건**이 나왔고 전부 산문 쪽이었다(구조화 표는 오귀속 0·밀림 0).
- 대상 함수가 `pub` 이면 → **진리표를 만들어 표를 대조**하라. `rule_scope` 13개에서 (A)~(E) **틀린 칸 0** 이 나왔고,
  덤으로 IR 로는 못 폈던 함수 6개가 **새로 완전 규정**됐다.

⚠**두 경로가 서로 어긋나면**: 구조/타입은 **`tcx` 가 정본**(컴파일러가 직접 말한다).
실행 결과가 IR 표와 어긋나면 **입력 구성과 IR 독해를 양쪽 다 의심**하라 — 오라클은 입력이 틀리면 **조용히** 틀린다.
★그리고 **오라클이 IR 표와 "어긋나지 않을" 때도 의심할 것이 하나 있다** — 4차 배치D 에서
「9축 전부 무관」이라는 **실행 결과 자체가 거짓**이었다(TLS 메모 캐시가 첫 값을 재생).
⟹ **오라클을 검증하는 것은 `_gaibc` 에 `define` 이 있는지 확인하는 것**이다. 판별력이 없다고 쓰기 전에
본문을 먼저 찾아라. 함정 목록 정본 = `_verify3\TEMPLATE.rs` 머리말(①~⑤).

### ★기준선은 숫자로 인용하지 말고 **그 자리에서 재측정**하라
4차에서 **네 배치가 전부** 내 브리핑의 기준선 수치를 오류로 지적했다. 원인은 단순하다 —
내가 **인자를 다르게 준 실행의 숫자**를 무인자 기준선으로 옮겨 적었다. 세 값이 전부 다르다:

```bash
python -X utf8 tcxaudit.py                      # 구조화 표만
python -X utf8 tcxaudit.py --prose              # + specs20/resolved 산문
python -X utf8 tcxaudit.py --prose <문서.md>    # + 그 문서
```

⟹ **브리핑에는 숫자가 아니라 위 명령을 적어라.** 배치는 자기가 재서 자기 델타를 본다.
(2026-09-11 4차 반영 시점 실측은 각각 `551 / 686 / 778`, 오귀속 0 · 밀림 0, 확인불가 18 고정.
이 숫자는 명세가 자라면 당연히 변한다 — **변하지 않는 것은 `오귀속 0 · 밀림 0`** 이고 그것만이 게이트다.)
⚠`--prose` 는 `~~취소선~~`(§7 이 남기라고 요구하는 옛 값)을 **마스킹**하고 그 건수를
`masked` 로 헤더에 찍는다. 조용히 억제하지 않으므로 **헤더의 `masked` 가 늘었으면 정정을 쓴 것**이고,
`오귀속` 이 늘었으면 **진짜 오귀속**이다.

## S7. 병합 · 투영 · 저장

```bash
cd /c/tfm2mods/MIG
python mkspec20.py     # r1..rN + resolved 오버레이 → _spec/specs20.json  (★정본)
python mkspec_md.py    # → REPORT\<MOD_ID>\06_판단함수_상세명세_20.md  (사람이 읽는 판)
python mkmap_html.py   # → REPORT\<MOD_ID>\AI함수지도.html            (지도 아티팩트)
```

★**JSON 이 정본이고 문서는 그 투영이다.** 마크다운을 손으로 고치면 다음 재생성에서 날아간다. **고칠 것은 JSON 을 고치고 재생성하라.**

병합 규칙(이번에 실제로 필요했던 것):
- **디렉터리 우선순위 merge** — 같은 이름이 여러 곳에 있으면 우선순위 높은 디렉터리 것을 쓰고, 동순위면 필드가 많은 쪽
- **오버레이 키 정합성 검사** — 고아 키(`is_end` vs `epic_hunt_and_poke__is_end`)가 실제로 생겼다. 병합 후 반드시 키 매칭 리포트를 확인하라

그리고 **§11 의무**:
- RE 결과는 **돌아오는 즉시** `REPORT\<MOD_ID>\RE\YYYY-MM-DD_<주제>.md` 에 저장. 형식 = 첫 줄 `지시:` → `요약:` 3~5줄 → **반환 원문 통째 복사**(가공 금지)
- 확정 오프셋·함수 계약은 `02_구현정보.md` 에
- 실패·함정은 `03_시행착오.md` 에
- 결론은 record-keeper 로 `MEM\`·`ANA\` 반영(§9 — 메인 세션이 직접 쓰지 말 것)

---

# 4. 판정 어휘와 **범위** 규칙 (이게 제일 중요하다)

★**"X 불가능"이라고 쓰지 마라. "A 방식으로는 X 불가능, 미탐색 = B" 라고 써라.**
범위를 안 적으면 다음 세션이 **다른 방식조차 시도하지 않는다.** 지금까지 **11건**이 그 함정이었다.

명세 JSON 의 `unknown`/`still_unknown` 항목은 이 **넷** 중 하나로 분류해서 쓴다
(`mkspec3.classify` 가 문면에서 자동 판정하고, `specgate G10` 이 오분류를 잡는다):

| 분류 | 뜻 | 예 |
|---|---|---|
| **표기 불가** | 동작은 확정, 소스 표기만 불가 | `column:` 이 전 모듈 0이라 한 줄 안의 `A \|\| B` 순서 복원 불가 |
| **재료 부재** | 이 재료에 실체가 없음(탐색 범위를 열거) | `MF_SRC_NAMES` — IR 3종·rmeta 3종·전 rlib·exe 전량 0건 |
| **미탐색** | 불가가 아니라 아직 안 읽음 | `resolve_join_stake` 1,353줄 |
| ★**사실 서술** | **물음이 아니다** — 이미 확정된 관측 | "`_version`·`_debug` 는 본문에서 전혀 쓰이지 않는다" |

**세 번째를 첫 번째로 적는 것이 최악의 오염이다.** 다음 세션이 진짜로 포기한다.
★**네 번째를 세 번째로 적는 것이 두 번째로 나쁘다.** `미탐색` 은 "더 파면 나온다"는 약속이라,
확정된 사실을 거기 앉혀 두면 **다음 라운드가 그걸 '새 발견'으로 또 집는다**(4차 배치A 지적).
⟹ 사실 서술은 `open[]` 이 아니라 **`notes[]`** 로 간다. 다음 라운드는 `notes[]` 를 **파지 않고,
틀렸다고 보면 반증**한다.
★그리고 **동의 표현을 놓치지 마라** — "확정 불가"·"확인 불가"·"알 수 없다"·"복원 불가" 는 전부
**재료 부재**다. 초판 `classify` 가 이것들을 몰라 8건이 `미탐색` 으로 앉아 있었다(4차 반영 시 교정).

확정된 재료 한계(현재까지):
- **`column:` 이 `_gaibc`/`_gcbc`/`_gvbc` 전 모듈 0건** — 같은 줄 안 표현식 순서는 **IR로는** 불가. ★단 **rmeta MIR/Span 은 미탐색**이라 원리적 불가가 아니다.
- **빈 match arm(`{}`)은 명령을 안 만들어 `!DILocation` 이 없다** — 줄 배정 불가(동작은 확정 가능)
- **의미가 동일한 표기는 정규화된다**(`==5` / `>=5` / `>4`) — 원본 표기 소멸

---

# 5. 함정 톱 10 (전부 이번에 실제로 밟았다)

1. ★**열거형 태그 ≠ variant 인덱스.** 니치 최적화로 밀린다(`SubPlan`=idx+2, `LineGankerPhase`=idx+6). **반드시 `dienum.py` 의 `DISCR_EXACT` 를 인용하라.**
2. ★**`!dbg` 가 없어 보여도 `inlinedAt` 루트까지 타라.** scope 만 보면 `line: 1` 이 나오지만 루트를 따라가면 진짜 줄이 나온다.
3. ★**`DW_OP_not` 개수로 극성을 읽지 마라.** 이중 이상 NOT 은 컴파일러 아티팩트(전 모듈 57건/12모듈, 개수가 3·5·6인 사례 5건). **극성은 분기 방향으로 확정.** 단일 NOT 은 논리 부정이 맞다.
4. **동명 다른 타입 / 사전의 동명 충돌.** 사전 확장 시 라이브러리 타입이 게임 타입을 덮어썼다(`LineType` Top/Mid/Bottom → EXIF 태그, `State` → zlib). **디렉터리 우선순위**로 막아라.
5. **`Vec<T>` descent 시 같은 크기의 다른 인스턴스를 집는다.** leaf+size 만으로 매칭하지 말고 타입명 정규화까지 하라.
6. **오프셋만으로 필드 스캔하면 다른 구조체가 딸려 온다.** `+0x108` = 2,339곳 → `--src battle.rs` 한정 45곳. ★`fieldcodes.py` 는 gep 레지스터를 **파일 전역**으로 매칭하는 오염이 있다 — 후속 정본은 **`fieldall2.py`**(함수 스코프 + gep 체인 누적).
7. **상수 스캔이 값을 놓친다.** 비상수 `phi` 로 들어오는 값은 안 잡힌다(`exit_sub` 의 6·7, `dive_abort_src` 의 1·11이 그랬다). **store 지점의 phi 까지 봐야 전수다.**
8. **주석은 stale 일 수 있다.** rmeta 주석이 IR 과 충돌하면 **IR 이 정본**. (실제 사례: `ff_battle_exit.__1` 은 주석대로면 `exit_sub` 인데 IR은 `entry_src`)
9. **"노브"라고 적기 전에 소비처를 확인하라.** `BattlePlanGoal.__1` 은 생성 14곳 전부 리터럴 60인데 **산술·비교 소비처 0건, `Debug::fmt` 만 읽는다** — 노브가 아니다. `mf_swap.__0` 도 load 0건 = 순수 텔레메트리.
10. ⚠**성능/동시성 사고.** (a) 거대 `.ll`(60~75MB)에 줄 단위 `sed -n "${k}p"` 루프 금지 — 1시간 낭비 후 강제종료했다. 파이썬 통째 읽기 또는 `awk` 단일 패스. (b) **같은 산출물을 쓰는 배치를 두 번 띄우지 마라** — `dienum.json` 이 경합으로 덮어써진 사고가 **두 번** 났다. 새 배치 전에 이전 배치가 살아있는지 먼저 확인하라.

---

# 6. 실측 수치 요약 (일정 추정용)

| 항목 | 값 |
|---|---|
| game_ai 소스 함수 총량 | **753개 / IR 507,482줄** (`corpus2.py`) |
| r1 20개 동시 | 벽시계 **11분**, 개당 평균 **8분 26초** |
| 사전 도구 투입 효과 | 일치도 87% → 96.6%(판정상수), 병목 60~70% 감소 |
| 수렴까지 필요한 라운드 | **4라운드 전량 + 잔여만 2라운드** = 실질 4.4회/함수 |
| S6(미확정 해소) 성과 | unknown 157→22, knobs 99→208, resolved 104 |
| rmeta 주석 추출 | 2,925개 / 132,551자 / **7초** |
| game_view IR 추출 | **25초** (1,270MB) |

⚠**거대 함수(2000줄 초과) 56개는 미측정**이다. 분량의 54%를 차지하므로 전체 일정 추정의 최대 불확실성이다. **다음 배치에서 하나만 시범으로 재보면 확정된다.**

---

# 7. 그대로 복사해 쓰는 의뢰 문안

## A. 명세 라운드 (S2)

```
TFM2 game_ai IR 독해. **먼저 MIG\METHOD_MAP.md(§0 라우팅표·§3 판정어휘·§4 함정10)와 MIG\SPEC_GUIDE.md 를 끝까지 읽어라** —
재료·형식·함정·자체검사가 다 있다. 특히 §1의 distruct.py(오프셋 사전)를 반드시 쓸 것. 한국어로 작성·보고.

담당: <함수명> · 소스 <src:line> · IR C:\tfm2mods\_gaibc\<m??.ll> <from>~<to>줄
출력 = C:\tfm2mods\MIG\_spec\r<N>\<파일명>.json
자체검사 = cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python qcspec.py _spec/r<N>/<파일명>.json → [PASS] 까지.

★IR 을 읽기 전에 C:\tfm2mods\MIG\_docs\*.txt(개발자 한국어 주석 2,925개)를 먼저 grep 하라.
★_gaibc 에 declare 만 있으면 _gcbc / _gvbc 에서 define 을 찾아라.

⚠독립 재현 실험이다. 다른 사람이 같은 함수를 이미 명세했지만 일부러 안 보여준다 —
두 결과의 일치도로 정확도를 잰다. _spec/r<N-1>/ 을 절대 열지 마라. 네가 IR에서 직접 읽은 것만 써라.

보고에 막힌 지점과 체감 소요를 포함할 것.
```

## B. 미확정 해소 배치 (S6)

```
TFM2 0.5.8 리버스엔지니어링. 전부 한국어로 보고.

# 재료
_gaibc(game_ai 303MB) / _gcbc(game_core 992MB) / _gvbc(game_view 1,270MB) = C:\tfm2mods\_*bc\*.ll
개발자 한국어 주석 2,925개 = C:\tfm2mods\MIG\_docs\*.txt — ★IR 읽기 전에 여기부터 grep
★상황별 방법 선택 = MIG\METHOD_MAP.md (§0 라우팅표에서 이 질문에 맞는 방법부터 고르고 시작하라)
방법론 = MIG\IR_TOOLKIT.md, 명세 규격 = MIG\SPEC_GUIDE.md — 셋 다 먼저 읽어라
도구 = fnparts / divtable / distruct / dienum / dloc(inlinedAt 체인) / fieldall2
  ★fieldcodes.py 는 오염이 있으니 쓰지 마라

# 이미 확정된 주변 사실 (다시 조사하지 말 것)
<여기에 관련 오프셋·태그·계약을 전부 나열 — 이게 길수록 배치가 빨라진다>

# 임무
<미확정 항목을 하나씩, "현재 어디까지 확정됐고 무엇이 비었는지"까지 써서>

# 보고 형식 (SPEC_GUIDE 규격)
signature / logic(의사코드) / reads(구조체+오프셋+의미) / writes / constants(값+IR위치+의미)
/ calls / knobs(조정하면 게임에서 무엇이 달라지나) / unknown

# 규칙
- 판정에는 적용 범위를 써라: "X 불가능"이 아니라 "이 재료로는 X 불가능, 미탐색 = Y"
- 모든 주장에 근거(파일:라인) 필수. 없으면 "추정"이라 명시하고 검증 방법을 붙여라
- 같은 가설 2회 실패하면 멈추고 그 사실 + 남은 재료를 적어라
- ⚠성능: .ll 은 60~75MB. 줄 단위 sed 루프 절대 금지. 파이썬 통째 읽기 또는 awk 단일 패스
- ⚠니치 최적화: 태그 ≠ variant 인덱스. dienum.py 로 확인하고 DISCR_EXACT 인용
- ⚠DW_OP_not 개수로 극성 읽지 마라 — 이중 이상은 컴파일러 아티팩트. 극성은 분기 방향으로
- rmeta 주석이 IR 과 충돌하면 IR 이 정본
```

## C. 코드표 복원 (진단 필드 → 의미 표)

B에 아래를 덧붙인다:

```
대상 필드 = <구조체>+<오프셋> (<타입>)
1) fieldall2.py 로 그 필드에 store 하는 지점을 전수 수집 (--src 로 소스 파일 한정)
2) ★상수 스캔만으로는 놓친다 — store 지점이 phi 면 phi 의 incoming 값까지 봐라
3) 각 값의 "왜"를 소스 줄 + 인접 조건으로 붙여라
4) 값이 안 나오는 코드는 "이 크레이트 범위에서 store 0건"이라고 범위를 명시
5) 같은 이름표를 쓰는 다른 필드가 있는지 확인하라 (실제로 exit_src 가 그랬다 —
   플랜 내 전환은 BattlePlan+0x108, 플랜 교체는 LegacyPlanHandler.ff_battle_exit.__0)
```

---

# 8. 다음 배치 착수 체크리스트

- [ ] `MEM\DONE.md` grep — 판정 어휘 6종(`DONE`/`DIFF=0`/`폐기`/`재시도금지(범위)`/`교훈`/`보류`). 앞 4종이면 **재조사 금지**
- [ ] `MEM\CURRENT.md` 로 현행 게임 버전 확인 (구버전 검증결과를 현행으로 믿는 게 대표 사고)
- [ ] 사전 6종이 **현행 버전으로** 빌드돼 있나 (`distruct`/`dienum`/`divtable`/`knobscan`/`_docs`/`fnparts` 인덱스)
- [ ] 대상 목록 JSON 작성 — 함수명 · `src:line` · **IR 파일 + 줄범위**. `sym` 은 믿지 말고 `define` 줄에서 읽을 것
- [ ] 라운드 디렉터리(`_spec/rN`) 비어 있나 — **이전 라운드를 보여주면 독립성이 깨진다**
- [ ] **이전 배치가 아직 도는지 확인** — 같은 산출물에 두 배치를 띄우면 경합으로 덮어쓴다(2회 발생)
- [ ] 라운드마다 `qcspec` → `speccmp` → **판정상수 100% 될 때까지 잔여만 재투입**
- [ ] S6 재료 확장 배치 → `resolved_<날짜>.json` 오버레이
- [ ] ⛔**기존 정본에 함수를 더할 땐 `mkspec20` 을 돌리지 말 것**(r1..r6 로 통째 재생성 = applypatch 정정 소실 · 2026-09-13 가드) → **`addspec.py <id> <라운드JSON> --exe 0x…`** → `mkspec3.py` → `specgate --only N`. 절차 전문 = `REPORT\tfm2_judge_verify\04_분석방법_정리.md §8`
- [ ] (처음부터 새 정본을 만들 때만) `mkspec20 --force` → `mkspec_md` → `mkmap_html`, **JSON 이 정본**
- [ ] RE 원문 즉시 저장(§11) · `02_구현정보.md` 갱신 · `03_시행착오.md` 에 실패 기록 · record-keeper 로 `MEM`/`ANA` 반영(§9)
