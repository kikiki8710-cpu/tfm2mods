# -*- coding: utf-8 -*-
u"""REPORT.md 생성기 (본문을 파이썬 문자열로 들고 쓴다 — heredoc 인용 사고 회피)."""
import io, os, sys
sys.stdout.reconfigure(encoding="utf-8", errors="replace")
HERE = os.path.dirname(os.path.abspath(__file__))

BODY = u'''# 9차 배치 B — 축 `sig.params[].role` · **G16 승격 판정** (게임 0.5.8 · 2026-09-11)

> 임무 = 8차 통합판(`MIG\\paramrole.py`)이 내던 **`P4` 6건 · `P6` 14건**을 IR 원문으로 전수 반증하고,
> 살아남는 것만 남겨 **G16 으로 승격 가능한 상태**를 만드는 것.
> **결론: 20/20 전부 오탐. 고친 판의 전 20함수 적발 = 0. 변이 599/599 포착. ⟹ 승격 가능(조건 1개).**

## §0 실제로 실행한 것 (명령줄 그대로)

```bash
cd /c/tfm2mods/MIG && python -X utf8 dossierfresh.py 9 B              # FRESH (착수 시 · 제출 직전 재확인)
PYTHONIOENCODING=utf-8 python -X utf8 paramrole.py                    # 8차 통합판 기준선 → 적발 20
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/dump.py 2:1 17:1     # role 원문·백틱 조각·인용 위치 덤프
cd /c/tfm2mods/_gaibc && sed -n '34867p' m12.ll ; sed -n '17695p' m05.ll ; sed -n '33864p' m10.ll
cd /c/tfm2mods/_gaibc && for L in 33929 33981 33982 33983; do sed -n "${L}p" m10.ll; done
cd /c/tfm2mods/_gaibc && sed -n '33864,34294p' m10.ll | grep -cE 'ptr %4, i64 1(632|640)'   # → 0
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/gate.py              # 고친 판 → 적발 0
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/gate.py --weak       # 약한 후보 12 (기각 근거 아님)
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/mutate.py [--list]   # 변이 599/599
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/coverage.py          # 검사별 도달률
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/mkpatch_b.py         # patch.json 생성(old 는 정본에서 직접 뜸)
PYTHONIOENCODING=utf-8 python -X utf8 _verify9/B/simpatch.py          # 패치 적용본도 적발 0 인지 메모리 검증
cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python -X utf8 applypatch.py 9 --only B --dry   # 정정 2/2 · ev상향 2/2
PYTHONIOENCODING=utf-8 python -X utf8 _gates/params_role/A_paramuse.py 0 1 … 19   # 10건
PYTHONIOENCODING=utf-8 python -X utf8 _gates/params_role/B_rolecheck.py           # 9건
PYTHONIOENCODING=utf-8 python -X utf8 _gates/params_role/C_rolechk.py             # ★크래시
PYTHONIOENCODING=utf-8 python -X utf8 _gates/params_role/D_paramrole.py           # 14건
```

★**적발 20건이 나온 자리는 126행 중 단 3행**이었다 — `02 sub_plan` p[1] `self` · `03 defensive_crisis` p[4] `target` · `17 new` p[1] `version`. 「20건」이라는 수가 겁을 준 것이지 조사량이 컸던 게 아니다.

## §1 판정 요약

| | 8차 통합판 | 9차 고친 판(`_verify9\\B\\gate.py`) |
|---|---|---|
| 전 20함수 · 126행 적발 | **20**(P4 6 · P6 14) | **0** |
| 그중 IR 원문으로 반증된 것 | — | **20 / 20 = 전부 오탐** |
| `--weak`(기각 근거 아님) | 0 | 12 |
| 변이 포착률 | (측정 안 함) | **599 / 599 = 100.0%** (음성 대조 129건 포함) |

후보 4벌 재실측(갈림의 이유는 8차가 규명했고 여기선 **현재 상태**만 센다):
`A_paramuse` 10 · `B_rolecheck` 9 · **`C_rolechk` 크래시**(`AttributeError: module 'memdir' has no attribute 'offsets'` — 8차에 `memdir`(G14)의 API 가 바뀌어 이 후보는 지금 **돌지도 않는다**) · `D_paramrole` 14 · 8차 통합판 20 · **9차 판 0**.

## §2 P4 6건 · P6 14건 전수 반증 (20/20 오탐)

### §2-a `02 sub_plan` p[1] `self` — P4 2건 · P6 1건 → **전부 오탐**

role 원문(발췌):

> ★IR %1 (=%0 은 반환 out-ptr). … ⚠**이 params 표는 sret out-ptr 을 빠뜨렸다** — m12.ll:34867 `define void @…AttackNexusPlan8sub_plan(ptr dead_on_unwind noalias noundef writable writeonly sret([72 x i8]) align 8 captures(none) dereferenceable(72) %0, ptr … %1, i64 noundef %2, …)` 로 IR 인자가 8개인데 표는 7행뿐이라 … `(sret)`(i=0) 행을 params[0] 로 싣는 것인데 `applypatch` 에 행 추가가 없어 기계로 못 넣었다(8차 배치C)

| 적발 | 판정 | 근거 |
|---|---|---|
| P4 `writeonly` 가 IR %1 에 없다 | **오탐** | `writeonly` 는 **role 이 인용한 %0(sret)의 define 헤더 안**에 있다. p[1] 의 주장이 아니다. m12.ll:34867 실측 %1 = `ptr noalias noundef readonly align 8 captures(none) dereferenceable(16) %1` — role 이 %1 에 대해 주장한 것은 「필드 team@0x0 · line@0x8, line 만 읽는다」뿐이고 그것과 `readonly` 는 모순이 없다 |
| P4 `sret` 가 IR %1 에 없다 | **오탐** | 「이 params 표는 **sret** out-ptr 을 빠뜨렸다」는 **명세 표에 대한 메타 서술**이지 p[1] 이 sret 속성을 가졌다는 주장이 아니다 |
| P6 인용한 IR 명령 `(i=0) 행을 params[0] 로 싣는 것인데` 가 m12.ll:34867 에 없다 | **오탐**(★가장 위험한 형태) | 그 문자열은 **role 에 백틱으로 인용된 적이 없다.** 8차 판의 인용 정규식이 백틱쌍 `[^백틱]{6,90}` 이라 **184자짜리 define 인용이 길이 상한에 걸려 짝짓기가 한 칸 밀렸고**, 그 결과 `(sret)` 의 닫는 백틱과 `applypatch` 의 여는 백틱 사이의 **한국어 산문이 「인용」으로 날조**됐다. 그 뒤 「IR 처럼 보임」 필터가 `(i=0)` 의 `=` 하나로 통과시켰다 |

⟹ 게이트가 **존재하지 않는 인용을 만들어 내고 그것을 반증**했다. 길이 상한을 정규식 안에 넣은 대가다.

### §2-b `03 defensive_crisis` p[4] `target` — P6 12건 → **전부 오탐 · 인용 5군데 전부 IR 과 일치**

IR 원문 대조(`sed -n '<L>p' m10.ll` 출력 그대로):

```
33929:   store ptr %4, ptr %33, align 8, !dbg !40740
33981:   %46 = getelementptr inbounds nuw i8, ptr %4, i64 1472, !dbg !40815
33982:   %47 = load i64, ptr %46, align 8, !dbg !40815, !noundef !8
33983:   %48 = call fastcc noundef align 8 ptr @_RNv…AbstractGameWithCache21player_by_champion_id(ptr … %18, i64 noundef %47), !dbg !40815
33864,34294 범위 `ptr %4, i64 1632|1640` grep -c → 0
```

role 의 인용 **5군데가 전부 정확**하다(+0x5c0 = 1472 = 챔피언 id, 좌표 gep 0건이라는 부정 주장도 실측 0). 그런데 8차 판은 **IR 조각 3개 × 인용 5군데의 교차곱**으로 대조해 12건을 냈다 — 조각이 *자기 인용*이 아닌 남의 줄번호와 맞춰졌을 때 당연히 안 맞는다.

산술 검산: 조각 3개(`%46 = getelementptr…` · `%47 = load…` · `store ptr %4, ptr %33`) × 인용 5군데 = 15, 그중 제 짝 3개가 맞으니 **15 − 3 = 12** — 적발 수와 정확히 일치한다. 이것이 오탐 기제의 결정적 증거다.

부수 결함 1개: 8차 판은 `m10.ll:33864~34294`(**범위** 인용)를 `/` 와 같은 **줄 목록**으로 읽어 33864 와 34294 두 줄만 봤다.

### §2-c `17 new` p[1] `version` — P4 4건 · P6 1건 → **전부 오탐**

| 적발 | 판정 | 근거 |
|---|---|---|
| P4 `writeonly` · `captures(none)` · `noalias` | **오탐** | 셋 다 role 이 인용한 **%0(sret) define 헤더 안**의 낱말이다. m05.ll:17695 실측 %1 = `i64 noundef %1`(정수 인자라 애초에 포인터 속성이 붙을 자리가 없다) |
| P4 `sret` | **오탐** | §2-a 와 같은 메타 서술 |
| P6 날조된 인용 | **오탐** | §2-a 와 같은 **백틱 짝짓기 붕괴** |

### §2-d 오탐 4형태 대조 — 8차가 정리한 목록에 **2형태를 추가**해야 한다

8차 배치C 가 정리한 4형태(절 분해 실패 · 부정절의 오프셋을 긍정 주장으로 읽기 · 베이스가 다름 · 클로저 환경 오프셋)는 전부 **오프셋 귀속**의 문제였고, 이번 20건은 **하나도 그 형태가 아니다.** 새로 붙일 두 형태:

- **⑤ 인용문 안의 낱말을 주장으로 읽기** — `role` 이 IR 을 인용하면 그 안의 속성어는 **근거**이지 주장이 아니다. (P4 6/6 이 이것)
- **⑥ 인용 경계 붕괴 / 인용 × 줄번호 교차곱** — 인용을 자기 줄번호에 귀속시키지 않으면 조각 수 × 인용 수만큼 적발이 부풀고, 길이 상한을 정규식에 넣으면 **짝이 밀려 산문이 인용으로 날조**된다. (P6 14/14 가 이것)

## §3 고친 내용 (`_verify9\\B\\gate.py`)

| # | 결함 | 고침 |
|---|---|---|
| **F1** | P4 가 인용문 안 속성어를 주장으로 읽음 | 백틱 스팬이 **IR 인용**이면(=`define`/opcode/`%N =` 포함) 주장 스캔 전에 제거 |
| **F2** | `sret` 주장을 아무 행에서나 검사 | `sret` 속성 주장은 **sret 행**(`name` 이 `(sret` 로 시작)에서만 검사 |
| **F3** | ★백틱 **짝짓기 붕괴**(`{6,90}` 이 정규식 안에 있었다) | 백틱쌍을 **길이 무관**으로 먼저 짓고, 길이 필터는 **그 뒤에** |
| **F4** | 인용 × 조각 **교차곱** | 조각은 **직전 인용**(없으면 직후)에만 귀속. 사이 산문이 40자 초과면 **귀속 포기(=기각 안 함)** |
| **F5** | 「IR 처럼 보임」 필터가 한글 산문 통과 | **한글이 들어간 조각은 IR 이 아니다** + opcode/`%N =` 요구 |
| **F6** | 생략기호(`…`)·축약 인용을 불일치로 셈 | ① `…` 와일드카드 **조각 순서 일치** ② **하드 토큰**(`%N`·홀로 선 정수) 전부 + 나머지 ≥80% 순서부분수열로 구제 |
| **F7** | `33864~34294` 를 줄 목록으로 오독 | `/ · ,` = 목록 · `~` = **범위**. 그 절이 부정(`0건`·`없다`)이면 귀속 포기 |
| **F8** | (변이 시험이 찾은 사각) sret 행 표식만 지우면 아무것도 안 잡힘 | `len(args)==len(ps)` 인데 IR 첫 인자가 sret 인 경우도 **P2** 로 적발 |
| **F9** | (변이 시험이 찾은 사각) `params[]` 행 **수**가 `sig.tcx` 소스 인자 수와 달라도 침묵 | **P3** 에 행 수 검산 추가 — 행 수가 틀리면 그 아래 모든 role 이 한 칸 밀린다 |

⚠F8·F9 는 **적발을 늘리는 방향**의 수정이다. 7차 교훈(「후보를 늘리면 오탐이 는다」)에 걸리지 않는 이유는 이 둘이 **귀속·추론이 아니라 IR `define` 헤더와 `sig.tcx` 원문과의 산술 대조**이기 때문이다. 실제로 둘을 넣고도 정본 126행 적발은 **0 그대로**다.

## §4 검출력 — 변이 시험 (`_verify9\\B\\mutate.py`)

「적발 0」을 **무능**이 아니라고 말할 수 있는 유일한 수단. 정본은 안 건드리고 메모리 사본만 변형한다.

| 변이 | 내용 | 포착 |
|---|---|---|
| `A` | role 에 **IR 에 없는 속성어** 덧붙임 | **123 / 123 (100%)** |
| `B` | 인용 **줄번호 +3** | 4 / 4 (100%) |
| `C` | 인용 **IR 조각의 마지막 정수 +7** 위조 | 4 / 4 (100%) |
| `D` | 실제로 쓰이는 인자를 「본문에서 전혀 안 씀」으로 위조 | 68 / 68 (100%) |
| `E` | `params[j].i` +1 | 126 / 126 (100%) |
| `F` | `params[]` 한 행 **삭제** | 126 / 126 (100%) |
| `G` | sret 행의 `(sret` 표식 제거 | 5 / 5 (100%) |
| `H1` | **맞는** 속성 주장을 틀린 속성으로 바꿔치기 | 13 / 13 (100%) |
| `H2` | 인용 두 개의 줄번호를 **서로 맞바꿈** | 1 / 1 (100%) |
| **`N1`** | (음성 대조) 인용을 `…` 로 **더 줄여 씀** — 잡히면 오탐 | **3 / 3 안 잡힘 = 정상** |
| **`N2`** | (음성 대조) 무해한 산문 덧붙임 — 잡히면 오탐 | **126 / 126 안 잡힘 = 정상** |
| | **합계** | **599 / 599 (100.0%)** |

★음성 대조 129건을 섞은 이유: 「포착률 100%」는 **모든 변형을 무조건 적발하는 게이트**로도 만들 수 있다. 그건 검출력이 아니라 소음이다. 음성 대조가 전부 통과했다는 것이 **100% 가 소음이 아님**의 증거다.

중간 실측 이력(정정형): 첫 판은 **456/456 중 C 0/4 · G 0/5 · F 121/126 = 442/456(96.9%)** 였다. C 의 원인은 구제식이 「토큰 ≥80%」라 **정수 하나를 위조해도 통과**한 것(→F6 하드 토큰), G·F 의 원인은 §3 F8·F9. 고친 뒤 599/599.

**어느 검사가 잡았나**(변이별 귀속 실측):

```
A→P4 123 · H1→P4 13            (P4 136)
E→P3 126 · F→P3   8            (P3 134)
F→P1 113 · D→P1   3            (P1 116)
D→P5  65                       (P5  65)
F→P2   5 · G→P2   5            (P2  10)
B→P6   4 · C→P6   4 · H2→P6 1  (P6   9)
```
여섯 검사 **전부**가 독립적으로 무언가를 잡는다 — 죽은 검사가 없다.

## §5 도달률 — 「검사할 주장 자체가 있는가」 (`_verify9\\B\\coverage.py`)

「적발 0」의 세 번째 독법(**검사할 거리가 애초에 없었다**)을 막기 위해 같이 잰다.

| 검사 | 도달 | 비고 |
|---|---|---|
| P1 정렬 | 20함수 전부 | `define` 헤더를 읽을 수 있는 함수 전부 |
| P2 sret | **5함수** | `00 02 07 15 17` |
| P3 `i`·행 수 | **20함수** | `sig.tcx` 를 전부 파싱했다 |
| P4 속성 주장 | **15행 / 126** | 인용 제거 후에도 속성어를 주장하는 행 |
| P5 전면 미사용 | **59행 / 126** | 가장 넓다 |
| P6 IR 인용 | **4행 / 126** | `02 p[1]` · `03 p[0]` · `03 p[4]` · `17 p[1]` **뿐** |
| E 오프셋(약함) | 45행 / 126 | 기각 근거 아님(`--weak` 12건) |

⚠**P6 의 도달률이 3%** 라는 것이 이 축의 한계다. role 에 IR 을 백틱으로 인용하는 관행이 4행에만 있다. 검출력이 100%여도 **볼 거리가 없으면 못 본다** — 이건 게이트의 결함이 아니라 **명세 작성 관행**의 문제이고, 인용을 늘리면 P6 이 그만큼 강해진다.

## §6 `G16` 승격 판정 — **가(可). 단 조건 1개**

**가능하다.** 근거:
1. 8차 통합판이 내던 적발 20건이 **전수 반증됐고**(20/20 오탐) 그 원인 9개를 전부 고쳤다.
2. 고친 판의 정본 적발 = **0**(126행 · 20함수). patch.json 적용본으로도 **0**(`simpatch.py`).
3. 「0」이 무능이 아님을 **변이 599/599 + 음성 대조 129/129** 로 보였다.
4. `check_spec(sp) -> [(idx, why, det)]` 계약이 `G13`(`whereline`)·`G14`(`memdir`)와 동일해 `specgate.py` 에 그대로 꽂힌다.

★**조건** — `specgate.py` 의 게이트 함수는 전부 `except Exception: return` 으로 감싸여 있다. 즉 **모듈이 죽으면 그 게이트는 조용히 `0` 을 찍는다**(`C_rolechk` 가 지금 `memdir.offsets` 로 죽는데 아무도 몰랐던 것과 같은 사고다). G16 을 붙일 때 **import 실패·예외를 `0` 이 아니라 `-`(안 돌림)로 찍게** 하거나, 최소한 `gate16` 에서만은 예외를 그대로 올려라. 지금 형태로 붙이면 「G16 0건」이 **깨끗함**인지 **죽음**인지 구별되지 않는다.

승격 시 붙일 코드(참고):

```python
def gate16(i, sp):
    # `sig.params[].role` 대조. 9차 배치B 신설. 변이 599/599(음성 대조 129 포함).
    if not IS_V3:
        return
    import paramrole as PR          # ★except 로 삼키지 마라 — 죽은 게이트와 0건이 구별돼야 한다
    for j, why, det in PR.check_spec(sp) or []:
        flag("G16", i, u"params[%d] %s" % (j, why), det)
```

## §7 실오류 (patch 제출분)

`errors[]` **2건** · `ev_up[]` **2건** · `brief_errors[]` **4건**.

| path | kind | 무엇 | behavior_change |
|---|---|---|---|
| `/specs[2]/sig/params[1]/role` | 실오류 | 「⚠**이 params 표는 sret out-ptr 을 빠뜨렸다** … 표의 자리 번호가 IR 보다 한 칸 앞선다 … `applypatch` 에 행 추가가 없어 기계로 못 넣었다」가 **8차 시점의 서술로 굳어 있다.** 9차에 sret 행이 `params[0]`(`(sret)`·i=0)로 **실제로 들어갔고** params 는 8행 = IR 인자 8개다 | **true** |
| `/specs[17]/sig/params[1]/role` | 실오류 | 같은 서술. params 5행 = IR 인자 5개 | **true** |

★`behavior_change = true` 인 이유(도시에 §5 표의 뜻대로 「이 명세대로 재구현하면 틀린 동작이 나오는가」로 읽었다): 이 문장은 **「표의 자리 번호가 IR 보다 한 칸 앞선다」고 지시**한다. 표에 sret 행이 이미 들어간 지금 그 지시를 따르면 재구현자는 `p[1] self` 를 IR `%2`(=`version`)로 읽는다 — **인자 전체가 한 칸 밀린 재구현**이 된다. 문면 정정이지만 결과는 값 오기와 같다.

`ev_up` 2건 = `/specs[2]/sig/params[0]` · `/specs[17]/sig/params[0]` 를 **ev4 → ev3**. 근거는 `sig.tcx` 정본 대조다 — `fn(&AttackNexusPlan, usize, &mut StdRng, &PlayerState, &OperationData, &TeamPlan, &mut DebugFrameData) -> SubPlan`(소스 인자 7) + sret 1 = IR 인자 8 = params 8행이고, `sret([72 x i8])` 의 72B 가 반환 타입 크기와 일치한다. `17` 은 소스 인자 4 + sret 1 = 5 · `sret([384 x i8])`.

**P4/P6 오탐 20건은 `errors[]` 로 못 낸다** — 고칠 대상이 **명세가 아니라 게이트**여서 `path`/`old`/`new` 가 존재하지 않는다(§8 에 지시 오류로 적었다). 고침은 `_verify9\\B\\gate.py` 로 제출한다.

사전 검증 결과:
```
cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python -X utf8 applypatch.py 9 --only B --dry
[B] 정정 2/2 · ev상향 2/2
정정 2 성공 / 0 실패 · ev상향 2 성공 / 0 실패 · **동작 변경 2건**
```

## §8 내 지시(도시에 + 부모 메시지)의 오류

1. **§1 이 8차용 본문 그대로다.** 「`_gates/params_role/` 의 후보를 전부 돌려 갈림을 규명하고 통합하라」(§1 할 일 1~2)는 **8차 배치C 가 이미 끝낸 일**이고, 그 산출물이 `MIG\\paramrole.py` 다. 9차 지시(부모 메시지)는 「그 통합판의 P4/P6 을 반증하라」인데 도시에 §1 이 옛 과업을 그대로 실어 **두 지시가 어긋난다.**
2. **「P4 6건·P6 14건」이 126행 중 단 3행에서 나온다는 사실이 빠져 있다.** 건수만 주면 조사량을 20배로 오산한다. 도시에가 **행 위치**(`02` p[1]·`03` p[4]·`17` p[1])를 같이 줬다면 착수가 훨씬 빨랐다.
3. **§5 표가 `errors[].kind` 에 `오탐`(게이트가 틀렸다)을 허용하지만 `applypatch.apply_error` 는 `path`/`old`/`new` 없이는 못 돌아간다.** 「게이트가 틀렸다」는 명세에 고칠 문면이 없어 **구조적으로 `errors[]` 로 낼 수 없다.** 이번 라운드의 최대 산출(오탐 20건)이 기계 적용분에 한 줄도 안 들어가는 이유다 — 스키마에 `gate_fix[]` 같은 칸이 필요하거나, `kind:"오탐"` 은 `brief_errors` 로 가라고 못 박아야 한다.
4. **§5 가 `ev_up[].from` 을 요구하지만 `apply_evup` 는 `to` 만 읽는다**(`from` 은 검증에도 안 쓰인다). `from` 을 잘못 적어도 조용히 통과하므로 **틀린 근거가 남는다.**
5. ⚠**「8차 배치C 가 정리한 오탐 4형태를 먼저 읽어라」는 이번 20건에 하나도 안 맞았다.** 4형태는 전부 **오프셋 귀속**의 문제인데 이번 20건은 **인용 처리**의 문제다(§2-d). 「같은 함정을 다시 밟지 마라」를 곧이곧대로 따르면 **오프셋만 들여다보다 인용 경계 붕괴를 못 본다.** 목록은 ⑤·⑥ 을 더해 6형태로 갱신해야 한다.

**판정 반전 (유저 규칙상 오류로 셈) — 2건**

6. 8차 배치C 는 `P4` 를 「`define` 줄 원문. **반증이 결정적**」, `P6` 을 「절 분해·오프셋 귀속 문제가 없다」고 **강한 후보**로 분류했다. 9차 실측 = **P4 6/6 오탐 · P6 14/14 오탐.** 두 검사 모두 「결정적」이 아니었다. 결정적이었던 것은 **검사의 종류**가 아니라 **인용 경계를 지켰는가**였다.
7. 8차 배치C 는 **오프셋 귀속만이 오탐의 근원**이라고 적었다(「오탐의 근원은 하나였다」). 9차 실측 = 오프셋 귀속은 `--weak` 로 격리돼 **강한 적발 0건**을 냈고, 실제 오탐 20건은 전부 **다른 근원**에서 나왔다. 「근원은 하나」라는 단정이 틀렸다.

## §9 판정 어휘 (도시에 §5-b)

- `DIFF=0` — 8차 통합판 적발 20건 대 IR 원문: **20건 전부 오탐**. 축의 실오류는 **문면 2건뿐**이고 값·자리·속성 주장은 126행 전부 IR 과 일치한다.
- `재료 부재` — **없음.** 이 축은 `define` 헤더 + 본문 IR + `sig.tcx` 만으로 전부 판정됐다(Ghidra·오라클·런타임 불요).
- `표기 불가` — **없음.**
- `미탐색` — **role 산문의 「배타성」 주장.** 이 게이트가 보는 것은 ①인자 수·번호 ②IR 속성 주장 ③전면 미사용 주장 ④인용 대조 넷뿐이다. 「`info.team(0x930)` **만** 읽는다」의 **「만」**은 여전히 검사되지 않는다 — `--weak` 의 오프셋 후보가 그 자리인데 8차에 29/29 오탐이 나와 기각 근거에서 뺐다. 다음 라운드가 이 축을 더 조이려면 **배타성 주장**부터다.
- `사실 서술` — P6 도달률 3%(4행/126)는 물음이 아니라 관행의 기록이다(§5).

## §10 남긴 파일

| 파일 | 무엇 |
|---|---|
| `_verify9\\B\\gate.py` | **G16 승격판**(`check_spec` 계약 동일) |
| `_verify9\\B\\patch.json` | 기계 적용분 — errors 2 · ev_up 2 · brief_errors 4 |
| `_verify9\\B\\mutate.py` | 변이 시험 11종(양성 9 + 음성 대조 2) |
| `_verify9\\B\\coverage.py` | 검사별 도달률 |
| `_verify9\\B\\mkpatch_b.py` | `old` 를 정본에서 직접 떠서 patch 생성 |
| `_verify9\\B\\simpatch.py` | patch.json 적용본도 적발 0 인지 메모리 검증 |
| `_verify9\\B\\dump.py` | role 원문·백틱 조각·인용 위치 덤프(반증용) |
'''

p = os.path.join(HERE, "REPORT.md")
io.open(p, "w", encoding="utf-8").write(BODY)
print(u"wrote %s (%d chars)" % (p, len(BODY)))
