# -*- coding: utf-8 -*-
u"""9차 배치C 산문 보고서(`REPORT.md`) 생성기. 파이프라인 산출물이라 스크립트로 낸다."""
import io, os

BODY = u'''# 9차 배치C — 축 `knobs[].value`(노브의 **값**) 게이트 수립

> 게임 0.5.8 · 2026-09-11 · 담당 221행 · 산출물 = `gate.py`(G18 후보) · `mutate.py` · `patch.json`
> 신선도: 착수 시 `FRESH` · **patch 제출 직전 재확인 `FRESH`**(도장 2026-09-11 20:24:40)

---

## 0. 한 줄

**게이트 최종 적발 15건**(값 미관측 **1** + `value` 칸 부재 **14**) · **반증으로 걸러낸 오탐 25건** ·
**변이 시험 검출력 34.3%**(104행 × 3변이 = 303) · **`patch.json` 18항목**(그중 `behavior_change` **1건**) ·
**내 지시(도시에)의 오류 5건**(+ 내 판정 반전 1건).
패치를 (모의) 적용하면 **`G13` = 0 을 유지한 채 이 축이 0** 이 된다.

---

## 1. 실제로 실행한 것 — 명령줄 그대로

```bash
cd /c/tfm2mods/MIG && python -X utf8 dossierfresh.py 9 C          # 착수·제출 직전 2회, 둘 다 FRESH
cd /c/tfm2mods/MIG/_verify9/C && python -X utf8 gate.py           # 단독 리포트
cd /c/tfm2mods/MIG/_verify9/C && python -X utf8 gate.py --detail  # 행별 관측 덤프
cd /c/tfm2mods/MIG/_verify9/C && python -X utf8 mutate.py         # 변이 시험(검출력)
cd /c/tfm2mods/MIG/_verify9/C && python -X utf8 decode_grid.py    # 13[7] 부시 격자 상수 디코드
cd /c/tfm2mods/MIG && python -X utf8 specgate.py                  # 기준선(G1~G17 전부 0)
cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python -X utf8 applypatch.py 9 --only C --dry
```

IR 원문 반증에 쓴 것(예시, 전부 `_gaibc`/`_gcbc` 직접 열람):

```bash
awk "NR>=65000&&NR<=65600 && /icmp (ult|ugt) i64/ {print NR\\": \\"$0}" _gaibc/m09.ll
awk "NR>=48310&&NR<=48346 {print NR\\": \\"$0}" _gaibc/m07.ll
awk "NR>=59275&&NR<=59290 {print NR\\": \\"$0}" _gcbc/g09.ll
sed -n "275p" _gcbc/g07.ll
```

⚠**awk 의 `\\b` 는 POSIX ERE 가 아니다.** `icmp ugt i64 %[0-9]+, 29\\b` 로 걸었더니 **매치 0건**이
나와 하마터면 「그 명령이 없다」로 결론낼 뻔했다. `\\b` 를 빼니 5건이 나왔다.
⟹ **도구의 상태를 세계의 상태로 착각**하는 `METHOD_MAP §0` 의 그 실패를 한 번 밟았다.

---

## 2. 설계 — 무엇을 신호로 썼고 왜 그것인가

### 2-a. `G13` 이 보는 것 / 안 보는 것 (출발점)

`whereline.py`(G13)는 `where` 에서 `m04.ll 44025` 꼴 **줄번호**를 뽑아 그 줄이 존재하는지 보고,
백틱 인용이 **IR 오프코드로 시작**하면 그 줄 ±2 에 그 문면이 있는지 본다. 그게 전부다.
**`value` 는 읽지도 않는다.** 게다가 인용에 `..` 나 `::` 가 있으면 통째로 건너뛴다
— 이번에 나온 유일한 실오류 `18 knobs[8]`(`(ugt .., 29)`)이 **정확히 그 구멍**에 있었다.

### 2-b. 판정식 = 반증 (8차 배치B 설계 계승)

| | 분류식 | **반증식(채택)** |
|---|---|---|
| 규칙 | 후보가 있는데 주장이 없으면 불일치 | **주장을 지지하는 관측이 0건일 때만** 불일치 |
| 관측을 늘리면 | 오탐이 **는다** | 오탐이 **준다** |

`value` 가 주장하는 정수 중 **하나라도** IR 관측에 있으면 무죄. 그래서 창을 넓히거나
리터럴 문법을 늘리는 모든 개선이 **오탐을 줄이는 방향**으로만 작용한다.
「귀속은 구제에만, 기각에는 쓰지 않는다」가 규칙이 아니라 **판정식의 성질**로 성립한다.

### 2-c. 관측원 3종 — **등급이 다르다**

| 등급 | 관측원 | 판정에서의 역할 |
|---|---|---|
| **강** | `where` 의 백틱 IR 인용의 리터럴 | **그 행의 정본.** 인용이 있으면 창으로 구제하지 않는다 |
| 중 | 앵커 줄 ±4 창의 리터럴 | 인용이 없는 행의 판정 근거 |
| 약(구제 전용) | 광역 창 ±60 · 16진 · **시프트 접힘** · **제곱거리** | 무죄 지지에만 |

★**인용을 정본으로 승격한 것이 검출력의 핵심**이다. 인용까지 구제에 쓰면 IR 어디에나 흔한
수(0·1·2·20·100)가 **무엇이든 무죄**로 만든다. 이 한 줄로 31.5% → 35.5% 가 됐고
정본 적발은 늘지 않았다(= 오탐 0 으로 얻은 검출력).

### 2-d. 「`G13` 통과를 전제로 할 것인가」 — **아니오, 그러면 안 된다**

지시가 물은 질문이다. 답은 **전제로 깔면 안 된다**. 이유가 실측으로 나왔다:
`18 knobs[6]/[7]/[8]` 은 셋 다 `where` 줄이 어긋나 있는데(7·37·117줄) **`G13` 은 0 을 내고 있다**
— 인용에 `..` 가 들어 있어 G13 이 건너뛰기 때문이다. 「G13 통과」를 전제로 삼았으면
이 세 행을 **검사 대상에서 제외**했을 것이고, 유일한 실오류를 놓쳤다.
⟹ 값 게이트는 `where` 의 정확성을 **가정하지 않고**, 값이 안 보이면 그 자체를 신호로 쓴다.
(그래서 이번 적발의 정정 대상이 `value` 가 아니라 `where` 인 것이 이상하지 않다 —
**값 축에서 본 결과, 위치가 틀렸다**는 판정이다.)

### 2-e. 규칙 4개

- **V1 값 미관측** ★주력 — 수 주장인데 인용·창 어디서도 관측 안 됨
- **V2 인용 불일치**(기본 OFF) — 인용엔 없고 창엔 있음. **실측 0건**이라 끄고 두었다
  (0 이라는 사실 자체가 2-c 의 「인용=정본」 승격을 정당화한다)
- **V3 `value` 칸 부재** — 14행. 노브는 바꿔 쓰라고 있는 칸인데 **칸이 비면 사용자가 못 고친다**
- **V4 arm 충돌** — 같은 `select`/`phi` 를 인용한 두 노브가 **같은 arm 값**을 주장

---

## 3. ★적발을 내가 반증했다 — 후보 26 중 진짜 1

**적발 건수는 게이트의 성능이 아니라 가설이다.** 판을 6번 고치며 매번 표본을 IR 원문으로 열었다.

| 판 | V1 적발 | 무엇이 틀렸나 | 고친 법 |
|---|---|---|---|
| ① 초판 | **19** | IR 리터럴 정규식이 `i64 <수>` 꼴 **타입 접두만** 셌다. LLVM 은 이항 연산 타입을 한 번만 적는다(`icmp ugt i64 %30, 4`) ⟹ **두 번째 피연산자를 통째로 못 봤다.** `00 knobs[0] value=4` 가 인용에 4 가 **적혀 있는데** 적발됐다 | 리터럴 추출을 **지워서 남기는 방식**으로 교체(SSA·메타·전역·속성·타입어·`align N`·`0x…` 제거 후 남은 정수) |
| ② | **4** | ㉠산문 값의 수를 주장으로 오독(`미열거 15개 태그는 무조건 허용` → 15) ㉡백틱을 전부 지워 **백틱 안의 줄 참조**(`_gaibc/m08.ll:94136~94353`)를 잃음 ㉢제곱거리(`200000` ↔ IR `39999999999`) | ㉠`numeric_claim`(한글 섞이면 수 주장 아님) ㉡**IR 명령인 인용만** 지움 ㉢제곱 구제 |
| ③ | **1** | — | (여기서 관측원 확장 착수) |
| ④ 본문창 폴백 | **8** | 앵커 없는 53행을 살리려고 「함수 IR 본문 전체」를 창으로 썼더니 `where` 가 **콜리**를 가리키는 행이 전부 걸렸다(`18[22]` = `serpen.rs:26` 인데 본문은 `epic.rs`) | 「같은 소스 파일일 때만」으로 1차 제한 → 여전히 3건(같은 파일 **다른 함수**) |
| ⑤ | **4** | `abstract_input.rs:134` 는 `ult`(:286~333) 본문이 아니다 — 같은 파일이라는 이유로 남의 함수 창에서 값을 찾고 있었다 | **`src_anchors`** — `!dbg` inlinedAt 사슬(`srclinecheck.chain`)로 그 소스 줄이 **실제로 이 함수 본문에 있는지** 확인. 없으면 앵커를 만들지 않는다(판정 유보). 본문창 폴백은 **폐기**(`USE_BODY=False`) |
| ⑥ 최종 | **1** | `09 knobs[2] value=4` — IR 은 `shl i128 %176, 2`(×4) 였다 | 시프트 접힘 구제(`shl k` ⟺ `×2^k`) |

**후보로 올렸다가 반증한 행 = 25개**(①의 19 + ④에서 새로 들어온 7 − 최종 1).
⛔이 중 단 하나도 「사람이 눈으로 넘긴 것」이 없다. 전부 **게이트를 고쳐서** 없앴다
(`SPEC_RUNBOOK §S5-c`: 게이트가 오탐을 내면 게이트를 고쳐라, 손으로 넘기지 마라).

### 3-b. 살아남은 1건 — `18 knobs[8]`

```
where: "m09.ll:65273 (ugt .., 29)"
m09.ll:65273 =     #dbg_value(ptr %110, !54872, !DIExpression(), !54879)
실제        : 65390  %157 = icmp ugt i64 %156, 29, !dbg !55098   (+ 65424·65459·65494·65529, 슬롯 5개 언롤)
```
**값 29 는 맞고 위치가 117줄 어긋나 있었다.** 같은 함수의 형제 2행도 같은 병이었다
(`[6]` 65212→65185, `[7]` 65301→65338). `16 knobs[8]` 도 같은 형태(g06.ll:79437 = `#dbg_value` 줄 → 79434).
⟹ **정정 4건은 `where` 에 들어갔다. 값 축에서 봤더니 위치가 틀렸다는 판정이다.**

---

## 4. ★검출력 — 변이 시험

「적발 1건」이 *축이 깨끗해서*인지 *게이트가 무능해서*인지 가르는 **유일한 수단**이다.
정본은 안 건드리고 메모리 사본의 `value` 만 틀리게 넣어 되잡는지 센다(`mutate.py`).

```
== 변이 시험 (knobs[].value 를 일부러 틀리게 넣고 되잡는가) ==
  off1  포착  33 / 104  (31.7%)     값 ±1 (자리 옮겨적기)
  x10   포착  48 / 104  (46.2%)     자릿수 밀림
  swap  포착  23 /  95  (24.2%)     ★arm 방향 뒤집힘(같은 명세 다른 노브 값으로 교체)
  합계: 포착 104 / 303 (34.3%)
```

비교 기준: 8차 `G14`(`mem.dir`) 실측 **53.1%**. 이쪽이 낮은 이유는 명확하다 —
`dir` 은 `r`/`w` 2치라 뒤집으면 반드시 반대 관측과 충돌하지만, **수는 IR 창에 이웃이 흔하다.**

### 4-b. 검출력은 **운영점 선택**이다 (광역 구제 반경 스윕)

```
W_WIDE=   0  정본적발 11   검출력 156/303 = 51.5%
W_WIDE=  10  정본적발  4   검출력 145/303 = 47.9%
W_WIDE=  20  정본적발  3   검출력 133/303 = 43.9%
W_WIDE=  40  정본적발  1   검출력 114/303 = 37.6%
W_WIDE=  60  정본적발  1   검출력 104/303 = 34.3%   ← ★채택
W_WIDE= 120  정본적발  0   검출력  89/303 = 29.4%
```

`W_WIDE=20` 의 추가 2건을 원문으로 열어 본 결과 **1건은 오탐**이었다
(`18 knobs[5]` = `Chat::Press(21)/PressChange(22)` — 태그 21/22 는 **인라인 안 된 콜리**에서 만들어지므로
그 창에 없는 게 정상. 「부재를 결함으로 세지 마라」). 나머지 1건(`11 knobs[3] value=0`)은
집합형 노브에 `0` 이 앉아 있는 **자리표시 의심**이라 판단이 갈린다.
⟹ **오탐 0 을 유지하는 가장 높은 검출력 지점인 60 을 채택**했다. 스윕표를 남기는 이유는,
다음 라운드가 「더 잡고 싶다」고 할 때 **무엇을 지불해야 하는지**가 이미 계량돼 있어야 하기 때문이다.

### 4-c. V4(arm 방향)는 **표적 시험**으로 쟀다

`swap` 변이가 24% 로 낮아 보이지만, 이 축에서 가장 값나가는 결함
— **7차에 실제로 있었던 `01 knobs[2]/[3]` 20↔40 뒤집힘** — 은 V4 가 **양방향 모두** 잡는다:

```
원본 V4: []
20↔40 뒤집음 V4: [(3, "같은 select/phi 의 같은 arm 을 두 노브가 주장", "knobs[2] 와 겹침 · arm=20 · 인용=select i1 %43, i64 20, i64 40")]
반대로 뒤집음 V4: [(3, ... arm=40 ...)]
```

V4 는 **귀속 추론을 쓰지 않는다** — `%43` 이 무슨 술어인지 해석하지 않고, 「두 노브가 같은 arm 을
주장하면 하나는 틀렸다」는 **순수 관측 모순**만 본다. 그래서 기각 근거로 써도 된다.

---

## 5. 판정 어휘로 정리한 결과

| 판정 | 대상 | 내용 |
|---|---|---|
| `실오류` | `18 knobs[6]·[7]·[8]` · `16 knobs[8]` | `where` IR 줄 어긋남(7·37·117·3줄). **값은 전부 맞다** |
| `보강` | `value` 칸 부재 **14행 전부** | IR 원문에서 값을 복원해 채웠다(§6) |
| `재료 부재` | 없음 | — |
| `표기 불가` | **없음**(← 초판 판단 `13 knobs[7]` 철회) | 「30×30 격자는 값 칸에 담을 형식이 없다」고 닫으려다, **상수를 디코드하니 한 줄로 담긴다**(`decode_grid.py`). ⟹ **판정 반전 1건 = 내 오류 1건** |
| `사실 서술` | 아래 3건 | `notes` 감. `applypatch` 에 추가 연산이 없어 산문으로만 남긴다 |
| `미탐색` | `value` 가 산문인 56행의 「값 표기 규격」 | 이 축의 게이트가 볼 수 없는 영역. 별도 축으로 세워야 한다 |

### 사실 서술(누가 `notes` 로 옮겨 주면 좋을 것)
1. `09 knobs[7]` 의 `what` 「미니언 위험 임계표 **8개 값**」 — IR 실측은 **엄격표 7개 리터럴 + 완화표 4개 = 11개**다
   (4단 × 2열로 세면 8이 되지만 1단의 상한은 리터럴이 아니라 `damage >= hp` 비교다). 「8」의 근거 미상.
2. `13 knobs[7]` 의 `effect` 「이 함수는 2~21 만 낸다」는 **함수의 치역**이지 **격자의 값역**이 아니다 —
   격자에는 1~24 가 전부 들어 있다(24 ID × 1~5칸 + 0 이 829칸).
3. `18 knobs[6]·[8]` 이 가리키는 명령은 **슬롯 5개로 언롤된 사본**이다(65185·65219·65254·65289·65324 등).
   「한 줄」을 적는 규격이 이런 행을 표현하지 못한다 — 첫 사본 + 전 사본 나열로 적었다.

---

## 6. `value` 칸 부재 14행 — 무엇으로 채웠나

전부 **IR 원문 인용**을 근거로 채웠다(`patch.json` 의 `evidence` 에 줄 원문이 들어 있다).

| 행 | 채운 값 | 근거 |
|---|---|---|
| `00[6]` | `Position → on_caster()(vt+0x118) · Direction → linear_move_speed().is_some()(vt+0xf8)` | m04.ll 44068 gep 280 → 44070 i1 호출 / 44085 gep 248 → 44087 `{i64,i64}` + 44089 `icmp eq %70, 1` |
| `01[6]` | `camp_type.__0 < 2` | m05.ll:40061 `icmp ult i64 %40, 2` |
| `05[5]` | `end_reason != 7 && team_holder_ticks == 0 && in_range_ticks == 0` | m13.ll 29015·29017·29018 의 or-게이트 |
| `05[6]` | `BigPlan::get_name() 의 반환 문자열` | 수가 아니라 문자열이 조절 대상 |
| `09[7]` | 엄격표/완화표 전 임계 | m07.ll 48321·48326~48339·48371~48378 ★**behavior_change = true** |
| `09[8]` | `tps*2 (2초)` | m15.ll 35469 gep 4856 → 35471 `shl i64 %30, 1` |
| `09[9]` | `(champion_action=true, predict_retarget=false)` | m15.ll:35472 호출 인자 리터럴 |
| `13[7]` | `30×30 u64 격자(900칸) — 0=부시 없음(829칸) / 1~24=부시 ID` | g07.ll:275 `[7200 x i8]` 디코드 |
| `16[6]` | `{0,1,2,6,8,9}` | g06.ll 87035~87042 switch case |
| `16[7]` | `100` | g06.ll 66668·66669 `add i32 %4, 100` / `select .., i32 100` |
| `16[8]` | `3(skill_cooltime) / 1(그 외)` | g06.ll:79434 `umax(.., 3)` · 79356/79386/79472/79489 `umax(.., 1)` |
| `19[5]` | `tick_per_second (1초)` | m04.ll:62501 gep 4856 |
| `19[6]` | `tps*5 (5초)` | m04.ll:62554 `mul i64 %23, 5` |
| `19[7]` | 캠프 좌표 6쌍 | g09.ll 59281/59282 phi. **4쌍이 이 명세 `history` 좌표표와 정확히 일치** |

★`09[7]` 만 `behavior_change = true` 다 — 나머지 13행은 값이 같은 행의 `what`/`where`/`effect`
또는 `history` 에 남아 있어 **재구현이 틀리지 않는다**. `09[7]` 의 임계 8개(실측 11개)는
**이 명세 어디에도 없었다**(`history` 의 확정 항목조차 「두 표 중 하나로 판정」이라고만 적었다).
⟹ 그 표를 보고 재구현하면 미니언 위험 판정이 **통째로 틀린다**.

---

## 7. ★내 지시(도시에)의 오류 — 5건 (+ 내 판정 반전 1건)

1. **§1 할 일 1·2 와 §2 가 존재하지 않는 디렉터리를 가리킨다.**
   「`MIG\\_gates\\knobs_value\\` 에 다른 배치들이 만든 검사기 후보가 모여 있다 → 전부 돌려 보고
   왜 갈리는지 규명 → 통합하라」 — `_gates\\` 아래는 `consts_kind`·`mem_dir`·`params_role`·`src_line`
   **4개뿐**이고 `knobs_value` 는 없다. 지시의 절반이 **수행 불가능**했다. 처음부터 설계했다.
2. **「221행 전량 대조」는 재료상 성립하지 않는다.** 실측 분류:
   `value` 칸 부재 14 · 산문이라 수 주장 아님 56 · 값에 정수 없음 30 · 앵커 미성립 21
   ⟹ **IR 과 실제로 대조되는 행 = 104행(47%)**. 「전량」을 목표로 잡으면 나머지 117행에
   억지 판정을 내게 된다(= 8차 `G14` 가 「부재」로 46건을 헛되이 낸 그 실패).
3. **`applypatch.py` 계약에 「없는 키를 채우는」 연산이 없다.** 실측으로 `old: null` 이 동작하지만
   (`apply_error` 의 `conv(old, cur=None)` 경로), **`new` 가 문자열이면 `already()` 가 대상(None)을 못 찾아
   `tgt = spec` 으로 폴백해 명세 전체를 훑고, 그 문자열이 다른 칸에 우연히 있으면 「이미 적용」으로
   조용히 버린다.** 실제로 겪었다 — 첫 `--dry` 가 `정정 15/15 (이미 적용 2)` 를 냈고,
   버려진 2건은 `01[6]`·`05[5]`(내가 그 행의 `where` 에서 문면을 옮겨 적었으니 당연히 겹친다).
   문자열을 길게 바꿔 회피했지만 **이 도구의 존재 이유(조용한 no-op 금지)를 뚫는 다섯 번째 구멍**이다.
4. **`ev_up[]` 에 「IR 원문 대조」 등급이 없다.** `EVMARK` 는 1 런타임 · 2 오라클 · 3 tcx 뿐이라,
   이번 근거(전부 `.ll` 원문 인용)를 `ev_up` 으로 내면 **하지도 않은 「오라클 실행 확증」**이 본문에 박힌다.
   ⟹ `ev_up` 을 비웠다. 계약에 IR 등급 표기가 필요하다.
5. **§4 가 게이트 건수를 본문에 박아 두는데 이미 어긋나 있다.** 도시에는 `G12 src_line 대조=8` 인데
   같은 세션의 `python -X utf8 specgate.py` 는 **G12=0**, 그리고 목록에 없는 **G16 params.role 대조=0**
   이 이미 붙어 있다. `SPEC_RUNBOOK §S5-b` 가 「★게이트 건수를 이 문서에 적지 마라 — 기준선은
   숫자가 아니라 명령이다」라고 못 박는데 **도시에 생성기가 그 규칙을 어긴다**
   (8차 배치A 가 적발한 「수치가 셋 다 다름」의 재발).

**+ 판정 반전 = 내 오류 1건**: `13 knobs[7]` 을 `표기 불가`(30×30 격자를 값 칸에 담을 형식 없음)로
닫으려다, 상수를 실제로 디코드하니 **한 줄로 담겼다**. 「담을 형식이 없다」가 아니라
**내가 상수를 안 열어 본 것**이었다 — `METHOD_MAP §0` 의 「가진 재료의 한계를 문제의 한계로 착각」
그대로다. 유저 확정 규칙대로 **오류 1건으로 센다.**

---

## 8. 산출물 · G18 승격 시 할 일

```
MIG/_verify9/C/gate.py          <- G18 후보. check_spec(sp) = (knobs 인덱스, 사유, 상세) 리스트
MIG/_verify9/C/mutate.py        <- 변이 시험(검출력). gate.py --mutate 로도 호출
MIG/_verify9/C/decode_grid.py   <- 13[7] 부시 격자 상수 디코더
MIG/_verify9/C/mkreport.py      <- 이 보고서 생성기
MIG/_verify9/C/patch.json       <- 18항목(실오류 4 · 보강 14) · behavior_change 1 · brief_errors 5
MIG/_verify9/C/REPORT.md        <- 이 문서
```

`check_spec(sp)` 시그니처는 `whereline.py`(G13)·`kindchk.py`(G15)와 **동일**하므로
`specgate.gate13` 을 그대로 베껴 `gate18` 을 붙이면 된다(⛔`specgate.py` 는 MIG 루트 도구라
이 배치가 손대지 않았다). 승격 시:

- 파일명을 **`knobval.py`** 로(루트에 `gate.py` 라는 이름은 너무 일반적이다)
- `MIG` 상수를 `HERE` 로(루트로 올라가면 `srclinecheck` 가 같은 폴더다)
- `python -X utf8 mktools.py` 를 돌려 `TOOLS.md` 재생성
- `SPEC_RUNBOOK §S5-b` 표에 한 줄 — **단 건수는 적지 말 것**(§7-5 가 그 실례다)

### 한계 (다음 라운드가 알아야 할 것)

- 검사 가능 행 **104/221(47%)**. 나머지는 값이 산문이거나 앵커가 안 선다.
- 검출력 **34.3%** — ±1 변이가 가장 안 잡힌다(31.7%). 창 안에 이웃 수가 흔하다.
- **콜리 안에서 쓰이는 값·접힌 상수는 원리적으로 안 보인다.** 그걸 결함으로 세지 않는 것이 이 설계다.
  (`USE_BODY = False` 로 남긴 `body_anchors` 가 그 유혹의 기록이다 — 켜면 오탐 3~5건이 돌아온다.)
- `value` 에 한글이 섞이면 V1 이 유보한다(56행). **「값 칸 표기 규격」은 아직 아무 게이트도 안 보는 축**이다.
'''

io.open(os.path.join(os.path.dirname(os.path.abspath(__file__)), "REPORT.md"),
        "w", encoding="utf-8").write(BODY)
print(u"REPORT.md %d chars" % len(BODY))
