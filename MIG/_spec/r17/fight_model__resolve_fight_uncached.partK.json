{
 "name": "resolve_fight_uncached",
 "sym": "_RNvNtNtNtCshdEBA0ozCnw_7game_ai11plan_legacy3old11fight_model22resolve_fight_uncached",
 "src": "game-ai\\src\\plan_legacy\\old\\fight_model.rs",
 "src_line": 378,
 "ir_file": "m10.ll",
 "ir_from": 43974,
 "ir_to": 47451,
 "aux": [
  {
   "ir_file": "m12.ll",
   "ir_from": 7283,
   "ir_to": 7295,
   "what": "closure#2(L421) 가장 가까운 적 min_by_key — Map::fold 래퍼(s0_0). Copied::fold(m11) 로 위임"
  },
  {
   "ir_file": "m11.ll",
   "ir_from": 19044,
   "ir_to": 19190,
   "what": "closure#2(L421) Copied<Iter<&Entity>>::fold 본체(s0_0) — 키 = (|x1-x2|^2+|y1-y2|^2) · min_by_key compare(동률이면 먼저 것 유지). exe 0xe33570/0xe33700(391B) 짝 후보"
  },
  {
   "ir_file": "m12.ll",
   "ir_from": 7298,
   "ir_to": 7310,
   "what": "closure#4(L431) 적 기준 가장 가까운 아군 min_by_key — Map::fold 래퍼(s2_0)"
  },
  {
   "ir_file": "m11.ll",
   "ir_from": 19193,
   "ir_to": 19339,
   "what": "closure#4(L431) Copied::fold 본체(s2_0) — s0_0 과 문자 단위 동일 구조(키 = dist^2)"
  },
  {
   "ir_file": "m12.ll",
   "ir_from": 22433,
   "ir_to": 22552,
   "what": "closure#5::{closure#0}(L450) (0..our_n).max_by_key(|i| near_allies[i].stat_cached.hp) fold(s3_00) — 동률이면 뒤의 것 채택 · near_allies[i] 경계검사 포함"
  },
  {
   "ir_file": "m12.ll",
   "ir_from": 17275,
   "ir_to": 17434,
   "what": "closure#6/#7(L454) (0..our_n).filter(|i| our_alive[i]).max_by_key(|i| near_allies[i].stat_cached.hp) fold(s4_0/s5_0) — our_alive[i](5칸 경계검사)·near_allies[i] 경계검사 포함"
  }
 ],
 "one_line": "교전 예측 순수 코어: 아군·적 각 ≤5명의 EHP·DPS·CC·도착틱을 만들어(오판 노이즈 포함) 6초 창을 틱 단위로 전개하고 승패 라인(Commit/CommitAfterJoin/Disengage/Hold)·net_value·focus/soaker/rescue 를 FightPrediction(64B) 으로 낸다",
 "signature": {
  "tcx": "fn(usize, &'_ game_core::OperationData<'_, '_>, &'_ game_core::Entity, &'_ [&'_ game_core::Entity], &'_ [&'_ game_core::Entity], i8, std::option::Option<&'_ game_core::Entity>, usize, &'_ [i64], i64) -> game_ai::plan_legacy::old::FightPrediction",
  "abi": "internal fastcc · IR 인자 15 = sret 1 + 소스 인자 10 이 (data 가 ArgumentPromotion 으로 2슬롯, 슬라이스 3개가 팻포인터 2슬롯씩) 확장. exe 0xe083c0 도 15 인자(argscan 스택 11 + 레지스터 4)로 IR 과 같다(지시문 승계 · 이 배치에서 재확인한 것은 IR 쪽 15슬롯). 호출자는 exe 콜그래프에 없음(resolve_fight_full 캐시 미스 경로에서 인라인 호출로 추정 — 미확인)",
  "params": [
   {
    "i": 0,
    "name": "(sret) out",
    "type": "*mut FightPrediction(64B)",
    "role": "`%0` — sret out-ptr. define 속성 `dead_on_unwind noalias noundef nonnull writable writeonly align 8 captures(none) dereferenceable(64)`(m10.ll:43974). 배치 K 범위에서는 L381 조기 반환만 여기에 쓴다(writes 참조). 정상 경로 기록은 배치 L(L515~521)",
    "note": "레이아웃 = signature.return"
   },
   {
    "i": 1,
    "name": "version",
    "type": "usize",
    "role": "`%1` — AI 버전 게이트. L392 `version > 1` 로 시드 생성 방식을 가른다(>1 = 집합 해시 · ≤1 = 틱 버킷). 그 외엔 fight_check 콜리(self_sustain_in_window/available_cc_in_window/fight_dps) 에 그대로 전달. IR 속성 `noundef` 만",
    "note": "reach version=2 접기에서 %58(=version>1) → 1 로 접혀 L402~403 이 사장"
   },
   {
    "i": 2,
    "name": "data",
    "type": "&OperationData(24B)",
    "role": "ArgumentPromotion 으로 두 슬롯으로 승격: `%2` = data.cache(&AbstractGameWithCache) — +0x0 `game` 팻포인터(data ptr, vtable)만 읽어 vtable 슬롯 0x20 seed()(L400) / 0x28 tick()(L402·NA) 를 호출한다. `%3` = data.context(&GameContext) — +0x8 setting → GameSetting+0x12f8 tick_per_second 를 읽고(L402·L415) 콜리(self_sustain_in_window/fight_dps/expected_dps) 에 전달한다. define 속성: %2 `readonly captures(address_is_null)` · %3 `captures(address, read_provenance)`(readonly 없음 · 동작상 읽기만). data.blackboard(+0x10) 은 승격되지 않았다 = 이 함수가 안 읽는다",
    "note": "DILocalVariable `data` arg 2 · `context` 는 L379 지역(let context = data.context)"
   },
   {
    "i": 3,
    "name": "champ",
    "type": "&Entity(1728B)",
    "role": "`%4` — 판정 주체 챔피언. +0x5c0 id 를 시드에 섞고(L400/L403), L431 에서 적의 최근접 아군이 없을 때(near_allies 가 비는 일은 L380 에서 배제되므로 실질 도달 불가) unwrap_or 기본값으로 쓴다. define 속성 `noalias noundef nonnull readonly align 8 captures(address, read_provenance) dereferenceable(1728)`",
    "note": ""
   },
   {
    "i": 4,
    "name": "near_allies",
    "type": "&[&Entity]",
    "role": "`%5`(data_ptr) / `%6`(len) — 아군 후보 슬라이스(나 포함 여부는 호출자 소관). 앞 5명만 소비(take(5)). 원소 Entity 의 +0x5c0 id · +0x628 stat_cached.hp · +0x660 x · +0x668 y · +0x670 hp 를 읽는다. define 속성 %5 `noalias noundef nonnull readonly align 8 captures(address, read_provenance)` · %6 `noundef range(i64 0, 2^60)`",
    "note": "len==0 이면 L381 조기 반환(Hold)"
   },
   {
    "i": 5,
    "name": "near_enemies",
    "type": "&[&Entity]",
    "role": "`%7`(data_ptr) / `%8`(len) — 적 후보 슬라이스. 앞 5명만 소비(take(5)). 원소 +0x5c0 id · +0x660 x · +0x668 y · +0x670 hp 를 읽는다. define 속성 %7 `noalias noundef nonnull readonly align 8 captures(address, read_provenance)` · %8 `noundef range(i64 0, 2^60)`",
    "note": "len==0 이면 L381 조기 반환(Hold)"
   },
   {
    "i": 6,
    "name": "committed_dir",
    "type": "i8",
    "role": "`%9` — 히스테리시스용 직전 커밋 방향(_docs: dir=0 없음·-1 이탈 커밋). 배치 K 범위(L0~464)에선 참조 없음 · 배치 L(L465~521) 에서 읽는다. define 속성 `noundef`",
    "note": "(배치 L) 소비처 서술 필요"
   },
   {
    "i": 7,
    "name": "tower",
    "type": "Option<&Entity>",
    "role": "`%10` — 교전에 끼어드는 타워(None=null 포인터, 니치). L449 에서 Some 이면 tower_dps = expected_dps(context, tower, 최대 stat_cached.hp 아군) 를 계산해 배치 L 의 틱 전개에 넘긴다. define 속성 `noalias noundef readonly align 8 captures(address, read_provenance) dereferenceable_or_null(1728)`",
    "note": ""
   },
   {
    "i": 8,
    "name": "judge_accuracy",
    "type": "usize",
    "role": "`%11` — 판단 정확도(0~1000 가정). `> 999` 이면 오판 노이즈 없음, 아니면 EHP·DPS 마다 error_ratio_noise(&mut jrng, judge_accuracy) 배율(%)을 곱한다(L406 misjudge). define 속성 `noundef`",
    "note": "999 비교는 dbg 없는 호이스트 명령(%93) — 소스는 L406 클로저"
   },
   {
    "i": 9,
    "name": "arrivals",
    "type": "&[i64]",
    "role": "`%12`(data_ptr) / `%13`(len) — 아군 i 의 도착 틱(창 시작 기준). L420 에서 our_arrive[ai] = arrivals.get(ai).copied().unwrap_or(0) 로만 읽는다(len 이 짧으면 0=즉시 도착). define 속성 %12 `noalias noundef nonnull readonly align 8 captures(none)` · %13 `noundef range(i64 0, 2^60)`",
    "note": "arrivals[ai] 값은 L463 arrived 클로저(our_arrive[i] <= t)에서 소비"
   },
   {
    "i": 10,
    "name": "baseline",
    "type": "i64",
    "role": "`%14` — 절대 판정 대비 기준값(_docs: baseline 0 = 절대 판정). 배치 K 범위(L0~464)에선 참조 없음 · 배치 L 에서 net_value 판정에 읽는다. define 속성 `noundef`",
    "note": "(배치 L) 소비처 서술 필요"
   }
  ],
  "return": "sret FightPrediction(64B · tcxdict game_ai::plan_legacy::old::FightPrediction · align 8) — 0x00 focus_target@tag(8B · 0=None/1=Some) · 0x08 focus_target@Some.0(usize · None 이면 미기록=죽은 슬롯) · 0x10 soaker@tag · 0x18 soaker@Some.0 · 0x20 rescue_ally@tag · 0x28 rescue_ally@Some.0 · 0x30 net_value(i64) · 0x38 line(FightLine u8: 0 Commit/1 CommitAfterJoin/2 Disengage/3 Hold · Direct) · 0x39 line_absolute(FightLine u8) · 0x3a~0x3f 패딩 6B(어느 경로도 안 씀). ★배치 K 의 유일한 기록 경로 = L381 조기 반환: tag 3개 = 0(None) · net_value = 0 · line = line_absolute = 3(Hold) — 살아있는 바이트 = 0x00~0x07 · 0x10~0x17 · 0x20~0x27 · 0x30~0x39 (합 34B), 0x08/0x18/0x28 페이로드와 0x3a~0x3f 는 미기록. 정상 경로의 live 표는 배치 L(L515~521) 담당",
  "mut_self": "없음 — &mut 인자 0개(sret 제외). 힙 쓰기 0. 모든 상태는 스택 지역(our_hp[5] %37 · our_dps[5] %36 · our_alive[5] %35 · our_arrive[5] %34 · their_hp[5] %32 · their_dps[5] %31 · their_alive[5] %30 · their_id[5] %29 · t %28 · jrng %38 · 배치 L 배열 %18~%23·%26)",
  "tls": "없음 — 함수 전체(m10.ll:43974~47451) 에 `LocalKey`/`call_once`/`llvm.threadlocal.address`/`@anon…call_once` 상수 참조 0건. rnd(StdRng) 인자도 없고 유일한 난수원은 시드에서 만든 NoiseRng jrng(8B 지역) 이다"
 },
 "reads": [
  {
   "base": "AbstractGameWithCache(data.cache, %2)",
   "offset": "0x0",
   "name": "game.data_ptr",
   "note": "L400·L402 — &dyn AbstractGame 팻포인터의 데이터 포인터"
  },
  {
   "base": "AbstractGameWithCache(data.cache, %2)",
   "offset": "0x8",
   "name": "game.vtable_ptr",
   "note": "L400·L402"
  },
  {
   "base": "AbstractGame vtable",
   "offset": "0x20",
   "name": "seed()",
   "note": "L400 (version>1) — divtable: ExpectedGame::AbstractGame::seed. 반환 i64 게임 시드"
  },
  {
   "base": "AbstractGame vtable",
   "offset": "0x28",
   "name": "tick()",
   "note": "L402 (version≤1 · NA) — divtable: ExpectedGame::AbstractGame::tick"
  },
  {
   "base": "GameContext(%3)",
   "offset": "0x8",
   "name": "setting",
   "note": "L402·L415 — &GameSetting"
  },
  {
   "base": "GameSetting",
   "offset": "0x12f8",
   "name": "tick_per_second",
   "note": "L402(bucket 분모 · NA)·L415(horizon_ticks = tps*6)"
  },
  {
   "base": "Entity(champ %4)",
   "offset": "0x5c0",
   "name": "id",
   "note": "L400·L403 시드 xor 재료"
  },
  {
   "base": "Entity(near_allies[i])",
   "offset": "0x5c0",
   "name": "id",
   "note": "L395 집합 해시 · L455 soaker_id"
  },
  {
   "base": "Entity(near_enemies[j])",
   "offset": "0x5c0",
   "name": "id",
   "note": "L398 집합 해시 · L435 their_id[j]"
  },
  {
   "base": "Entity(near_allies[i])",
   "offset": "0x628",
   "name": "stat_cached.hp",
   "note": "L450 타워 대상 max_by_key 키 · L454 soaker max_by_key 키 (aux m12 클로저 안 gep 1576)"
  },
  {
   "base": "Entity(near_allies / near_enemies)",
   "offset": "0x660",
   "name": "x",
   "note": "L421·L431 최근접 거리 dx"
  },
  {
   "base": "Entity(near_allies / near_enemies)",
   "offset": "0x668",
   "name": "y",
   "note": "L421·L431 최근접 거리 dy"
  },
  {
   "base": "Entity(near_allies[i])",
   "offset": "0x670",
   "name": "hp",
   "note": "L423 ehp = hp + self_sustain_in_window"
  },
  {
   "base": "Entity(near_enemies[j])",
   "offset": "0x670",
   "name": "hp",
   "note": "L432 ehp = hp + self_sustain_in_window"
  },
  {
   "base": "arrivals(&[i64] %12)",
   "offset": "0x0 + ai*8",
   "name": "arrivals[ai]",
   "note": "L420 — ai < len 일 때만(get) · 아니면 0"
  }
 ],
 "writes": [
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x0",
   "name": "focus_target@tag",
   "value": "0 (None)",
   "note": "L381 조기 반환(m10.ll:44120) — near_allies 또는 near_enemies 가 빈 경우"
  },
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x10",
   "name": "soaker@tag",
   "value": "0 (None)",
   "note": "L381 (m10.ll:44122)"
  },
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x20",
   "name": "rescue_ally@tag",
   "value": "0 (None)",
   "note": "L381 (m10.ll:44124)"
  },
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x30",
   "name": "net_value",
   "value": "0",
   "note": "L381 (m10.ll:44119)"
  },
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x38",
   "name": "line",
   "value": "3 (FightLine::Hold)",
   "note": "L381 (m10.ll:44117)"
  },
  {
   "base": "FightPrediction(sret %0)",
   "offset": "0x39",
   "name": "line_absolute",
   "value": "3 (FightLine::Hold)",
   "note": "L381 (m10.ll:44126). 0x08/0x18/0x28 페이로드·0x3a~0x3f 패딩은 이 경로에서 미기록"
  }
 ],
 "constants": [
  {
   "value": 1,
   "src_line": 392,
   "meaning": "version > 1 (icmp ugt) — 시드 생성 분기. ≤1 = 틱 버킷 시드(NA · version<2 전용) / >1 = 집합 해시 시드. 표기 불가: `>1` 과 `>=2` 외연 동일 (qcspec shl 경고 = `shl i64 %74, 1`/`shl nuw nsw i64 %8, 3` 과 문자 겹침 · 이 값은 비교 상수 — 접힌 `tps*2` 는 별도 항목 folded_from=2)"
  },
  {
   "value": 3,
   "src_line": 381,
   "meaning": "FightLine::Hold 메모리 태그(tcxdict --enum FightLine: idx3 discr3 tag3). 조기 반환의 line·line_absolute (qcspec shl 경고 = `shl nuw nsw i64 %6, 3`(슬라이스 len*8 stride · L394/L397)와 겹친 오탐 · 이 값은 시프트량이 아니라 태그)"
  },
  {
   "value": 5,
   "src_line": 394,
   "meaning": "take(5) 상한 — 아군/적 각 최대 5명만 모델링(L394·L397 해시 루프 · L419 아군 루프 · L430 적 루프 · L420/L425 5칸 배열 경계검사). 팀 정원과 같은 값이라 노브가 아니다"
  },
  {
   "value": -7046029254386353131,
   "src_line": 395,
   "meaning": "0x9E3779B97F4A7C15 (황금비 64bit 상수) — set_h ^= ally.id * C (wrapping). 시드 집합 해시 재료 · 판정값 아님"
  },
  {
   "value": 5871781006564002453,
   "src_line": 398,
   "meaning": "0x517CC1B727220A95 — set_h ^= enemy.id * C (wrapping). 아군과 다른 상수로 진영 구분 · 판정값 아님"
  },
  {
   "value": 17,
   "src_line": 400,
   "meaning": "rotate_left(set_h, 17) (`llvm.fshl.i64`) — seed = game.seed() ^ rotl(set_h,17) ^ champ.id"
  },
  {
   "value": 1,
   "folded_from": 2,
   "src_line": 402,
   "meaning": "NA(version<2 전용): bucket = game.tick() / max(tps*2, 1) — `shl i64 %74, 1` 로 접힌 tps*2(=2초 버킷)"
  },
  {
   "value": 1,
   "src_line": 402,
   "meaning": "NA(version<2 전용): umax(tps*2, 1) — 0 나눗셈 방지 하한 (shl 경고 사유 = L402 의 `shl i64 %74, 1` 은 옆 항목(folded_from=2)이 담당 · 이 값은 umax 하한)"
  },
  {
   "value": 40,
   "src_line": 403,
   "meaning": "NA(version<2 전용): seed = champ.id ^ (bucket << 40)"
  },
  {
   "value": 6,
   "src_line": 415,
   "meaning": "horizon_ticks = tick_per_second * 6 — 교전 예측 창 6초(틱). 창=0 이면 L440~443 CC 보정 생략 (qcspec shl 경고 = 레지스터 `%6`(near_allies.len) 오탐 · 이 값은 `mul i64 %83, 6` 의 곱수)"
  },
  {
   "value": 999,
   "src_line": 406,
   "meaning": "misjudge: judge_accuracy > 999 이면 노이즈 없이 v 그대로(dbg 없는 호이스트 %93 · 클로저 정의 L405~406). 표기 불가: `>999` vs `>=1000`"
  },
  {
   "value": 100,
   "src_line": 406,
   "meaning": "misjudge: v * error_ratio_noise(&mut jrng, judge_accuracy) / 100 (sdiv) — 노이즈가 백분율 배율"
  },
  {
   "value": 1000,
   "src_line": 425,
   "meaning": "our_hp[i] = misjudge(ehp * 1000) (L425) · their_hp[j] = misjudge(ehp * 1000) (L434) — EHP 를 DPS 단위와 맞추는 ×1000 스케일(틱 전개에서 dps 누적과 비교 · 배치 L)"
  },
  {
   "value": 1,
   "folded_from": 2,
   "src_line": 440,
   "meaning": "h/2 (`ashr exact i64 %85, 1`) — CC 합이 창을 깎는 상한 = 창의 절반(L440 their_eff · L441 our_eff)"
  },
  {
   "value": 1,
   "src_line": 440,
   "meaning": "their_eff = smax(h - smin(h/2, our_cc_sum), 1) · our_eff = smax(h - smin(h/2, their_cc_sum), 1) — 유효시간 하한 1틱(0 나눗셈·0 DPS 방지) (shl 경고 사유 = `ashr exact i64 %85, 1` 은 옆 항목(folded_from=2)이 담당 · 이 값은 smax 하한)"
  }
 ],
 "calls": [
  "game_ai::fight_check::self_sustain_in_window",
  "game_ai::fight_check::available_cc_in_window",
  "game_ai::fight_check::fight_dps",
  "game_ai::fight_check::expected_dps",
  "game_ai::utils::error_ratio_noise",
  "core::iter::adapters::copied::Copied::next",
  "core::iter::adapters::map::Map::fold",
  "core::iter::traits::iterator::Iterator::min_by_key",
  "core::iter::traits::iterator::Iterator::max_by_key",
  "core::panicking::panic_bounds_check",
  "llvm.fshl.i64",
  "llvm.umax.i64",
  "llvm.smin.i64",
  "llvm.smax.i64",
  "llvm.memset.p0.i64"
 ],
 "logic": "// fight_model.rs:0~464 (배치 K)\n// 인자: version, data(&OperationData → %2=data.cache · %3=data.context), champ, near_allies(&[&Entity]), near_enemies(&[&Entity]), committed_dir(i8), tower(Option<&Entity>), judge_accuracy, arrivals(&[i64]), baseline(i64) → sret FightPrediction(64B)\n// 콜리 계약(자식 명세 별도): self_sustain_in_window(version,&GameContext,&Entity,horizon)->i64(창 내 자기 힐+실드 1회분) · available_cc_in_window(version,&Entity,horizon)->i64(창 내 CC 틱 합) · fight_dps(version,&GameContext,att,tgt)->i64 · expected_dps(&GameContext,tower,tgt)->i64 · error_ratio_noise(&mut NoiseRng,j)->i64 (m04.ll:49631 본문 확인: e=(1000-j)/20(udiv) · state+=0x9E3779B97F4A7C15 · splitmix64 믹스 · mulhi(mix, 2e+1) - e + 100 ⇒ [100-e, 100+e] 균등 정수 백분율)\n\nL379: let context = data.context;                                             // %3 (승격 슬롯)\nL380: if near_allies.is_empty() || near_enemies.is_empty() {                // %40 = len_a==0, %41 = len_e==0, or\nL381:   return FightPrediction { focus_target: None, soaker: None, rescue_ally: None, net_value: 0, line: Hold(3), line_absolute: Hold(3) };  // 0x0/0x10/0x20 ← 0, 0x30 ← 0, 0x38/0x39 ← 3 · 페이로드/패딩 미기록 → 배치 L(줄 521) ret\n}\n\n// ── 시드 (L392~403) ──\nL392: let seed: u64 = if version > 1 {                                       // %58 = icmp ugt %1, 1 (reach: 항상 true 로 접힘)\nL394:   let mut set_h: u64 = 0;\nL394:   for a in near_allies.iter().take(5) {                                // %97 루프(카운트 5↓ · end 포인터)\nL395:     set_h ^= a.id.wrapping_mul(0x9E3779B97F4A7C15);                    // Entity+0x5c0\n        }\nL397:   for e in near_enemies.iter().take(5) {                               // %128 루프\nL398:     set_h ^= e.id.wrapping_mul(0x517CC1B727220A95);\n        }\nL400:   game.seed() ^ set_h.rotate_left(17) ^ champ.id                        // vtable+0x20 seed() · llvm.fshl 17 · champ+0x5c0 (xor 순서: (seed ^ rotl) ^ id)\n      } else {                                                               // NA(version<2 전용) — reach version=2 접기로 사장\nL402:   let bucket = game.tick() / max(tps * 2, 1);                          // vtable+0x28 tick() · GameSetting+0x12f8 · `shl 1` · umax\nL403:   champ.id ^ (bucket << 40)\n      };\nL392: let mut jrng = NoiseRng(seed);                                          // %38 ← seed 그대로(믹스 없음 · ai_interface.rs:177 인라인)\n\n// ── 오판·DPS 클로저 (L405~412) ──\nL405: let misjudge = |v: i64| -> i64 {\nL406:   if judge_accuracy > 999 { v } else { v * error_ratio_noise(&mut jrng, judge_accuracy) / 100 }   // %93 호이스트 · sdiv 100 · 호출마다 jrng 전진\n      };\nL411: let dps_of = |att: &Entity, tgt: &Entity| -> i64 {\nL412:   fight_dps(version, context, att, tgt)\n      };\n\n// ── 창·아군 표 (L415~425) ──\nL415: let horizon_ticks: i64 = tps * 6;                                       // GameSetting+0x12f8 (version≤1 경로는 L402 에서 읽은 값 재사용)\nL416: let mut our_hp = [0i64; 5]; let mut our_dps = [0i64; 5]; let mut our_alive = [false; 5]; let mut our_n = 0usize; let mut our_cc_sum = 0i64;\nL418: let mut our_arrive = [0i64; 5];\nL419: for (ai, a) in near_allies.iter().copied().take(5).enumerate() {         // %33 = {ptr,end,take=5,idx} · Copied::next 아웃오브라인\nL420:   our_arrive[ai] = arrivals.get(ai).copied().unwrap_or(0);              // %152 = ai < arrivals.len · 5칸 경계검사(%909, take(5) 로 실질 불발)\nL421:   let tgt = near_enemies.iter().copied().min_by_key(|e| { let dx=|a.x-e.x|; let dy=|a.y-e.y|; dx*dx+dy*dy });   // aux s0_0 · 첫 원소 next 뒤 fold · 동률=먼저 것\nL422:   let dps = tgt.map(|t| dps_of(a, t)).unwrap_or(0);                     // near_enemies 비면 0(L380 으로 실질 불발) · fight_dps(version, context, a, t)\nL423:   let ehp = a.hp + self_sustain_in_window(version, context, a, horizon_ticks);   // Entity+0x670\nL424:   our_cc_sum += available_cc_in_window(version, a, horizon_ticks);\nL425:   our_hp[our_n] = misjudge(ehp * 1000); our_dps[our_n] = misjudge(dps); our_alive[our_n] = true; our_n += 1;   // 노이즈 호출 순서: hp → dps · 5칸 경계검사(%953/%955)\n      }                                                                       // our_n == min(len_a, 5) · ai == our_n 항상\n\n// ── 적 표 (L427~435) ──\nL427: let mut their_hp = [0i64; 5]; let mut their_dps = [0i64; 5]; let mut their_alive = [false; 5]; let mut their_n = 0usize;\nL428: let mut their_id = [0usize; 5]; let mut their_cc_sum = 0i64;\nL430: for e in near_enemies.iter().take(5) {                                  // %158 루프(end 포인터 · 카운트 5↓)\nL431:   let nearest_a = near_allies.iter().copied().min_by_key(|a| dist2(a, e)).unwrap_or(champ);   // aux s2_0 · (near_allies 비면 champ — L380 으로 실질 불발)\nL432:   let ehp = e.hp + self_sustain_in_window(version, context, e, horizon_ticks);\nL433:   their_cc_sum += available_cc_in_window(version, e, horizon_ticks);\nL434:   their_hp[their_n] = misjudge(ehp * 1000); their_dps[their_n] = misjudge(dps_of(e, nearest_a));   // 노이즈 순서: hp → dps · fight_dps(version, context, e, nearest_a)\nL435:   their_alive[their_n] = true; their_id[their_n] = e.id; their_n += 1;\n      }                                                                       // their_n == min(len_e, 5) ≥ 1\n// ⇒ jrng 소비 순서(judge_accuracy ≤ 999 일 때만): 아군 i=0..our_n (hp, dps) → 적 j=0..their_n (hp, dps) · 최대 20회\n\n// ── CC 로 유효시간 보정 (L437~443) ──\nL437: if horizon_ticks != 0 {                                                 // %168 = icmp eq %85, 0 → 건너뜀\nL440:   let their_eff = max(h - min(h / 2, our_cc_sum), 1);                   // h = horizon_ticks · smin/smax(i64) · ashr 1\nL441:   let our_eff   = max(h - min(h / 2, their_cc_sum), 1);\nL442:   for j in 0..their_n { their_dps[j] = their_dps[j] * their_eff / h; }    // sdiv · 5칸 언롤(%303,%879~%897)\nL443:   for i in 0..our_n   { our_dps[i]   = our_dps[i]   * our_eff   / h; }    // 5칸 언롤(%317,%854~%872) · our_n==0 이면 통째 생략(%316)\n      }\n\n// ── 타워·소커 (L449~455) ──\nL449: let tower_dps: i64 = tower.map(|t| {\nL450:     (0..our_n).max_by_key(|&i| near_allies[i].stat_cached.hp)            // aux s3_00 · Entity+0x628 · 동률=뒤의 것 · our_n==0 → None\nL451:       .map(|i| expected_dps(context, t, near_allies[i]))                 // near_allies[i] 경계검사(%246)\n          }).flatten().unwrap_or(0);                                          // tower None(null) 또는 our_n==0 → 0\nL454: let soaker: Option<usize> = (0..our_n).filter(|&i| our_alive[i]).max_by_key(|&i| near_allies[i].stat_cached.hp);   // aux s4_0/s5_0 · 첫 alive 원소는 인라인 언롤(%263~%295), 나머지는 fold 호출\nL455: let soaker_id: Option<usize> = soaker.map(|i| near_allies[i].id);         // Entity+0x5c0 · 경계검사(%329)\n\n// ── 틱 전개 준비 (L459~464) ──\nL459: let mut t: i64 = 0; let mut our_dead_v: i64 = 0; let mut their_dead_v: i64 = 0; let mut first_focus: Option<usize> = None;   // t=%28(스택 · 클로저가 &t 캡처)\nL461: let horizon = horizon_ticks;\nL461: loop {                                                                  // 헤더 %765 — 루프 캐리: our_dead_v(%766) · their_dead_v(%767) · 첫 반복 플래그(%768) · first_focus(%769/%770) · soaker(%771/%772). 종료 조건·역방향 엣지(%761 ← L499/L500/L501)는 → 배치 L(줄 465~521)\nL463:   let arrived = |i: usize| -> bool { our_arrive[i] <= t };              // %27 = {&our_arrive, &t}\nL464:   let our_dps_sum: i64 = (0..our_n).map(|i| if our_alive[i] && arrived(i) { our_dps[i] } else { 0 }).sum();   // 5칸 언롤(%398~%431) · our_dps[i] 는 루프 진입 전 호이스트 로드(%378~%382 — 루프 안에서 our_dps 불변) · our_n==0 이면 0(%331)\n        // → 배치 L(줄 465): their_dps_sum 등 이어짐 (%439)\n      }\n// L465~521 = 배치 L (틱 전개 · 사망 처리 · focus/rescue · line 판정 · sret 기록)",
 "knobs": [
  {
   "what": "교전 예측 창 길이(초)",
   "where": "fight_model.rs:415",
   "value": 6,
   "effect": "올리면 더 긴 시간의 DPS 누적을 가정해 체력 우위(탱커)보다 DPS 우위가 커지고, 자기힐/CC 창(self_sustain/available_cc)도 함께 넓어진다. 내리면 순간 EHP 비중이 커진다"
  },
  {
   "what": "오판 노이즈 비활성 임계",
   "where": "fight_model.rs:406",
   "value": 999,
   "effect": "judge_accuracy 가 이 값을 넘으면 EHP·DPS 에 노이즈를 곱하지 않는다. 낮추면(예: 900) 900~999 구간도 무노이즈가 된다"
  },
  {
   "what": "CC 가 유효시간을 깎는 상한(창의 1/N)",
   "where": "fight_model.rs:440",
   "value": 2,
   "effect": "h/2 → h/N. N 을 키우면 CC 합이 아무리 커도 상대 DPS 를 덜 깎는다(CC 가치 하락), 줄이면(1) CC 만으로 상대 DPS 를 0 근처까지 깎을 수 있다(하한 1틱)"
  },
  {
   "what": "유효시간 하한(틱)",
   "where": "fight_model.rs:440",
   "value": 1,
   "effect": "their_eff/our_eff 의 최소값. 올리면 CC 로 깎이는 DPS 의 바닥이 올라간다"
  }
 ],
 "unknown": [
  "L461 루프의 종료 조건(t >= horizon 인지, 전멸 break 인지)과 t 증가 위치 — 배치 L 범위(줄 465~521)라 이 조각에서 확정 안 함. 헤더 %765 에는 조건 분기가 없다(루프 꼬리 %761 에서 되돌아옴)",
  "%768(첫 반복 true 플래그 · 루프 캐리)의 소스 표현 — 배치 L 의 L477 에서 소비(`br i1 %768`). K 범위에선 정의만 보인다",
  "%26(48B)·%18~%23(32~40B) 지역 배열의 정체 — 배치 L 의 틱 전개용(추정: 대상별 누적 피해/사망 시각). K 범위에선 gep 만 준비되고 접근이 없다",
  "L419 아군 루프에서 arrivals.len() < ai 인 경우 0 으로 채우는 것이 소스의 unwrap_or(0) 인지 get().copied().unwrap_or(0) 인지 — 외연 동일(표기 불가)",
  "L464 `our_alive[i] && arrived(i)` 의 두 조건 순서 — column 0 이라 같은 줄 안 순서 복원 불가(표기 불가 · 부작용 없어 외연 동일)",
  "misjudge(L406) 의 `>999` vs `>=1000` · L392 `>1` vs `>=2` — 표기 불가(외연 동일)",
  "seed 의 xor 결합 순서(L400) — IR 은 (game.seed() ^ rotl(set_h,17)) ^ champ.id 이지만 xor 결합법칙으로 소스 순서는 복원 불가(외연 동일)",
  "L402~403(version≤1 시드) 은 reach version=2 접기에서 사장(NA) — 내용은 적었으나 현행 판정에는 안 쓰인다",
  "exe 인자 배치(스택 11+레지스터 4)는 지시문 승계값이며 이 배치에서는 argscan 을 다시 돌리지 않았다(IR 15슬롯만 재확인)",
  "closure#3(L422 `|t| dps_of(a,t)`)·closure#8(L455)·closure#9(L463)·closure#10/#11(L464)·closure#5::{closure#1}(L451) 은 전부 본문에 인라인(fnparts: DWARF 257 vs define 12) — 별도 aux 없음"
 ]
}
