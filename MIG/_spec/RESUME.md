# 이어받기 — IR 명세 트랙 (2026-09-10 저녁 시점)

> 세션이 끊겼다면 **이 파일부터 읽어라.** 대화 맥락 없이 이어갈 수 있게 쓴 것이다.

## 지금 무엇을 하고 있었나

**방향 전환이 있었다.** 손포팅(judge 계층 재구현, 4일에 51함수)은 주력에서 내려왔고,
**SDK 의 `game_ai` rlib 을 모드에 직접 링크**해서 640함수를 통째로 갖는 쪽으로 간다.
그러면 "바꿀 것만 이해하면" 되지만, 유저 요구는 **753개를 거의 다 파악하는 것**이라
그걸 위해 **IR 독해 → 함수 명세** 파이프라인을 만들고 속도를 실측 중이었다.

- rlib 링크 정본 = `REPORT\tfm2_ai_adjust\RE\2026-09-09_★비트코드-링크-대체-타당성-....md`
  + 메모리 `tfm2-agent-link` (모드2 한 경기 완주 확인 · 트윈 get_input 99.974~99.978%)
- ⚠잔차 원인은 **미확정(보류)**. 다만 §5f 로 "끝단 비교는 방법 자체가 무효"가 확정돼 있어
  (게임이 같은 리플레이를 두 번 돌려도 29,549틱 vs 22,720틱으로 갈림) **모드2 진행을 막지 않는다.**

## 완료된 것 (커밋 `8abd618`)

- 도구 5종 = `distruct.py` / `qcspec.py` / `speccmp.py` / `knobscan.py` / `pick20.py` + `SPEC_GUIDE.md`
- **1차 배치 `_spec/r1/` 20개 · QC 전량 통과** · 벽시계 11분 · 개당 평균 8분 26초
- 분량 실측: game_ai 소스함수 **753개 / IR 507,482줄** (`corpus2.py`)
- 퍼콜레이션 연구 = **판정 (나) 안 넘는다** (`percolate.py` · 지문 단독 대조군이 더 낫다)

## 중단 지점 — 2차 배치가 돌고 있었다

`_spec/r2/` 에 **같은 20개를 독립 재현** 중이었다. 목적은 결과물이 아니라 **정확도 측정**:
`qcspec.py` 는 *지어낸 것*만 잡고 *빠뜨린 것*은 못 잡는데, 독립 2회 대조가 그걸 드러낸다.

### 끊겼을 때 확인·재개 절차

```bash
cd /c/tfm2mods/MIG
ls _spec/r2 | wc -l                                   # 몇 개나 완료됐나 (목표 20)
PYTHONIOENCODING=utf-8 python qcspec.py _spec/r2       # 완료분 무결성
PYTHONIOENCODING=utf-8 python speccmp.py _spec/r1 _spec/r2            # 일치도
PYTHONIOENCODING=utf-8 python speccmp.py _spec/r1 _spec/r2 --detail "" # 불일치 상세
```

- **r2 가 20개 미만이면** 빠진 것만 다시 돌리면 된다. 대상 목록 = `spec_batch1.json`
  (각 항목의 `ir_file`/`ir_from`/`ir_to` 가 담당 범위. `sym` 필드는 **믿지 마라 — 메타데이터 id 가 들어간 버그**,
   심볼은 `ir_from` 줄의 `define` 에서 직접 읽을 것.)
- 재의뢰 프롬프트 형식은 아래 「2차 의뢰 문안」 그대로.
- ⚠**r1 을 보여주면 안 된다.** 독립 재현이 아니게 된다.

### 2차 의뢰 문안 (그대로 복사해 쓸 것)

```
TFM2 game_ai IR 독해. **먼저 C:\tfm2mods\MIG\SPEC_GUIDE.md 를 끝까지 읽어라** —
재료·형식·함정·자체검사가 다 있다. 특히 §1의 distruct.py(오프셋 사전)를 반드시 쓸 것. 한국어로 작성·보고.

담당: <함수명> · 소스 <src:line> · IR C:\tfm2mods\_gaibc\<m??.ll> <from>~<to>줄
출력 = C:\tfm2mods\MIG\_spec\r2\<파일명>.json
자체검사 = cd /c/tfm2mods/MIG && PYTHONIOENCODING=utf-8 python qcspec.py _spec/r2/<파일명>.json → [PASS] 까지.

⚠독립 재현 실험이다. 다른 사람이 같은 함수를 이미 명세했지만 일부러 안 보여준다 —
두 결과의 일치도로 정확도를 잰다. _spec/r1/ 을 절대 열지 마라. 네가 IR에서 직접 읽은 것만 써라.

보고에 막힌 지점과 체감 소요, 그리고 distruct.py 가 실제로 도움이 됐는지를 포함할 것.
```

## 다음 결정 지점 (유저와 상의할 것)

1. **2차 일치도를 보고 3차가 필요한지** 판단 — 일치도가 낮으면 지침·양식에 결함이 있다는 뜻
2. **거대 함수 56개 중 1개 시범** — 이게 전체 일정 추정의 최대 불확실성(분량의 54%를 차지)
3. 2차 결과가 안정적이면 **대량 배치**(100개 단위)로 진도

## 일정 추정 (1차 실측 기반 · 미확정)

`distruct.py` 로 60~70% 병목이 줄 것을 보수적으로 40% 절감으로 잡으면
에이전트시간 **약 98시간** → 20개 병렬이면 벽시계 **5~6시간**.
⚠거대 함수 56개(2000줄 초과)의 개당 35분 추정이 가장 약하다. 하나 재보면 확정된다.

## 절대 잊지 말 것

- 명세 작성자에게 **`unknown` 은 필수 필드**다. 모르는 걸 적는 게 감점이 아니다.
- `qcspec.py` 가 사실을 지우도록 강요하는 결함이 4개 있었다(전부 수정). 새 결함이 보이면
  **관대한 쪽으로** 고쳐라 — 검사기의 오탐은 작성자가 진짜 사실을 빼게 만든다.
- `speccmp.py` 의 일치도는 **하한**이다. 둘 다 같은 걸 놓치면 100% 로 나온다.
