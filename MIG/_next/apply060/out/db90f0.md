# db90f0→faa440 update (LineGankerPlan::update)

## logic_060
```
fn update(&mut self, _version, _rnd, player, data, goal_data, _positioning_score, _debug)
// ganker.rs:39. 반환값 없음 — self.chats / self.phase / self.low_hp_cancel / self.arrived_tick 만 바꾼다.   // ★0.6.0 신규 필드 2

team = player.info.team // PlayerState+0xa00, team<2 아니면 panic_bounds_check(ganker.rs:43)   // ★0.6.0 +0x930→+0xa00
pos = player.info.position.as_index() // PlayerState+0xa90, entity.rs:580, range 0..5             // ★0.6.0 +0x9c0→+0xa90
cache = data.cache // OperationData+0x0
champ = cache.player_champion[team][pos].unwrap() // cache+0x1e0 + team*40 + pos*8, None 이면 panic(ganker.rs:43)

// ganker.rs:44
max_hp = champ.stat_cached.hp // Entity+0x628 (0 이면 div_by_zero panic)
hp_ratio = champ.hp * 100 / max_hp // Entity+0x670

// ganker.rs:47 — 취소 사유 ①
if hp_ratio < 41 {
 self.chats.push(Chat::Cancel(CancelReason::LowHpSelf)) // ganker.rs:48, 태그17/사유0 (u16 0x11 을 ptr[len*0x18] 에 store)
 self.phase = LineGankerPhase::Cancel // self+0xad = 8                                            // ★0.6.0 +0x29→+0xad
 self.low_hp_cancel = true // self+0xaf = 1                                                        // ★0.6.0 신규 플래그(소비처 미발견 — next_plan faa7c0 에 없음)
 return
}

// ganker.rs:54 — bush = self.target_bush_v30(player, data) [전량 인라인]
line = self.line // self+0xab, 0=Top/1=Mid/2=Bottom                                               // ★0.6.0 +0x28→+0xab
context = data.context // OperationData+0x8
tower = cache.<line>_tower[team].or(cache.<line>_tower2[team]) // (cache+0x180+line*0x20)[team] .or( (cache+0x190+line*0x20)[team] ) ganker.rs:249

if tower.is_none() { // ganker.rs:250
 bush = match line { // ganker.rs:251
 Top => if team==0 {2} else {16} // 252
 Mid => if team==0 {4} else {17} // 253
 Bottom => if team==0 {9} else {21} // 254
 }
} else {
 t = tower.unwrap() // ganker.rs:258
 is_tower_variant = (t.ty 판별자 == 2) // Entity+0x68 == EntityType::Tower
 match line { // ganker.rs:261
 Top => { // ganker.rs:263 info = t.ty.Tower 페이로드(Entity+0x70)
 if !is_tower_variant { unreachable!() } // ganker.rs:274
 if t.ty.is_tower2() { // ganker.rs:264 → Entity+0x128 > 4
 bush = if team==0 {3} else {6} // 265
 } else if info.nearest_enemy.is_some() { // 267, Entity+0x88 != 0
 bush = if team==0 {6} else {3} // 268 ← 좌우 반전
 } else {
 bush = if team==0 {3} else {6} // 270
 }
 }
 Mid => {
 if t.ty.is_tower2() { // ganker.rs:278 (판별자==2 && Entity+0x128>4)
 // ganker.rs:279 — map_regions::is_top_side(context, champ.x, champ.y)
 // ry = context.setting.height(+0x12c0) - champ.y ; 술어 = (ry < champ.x) = !is_top_side
 if ry_lt_x /* = !is_top_side */ {
 bush = if team==0 {13} else {18} // 282
 } else {
 bush = if team==0 {8} else {12} // 280
 }
 } else {
 // ganker.rs:285 — 여기만 team 에 무관
 bush = if ry_lt_x /* = !is_top_side */ {14} else {11}
 }
 }
 Bottom => { // ganker.rs:293
 if !is_tower_variant { unreachable!() } // ganker.rs:304
 if t.ty.is_tower2() { // 294
 bush = if team==0 {15} else {20} // 295
 } else if info.nearest_enemy.is_some() { // 297
 bush = if team==0 {20} else {15} // 298 ← 좌우 반전
 } else {
 bush = if team==0 {15} else {20} // 300
 }
 }
 }
}

// ganker.rs:55
map = context.map // GameContext+0x20 (MapDef)
cy = min(champ.y / 32000, 29)
cx = min(champ.x / 32000, 29)
champ_bush = map.bushes[cy][cx] // MapDef+0x1c98 + cy*0xf0 + cx*8, [30][30] usize (w3 는 이 격자를 region-id 격자로 부름 — 같은 표)

// ganker.rs:56 — 취소 사유 ②
if champ_bush == bush {
 if self.arrived_tick == 0 { self.arrived_tick = game.tick() } // self+0x38 (vtable+0x28)      // ★0.6.0 신규: 부시 도착 tick 1회 기록(next_plan faa7c0 이 소비)
 // ganker.rs:57
 if !goal_data.has_near_line_enemy(self.line, map) {   // 콜리 v3: dcc680→ff0c10 (goal_data, line, map) 3인자 · 본체 동치
 self.chats.push(Chat::Cancel(CancelReason::TargetMissing)) // ganker.rs:58, 태그17/사유2 (u16 0x211)
 self.phase = LineGankerPhase::Cancel // self+0xad = 8   (low_hp_cancel 은 건드리지 않음)
 }
}
// ganker.rs:62 return
```

## changes
- self 레이아웃: line `+0x28→+0xab` · phase `+0x29→+0xad` · 신규 `+0x38 arrived_tick`(0=미설정) · 신규 `+0xaf low_hp_cancel`(bool).
- PlayerState team/position `+0x930/+0x9c0 → +0xa00/+0xa90`.
- ganker.rs:48 뒤: `self.low_hp_cancel = true` 추가(저HP 취소 시에만 · TargetMissing 취소는 안 세움).
- ganker.rs:56 부시 도착 확인 직후(has_near_line_enemy 호출 전): `if self.arrived_tick == 0 { self.arrived_tick = game.tick() }` 추가.
- 콜리 has_near_line_enemy `dcc680→ff0c10`(동치).
- 취소 판정(hp<41 · 부시 표 · is_top_side 술어 · 좌우 반전) 전부 0.5.8 과 동일. version 게이트 없음.

## verified
- Ghidra 0.6.0 `FUN_140faa440` 전문 디컴 확인: `hp<0x29` → chat 0x11 · `[self+0xad]=8` · `[self+0xaf]=1` / 부시 표(2,16/4,17/9,21 · 3,6 · 13,18/8,12 · 14/11 · 15,20) 및 `(height−y) < x` 술어 / `map+0x1c98` 격자 클램프 29 / `[self+0x38]==0 → vt+0x28 → store` / `FUN_140ff0c10(goal_data, line, map)` → chat 0x211 · phase 8.
- 미확인: `+0xaf` 의 소비처(RE 배치 G 도 미발견) · ff0c10 내부는 RE 판정(동치)만 신뢰.

## confidence
A — 소형 함수 전문 디컴이 RE 배치 G §3 과 줄 단위로 일치.
