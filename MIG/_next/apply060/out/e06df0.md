# e06df0→ee5e80 resolve_fight_stake

## logic_060
```
fn resolve_fight_stake(version, rnd, data, player, champ, near_allies, near_enemies, committed_dir, tower, judge_accuracy, debug) -> FightPrediction
// 인자 레지스터: rcx sret · rdx version · r8 rnd · r9 data · [rsp+0x28] player · [+0x30] champ · [+0x38/+0x40] near_allies ptr/len · [+0x48/+0x50] near_enemies ptr/len · [+0x58] committed_dir · [+0x60] tower · [+0x68] judge_accuracy · [+0x70] debug
// ★v2 사장: [L574] version<2 → resolve_fight_full(.., bias, &[], 0) 한 번 — v3 에서 도달 불가

// ── ★0.6.0 bias: 플레이어 성향 → 저울 편향 ─────────────────────────
bias: i8 = if player.aggr(+0x49d) != 0 { 1 } else if player.def(+0x49e) != 0 { -1 } else { 0 }   // ★0.6.0 신규 · -(def byte) 로 계산(def=1 → 0xff)

// ── remaining = '교전에 묶인' 아군(나 제외) ─────────────────────
[L582~585] remaining: Vec<&Entity, &Bump> = near_allies.iter().copied()
             .filter(|a| {                      // ★0.6.0 closure e11e30(구 ca4900) · rnd 인자 DAE
[L584]         a.id(+0x5c0) != champ.id(+0x5c0)  // 자기 자신 제외(같으면 즉시 false)
               && fight_model::ally_is_bound(version, rnd, data, player, a, near_enemies, debug)   // 콜리 v3: ee3000(구 e04c60)
             })
             .collect_in(data.context(+0x8).pool(+0x0))

// ── ★0.6.0 arrivals: 포킹취약 아군은 합류 지연 6초+1틱 ─────────────
tps = data.context.setting(+8).tps(+0x12f8)
arrivals: Vec<i64, &Bump> = near_allies.iter().map(|a|
             if ee3c80(version, ctx=data.context, a, near_enemies /*ptr,len*/) { tps*6 + 1 } else { 0 }   // ★0.6.0 신규 콜리 v3: ee3c80 「포킹취약」 (아래 계약)
          ).collect_in(pool)            // dc0f90 grow · near_allies 와 같은 인덱스

// ── 절대 판정 ───────────────────────────────────────────────
[L586] absolute = resolve_fight_full(version, data, champ, near_allies, near_enemies,
                                     committed_dir, tower, judge_accuracy, bias, &arrivals, baseline=0)   // ★0.6.0 +bias 12번째 · arrivals=&arrivals(구 &[]) · 콜리 v3: ee4420(구 e05450 · ABI +bias)

// ── 경로 B: 묶인 아군이 없으면 절대 판정 그대로 ─────────────────
[L587] if remaining.len(+0x18) == 0 { drop(remaining, arrivals); return absolute }

// ── 경로 C: 차분 저울 ─────────────────────────────────────────
[L591] abandon = resolve_fight_full(version, data, champ,
                                    allies = remaining(ptr +0x0, len +0x18),   // 묶인 아군만
                                    near_enemies, committed_dir = 0,           // 히스테리시스 없음
                                    tower, judge_accuracy, bias = 0, &[], baseline=0)   // ★0.6.0 bias 0 고정 · arrivals 빈 슬라이스(ptr 8, len 0)
[L592~593] diff = resolve_fight_full(version, data, champ, near_allies, near_enemies,
                                     committed_dir, tower, judge_accuracy, bias, &arrivals,   // ★0.6.0 bias · &arrivals
                                     baseline = abandon.net_value(+0x30))
[L594] diff.line_absolute(+0x39) = absolute.line(+0x38)
[L595] if diff.line(+0x38) != absolute.line(+0x38) {
[L597]   diff.rescue_ally(+0x20 tag=1 / +0x28 id) = remaining.iter()
             .min_by_key(|a| ( abs_diff(a.x(+0x660), champ.x)² + abs_diff(a.y(+0x668), champ.y)², a.id(+0x5c0) ))   // 키 = (dist², id) · 동거리면 id 작은 쪽 · 동일키 first-wins
             .map(|a| a.id(+0x5c0))
       }
       // ★0.6.0 디버그(ctx.+0x3b != 0): arrivals>0 개수 등을 1413f0790 로그 — 부작용 없음
[L599] drop(remaining, arrivals); return diff

※ absolute/diff 는 같은 arrivals 를 받고 abandon 만 (bias 0, &[]). 순서 absolute → abandon → diff 동일. 부작용은 rnd/debug 가 ally_is_bound 로 &mut 전달되는 것 + 디버그 로그뿐.

// ── 콜리 v3 계약: ee3c80(version, ctx, ally, enemies_ptr, enemies_len) -> bool 「포킹취약」(Ghidra 디컴 요약 · ee0800 세부 미독) ──
//   version<2 → false ; enemies 비면 false
//   reach = ally.attack(+0x4c0 tag != -1) ? ally.+0x4a0(range) + ally.+0x438 + (ally.level(+0x5c8)−1)*ally.+0x4a8 : 0
//   dmin = min over enemies distance(ally, e)(1660a00 isqrt) ; dmin <= reach → false   (사거리 안이면 취약 아님)
//   dps = Σ_e ee0800(…)(적 DPS 추정) ; dps <= 0 → false
//   ttd = ally.hp(+0x670)*1000 / dps ; dur = max(3, ally.attack_action(+0x570 vt+0x80).duration*100 / max(1, ally.+0x3fc + 100))
//   return ttd < dur                                                   // 한 번 때리는 시간 안에 죽는다 = 포킹취약
```

## changes
- 신규 `bias`(aggr +0x49d → 1 / def +0x49e → −1 / 0) — absolute·diff 에 전달, abandon 은 0.
- 신규 `arrivals[i] = ee3c80(version, ctx, near_allies[i], near_enemies) ? 6·tps+1 : 0` — absolute·diff 의 arrivals 인자(구 `&[]`).
- resolve_fight_full ABI: 12번째 `bias: i8` 삽입(arrivals ptr/len, baseline 이 뒤로 밀림).
- 디버그 로그 1건 추가(ctx+0x3b).
- L582/L587/L594~597 동치 · PlayerState 오프셋 +0x49d/+0x49e 신규 사용.
- 콜리 v3: `ee4420` resolve_fight_full(+bias) · `e11e30` 필터 클로저 · `ee3000` ally_is_bound · `ee3c80` 신규 · `dc0f90` Vec grow.

## verified
- Ghidra 0.6.0 ee5e80 디컴: bias 계산(`1 : -(def)`) · v<2 경로 인자 `(…, bias, 8, 0, 0)` · ee3c80 루프와 `tps*6+1` · absolute(bias, arrivals ptr/len, 0) · abandon(remaining, committed 0, bias 0, ptr 8/len 0, 0) · diff(baseline = abandon+0x30) · +0x39 ← +0x38 · rescue min_by (dist², id) fold(교체 조건 `!(cur_d ≤ new_d)`/동거리 `!(cur_id ≤ new_id)`) · +0x20=1/+0x28=id · 디버그 게이트 ctx+0x3b.
- ee3c80 디컴 골격(위 계약) — ee0800 본체·인자 미독(Ghidra 인자 복원 실패).
- 미확인: resolve_fight_full ee4420 내부에서 bias/arrivals 가 어떻게 쓰이는지(별도 명세).

## confidence
A — 본체 전 분기·오프셋 디컴 확인. ee3c80 내부 산식만 B.
