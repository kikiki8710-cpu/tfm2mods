# e7b9f0→f28320 end_check

## logic_060
```
fn end_check(version, rnd, player, data, line, debug) -> bool   // lib.rs:1211
ctx = data.context; cache = data.cache; game = cache.game(&dyn AbstractGame)
L1212: if !rule_scope::line_exists(ctx, line) { return false }           // 인라인: tutorial(ctx+0x38) ≤ 8 && 마스크 (Top 0x185 / Mid 0x1b1 / Bottom 0x1ab) >> tutorial & 1
L1216: strat = player.strategy(rnd, game)  (sret Strategy 24B)   // 인라인: game.is_solorank()(vt +0xe8) ? player.strategy(+0x568 · 24B) : game.strategy(team)(vt +0x108) · game_finish = 바이트 +0x15 · ★0.6.0 PS +0x4f8→+0x568(game_finish +0x50d→+0x57d)
// ★0.6.0 신설 프렐류드(f2838a~f2847e · line_exists 뒤 · 본문 앞) — 마무리 경주: 「총 마무리 시간 + 전략별 여유 < 수비 최소 도착」이면 즉시 true
if version >= 3 {                                                          // v3 ⇒ 항상
  if let Some(r) = finish_race(player, data) {                             // 콜리 v3: fe1ad0(&out, player, data) · out: tag@0(1=Some) · +8 min_defender_arrival · +0x10 total · +0x18 defender_kill_time
     let margin = match strat.game_finish(+0x15) { Aggressive(2) => tps, Flexible(1) => tps*2, _ /*Stable(0)*/ => tps*3 };   // tps = setting(+0x8)+0x12f8
     if r.total.saturating_add(margin) < r.min_defender_arrival { return true; }
  }
}
match strat.game_finish(+0x15) {   // (strategy 를 본문에서 한 번 더 조회 — 동일 값)

== Stable (tag 0) → end_check_stable(player, data, line)  lib.rs:1279~1302 ==
L1279: state = build_game_finish_check_state(player, data, line)?   // None(+68==2) → return false · 콜리 v3: f27910(구 e7b0b0)
L1280: required_allies = min(ctx.tutorial.player_count(), 4)   // 표 3a8cf78[tutorial] (구 143a8cf78 ↔ 33ef840 값 일치) · None/Total=5→4 · Line=4 · First/Bottom=2 · TopSolo/MidSolo/JungleOnly=1 · MidBottom=3
L1282: if (state.line_tower_alive(raw u8 +65) != state.has_enemy_twin_tower(raw u8 +64)) || state.line_tower_alive { return false }
        // 불 대수상 line_tower_alive || has_enemy_twin_tower 와 동치
L1286: if !(state.live_ally_count >= required_allies && state.healthy_ally_55_count >= state.live_ally_count && state.near_player_ally_count == state.live_ally_count && state.stable_pushed_line) { return false }
L1298: match state.live_enemy_count { 0 => return true,
L1302:   1 => return state.near_enemy_nexus_healthy_ally_55_count >= required_allies,
         _ => return false }

== Flexible (tag 1) → end_check_flexible(player, data, line)  lib.rs:1225~1271 ==
team = player.info.team(+0xa00); enemy = 1 - team; pos = player.info.position(+0xa90) as usize
L1225: if !(game.tick() > setting.tower_attack_disable_tick(+0x13f8)) {
L1226:   (t1, t2) = cache.tower(line, enemy)   // (top|mid|bottom)_tower[enemy] = cache+0x180/+0x1a0/+0x1c0 + enemy*8, _tower2 = +0x190/+0x1b0/+0x1d0
L1230:   if !(t1.is_none() && t2.is_none()) { return false } }
L1234: enemy_nexus = cache.nexus[enemy](+0x170+enemy*8).unwrap()   // None → unwrap_failed 패닉
L1235~1236: live_enemy_count = cache.iter_champions(enemy).count()   // player_champion[enemy] 의 Some 개수(5칸 완전 언롤)
L1237: has_enemy_twin_tower = if game.tick() > setting.tower_attack_disable_tick { false } else {
L1238:     cache.twin_towers[enemy].len()(+0x148+enemy*0x20) != 0 }
L1243: champ = cache.player_champion[team][pos].unwrap()   // None → unwrap_failed 패닉
L1244: near_ally = cache.iter_champions(team).filter(|a| a.distance_sq(champ) < 1440000000001).count()   // 자기 자신 포함(dist 0) · 0x14f46b04001
L1247: if near_ally != cache.iter_champions(team).count() { return false }   // 생존 아군 전원이 1,200,000 안에 모여야 함(movmskps 언롤)
L1253: ok_ally = cache.iter_champions(team).filter(|a| a.hp*100 / a.stat_cached.hp > 39).count()   // stat_cached.hp==0 → div_by_zero 패닉
L1254: epic_remain = game.get_game_mode().as_moba().map_or(0, |m| m.remain_epic_time(team))   // = MobaMode.epic_minion_buff_time[team](+0x240+team*8)
       if epic_remain < setting.tick_per_second * 20 {
L1260:   if live_enemy_count == 0 { return true }  if has_enemy_twin_tower { return false }
       } else {
L1255:   cond = (ok_ally >= live_enemy_count.saturating_sub(1) && blackboard[team].minion_state(line).minion_count(+0x20 · 라인블록 +0x0/+0x28/+0x50) > 5) || live_enemy_count == 0
         if cond { return true }  if has_enemy_twin_tower { return false }
       }
L1266~1269: required_edge = 표 3a8cee8[tutorial] = match player_count { 2 => 1, 3 => 2, _ => 3 }   // (구 33ef7b0 값 일치)
L1271: near_nexus = cache.iter_champions(team).filter(|a| a.distance_sq(enemy_nexus) < 360000000001).count()   // 0x53d1ac1001
       return near_nexus >= required_edge + live_enemy_count

== Aggressive (tag 2) → end_check_aggressive(player, data, line)  lib.rs:1307~1342 ==
L1307: if !(game.tick() > setting.tower_attack_disable_tick) {
L1308:   (t1,t2) = cache.tower(line, 1-team); if !(t1.is_none() && t2.is_none()) { return false } }
L1311: state = build_game_finish_check_state(player, data, line)?   // None → false · f27910
L1312: required_group = 표 3a8cf30[tutorial] = min(player_count, 3)   // (구 33ef7f8 값 일치)
L1314: if state.line_tower_alive { return false }
L1318: if !(state.healthy_ally_50_count >= required_group && state.near_finish_line_healthy_ally_50_count >= required_group && state.aggressive_pushed_line) { return false }
L1326: if !(state.live_enemy_count != 0 && state.healthy_ally_50_count < state.live_enemy_count + 2) { return true }   // 적 전멸 또는 건강 아군이 적+2 이상
L1334: if !state.has_enemy_twin_tower {
L1335:   if state.healthy_ally_50_count > state.live_enemy_count && state.near_enemy_nexus_healthy_ally_50_count >= required_group { return true } }
L1340: if state.healthy_ally_50_count > state.live_enemy_count && state.has_epic_buff {
L1342:   return state.near_finish_line_healthy_ally_50_count >= min(ctx.tutorial.player_count(), 4) }   // 표 3a8cf78
       return false
}

rnd: 본문 gen_range 0회(strategy 인라인에서도 미사용). version 은 프렐류드 게이트에만. debug 미사용.
```

## changes
- 신설 프렐류드(line_exists 뒤): `if v>=3 { if let Some(r)=finish_race(player,data) { margin = Aggr?tps : Flex?2tps : 3tps; if r.total.saturating_add(margin) < r.min_defender_arrival { return true } } }`.
- 본문 3전략 동일. 오프셋: PS +0x930/+0x9c0→+0xa00/+0xa90 · Strategy +0x4f8→+0x568(game_finish +0x57d) · 표 33ef840/7f8/7b0→3a8cf78/cf30/cee8 · bb stride 0x5c8(라인블록 +0x28/+0x50 유지).
- 콜리 v3: state 빌더 e7b0b0→f27910 · 신규 fe1ad0(finish_race) · strategy 인라인 vt 0xe8/0x108.

## verified
- Ghidra 0.6.0 f28320 디컴 전문: line_exists 마스크 0x185/0x1b1/0x1ab · `2 < version` → fe1ad0(&local_a0, player, data) · tag==1 · vt 0xe8/0x108 또는 +0x568 → game_finish 바이트(+0x578>>0x28 = +0x57d) · `!=2 → (==1 ? tps*2 : tps*3)` · CARRY8 saturating_add · `< min_defender_arrival(+8)` → true · 이후 Stable(f27910 · +68==2 · 표 3a8cf78 · live_enemy 0/1) · Flexible(+0x13f8 · 타워 슬롯 0x180~0x1d0 · 0x14f46b04001 · movmskps 전원집결 · hp%>39 · vt 0x40 → +0x240+team*8 · tps*20 · +0x20>5 · 표 3a8cee8 · 0x53d1ac1001) · Aggressive(표 3a8cf30 · +2 · 표 3a8cf78).
- finish_race fe1ad0 내부 · f27910 내부 미대조(RE 배치 E 「테이블 값 일치·본문 동일」 채택).

## confidence
A — 프렐류드 디컴 확인 · 본문 3전략 디컴 대조(상수·표·오프셋 일치).