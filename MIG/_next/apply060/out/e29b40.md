# e29b40→100c2f0 action_eval::evaluate_action

## logic_060
```
fn evaluate_action(version, ctx, parameter, rnd, player, data, action, debug) -> Option<i64>   // 100c2f0
L77: let Anchor::Lane{line} = ctx.anchor (ctx+0x10 태그 1 · line = ctx+0x11) else → None
L78: idx = action.tag(+0xb1) > 2 ? tag-3 : 6 ; if (0xfc33 >> idx) & 1 → None   // ★0.6.0 마스크 0x1f863→0xfc33(AroundHide 제거 재번호) · 통과 = {Around(idx2), AroundRegion(3), AroundPosition(6), AroundPositionBush(7), AroundBush(8), LaneMinionPosition(9)} · None = RunAway·Recall·AroundRunAway·Positioning·Trace·Attack~Stop
L81/L89: return Some(lane_positioning_score(version, player, data, parameter, action, line) + aggressive_gain - defensive_penalty)   // ★0.6.0 항 2개 추가(아래)

── lane_positioning_score (인라인 L98~130) ──
L98 : champ = player_champion[team(+0xa00)][pos(+0xa90)].unwrap()  (None 이면 panic)   // ★0.6.0 +0x930/+0x9c0→+0xa00/+0xa90
L99 : purpose = line_phase_position_eval_purpose(player, data)  (i8 0..8 · 인라인: game_mode(ctx+0x38 u8)∈{0,5,7,8}(0x1a1 마스크) && tick >= max(0, setting.+0x8a8 - 30·tps) ? 8 : ((pos==1) as i8)<<3)
L100: dest = action.evaluation_position(_, player, data)                     [Option<(x,y)> sret 24B · e5d4e0]
L103: dest_score = dest.map(|(x,y)| position_eval_at(version, player, data, x, y, purpose))   [Option<PositioningScore> 56B · 니치 +0x31==2] · 콜리 v3: ff5ec0
L104: hp_value = champion_hp_value(data, parameter, &parameter.player(+0x918))   // db0400 (TLS 캐시 · 키 seed(vt+0x20)/tick(vt+0x28)/player.id(+0x970)) · ★0.6.0 parameter.player +0x918
L105: hp = champ.hp(+0x670).max(1)
L107~108: base_damage = dest_score.map(|s| hp * s.risk.max(0) / 100).unwrap_or(parameter.player.risk_damage(+0x998))
L109~112: possible_damage = parameter.player.possible_risk(data, 9999)[ff1fc0] + dest_score.map(|s| hp * s.tower_risk.max(0) / 100).unwrap_or(parameter.player.risk_possible_tower(+0x9b0)) / 3
L113~116: danger = if champ.stat_buff_cached.undying(+0x488) { 0 } else { base_damage*hp_value/hp + possible_damage*hp_value/hp }   (sdiv 각각)
L124~125: positional_gain = dest_score.map(|s| (s.gain - s.gain_me).max(0) * hp_value / 200).unwrap_or(0)
L127: (ax,ay) = dest.unwrap_or((champ.x, champ.y))
L128: last_hit_gain = lane_anchor_gain(data, player, parameter, ax, ay, line)
L130: return positional_gain - danger + last_hit_gain

── lane_anchor_gain (인라인 L141~259) — 0.5.8 동일 (db1950 TLS 메모 · 아래 요약) ──
L141: champ = player_champion[..]? (None→0) ; L142: atk = champ.attack_effect(+0x490, tag +0x4c0 != -1)? (None→0)
L143: move_speed = champ.stat_cached.move_speed.max(1) ; L144: start_tick = atk.start_timing*100 / champ.attack_speed_mult().max(1) ; L145: tps = ctx.setting.tick_per_second
L155: (n, entries) = LAG_MEMO.with(|c| … )  [aux: 키 (game.seed(vtable+0x20), game.tick(vtable+0x28), player.info.id, line) 일치 → 캐시 반환 · 불일치 → iter_minions(1-team) 를 visible_from(champ.team)&&Minion&&line 으로 걸러 (tx,ty,attack_range,dmg,tid,bonus) 를 최대 48개 저장 · 48 초과면 n=-1]
  attack_range = champ.stat_buff_cached.range(+0x438) + atk.range(+0x4a0) + (champ.level-1)*atk.growth_range(+0x4a8) + atk.range_adjust(champ, target)[Effect vt+0x108 · ★0.6.0 구 +0xe8] + radius(champ) + radius(target)
  dmg = atk.expected_damage_target(ctx, champ, target)[1643790 · ★0.6.0 구 12857f0] ; bonus = target.nearest_enemy 가 (아군 팀 && Tower/Nexus) 면 80 아니면 0
L197: if n == -1 (캐시 오버플로) →  L200~231 직접 순회: for target in iter_minions(1-team):
    L201 !target.is_visible_from(&champ.team) → skip ; L204 ty≠Minion → skip ; L207 line≠ → skip
    L210 attack_range(위 식) ; L211 walk_dist = distance(ax,ay,target).saturating_sub(attack_range) ; L212 walk_tick = walk_dist/move_speed ; L213 impact_tick = walk_tick+start_tick ; L214 dmg = expected_damage_target
    L216 s = 0 ; if let Some(snap)=parameter.wave_snapshot && let Some(traj)=snap.find(target.id):
        L217 predicted_hp = traj.hp_at_tick(impact_tick)
        L218 if predicted_hp <= 0            → s = (traj.expected_death_tick > tps*2) ? 0 : 25      (L222)
             elif predicted_hp <= dmg+5     → s = 140
        L220 elif predicted_hp > dmg*2       → s = (death_tick > tps*2) ? 0 : 25                    (L222)
             elif death_tick > impact_tick+tps → s = (death_tick > tps*2) ? 0 : 25                  (L222)
             else                            → s = 70
    L226~227: if target.nearest_enemy = Some(eid) && enemy=get_entity_by_id(eid) && enemy.team == champ.team && enemy.ty ∈ {Tower,Nexus} → s += 80
    L231 best = max(best, s)
  else → L237~256 캐시 순회: for (tx,ty,attack_range,dmg,tid,bonus) in entries[..n]:
    L238~240 walk_dist/walk_tick/impact_tick 동일 ; L243~250 snapshot 있으면 위 s 사다리 동일(find(tid)) 없으면 s=0 ; L254 s += bonus ; L256 best = max(best, s)
L259: return best (기본 0)

── ★0.6.0 신규 aggressive_gain (특성 AggressivePlay) ──
aggressive_gain = 0
if player.+0x49d == 1 && champ.is_some() && champ.attack_effect.is_some() && parameter.near_enemies(+0x14d8 ptr, +0x14f0 len · 항목 0xd8B).len() != 0 {
  // 가장 가까운 적 챔피언: 키 (distance(dest→e), ep.id) 최소 (동거리면 id 작은 쪽)
  (ep, e) = near_enemies.iter().filter_map(|ep| game.get_entity_by_id(ep.id(+0x58)).filter(|e| e.ty(+0x68)==Champion(13)).map(|e| (ep,e)))
              .min_by_key(|(ep,e)| (distance(ax,ay, e.x,e.y)[1660a00], ep.id))?   (없으면 0)
  dist  = distance(ax,ay, e.x,e.y)
  reach = champ.stat_buff.range(+0x438) + atk.range(+0x4a0) + (champ.level(+0x5c8)-1)*atk.growth_range(+0x4a8) + atk.range_adjust(champ, e)[vt+0x108] + radius(champ) + radius(e)
  th    = reach + max(champ.move_speed(+0x640),1) * 90
  if dist < th {
    g = estimate_damage_to(champ.atk, ctx, champ, static, e)[1643790] * champion_hp_value(data, parameter, ep)[de3d60] / max(e.hp(+0x670),1)   (sdiv)
    aggressive_gain = if dist <= reach { g } else { g * (th - dist) / (max(champ.move_speed,1)*90) }
  }
}
── ★0.6.0 신규 defensive_penalty (특성 DefensivePlay) — 대칭 ──
defensive_penalty = 0
if player.+0x49e == 1 && champ.is_some() && parameter.near_enemies.len() != 0 {
  (ep, e) = 위와 같은 선택(키 (distance(dest→e), ep.id)) · e.attack_effect(+0x4c0 != -1) 없으면 0
  reach = e.stat_buff.range(+0x438) + e.atk.range(+0x4a0) + (e.level-1)*e.atk.growth(+0x4a8) + e.atk.range_adjust(e, champ)[vt+0x108] + radius(champ) + radius(e)
  th    = reach + max(e.move_speed(+0x640),1) * 90
  if dist < th {
    p = estimate_damage_to(e.atk, ctx, e, static, champ)[1643790] * champion_hp_value(data, parameter, &parameter.player(+0x918))[de3d60] / max(champ.hp,1)
    defensive_penalty = if dist <= reach { p } else { p * (th - dist) / (max(e.move_speed,1)*90) }
  }
}
// 반환: Some(positional_gain - danger + last_hit_gain + aggressive_gain - defensive_penalty)
```
// ★v2 사장: 없음(이 함수는 version 분기 없음 · 두 신규 항은 특성 플래그 게이트).

## changes
- L78 마스크 0x1f863→0xfc33 · 니치 기본 idx 7→6(AroundHide 제거).
- 반환식에 `+ aggressive_gain - defensive_penalty` 추가(신규 블록 2 · 특성 플래그 player+0x49d/+0x49e · parameter.near_enemies +0x14d8/+0x14f0 Vec<ChampionScoreParameter>(0xd8B · +0x58 id)).
- 오프셋: PlayerState +0xa00/+0xa90 · ScoreParameter player +0x918(risk +0x998 · tower +0x9b0) · Effect vt range_adjust +0xe8→+0x108 · 콜리 estimate_damage_to 12857f0→1643790 · position_eval_at ff5ec0 · champion_hp_value db0400(TLS) / 신규 래퍼 de3d60.

## verified
- Ghidra 0.6.0 100c2f0 디컴: Anchor 태그 1 · `0xfc33>>idx` · purpose 산출(0x1a1 마스크·+0x8a8-30tps·(pos==1)<<3) · ff5ec0 · db0400 키 · undying +0x488 · risk +0x998 / tower +0x9b0 / 3 · aggr 블록(+0x49d · +0x14f0/+0x14d8 · 0xd8 · +0x58 · ty 13 · 1660a00 · (dist,id) 최소 · +0x438/+0x4a0/+0x4a8 · vt+0x108 · radius(+0x470/+0x680) · ms(+0x640)*0x5a · 1643790 · de3d60(data,parameter,ep) · /max(e.hp,1)) · def 블록(+0x49e · e 의 atk/range_adjust(e,champ) · de3d60(…, parameter+0x918) · /max(champ.hp,1)).
- 미확인: 반환 직전 합산 순서(RE §5 asm 인용 `sub r12,r14; add rbx,r11; add rbx,r12; sub rbx,rax`) · lane_anchor_gain 본체(db1950 memcpy 뒤 — Ghidra noreturn 오판으로 미표시 · RE 「동일」 인용) · 감쇠식 `g*(th-dist)/(ms*90)` 의 분자·분모 배치(디컴은 div_by_zero 가드만 노출 · RE 인용).

## confidence
A(게이트·선택·reach·th) / B(감쇠식 세부·합산 순서) — 핵심 상수·오프셋은 디컴 확인, 산식 꼬리는 RE 의존.
