# db9430→faa7c0 LineGankerPlan::next_plan

## logic_060
```
// sig 동일: fn next_plan(&mut self LineGankerPlan, version, rnd, player, data, positioning_score, team_plan: &TeamPlan, debug) -> Option<BigPlan(0x228)>   // sret · None = tag −1
// ★0.6.0 LineGankerPlan 레이아웃: +0x0 chats Vec{cap,ptr,len} · +0x20 wait_limit · +0x28 tick(fa3350 만 세팅) · ★+0x38 arrived_tick · +0x78/+0x80 = −1(Option 니치) · +0xa0 = 1 · ★+0xa7 exposed_at_start: bool · +0xab line · ★+0xac own_line: Option<u8>(0xff None · 0 Top/1 Mid/2 Bot) · +0xad phase(생성 7 · 저HP 8) · +0xaf cancel_code
//   ⇒ +0xa7/+0xac 를 Some/true 로 세팅하는 생성자는 LPH::update `[TRAIT_ROAM]` 강제 LineGanker(d44e15~) 뿐(own_line = 내 포지션 Top→0/Mid→1/Bot·Sup→2 · exposed = game.is_visible(1−team, my.id)) · d444bd/fa3350 경로는 None/false → branch2·존필터 사장
// ★0.6.0 콜리: is_recent_visible 17d3bc0(bb+0x3e8+pos*8 · 창 120 불변) · ★lane_zone 19903f0(ctx,x,y,side) · collect e15100/e156e0/e15850/e159c0(클로저 fbab90/fbacb0/fbadc0) · min_by fold dbb770(L124)/dbb9b0(branch2)/dbbc30(L132)/dbbdc0(L232) · BattlePlan::new dd8b60 · ★BattlePlan::update dd8e70(프로브) · drop d35450 · get_die_tick_player 100bb90(data,team,pos,&vec,0,&None) · check_kill d72e70(&Option<(usize,usize)>,version,rnd,player,data,ps,debug) · make_gank_battle fa9d60(sret,self,version,rnd,player,data,ps,tp,id,min_trace,★site:u8,debug) · distance 1660a00 · target_bush_v41 fb9b70(line,player,data) · near_jungle_bush 1990740 · range_adjust vt +0x108
// player: team +0xa00 · position +0xa90 · tp.objective +0xcd5..+0xcd7 · setting tps +0x12f8 · game vt +0xf8 is_visible · +0x28 tick · +0x1f0 get_entity_by_id
// version 분기: 본 함수 안 version 비교 0건(신규 분기는 self 필드 게이트) — v2/v3 동일 경로.

fn next_plan(self, version, rnd, player, data, positioning_score, team_plan, debug) -> Option<BigPlan>
 L101  if self.phase(+0xad) < 6 /*ChangeJungle*/ { return None }
 L106  team = player.team; pos = player.position; champ = cache.player_champion[team][pos].unwrap()
 L107  if game.is_visible(1−team, champ.id) && is_in_jungle_area(ctx, champ.x, champ.y) /*인라인 · 704000 사각·192000 코너·96000 미드띠*/ {
 L113    near_wide_allies_len  = cache.player_champion[team].flatten().filter(|x| x.distance2(champ) < 62500000001 && x.hp*100/x.stat_cached.hp > 39).count()   // 자기 포함
 L115    near_wide_enemies_len = cache.player_champion[1−team].flatten().filter(|x| is_recent_visible(bb[1−team], x) && x.distance2(champ) < 62500000001 && x.hp*100/x.stat_cached.hp > 39
 L117                               && !(self.own_line(+0xac).is_some() && self.exposed_at_start(+0xa7) && lane_zone(ctx, x.x, x.y, own_line)) /*★0.6.0 내 라인 존 안의 적 제외*/).count()
 L122    // ★0.6.0 3분기
         if near_wide_allies_len > near_wide_enemies_len && near_wide_enemies_len != 0 {   // branch1 (open)
 L124      nearest_enemy = iter_champions(1−team).filter(|x| is_recent_visible(x) && !(own_line.is_some() && exposed && lane_zone(ctx,x.x,x.y,own_line)) && (own_line.is_none() || dist²(x,champ) <= 250000²) /*★0.6.0*/).min_by_key(dist²(champ))   // dbb770
 L126      if let Some(e) = nearest_enemy { (debug "[GANK_EXPOSED] … branch=open") if let Some(p) = self.make_gank_battle(version, rnd, player, data, positioning_score, team_plan, e.id, 60, site=1 /*★0.6.0*/, debug) { return Some(p) } }
         } else if let Some(side) = self.own_line(+0xac) {   // ★0.6.0 branch2 (response · own_line Some 일 때만)
           if near_wide_enemies_len != 0 {
             ne = iter_champions(1−team).filter(|x| is_recent_visible(x) && dist²(x,champ) <= 250000² && x.hp*100/x.max_hp >= 40 && !(exposed && lane_zone(ctx,x.x,x.y,side))).min_by_key(dist²(champ))   // dbb9b0
             if let Some(e) = ne {
               b = BattlePlan::new(version, &BattlePlanGoal::Response(2), data, player)   // dd8b60 · 로컬 0x228
               b.entry_src(+0x213) = 3; b.gank_line(+0x205) = self.line(+0xab); b.main_objective(+0x208 u16) = tp.+0xcd5..cd6; b.+0x20a = tp.+0xcd7
               b.update(version, rnd, player, data, positioning_score, team_plan, debug)   // ★0.6.0 dd8e70 프로브
               b.chats.len(+0x98) = 0
               screened = b.sub_goal.tag(+0x78) >= 2 /*KitingBack·RunAway·End*/
               (debug "[GANK_EXPOSED] … branch=response … screened=")
               if screened { drop(b) /*d35450*/ → L142 }
               else { self.chats(+0x0).push(Chat::BattleHelp(e.id, 0) /*태그 6*/); return Some(BigPlan::Battle(b) /*니치 · memcpy 0x228*/) } } } }
         else {   // branch3 (0.5.8 L132~136)
 L132      nearest_enemy = iter_champions(1−team).filter(is_recent_visible).min_by_key(dist²(champ))   // dbbc30 · 거리 무관
 L134      if let Some(e) = nearest_enemy {
 L135        self.chats.push(Chat::BattleHelp(e.id, 0))
 L136        b = BattlePlan::new(version, Response(2), data, player); b.entry_src = 3; b.gank_line(+0x205) = self.line /*★0.6.0 추가*/;   // +0x208/+0x20a 는 안 씀
             return Some(BigPlan::Battle(b)) } } }
 L142  let _ = self.target_bush(player, data)   // 인라인 · 값 미사용 · unwrap 패닉 부작용만(0.5.8 동일): tower +0x180+line*0x20/+0x190 · twin +0x130/+0x148 · nexus +0x170 · minion_state bb+0/+0x28/+0x50 · front_minion → get_entity_by_id(vt +0x1f0) · lane_path[3] · near_jungle_bush(1990740).unwrap()
 L144  bush = target_bush_v41(self.line, player, data)   // fb9b70
 L148  champ_bush = map.bushes(+0x1c98)[min(champ.y/32000,29)][min(champ.x/32000,29)]
 L150  if ctx.debug { debug.add_text(champ.x, champ.y+20000, format!("bush: {:?}, champ_bush: {:?}", ..), Color(1,0,0,1), 1) }
 L154  if champ_bush != bush { return None }
 // ★0.6.0 L155 뒤 문장 추가
       if self.arrived_tick(+0x38) == 0 { self.arrived_tick = game.tick() }
 L158  dist_cut = 150000
 L160  near_allies_p: bumpalo Vec<usize>  = cache.player_champion[team].enumerate().filter(|(_,x)| x.is_some_and(|x| x.distance2(champ) <= dist_cut²)).map(i).collect_in(pool)   // e15100 · 자기 포함
 L165  near_enemies_p: bumpalo Vec<usize> = cache.player_champion[1−team].enumerate().filter(|(_,x)| x.is_some_and(|c| is_recent_visible(c) && c.distance2(champ) <= dist_cut²)).map(i).collect_in(pool)   // e156e0
 L170  nearest_enemy_tower = cache.iter_towers_without_nexus(1−team).min_by_key(dist²(champ))   // e7e1e0
 L173  for e in near_enemies_p.iter() {
 L175    near_allies_p  = player_champion[team].enumerate().filter(|(_,x)| x.is_some_and(|c| c.id == champ.id || c.distance2(player_champion[1−team][*e].unwrap()) <= dist_cut²)).map(i).collect_in(pool)   // e15850
 L182    near_enemies_p = player_champion[1−team].enumerate().filter(|(_,x)| x.is_some_and(|x| x.distance2(player_champion[1−team][*e].unwrap()) <= dist_cut²)).map(i).collect_in(pool)   // e159c0
 L187    enemy_die_tick = get_die_tick_player(data, 1−team, *e, &near_allies_p, None, None)   // 100bb90
 L188    me_die_tick    = get_die_tick_player(data, team, pos, &near_enemies_p, None, None)
 L190    e_ent = player_champion[1−team][*e].unwrap()
 L191    move_to_tower_tick = if let Some(tower) = nearest_enemy_tower { e_ent.distance(tower) / e_ent.stat_cached.move_speed(+0x640) /*0 → div0*/ } else { 99999999 }
 L199    if ctx.debug { debug.infos.entry(e_ent.id).or_default().push(format!(..)) }
 L205    if enemy_die_tick > 30 && near_allies_p.len() <= near_enemies_p.len() { continue }
 L209    if move_to_tower_tick <= 120 {
 L211      if enemy_die_tick <= 60 { if let Some(p) = make_gank_battle(…, e_ent.id, 60, site=2, debug) { return Some(p) } }
 L213      else if near_allies_p.len() > near_enemies_p.len() { if let Some(p) = make_gank_battle(…, e_ent.id, 60, site=3, debug) { return Some(p) } }
 L216    } else if me_die_tick > 120 && near_allies_p.len() >= near_enemies_p.len() { if let Some(p) = make_gank_battle(…, e_ent.id, 60, site=4, debug) { return Some(p) } }
 L218    else if self.wait_limit(+0x20).saturating_sub(game.tick()) > tps*3 { /*아무것도 안 함*/ }
 L220    else if near_allies_p.len() > near_enemies_p.len() { if let Some(p) = make_gank_battle(…, e_ent.id, 60, site=5, debug) { return Some(p) } } }
 L225  if let Some((e, t)) = check_kill(version, rnd, player, data, positioning_score, debug) /*★0.6.0 d72e70 · w1 §2*/ {
 L226    if near_allies_p.len() > near_enemies_p.len() { if let Some(p) = make_gank_battle(…, e, t /*min_trace*/, site=6, debug) { return Some(p) } } }
 L232  nearest_enemy = iter_champions(1−team).filter(is_recent_visible).min_by_key(dist²(champ))   // dbbdc0
 L235  if let Some(ne) = nearest_enemy {
 L236    d = ne.distance(champ)   // 1660a00
 L237    attack_range = champ.attack_effect.unwrap().range(champ) /*+0x4a0 + (level(+0x5c8)−1)*+0x4a8 + +0x438*/ + eff.range_adjust(champ, ne) /*vt +0x108 ★0.6.0*/ + champ.radius() + ne.radius()
 L240    if d > attack_range { return None }
 L241    return make_gank_battle(…, ne.id, 60, site=7, debug) }
 L245  None
// site 코드(★0.6.0 make_gank_battle 11번째 인자 · fa9d60 이 bp.+0x214 gank_open_site 에 기록): 1 branch1 open · 2 L212 · 3 L214 · 4 L217 · 5 L221 · 6 check_kill · 7 사거리 내 최근접
```

## changes
- L117~120 적 카운트 필터에 `!(own_line.is_some() && exposed_at_start && lane_zone(ctx,x,own_line))` 추가(내 라인 존 안의 적 제외).
- L122 2분기 → 3분기: `allies>enemies && enemies≠0 → branch1` / `own_line Some → branch2(신규 response 프로브: BattlePlan::new(Response) + entry 3 + gank_line + main_objective ← tp.+0xcd5 + update 프로브 → sub_goal≥2 면 폐기 · 아니면 BattleHelp + Battle)` / `else → branch3(구 L132~136 + gank_line 세팅)`.
- L124 branch1 필터에 존 제외 + `own_line.is_none() ‖ dist²≤250000²` 추가.
- L155 뒤 `arrived_tick(+0x38)==0 → tick` 세팅 추가.
- make_gank_battle 에 site 인자(1~7) 추가 · check_kill 7-arg(d72e70).
- ★lane_zone 19903f0 확정식(w8 A3): yf=H−y · 공통 `max(x,yf)≤192000 ‖ min(x,yf)≥W−192000 → true` · side 1: `|x−yf|<64000` · side 0: 중앙사각(x,y∈[(W−704000)/2, +704000]) 밖이면 `yf>192000 && yf≥x && yf−x≥64000`, 안이면 `yf≥x && 64000≤yf−x<96000` · side 2: x↔yf 대칭.
- 콜리 v3: 1323a00→17d3bc0 · ca7ba0/ca8180/ca82f0/ca8460→e15100/e156e0/e15850/e159c0(★mig060 의 e15290/e15400/e15570 짝은 오매핑) · e32470/e32600/e32790→dbb770/dbbc30/dbbdc0(+신규 dbb9b0) · dfb5d0→dd8b60 · de3630→100bb90 · e6b800→d72e70 · db8e60→fa9d60 · 12a07d0→1660a00 · vt +0xe8→+0x108.
- 오프셋: phase +0x29→+0xad · line→+0xab · wait_limit +0x20 · chats +0x0 · player team/pos +0xa00/+0xa90 · BattlePlan +0x213/+0x205/+0x208/+0x98/+0x78.

## verified
- capstone 0.6.0 faa7c0(2940줄): `lea [r12+0xac] / [r12+0xa7]`(faa99f) · 19903f0 호출 ×19 ✓ · branch2 `dd8b60(goal=2) → [+0x213]=3 · [+0x205]=[r12+0xab] · [+0x208]=tp.cd5(u16) · [+0x20a]=tp.cd7 → dd8e70 → [+0x98]=0 → setae ([+0x78]>=2)`(fac6c0~fac7b3) ✓ · make_gank_battle `[rsp+0x48]=0x3c · [rsp+0x50]=1`(fac124 site 1) 및 ×7 사이트 ✓ · `cmp [rbx+0x38],0 → tick 저장`(facdd3~facdee) ✓ · check_kill d72e70(fadaa0) ✓.
- 확인 못 한 지점: fa9d60 make_gank_battle 본체(site 소비·+0x38 사용 · w11 §B2) · +0xaf∈{7,9} 쓰기처 · L142 target_bush 인라인은 RE 「그대로」 판정.

## confidence
A — 3분기·필터·site·arrived_tick 전부 asm 직독 확정 · version 분기 없음. 유일한 유의점 = branch2/존필터는 [TRAIT_ROAM] 강제 생성 경로에서만 활성(다른 생성자는 own_line None).