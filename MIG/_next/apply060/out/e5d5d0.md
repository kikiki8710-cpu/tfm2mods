# e5d5d0→d606b0 LegacyPlanHandler::handle_interact_battle

## logic_060
```
// sig 동일: fn handle_interact_battle(&mut self LPH, version, rnd, player, data, debug)   (rcx,rdx,r8,r9,[sp+0x28],[sp+0x30])
// ★0.6.0 LPH 오프셋: team_plan +0xf8(tp.X = self+0xf8+X: objective +0xdcd/dce/dcf · serpen phase/take_born +0x4d8/+0x4dc(tp+0x3e0/+0x3e4) · epic +0x4f8/+0x4fc(tp+0x400/+0x404) · cc1 +0xdb9 · ally_battle_stop +0x118(tp+0x20+i*0x10) · last_battle_tick +0x608(tp+0x510) · steal_session +0x56a(tp+0x472))
//   · plan +0xf58(Battle 니치 tag<2 · BattlePlan 페이로드 = plan+0 · PassiveJungle team/player_team +0xfc0/+0xfc8 · PassiveLine.line +0x107b · sub_plan +0x13a0) · positioning_score +0x1408(구 +0x990 · ⚠§A 「team_plan +0x1408」 은 오기) · chats +0x1198/11a0/11a8(elem 0x18: tag@0,id@8,0@0x10)
//   · ★pending tower_binary +0x1ef8{tower@0, target@+8, expire@+0x10, state u8@+0x18(0 후보/1 콜됨/2 None)} · ★+0x24aa flee_latch · ★+0x24ac latch_v3 · +0x24ad(감사 게이트) · ★+0x24ae one_shot 미러 · +0x24d4 v3_epicops_armed · +0x24c7/+0x24dc(Support) · +0x24f2(join window 무시) · +0x1f50 last_dive_abandon · +0x1f58 last_response_bail · +0x2050/58 last_lost_fight · ★+0x2060/68/70 last_failed_engage(target,tick,reason=bp.exit_src) · ★+0x2090/+0x2098 last_support_reject(tick, reason u8) · +0x23d0 tick · +0xea8..+0xf41 support-join 에피소드 · 카운터 +0x1f60/+0x1f78/+0x1f80[3]/+0x1f88/+0x1f90/+0x1f98 · epic_ally_tick +0x98
// ★0.6.0 BattlePlan(0x228): +0x00/+0x08 support_target · +0x60 main_goal · +0x78 sub_goal · +0x98 chats.len · +0xb8..+0xd8 tower_binary{tower,target,tick,state@+0xd0} · +0xe8 prev_die_eval · +0x1c0 dive_join_tick · +0x1f8 with_dive · +0x1f9 team_dive · +0x1fc flee_flag · ★+0x202 joiner_mode · +0x203 ff_stake_join · +0x204 screening · +0x207 dive_tower · +0x208..+0x20a main_objective · +0x213 entry_src · +0x215 exit_src
// ★0.6.0 Blackboard(0x5c8): +0x1d0+i*0x18 소액션 레코드{tag@0,target@+8} · +0x250/+0x258 Vec<0x50B>{+0x40..+0x44 pos별 byte, +0x46 active}(gank call 추정) · +0x260 tower_binary{tower,target,expire,state@+0x278} · +0x280+i*0x18 prev big_goal · +0x2f8+i*0x20 big_goal{update_tick@0, tag@+8(5=Battle), focus Some@+0x10, id@+0x18} · +0x3e8+pos*8 last_seen · +0x4d8+i*8 stop tick
// player: team +0xa00 · pos +0xa90 · roaming +0x228 · aggressive +0x230 · 특성 aggr +0x49d/def +0x49e/FightJoiner +0x49f · +0x4c8/+0x4d0 join window Vec<{s,e,kind@+0x10}>(elem 0x18) · Strategy +0x568
// 유효 objective(§A/§B/§D/§G/§I/§J 공통 인라인 ★0.6.0): obj = if tp.+0x3e4 != 2 { (Serpen 1, phase=tp.+0x3e0, with_battle=tp.+0x3e1) } else if tp.+0x404 != 2 { (Morgard 0, tp.+0x400, tp.+0x401) } else { tp.objective(+0xcd5..) }
// 콜리(0.6.0): try_engage d5f3c0 · try_engage_dive d5ff20 · check_favorable_engage_formation edfb50 · tower_dive_is_viable ee6d40 · BattlePlan::update dd8e70 / new dd8b60 · can_near_enemies_range f10c60 · max_range_cached de9900 · v25_objective_posture f09860 · v27_discipline f0c310(4번째 JungleType 4/5 추가 · 결과 +0x19 = blocks 추정) · v2_response_retreat_stance d6bf10 · resolve_join_stake 클로저 d380c0(→ee4e70) · can_battle_triggered_filtered dc1c10 · v3_epic_formation fb78b0 · check_kill d72e70 · camp_pos 16a7af0/157b720 · is_near_line 19903f0 · BigPlan::goal ec21b0 · drop<BigPlan> d35010 · distance 1660a00 · gen_range dcae80 · can_enemy_hit_objective eeae70 · is_ignored_well_enemy ee7fd0 · ★db0160 last_stand_flags · ★d37d90 tower_binary 유효성 · ★d601f0 one_shot_by_single_enemy · ★eda920 ckdt · ★eeb5b0 hopeless · ★ee3000 ally_is_bound · ★ee8090 · ★d38030 · ★d354c0 · ★d588e0 · ★d359f0
// ★v2 사장: `version>=2` 게이트 전부 고정 참 · `version<3` 가지(§C v2 commit=line==Commit · §D 2차 update 조건부 · `v<3 ‖ isolated`) 는 제외하고 v3 가지만 적음.

fn handle_interact_battle(self, version, rnd, player, data, debug)
 champ = cache.player_champion[team][pos].unwrap() ; _t_ob = ProfTimer::start(86)
 // ═══ ★0.6.0 프롤로그 P1~P5 + latch (신규) ═══
 // P1
 if self.plan.tag < 2 /*Battle*/ && bp.flee_flag(+0x1fc) { self.flee_latch(+0x24aa) = 1; self.latch_v3(+0x24ac) = 1 /*v3*/ }
 // P2 (v3)
 if Battle && bp.tower_binary.state(+0xd0) != 2 { self.pending(+0x1ef8) = bp.tower_binary(32B) }
 if pending.state != 2 && !d37d90(data, player, &pending) /*아래*/ { (debug) if Battle && bp.tb.state != 2 && bp.tb.tower == pending.tower { bp.tb.state = 2 } pending.state = 2 }
 //   d37d90(data,player,&tb) = tower=get(tb.tower) is Tower(2) && alive && target=get(tb.target) alive && (!tb.state || 아군 5명 중 dist(ally,tower)≤250000 존재) && (ee3600(target in tower zone: dist ≤ target.range*(100+bonus)/100 + tower.reach + 8000) || (dist(target,tower)≤250000 && !d35c00(어느 아군 사거리 안에 target)))
 // P3 (v3)
 if bb[team].tower_binary.state(+0x278) != 2 { bb_tb = bb[team].+0x260; keep = pending.state != 2 && pending.tower == bb_tb.tower && pending.expire > bb_tb.expire; if d37d90(bb_tb) && !keep { pending = bb_tb } }
 // P4 (v3)
 if pending.state != 2 && pending.expire > tick && let Some(tower) = get(pending.tower) && (첫번째로 dist(ally_i, tower) ≤ 250000 인 i) == my_pos && let Some(target) = get(pending.target) alive {
   if pending.state == 1 { if !tower_dive_is_viable(version, rnd, player, data, &tp, target, true, debug) { pending = {…, tick, state: 0} } }
   else /*0*/ { if tower_dive_is_viable(…, false, debug) && !open_chase_race_hopeless(version, data, player, champ, target) { pending = {…, tick, state: 1}; self.chats.push(Chat{tag: 4 /*다이브 콜*/, id: target}) } } }
 // P4' (v3)
 if pending.state == 0 && Battle && bp.sub_goal ∈ {KitingBack(2),RunAway(3)} && (!bp.with_dive || bp.team_dive) && bp.main_goal.tag <= 1 && dist(get(pending.tower), champ) <= 300000 { bp.exit_src = 15; bp.sub_goal = End(4) }
 // P5 (v3)
 one_shot = d601f0(version, player, data, champ)   // 적 각 1명에 대해 ckdt(simple=1,…) < tps 또는 == 0
 tp.+0xcd1 = self.+0x24ae = one_shot
 if !Battle { for e in enemies(visible ‖ last_seen+120 ≥ tick) with dist² ≤ 250000² { die = ckdt(version, data, player, e, &[champ], &[], true, false, false, &None); if die < tps { mr = max_range(champ, e); idx = d²≤mr² ? 0 : d²>150000² ? 1 : 2; self.+0x1f80[idx] += 1 } } }   // 감사 카운터
 if one_shot && !flee_latch { flee_latch = latch_v3 = 1; self.+0x1f60 += 1; if self.+0x24ad { self.+0x1f98 += 1 } }
 // latch 블록 (전 버전)
 if !flee_latch { latch_v3 = 0 }
 else {
   targeting_me = (0..5).any(|i| bb[1−team].small_action[i].tag(+0x1d0+i*0x18) ∈ 6..=9 && .target(+8) == my_id)
   chased = targeting_me && latch_v3   // [rbp+0x1530] (v3)
   keep = (!targeting_me || latch_v3) && dist(champ, well(team: 0→(0,960000), 1→(960000,0))) > 160000
   if !keep || last_stand_flags(&ctx.vt20(), &tick, player, data) /*db0160*/ & 1 { flee_latch = latch_v3 = 0 }
   else if champ.undying(+0x488) && min(활성 undying 버프(+0x2e0 Vec, elem 0x120, +0xd0==1) remaining) > tps { flee_latch = latch_v3 = 0 } }

 // ═══ §A 238~333 모가드 with_battle 오브젝트 교전 ═══
 (otag, phase, with_battle) = 유효 objective
 if otag == Morgard(0) && with_battle && !Battle {
   camp_pos = map.camp_pos(Morgard(4), team==0)
   in_camp_ally = iter_champions(team).filter(|x| dist_sq(x, camp_pos) <= 140000²).count()
   in_camp_enemy_list = iter_champions(1−team).filter(|x| x.is_visible_from(champ.team) && !is_ignored_well_enemy(version,player,x) && dist_sq(x,camp_pos) <= 140000²).collect_in(pool)   // e0c830
   in_camp_enemy = len ; can_near_enemies = tp.can_near_enemies_range(version, rnd, player, data, camp_pos.x, camp_pos.y, 150000, debug).len()
   skip = false ; strategy = player.strategy(rnd, game)
   if strategy.object_finish == KillPriority(0) { steal_risk = moba.epic.live_list.get(0).and_then(get_entity_by_id).is_some_and(|epic| iter_champions(1−team).any(|c| c.is_visible_from(champ.team) && !is_ignored_well_enemy(c) && can_enemy_hit_objective(c, epic, 25000) /*eeae70*/)); if !steal_risk { skip = true } }
   if phase == Hunt(3) { remain_time = self.data.epic.epic_ally_tick(+0x98); if let Some(epic) = live_list.get(0).and_then(get) { min_tick = iter_champions(1−team).filter(가시 && !우물).map(|e| dist(e,epic)/e.move_speed(+0x640)).min(); if let Some(m) = min_tick { if m > remain_time { skip = true } } } }
   if !skip {
     champ_is_in_camp = dist_sq(champ, camp_pos) <= 140000²
     if !(in_camp_ally <= can_near_enemies + in_camp_enemy || !champ_is_in_camp) {
       if let Some(nearest_enemy) = in_camp_enemy_list.min_by_key(dist_sq(champ)) /*d35fb0 (dist,id)*/ {
         mr = max_range_cached(data, champ, nearest_enemy)
         if dist_sq(nearest_enemy, champ) <= mr² {
           formation_ok = check_favorable_engage_formation(version, player, data, nearest_enemy, 200000)
           strategy = player.strategy(rnd, game)   // 재호출(rnd 2회째)
           aggr = player.param.aggressive(+0x230) >= 70 || player.trait_aggr(+0x49d)   // ★0.6.0 (구 aggressive_ratio>699)
           near_ally = iter_champions(team).filter(|x| x.id != champ.id && dist²(x, nearest_enemy) <= (max_range(x→nearest_enemy)+40000)²).count()   // d79490
           if strategy.object_battle == 0 { min_poke = aggr ? 2 : 3; if formation_ok && can_near_enemies == 0 && near_ally >= min_poke { if let Some(b) = self.try_engage(version, rnd, player, data, nearest_enemy.id, debug) { self.plan = Battle(b); return } } }
           else { min_engage = aggr ? 1 : 2; if formation_ok && near_ally >= min_engage { if let Some(b) = try_engage(..) { self.plan = Battle(b); return } } } } } } }
   drop(in_camp_enemy_list) }
 // ═══ §B 336~386 세르펜 (§A 동형 · 게이트만 유효 objective) ═══
 if otag == Serpen(1) && with_battle && !Battle {
   camp_pos = camp_pos(Serpen(5), team==0); in_camp_ally = …(140000²); in_camp_enemy_list = …(e0c9a0); in_camp_enemy; can_near_enemies = f10c60(…, camp_pos, 150000)
   if ctx.debug { debug.infos push }
   champ_is_in_camp = dist_sq(champ, camp_pos) <= 140000²
   kill_priority_skip = strategy.object_finish == 0 && !(moba.serpen.live_list.get(0).and_then(get).is_some_and(|s| iter_champions(1−team).any(|c| 가시 && !우물 && can_enemy_hit_objective(c, s, 25000))))
   if !(kill_priority_skip || !champ_is_in_camp || in_camp_ally <= can_near_enemies + in_camp_enemy) {
     strategy = player.strategy(rnd, game)
     if let Some(nearest_enemy) = in_camp_enemy_list.min_by_key(dist_sq(champ)) { max_range = de9900(data, champ, nearest_enemy)
       if dist_sq(nearest_enemy, champ) <= max_range² { formation_ok = edfb50(…, nearest_enemy, 200000)
         if strategy.object_battle == 0 { if can_near_enemies == 0 && formation_ok && in_camp_ally > in_camp_enemy + 1 { if let Some(b) = try_engage(..) { self.plan = Battle(b); return } } }
         else { if formation_ok { if let Some(b) = try_engage(..) { self.plan = Battle(b); return } } } } } }
   drop(in_camp_enemy_list) }
 drop(_t_ob) // ═══ §C 391~455 아군 교전 합류(Support) — ★0.6.0 대폭 ═══
 gank = bb[team].vec250.any(|r| r.+0x46 == 1 && r.+0x40[my_pos] != 0)   // ★0.6.0 (구 objective.is_some 게이트의 gank 부분 · Vec +0x250/+0x258 elem 0x50)
 objcontest = false
 passive = match self.plan.tag { PassiveLine(3)|SinglePlanLine(4) => true, PassiveJungle(7) => plan.team(+0xfc0) == plan.player_team(+0xfc8), EpicHuntAndPoke(12)|SerpenHuntAndPoke(14) if self.sub_plan.tag(+0x13a0) == ObjContest(0x8000000000000011) => { objcontest = true; true } /*★0.6.0 v3*/, _ => false }
 if passive && !Battle && (tp.objective(+0xdcd) != 0xff || gank || true /*v3*/) && !flee_latch /*★0.6.0*/ {
   roaming = min(player.roaming(+0x228), 100)
   joiner = player.trait_FightJoiner(+0x49f) || (!self.+0x24f2 && player.+0x4c8 Vec<{s,e,kind@+0x10}>.any(|w| w.kind == 2 && w.s <= tick && tick < w.e)) || (my_pos == Support(4) && self.+0x24c7 && self.+0x24dc == 0xff && tutorial ∈ {0,5,7,8} && tick >= first_spawn(+0x8a8) − 30*tps)   // ★0.6.0
   range = if joiner { 450000 } else if gank || true /*v3*/ { 250000 } else { roaming*1000 + 150000 }   // ★0.6.0 ⇒ v3: joiner ? 450000 : 250000
   range_sq = range²
   // 트리거 1 (동치)
   trigger: Option<(focus, kind)> = (0..5).filter(i != my_pos).find_map(|i| ally = champions[team][i]?; if dist_sq(ally,champ) > range_sq { None } else if bb[team].big_goal[i](+0x2f8+i*0x20).tag == Battle(5) && focus Some(+0x10==1) { Some((focus_id(+0x18), 1)) } else { None })
   // ★0.6.0 트리거 2 (v≥2): 적의 big_goal 이 내 근처 아군을 focus
   .or_else(|| enemies(visible ‖ last_seen+120 ≥ tick · dist² ≤ range_sq).find_map(|e| g = if bb[1−team].big_goal[e.pos].update_tick + 20 > tick { bb.big_goal[e.pos] } else { bb.prev_big_goal[e.pos](+0x280+p*0x18) }; if g.tag == Battle && g.focus == Some(j) && j ≠ me && dist(ally_j, champ) ≤ range { Some((e.id, 2)) } else { None }))
   // ★0.6.0 트리거 3 (joiner ‖ v3): ally_is_bound
   .or_else(|| (0..5).filter(j != me && dist(ally_j,champ) ≤ range).find_map(|j| list = collect_in(closure#10 /*j 주변 적*/, j); if ally_is_bound(version, rnd, data, player, ally_j, &list, debug) /*ee3000*/ { Some((list.min_by_key(dist(ally_j)).id, 3)) } else { None }))
   if let Some((focus_id, kind)) = trigger {
     far = dist²(get(focus_id), champ) > 250000²
     if objcontest { camp = camp_pos(self.plan.tag == 14 ? Serpen(5) : Morgard(4), team==0); if dist²(get(focus_id), camp) > 250000² { → skip §C } }   // ★0.6.0
     r3_hold = focus_id == self.last_lost_fight.0(+0x2050) && tick <= self.last_lost_fight.1(+0x2058) + tps*3
     // ★0.6.0 v3 게이트 A: 최근 실패 교전 대상 재교전 3초 금지
     r3_hold |= focus_id == self.+0x2060 && self.+0x2070(reason) ∈ {7,13,15,16} && tick <= self.+0x2068 + 3*tps && (reason != 15 || ee8090(data, player, get(focus_id)) /*적 타워 사거리+8000 안*/)
     // ★0.6.0 v3 bb 게이트: 팀 tower_binary state 0 이고 focus 챔프가 그 타워 250000 내
     r3_hold |= bb[team].tower_binary.state(+0x278) != 2 && d38030(cache, focus_id, bb_tb.tower, bb_tb.state)
     if !r3_hold {
       battle = BattlePlan::new(version, &BattlePlanGoal::Support(focus_id) /*tag 1 · +8 focus*/, data, player)   // dd8b60
       battle.entry_src(+0x213) = 5 ; battle.support_target(+0x00/+0x08) = Some(focus_id)
       battle.screening(+0x204) = 1; battle.update(version, rnd, player, data, &self.positioning_score(+0x1408), &self.team_plan(+0xf8), debug); battle.screening = 0   // ★0.6.0 screening 래핑
       battle.chats.len(+0x98) = 0
       stake: Option<FightPrediction> = get_entity_by_id(focus_id).and_then(|t| resolve_join_stake(version, rnd, data, player, champ, t, &tp, committed=0, horizon_sec = joiner ? 12 : 6, debug) /*d380c0→ee4e70 ★0.6.0 인자*/)
       stake_commit = stake.is_some_and(|s| s.line != Disengage(2))   // ★0.6.0 v3 (구 line==Commit)
       stake_veto   = stake.is_some_and(|s| s.line == Disengage(2))
       reject = battle.sub_goal(+0x78) == End(4)
       if stake_veto || (!stake_commit && battle.exit_src(+0x215) == 7) { reject = true }
       if !reject {
         if stake_commit { battle.ff_stake_join(+0x203) = battle.exit_src == 7 || battle.sub_goal ∈ {KitingBack(2),RunAway(3)} }
         else { if battle.sub_goal ∈ {KitingBack,RunAway} && battle.prev_die_eval(+0xe8) > tps*2 { reject = true }   // ★0.6.0 v3: 즉시 reject(구 v2 는 재update 후 End 검사)
                battle.ff_stake_join = false }
         if !reject { battle.joiner_mode(+0x202) = joiner /*★0.6.0*/; self.plan = BigPlan::Battle(battle) /*return 안 함*/
                      // ★0.6.0 텔레메트리: 수락 후 dist(target) ≥ 250001 이면 d354c0(최근 8s 레코드 · +0x23d0)/PassiveLine 이면 is_near_line → 에피소드 flush(d588e0)/open(+0xea8) — 판정 무관 } }
       if reject { self.+0x2090 = tick; self.+0x2098 = stake.is_none() ? 2 : 2 − (line==2 || pred.+0x39==2) as u8   // ★0.6.0 reject 기록
                   if battle.sub_goal ∈ {KitingBack,RunAway} && battle.prev_die_eval < tps && (!joiner || dist(target) ≤ 250000) { flee_latch = latch_v3 = 1; self.+0x1f78 += 1 }   // ★0.6.0 v3
                   drop(battle) } } } }
 // ═══ §D 461~563 피격/트리거 응전(Response) ═══
 objective_posture = match 유효 objective.tag { Morgard(0) => tp.v25_objective_posture(version, player, data, Morgard(4)) /*f09860*/, Serpen(1) => …(Serpen(5)), _ => None }
 objective_waiting_posture = objective_posture.is_some_and(|p| p.kind(+0x50) ∈ {3 WaitGroup, 4 SoftDisengage}) || tp.v27_objective_discipline_blocks_battle(version, data, jungle_type 4/5).+0x19 /*f0c310 4-arg ★0.6.0*/
 battle_area_filter = match 유효 objective { Morgard{phase} => { Hunt(3) => Some((camp_pos(Morgard), 140000²)), Setup(1)|Assemble(2) => Some((camp_pos(Morgard), 240000²)), _ => None }, Serpen{phase} => 동형(Serpen), _ => None }
 steal_session_active = my_pos == 1 && self.+0x56a != 2
 if flee_latch && !chased { → skip §D }   // ★0.6.0
 if !objective_waiting_posture && can_battle_triggered_filtered(version, player, data, battle_area_filter) /*dc1c10*/ {
   if !steal_session_active && !Battle {
     if tick > self.last_dive_abandon_tick(+0x1f50) + 1 + tps*4 {
       battle = BattlePlan::new(version, Response(2), data, player); battle.entry_src = 3
       isolated = !d359f0(cache, team, champ, cc1) /*아군 120000 내 없음 · cc1 게이트*/   // ★0.6.0 v3
       if chased { battle.exit_src = 14; battle.sub_goal = RunAway(3) }   // ★0.6.0
       else if isolated /*v3*/ && tp.last_battle_tick(+0x510) + tps*3 > tick { battle.exit_src = 14; battle.sub_goal = self.v2_response_retreat_stance(version, rnd, player, data, debug) /*d6bf10*/ }
       near_allies = (0..5).filter(|i| (cc1(+0xdb9) ? (bb[team].+0x4d8[i]==0 || +6tps < tick) : tp.ally_battle_stop_tick[i].is_none()) /*★0.6.0 cc1 게이트*/ && champions[team][i].is_some_and(dist_sq(c,champ) <= 120000²)).count()
       near_enemies = iter_champions(1−team).filter(|c| c.is_visible_from(champ.team) && !is_ignored_well_enemy(c) && dist_sq(c,champ) <= 120000²).count()
       can_near_enemies = tp.can_near_enemies_range(version, rnd, player, data, champ.x, champ.y, 150000, debug).len()
       strategy = player.strategy(rnd, game); is_focused = strategy.focused(+0x11).is_in_focused_area(ctx, champ.x, champ.y)
       serpen_alive = moba.map_or(false, |m| !m.serpen.live_list.is_empty())
       if !serpen_alive && near_allies <= can_near_enemies + near_enemies && !is_focused && isolated /*★0.6.0 v3*/ { battle.exit_src = 14; battle.sub_goal = v2_response_retreat_stance(..) }
       battle.screening = 1; battle.update(…, &self.positioning_score(+0x1408), &tp, debug); battle.screening = 0; battle.chats.len = 0
       if battle.sub_goal == End(4) { drop(battle); return }
       if battle.sub_goal ∈ {KitingBack(2),RunAway(3)} { if !(tick > self.last_response_bail_tick(+0x1f58) + tps*3) { if battle.prev_die_eval > tps*2 { drop(battle); return } } }
       // ★0.6.0 v3: 2차 update 무조건
       battle.update(…); battle.chats.len = 0; if battle.sub_goal == End { drop(battle); return }
       self.plan = BigPlan::Battle(battle) /*return 안 함*/ } } }
 // ═══ §E 567~618 press-line 타워 다이브 — 동치(정독 생략 · 콜리 순서 일치) ═══
 press_line = if self.v3_epicops_armed(+0x24d4) { moba.and_then(|m| if m.epic_minion_buff_time[team](+0x240+team*8) != 0 { v3_epic_formation(rnd, player, data) /*fb78b0 (tag,line) · tag 2=None · 1=is_split*/ .filter(!is_split).map(line) } else { None }) } else if tp.objective == PressEpic(5){line} { Some(line) } else { None }
 if let Some(line) = press_line { strategy = player.strategy(rnd, game)
   if !Battle && strategy.tower_press(+0xd) == 0 && tick > last_dive_abandon + 1 + tps*4 {
     nearest_tower = cache.tower(line)[enemy](+0x180+line*0x20+e*8).or(tower2(+0x190..)).or(twin_towers[enemy](+0x130/+0x148) min_by dist(line.get_start_position(setting, team))).or(nexus[enemy](+0x170)).unwrap()
     in_tower_ally = iter_champions(team).filter(dist_sq(c, nearest_tower) <= 140000²).count(); in_tower_enemy = iter_champions(enemy).filter(가시 && !우물 && dist_sq <= 140000²).count()
     if in_tower_ally > in_tower_enemy { nearest_enemy = enemies.filter(가시·!우물·dist_sq(x,nearest_tower)<=140000²).min_by_key(dist(champ))
       if let Some(ne) { max_range = de9900(data, champ, ne); if dist_sq(ne, champ) <= max_range² { formation_ok = edfb50(…, ne, 200000); dive_viable = ee6d40(version, rnd, player, data, &tp, ne, true, debug)
         if formation_ok && dive_viable { dive_tower = (nearest_tower.ty(+0x68)==Tower(2)).then(|| info.ty(+0x128)); if let Some(b) = self.try_engage_dive(version, rnd, player, data, ne.id, dive_tower, debug) /*d5ff20*/ { self.plan = Battle(b); return } } } } } } }
 // ═══ §F 626~661 Dive 목표 — 동치(정독 생략) ═══
 if tp.objective == Dive(9){line} && !Battle && tick > last_dive_abandon + 1 + tps*4 {
   if let Some(nearest_tower) = tower(line)[enemy].or(tower2).or(twin min_by).or(nexus[enemy]) {
     in_tower_ally/in_tower_enemy (140000²); if in_tower_ally > 1 && in_tower_ally > in_tower_enemy { nearest_enemy = …; if let Some(ne) { max_range; if dist_sq <= max_range² { dive_viable = ee6d40(..true..); formation_ok = edfb50(..200000) /*순서 §E 와 반대*/; if dive_viable && formation_ok { dive_tower; if let Some(b) = try_engage_dive(..) { self.plan = Battle(b); return } } } } } } }
 // ═══ §G 669~723 정글 캠프 주변 교전 ═══
 _t_mg = ProfTimer::start(85)
 if !objective_waiting_posture && tp.objective(raw +0xdcd).is_none_or(|o| o.can_battle() /*JT: Morgard/Serpen phase==Setup(1) · Repair(7) false · ComebackPick ready · 그 외 true*/) && !Battle {
   if !is_line_phase(ctx, tick) /*tutorial∈{0,5,7,8} && tick < first_spawn − 30tps 이면 스킵*/ {
     if champ.hp*100 / champ.stat_cached.hp > 59 {
       can_near_enemies = f10c60(…, champ.x, champ.y, 250000, debug).len()
       near_allies = (0..5).filter(|i| (cc1 ? bb[+0x4d8] 게이트 : ally_battle_stop.is_none()) && champions[team][i].is_some_and(|c| dist_sq(c,champ) <= 150000² && c.hp*100/c.max > 59)).count()   // ★0.6.0 cc1 게이트
       if near_allies > can_near_enemies + 1 {
         camp_initiate_filter = match 유효 objective { Morgard{phase} => { Hunt => (camp_pos(Morgard),180000), Setup|Assemble => (…,230000), None => None }, Serpen 동형, _ => None }   // ★0.6.0 유효 objective
         nearest_enemy = iter_champions(enemy).filter(|x| 가시 && !우물 && dist_sq(x,champ) <= 250000² && camp_initiate_filter.is_none_or(|(c,r)| dist_sq(x,c) <= r²)).min_by_key(dist(champ))
         if let Some(ne) { max_range = de9900(..); if dist_sq(ne, champ) <= max_range² { if edfb50(…, ne, 200000) { if let Some(b) = try_engage(…, ne.id, debug) { self.plan = Battle(b); return } } } } } } } }
 // ═══ §H 730~769 DefenseLine 방어 다이브 — 동치(정독 생략 · try_engage_dive 아웃라인 +af2d) ═══
 if tp.objective == DefenseLine(3){line} { strategy = player.strategy(rnd, game)
   if !Battle && strategy.morgard_defense(+0xe) == 1 { if let Some(nearest_tower) = tower(line)[team].or(tower2).or(twin[team] min_by).or(nexus[team]) {
     in_tower_ally = …(160000²); in_tower_enemy = …(가시·!우물·200000²)
     if in_tower_ally > in_tower_enemy { nearest_enemy = enemies.filter(가시·!우물·dist_sq(x,nearest_tower)<=200000²).min_by_key(dist(champ)); if let Some(ne) { if dist_sq(ne,champ) <= max_range² { formation_ok = edfb50; dive_viable = ee6d40(..true..)
         if formation_ok && dive_viable { dive_tower = iter_towers_without_nexus(enemy).min_by_key(dist_sq(ne)).and_then(Tower→info.ty); if let Some(b) = try_engage_dive(..) { self.plan = Battle(b); return } } } } } } } }
 drop(_t_mg)
 // ═══ §I 777~808 Defense TryKill ═══
 if tp.objective == Defense(2) {
   if let Some(ne) = iter_champions(enemy).filter(가시 && !우물 && dist_sq(x,champ) <= 150000²).min_by_key(dist(champ)) {
     nexus = cache.nexus[team].unwrap()
     if cache.twin_towers[team].is_empty() && ne.dist_sq(nexus) <= 120000² && !Battle {
       if open_chase_race_hopeless(version, data, player, champ, ne) /*eeb5b0*/ { skip }
       b = BattlePlan::new(version, TryKill(ne.id, 60), data, player); b.entry_src = 10
       b.main_objective(+0x208..+0x20a) = tp.objective(+0xdcd..)
       b.screening = 1; b.update(…, &self.positioning_score, &tp, debug); b.screening = 0; b.chats.len = 0
       if b.sub_goal.tag >= 2 /*KitingBack·RunAway·End ★0.6.0 재번호*/ { drop(b); return }
       order = player.param.order_ratio(); if gen_range(0..1000) /*dcae80*/ < order*700/1000 + 300 { self.chats(+0x1198..).push(Chat::Battle(ne.id, 0)) }   // ★미확인: order_ratio 0.6.0 오프셋
       self.plan = Battle(b); return } } }
 // ═══ §J 812~853 Nexus 다이브 TryKill ═══
 if tp.objective == AttackNexus(4){_line} {
   if let Some(ne) = …(150000²).min_by_key(dist(champ)) { nexus = cache.nexus[enemy].unwrap()
     if cache.twin_towers[enemy].is_empty() && ne.dist_sq(nexus) <= 120000² && !Battle && tick > last_dive_abandon + 1 + tps*4 {
       if open_chase_race_hopeless(..) { skip } ; if !tower_dive_is_viable(…, ne, true, debug) { skip }
       plan = BattlePlan::new(version, TryKill(ne.id, 60), data, player); plan.with_dive(+0x1f8) = 1; plan.dive_join_tick(+0x1c0) = tick; plan.dive_tower(+0x207) = TwinA(3); plan.entry_src = 11
       plan.main_objective = tp.objective; plan.screening=1; plan.update(..); plan.screening=0; plan.chats.len=0; if plan.sub_goal.tag >= 2 { drop; return }
       order; if gen_range(0..1000) < order*700/1000+300 { self.chats.push(Chat::Battle(ne.id, 0)) }
       self.plan = Battle(plan); return } } }
 // ═══ 858 handle_solokill 인라인 (136~231) — ★0.6.0 Gank 조건 삭제 ═══
 if my_pos != Jungle(1) && tp.objective == Gank(8){line} && my_line(position→line) == line { jungler = champions[team][1]; jungler_ready = jungler.is_some_and(|j| dist_sq(j,champ) <= 200000² && map.bushes[clamp(j.y/32000,29)][clamp(j.x/32000,29)] != 0 && !game.is_visible(1−team, j.id)); if jungler_ready { return } }   // ★0.6.0: 위 조건이 아니면 return 하지 않고 바로 진행(구: Gank 아니면 return)
 if iter_champions(enemy).any(|c| is_near_line(ctx, c.x, c.y, line?) && bb[team].is_recent_visible(c) && c.hp*100/c.max < 35) { return }   // (line = Gank 라인 · Gank 아니면 이 검사는 ★미독: 어떤 라인 기준인지)
 champ; if Battle { return }
 let Some((e, t)) = check_kill(version, player, data, debug) /*d72e70*/ else { return }
 idx = aggr(+0x49d) ? 0 : def(+0x49e) ? 1 : 2; CTR[14502a250 + idx*0x30].0 += 1   // ★0.6.0 감사
 target = get_entity_by_id(e).unwrap(); max_r = de9900(data, champ, target); t = t.saturating_sub(tick)
 if ctx.debug { add_log }
 if t >= tps*5 { CTR[idx].+8 += 1; return }
 near_enemies = cache.champions(enemy, pool); near_enemies.retain(|c| bb[1−team].is_recent_visible(c) && c.dist_sq(champ) <= (de9900(data,champ,c)+30000)²)   // e121d0
 can_near_enemies = f10c60(…, champ.x, champ.y, 150000, debug); can_near_enemies.retain(|c| near_enemies.any(id == c.id))
 near_allies = (0..5).filter(|i| (cc1 ? bb[+0x4d8] 게이트 : ally_battle_stop.is_none()) && champions[team][i].is_some_and(dist_sq(c,champ) <= 120000²)).count()   // ★0.6.0 cc1
 if ctx.debug { infos push }
 if near_allies < can_near_enemies.len()/2 + near_enemies.len() { return }
 if self.plan.goal() /*ec21b0*/ .tag < 2 /*Line|Jungle*/ { if let Some(b) = self.try_engage(version, rnd, player, data, e, debug) { self.plan = Battle(b) } }
 drop(can_near_enemies, near_enemies); drop(_t_ob) → return
```

## changes
- ★신규 프롤로그(≈0x1500B): P1 flee_latch(+0x24aa)/latch_v3(+0x24ac) · P2~P4' pending tower_binary(+0x1ef8) 동기·유효성(d37d90)·다이브 콜(chat tag 4)·state 전이·KB/RA 중 exit 15 End · P5 one_shot(d601f0 → tp.+0xcd1/+0x24ae)+감사 · latch 해제 규칙(적 소액션 타깃·우물 160000·last_stand·undying).
- §A: 게이트 = 유효 objective(serpen/epic 씬 우선) · `aggr = +0x230≥70 ‖ 특성 +0x49d`(구 aggressive_ratio>699) · closure#6/#8 확정식.
- §B: 게이트만 유효 objective.
- §C: gank = bb Vec +0x250 조회 · is_passive 에 ObjContest arm · `!flee_latch` 게이트 · joiner(특성 +0x49f ‖ 합류창 +0x4c8 ‖ Support 특례) · range 450000/250000 · 트리거 1→2→3 · objcontest 캠프 250000 · v3 게이트 A(+0x2060/68/70 reason∈{7,13,15,16}·ee8090)·bb 게이트(d38030) · screening 래핑 · resolve_join_stake(committed 0, horizon joiner?12:6) · commit = line≠Disengage · KB/RA&&prev_die>2tps 즉시 reject · reject 기록 +0x2090/+0x2098 · flee_latch 세팅 · `b.joiner_mode(+0x202)=joiner`.
- §D: posture/filter 유효 objective 기준 · v27 4-arg(+0x19) · `flee_latch && !chased → skip` · chased → RunAway · isolated(d359f0·cc1) 게이트 · near_allies cc1 게이트 · 2차 update 무조건 · screening 래핑.
- §E~§H: 동치(콜리 순서 일치 · §E 게이트 +0x24d4 · §G can_battle raw JT · near_allies cc1 · camp filter 유효 objective).
- §I/§J: `sub_goal.tag ≥ 2 → drop` · screening 래핑 · dive_join_tick +0x1c0 · main_objective +0x208.
- solokill: Gank 조건 삭제(jungler_ready 검사만 Gank 시) · check_kill 뒤 특성별 감사 카운터 · near_allies cc1.
- 콜리 v3: e5ca10→d5f3c0 · e5d300→d5ff20 · ebd570→edfb50 · e07430→ee6d40(+2 사이트) · dfb840/dfb5d0→dd8e70/dd8b60 · dd9f30→f10c60 · e0cf10→de9900 · dd26e0/dd50e0→f09860/f0c310 · e657a0→d6bf10 · e05e70→d380c0(→ee4e70) · e38c90→dc1c10 · dea800→fb78b0 · e6b800→d72e70 · ffa3e0/131f300→16a7af0/157b720 · 1453260→19903f0 · ca4cc0→e121d0 · caefe0→ec21b0 · e44690→d35010 · d25ea0→dcae80 · 신규 eeae70/ee7fd0/db0160/d37d90/d601f0/eda920/eeb5b0/ee3000/ee8090/d38030/d354c0/d588e0/d359f0.

## verified
- capstone 0.6.0 d606b0(9415 명령 · 0.6.0 어노테이션 asm): P1 `+0x24aa=1 · +0x24ac=1`(d607fb/d60813) · pending `+0x1ef8` movdqu + d37d90 ×2(d6085a~d60f50) · latch 해제 `+0x24aa=0 · +0x24ac=0`(d60cf3~d60d0c) · P5 d601f0(d61fe5) ✓ · §C joiner `[r9+0x49f] ‖ (![+0x24f2] && [r9+0x4c8] Vec elem 0x18 kind==2 && s≤tick<e)` · `roaming=min([+0x228],100)`(d63be4~d63c9a) ✓ · 게이트 A `[+0x2070]` reason 검사(d64a60/d64ad6 `cmp 0xf`) · d380c0 호출(d64dc8) · reject 시 flee_latch 세팅(d6513c) ✓ · 300000(0x493e0)/450000(0x6ddd0) 상수 존재 ✓.
- 확인 못 한 지점: §E/§F/§H 상수(RE 도 정독 생략 · 콜리 순서만 일치) · §I/§J 채팅 확률 `order*700/1000+300` 와 order_ratio 0.6.0 오프셋 · bb +0x250 Vec/player +0x4c8 Vec 의 enum 이름 · +0x24ad/+0x24c7/+0x24dc/+0x24f2 설정 주체 · v27 +0x19 의미 · solokill 「Gank 아닐 때 line 기준」.

## confidence
B — 프롤로그·§C·§D 의 신규 조건은 asm 근거로 확정이나 §E/F/H 는 미정독(동치 추정) · 신규 헬퍼(d37d90/d601f0/ee3000/d38030/d359f0) 의 내부식은 RE 요지 수준.