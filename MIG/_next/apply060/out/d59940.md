# d59940→f749a0 calculate_action_score

## logic_060
```
// action_score.rs:8 · 0.6.0 f749a0 · ★0.6.0 인자 11→10: (version, rnd, player, data, parameter, action:&Box<dyn Action>, effect, speed_mult, t, ty:MinionAction) — `_debug` 삭제
// ★0.6.0 오프셋: PlayerState team +0xa00 · pos +0xa90 · id +0x9f8 · last_hit_accuracy 원천 +0x1e0(min 100) · 특성 aggr +0x49d · def +0x49e
//   ScoreParameter: wave_snapshot 태그 +0 (==1 Some) · count +0x908 · 미니언 trajectory stride 0xc0(id +0x98) · near_enemies ptr +0x14d8 / len +0x14f0 (원소 0xd8: id +0x58 · risk_damage +0x80 · risk_possible_tower +0x98 · possible_risk Vec +0x18/+0x30) · player(ChampionScoreParameter) +0x918(id +0x970 · risk +0x998 · tower +0x9b0)
//   Entity: ty +0x68 · team +0(tag)/+8 · hp +0x670 · max_hp +0x628 · id +0x5c0 · level +0x5c8 · xy +0x660/+0x668 · attack_effect +0x490(range +0x4a0 · growth +0x4a8 · casting 태그 +0x4c0) · stat_buff.range +0x438 · radius +0x680 · mult +0x470 · Tower.ty +0x128(3|4 twin) · Minion.nearest_enemy +0x88/+0x90 · Tower.nearest_enemy +0x88/+0x98 · Box<dyn Action> +0x570/+0x578(vt+0x90 cooltime) · attack_speed_mult +0x3fc
//   Blackboard stride 0x5c8 · small_actions +0x1d0+pos*0x18 {tag +0 (6 Attack/7 Skill/8 Skill2) · target +8} · MobaMode epic_minion_buff_time +0x240+team*8 · Action vt +0x80 duration / +0x90 cooltime · Effect vt +0x108 range_adjust
// 콜리 v3: expected_damage_target 12857f0→1643790 · gen_range(0..=1000) d25d30→dcad10 · range_misjudge_roll_i64 d37710→de3df0 · hp_at_tick→f714f0 · player_by_champion_id d31bb0→d72be0 · iter_entity 14183ed40→1526cb0 · distance 12a07d0→1660a00 · projectile 14132b310/14129feb0/14132aa00→1514a50/165e4d0/1514140 · 신규 ff1fc0 possible_risk · db0400 champion_hp_value 캐시(본체 de5780)

// L10  team=player.team(<2); champ = cache.player_champion[team][position].unwrap()
// L11  acc = min(player.+0x1e0, 100)*99/10 + 10 (0..1000 스케일) ; base = 1000-acc ; min_v=acc ; max_v=2000-acc
// L18  jrng = range_misjudge_rng(version, data, player, t.id)   // ★v2 사장: version<2 는 rnd.gen_range(dcb010) 직접 롤. v≥2 = 결정적 splitmix 시드 = judger.id<<8 ^ (t.id<<0x14 | 0x5a3e) ^ seed(vt+0x20) ^ (tick/(max(tps,1)*6))<<0x28 → 롤마다 시드 += 0x9e3779b97f4a7c15 후 splitmix 로 [acc, 2000-acc] 추출(0.5.8 과 동일 구조)
// L19  value = effect.expected_damage_target(ctx, champ, t) * roll/1000        // 매번 새 롤
// L20  hp = t.hp*roll/1000;  L21 total = t.stat_cached.hp*roll/1000
// L22  start_timing = (effect.start_timing*100/speed_mult)*roll/1000   (speed_mult==0 → 패닉)
// L23  cooltime = (action.cooltime(vt+0x90)(champ)*100/speed_mult)*roll/1000    // roll*raw (÷1000 전) 이 뒤의 29999 비교에 쓰임
// L26  match t.ty {
//  Minion(1) =>
//   L29 if let Some(target)=t.Minion.nearest_enemy(+0x88 Some, +0x90 id).and_then(get_entity_by_id) { match target.ty {
//     Tower(2) => { L32 if Moba && epic_minion_buff_time[1-team]!=0 → return 30;  L37 if target.Tower.ty(+0x128) ∈{3,4 twin} { L38 if Moba && buff[1-team]!=0 → return 70 else → return 50 } }
//     Nexus(3) => { L46 if Moba && buff[1-team]!=0 → return 100 else → return 70 }
//     _ => {} } }
//   L55 if Moba && epic_minion_buff_time[1-team]!=0 → return 20        // 적 에픽버프 미니언
//   L62 if let Some(snap)=parameter.wave_snapshot(+0 == 1) { L63 if let Some(traj)=snap.minions[..count(+0x908)].find(id==t.id) {   // count>12 → 패닉
//     L68 error_prob = min(base*base/1000, 1000)
//     L72 if gen_range(0..=1000)(dcad10) < error_prob → return -9999               // 오판
//     L76 early = tutorial(ctx+0x38)∈{0,5,7,8} && tick < sat(setting.first_spawn_tick(+0x8a8) - 30*tps)
//         (flag, def_style) = if early { (false, false) }                                       // ★0.6.0 특성 플래그(v2/v3 공통)
//                             else if player.aggr(+0x49d)==1 { (true, false) }                    // ★0.6.0
//                             else { d = player.def(+0x49e); (d || position != Jungle(1), d) }    // ★0.6.0 (0.5.8: flag = !early && position!=Jungle)
//     L89 predicted_hp = hp_at_tick(traj, start_timing)*roll/1000 ; L90 death_tick = traj.expected_death_tick(+0xb8)
//     L93 if roll*hp_at_tick > 999 (생존) {
//        can_last_hit(DI명) = predicted_hp > value+5   // = 이번 타격으로 못 죽임
//        will_die_soon = death_tick ≤ cooltime+start_timing
//        L97 if !can_last_hit (지금 죽일 수 있음) {
//          L100 urgency = death_tick > start+5 ? (will_die_soon ? 25 : 15) : 30
//          L113 concurrent = #{other in snap: id≠t.id && 0 < hp_at_tick(other, cooltime+start) ≤ value+5}
//          L126 multi_bonus = concurrent>0 ? (L129 earlier=#{other: id≠t.id && other.death_tick < death_tick}; earlier==0 ? 5 : -5) : 0
//          L147 return !flag ? urgency+multi_bonus+3 : (def_style ? urgency+multi_bonus+5 : urgency+multi_bonus)   // ★0.6.0 def_style +5 분기 추가
//        } else { L152 if will_die_soon && predicted_hp ≤ 2*value → return ty==Pull ? -9999 : 5 }
//     }
//     L159 if predicted_hp > 3*value { L161 match ty { Pull(0)→-9999, Push(2)→10, Normal→ L168 gen=gen_range(0..=1000); if roll*cooltime_raw>29999 → (gen<err?10:-9999) else if (flag && !def_style) → (gen<err?10:-9999) else → (gen<err?-9999:10) } }   // ★0.6.0 `flag`→`flag && !def_style`
//     else { L178 match ty { Pull→-9999, Push→10, Normal→ L184 gen<err ? 10 : -9999 } }
//   } }
//   // 스냅샷 없음/미등재 → 레거시(L195~)
//   L199 (flag, def_style) 위와 동일 계산                                                        // ★0.6.0
//   L211 for e in iter_entity(1526cb0): match e.ty { Minion(1)|Tower(2)|Ghoul(7)|SmallJiangshi(8)|Bear(9)|Eagle(10) }: if e 가 t 를 조준(nearest_enemy/target_enemy == t.id) { dmg = e.attack_effect.expected_damage_target(ctx,e,t) (None→패닉); acd=attack_cooltime(e)=max(cool*100/max(as+100,1),3); ast = attack_effect.start_timing(+0x4b0)*100/max(as+100,1); if e.state==Attack(+0x70) && !(Minion && is_range(+0x118)) && time(+0x80)<ast && ast(+15 if Tower)-time < start_timing && Attack.target_id(+0x78/+0xb8)==t.id → applyed += dmg; expected += max((cooltime+start_timing)/acd,1)*dmg }
//   L297 for p in iter_projectile(vt+0x210): if p.move_type==Target(6) { if target_id==t.id { caster=get_entity_by_id(p.caster_id)?; dist=distance(p,t); dmg=p.expected_damage_target(1514a50)(ctx,caster,t); if start_timing ≥ dist/speed+5 → applyed+=dmg; expected+=dmg } } else { caster?; if p.applyed_target.check_projectile(165e4d0)(p,t) && p.is_in_orbit(1514140)(t.x,t.y, radius*(100+mult)/100) { dmg=…; applyed+=dmg; expected+=dmg } }
//   L319 applyed*=roll/1000; L320 expected*=roll/1000; L321 error_prob = 1000 - gen_range(acc..=1000)   // (v≥2: 두 롤 모두 splitmix)
//   L325 if value-5+applyed < hp (못 죽임) {
//      L365 if value + total*3/10 + expected < hp { L368 Pull→-9999 · Push→10 · Normal→ L378 cooltime>29999 ? (gen<err?10:-9999) : (flag&&!def_style) ? (gen<err?10:-9999) : (gen<err?-9999:10) }   // ★0.6.0
//      else { L393 Pull→-9999 · Push→10 · Normal→ L402 gen<err?10:-9999 }
//   } else { L329 if gen_range(0..=1000) < error_prob → return -9999
//      L333 if expected + total*3/10 < hp { L335 Pull→-9999 · Push→10 · Normal→ L345 cooltime>29999 ? (gen<err?10:-9999) : (flag&&!def_style) ? (gen<err?10:-9999) : (gen<err?-9999:10) }   // ★0.6.0
//      else { L359 gen; !flag ? (gen<err ? -9999 : 20) : (!def_style ? (gen<err ? -9999 : 15) : (gen<err ? -9999 : 25)) } }   // ★0.6.0 def_style → 25 분기 추가
//  Champion(13) =>
//   L414 bonus=0; if t.team != champ.team { L416 if let Some(ep)=player_by_champion_id(t.id) { L417 a = blackboard[1-team].small_actions[ep.position]; if a.tag∈{6,7,8} { L419 if let Some(tt)=get_entity_by_id(a.target_id) { L420 match tt.ty { Tower → bonus = twin(+0x128∈{3,4}) ? 80 : 10 (L424); Nexus → bonus = 100 (L428) } } } } }
//   L438 value = effect.expected_damage_target(ctx, champ, t) (롤 없음)
//   ★0.6.0 v3 적 챔피언 조기 반환 (L428 뒤 · f75173~/f75767~/f76614~):
//   if version >= 3 && t.team != champ.team && player.position != Jungle(1) {
//     if let Some(tp) = parameter.near_enemies.iter().find(|p| p.id == t.id) {
//       hv_t  = champion_hp_value_cached(db0400)(data, parameter, tp)
//       a = value + tp.risk_damage/2 + tp.possible_risk(ff1fc0)(data, action.duration(vt+0x80)+30)/4 /*signed, 0 방향 절삭*/ + tp.risk_possible_tower
//       s_t = hv_t * a / max(t.hp, 1)                                  // i64 나눗셈
//       me = &parameter.player(+0x918); hv_me = champion_hp_value_cached(data, parameter, me)
//       b = me.possible_risk(data, action.duration()+30) + me.risk_damage + me.risk_possible_tower
//       s_me = hv_me * b / max(champ.hp, 1)
//       return s_t + bonus - s_me
//     }  // 장부에 없으면 공통 경로(L438) 로 낙하
//   }
//   → L452 공통
//  _ => bonus = 0 → L452
// }
// L452 has_epic_buff(DI명) = !(Moba && ptr) || epic_minion_buff_time[team]==0     ⚠이름과 반대 극성처럼 보임 — IR 그대로: '아군 에픽버프 없음' 이 true
// L454 coef = match t.ty {
//   Nexus(3) → 200
//   Tower(2) → L457 in_range_minion = #{e in iter_entity: e.team==champ.team && e.ty==Minion && t.attack_effect.is_in_range(t, e)}   // ★t(타워) 자신의 attack_effect(사거리) — champ 것이 아니다. casting −1 이면 같은 팀 미니언을 만나는 순간 unwrap 패닉(L459)
//              L460 cond = match t.Tower.nearest_enemy(+0x88) { Some(id=+0x98) if id!=champ.id → match get_entity_by_id(id) { Some(e) → e.hp ≥ t.attack_effect.expected_damage_target(ctx, t, e) || in_range_minion>1, None → in_range_minion>1 }, _ → false }
//              L462 if cond { ty==Push(2) ? (has_epic_buff ? 160 : 240) : (has_epic_buff ? 80 : 160) } else { L476 t.hp > effect.expected_damage_target(ctx,champ,t) ? 0 : 30 }
//   _ → 0 }
// L516 return min(coef, coef*value / t.hp) + bonus     // t.hp==0 → 패닉 · (−1 && overflow 검사)
```

## changes
- 인자 11→10(`_debug` 삭제 · 본문 미사용이던 인자).
- L76/L199 flag 산출: `flag = !early && position!=Jungle` → `(flag, def_style) = early ? (false,false) : aggr ? (true,false) : (def || pos!=Jungle, def)` (특성 +0x49d/+0x49e · v2/v3 공통).
- L147: `flag ? urgency+multi : urgency+3+multi` → `!flag ? +3 : (def_style ? +5 : +0)`.
- L168/L345/L378 Normal arm: `flag` 조건이 `flag && !def_style` 로.
- L359: `flag ? 15 : 20` → `!flag → 20 · flag&&!def_style → 15 · def_style → 25`(각각 gen<err 이면 −9999).
- Champion arm L428 뒤(v3): near_enemies 장부에 t 가 있으면 `hv_t*(value + risk/2 + possible_risk(dur+30)/4 + tower)/max(t.hp,1) + bonus − hv_me*(possible_risk_me + risk_me + tower_me)/max(champ.hp,1)` 즉시 반환(정글러 제외). 없으면 공통 경로.
- last_hit_accuracy 원천 `player+0x1e0`(min 100)→`*99/10+10` 인라인(0..1000 재정규화 · 값 동치 추정).
- 오프셋/콜리: 위 헤더 표. 미니언 분기·타워 coef·L516 은 그대로.
- ★v2 사장: version<2 는 dcb010 직접 롤(L18~L23 · L321 등) — v≥2 는 splitmix 결정 롤(0.5.8 과 같은 구조).

## verified
- Ghidra 0.6.0 f749a0 디컴 전량 정독: 인자 10개(param_10 = ty) · +0x1e0 acc 산식 · v<2/v≥2 롤 분기 · Champion arm(bb +0x1d0 tag 6..8 · Tower twin +0x128−3<2 → 0x50/10 · Nexus 100) · v3 조기반환(`2 < version` · team tag/payload 비교 · `pos != 1` · near_enemies +0x14d8/+0x14f0 stride 0xd8 · id +0x58 · +0x80 /2 · ff1fc0 >>2(음수 +3 보정) · +0x98 · db0400 · max(t.hp,1) · player +0x918 · +0x998/+0x9b0 · `return s_t + bonus − s_me`) · early(0x1a1 마스크 · +0x8a8 − 30*tps) · +0x49d==1 → (1,0) · else (pos!=1 | +0x49e, +0x49e) · L147 (+3 / +5 / +0) · L359 (0x14 / 0xf / 0x19) · L168 계열 `(cool_raw<30000) && (!flag || def)` → (gen<err?−9999:10) else (gen<err?10:−9999) · 타워 coef(0xa0/0xf0 · 0x50/0xa0 · has_epic_buff) · L516 min/div.
- 미확인: de5780(champion_hp_value 본체) 산식이 0.5.8 utils::champion_hp_value 와 비트동일한지(RE 도 추정) · ff1fc0 내부(RE: +0x18 Vec<(id,tick,dmg)> tick≤인자 id별 max 합).

## confidence
A — 변경 5지점 + 신규 조기반환 전부 디컴에서 확인. de5780/ff1fc0 내부만 RE 의존.
