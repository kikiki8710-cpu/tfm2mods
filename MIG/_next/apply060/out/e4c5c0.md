# e4c5c0→d3d210 LegacyPlanHandler::update

## logic_060
```
// sig 동일: fn update(&mut self LPH, version, rnd, player, data, debug, lapse)   (rcx,rdx,r8,r9,[rsp+0x28],[rsp+0x30],[rsp+0x38])
// ★0.6.0 호출자 변경: AgentVerHamster::update_state f3adb0 → 래퍼 d5a940(1648B) = `update(); if version>2 { Battle 캐스팅 워치독(★미독: d5aaa3..d5afaf · 필드 +0x2078/+0x2080/+0x2088 · +0x24e9 · +0xf48/+0xf50 · bp+0x1a8/+0x1f8/+0x215 · 챔프 액션태그 JT 3a74744 · add_log) }` (f3b0a2·f3b100)
// ★0.6.0 LPH 오프셋: data(GoalData) +0x0 · team_plan +0xf8(tp.X = +0xf8+X) · pos_ring +0x458..+0x470(cap/ptr/head/len) · plan +0xf58(Battle 니치 tag<2 · 페이로드 plan+0 · 그 외 tag=idx+2 · 페이로드 +8) · sub_plan +0x13a0 · positioning_score +0x1408 · chats +0x1198/11a0/11a8 · received_chats/misunderstood/chats_wait(미확인 오프셋 · 구 0x7f8~) · pending_trace_events(미확인) · prev_plan_name(미확인 · 구 0x840) · gank_periods(미확인 · 구 0x810) · gank_cancel_tick +0x1f40 · last_dive_abandon +0x1f50 · last_response_bail +0x1f58 · last_lost_fight +0x2050/58 · ★+0x2060/68/70 last_failed_engage · ff_battle_exit +0x2218(tick)/+0x2220(src)/+0x2221(entry)/+0x2222/+0x2223 · latch +0x2228..+0x222f · mf_swap +0x2230(u8)/+0x2238 · mf src hist +0x1fe0[·] · ★+0x1ef8..+0x1f18 pending tower_binary · +0x1ef0 · v2_egowave +0x24f0 / line +0x24d8 · +0x24d9 취소 갱크 라인 · armed +0x24b4..+0x24c7, +0x24d4~d6 · 채팅 플래그 +0x24cb~cf/+0x24d0~d2 · 아군 라인슬롯 +0x24dc..+0x24e0 · ObjContest 표 +0x2488(+k*10), +0x249e/+0x24a0 · +0x24a4/+0x24a5 · ★v3 리콜 +0x24aa/+0x24ab/+0x24ac · ★정글 상태 +0x1ed0/+0x1ed1, +0x1f70 · dive_ep_live +0xe30(w5) · abort_src +0x24ec · v3_dest/v3_dest_obj(미확인 · 구 0x550/0x558/…) · counter_jungle_route_init(미확인) · v3_home_ladder(미확인 · 구 0x1640) · mf_ret25_src(미확인 · 구 0x1658)
// ★0.6.0 TeamPlan(self+0xf8 기준): anchor +0x120/+0x128/+0x130 · radius +0xb50 · plan_name &str +0x4d8/+0x4e0 · sub_plan_name +0x4e8/+0x4f0 · +0xccc(←self.24aa) · +0xcd0(←lapse) · objective +0xcd5/6/7 · mf_obj_clear +0x500/+0x508 · last_resolver_bail +0x518 · ETA +0x778/+0x788/+0x798(+k*8) · serpen +0x3d8/+0x3e0/+0x3e1/+0x3e2/+0x3e4 · epic +0x3f8/+0x400/+0x401/+0x402/+0x404 · 게이트 +0x70/+0x80 · 계약 +0xc0/+0xd0../+0xf0/+0x100.. · +0xd00 · chats +0x2e8 · comeback_pick_*(미확인)
// ★0.6.0 Blackboard(0x5c8): minion_state +0/+0x28/+0x50 · Vec +0x250/+0x258(레코드 0x50B: +0x20 pos · +0x30/+0x38 u64 · +0x40..+0x44 [bool;5] · +0x45 line · +0x46 kind(1/3/4/9/10 관측) · +0x47/+0x48 표결) · big_goal +0x2f8+i*0x20(tag@+8=+0x300, line@+0x301)
// player: team +0xa00 · pos +0xa90 · roaming +0x228 · ego +0x238 · Strategy +0x568 · champ id +0x9f0 · 특성 +0x49d/+0x49e/+0x4a0(LaneIntervention)
// ★v2 사장: `version >= 2` 게이트(armed) 고정 참 · `version < 2` 전용(1724 r2_saved None 가지 · 1756 ff_battle_exit) 은 v3 도달 불가 · `version>2/>=3` 블록은 v3 가지만 적음.

// ── L685~697 (배치 A) 게임모드 분기 — Moba 본류만
if game.get_game_mode()(vt +0x40).tag == DeathMatch(2) { self.update_deathmatch(..); return }   // 사장(Moba)
if game.get_game_mode().tag == SingleLane(1) { self.update_single_lane(..); return }              // 사장(Moba)

// ── ★0.6.0 §3-A v3 위치 링 (d3d6d4 · 본체 진입 전)
if version > 2 { t = game.tick(); if let Some(c) = champ { ring = &mut self.pos_ring(+0x458..) /*VecDeque<(tick,x,y)>*/; if ring.len()==0 || ring.back().0 != t { ring.push_back((t, c.x, c.y)) /*grow ff4380*/ } }
                 while ring.len() > 0 && t.saturating_sub(ring.front().0) > tps+1 { ring.pop_front() } }   // 최근 1초 위치 이력 · 소비처 = f71610 TurnBack veto(r22 a) · free_dist fafc10 추정

// ── L698 (배치 B)
698 _t_head = ProfTimer::start(77); (ctx.debug 시 이름 fmt 디버그 · tp.3e4/404)
699 self.sanitize_rule_scope(data)   // d560b0 (Ghidra hang · 내부 미탐색)
// ── ★0.6.0 §3-B 앵커/이름 계산 (d3dfbf~d3e3c6 · L3279~3296 · TeamPlan::update 직전)
tp.+0xccc = self.+0x24aa; tp.+0xcd0 = lapse
(anchor, r) = match self.plan.idx() {
  PassiveLine(idx1) => { ms = bb[team].{top|mid|bot}(+0/+0x28/+0x50 by line@+0x107b); if ms.tag==1 { if let Some(e) = get_entity(ms.+8) { (e.pos, 250000) } else { tower_fallback } } else { tower_fallback } }   // tower_fallback = cache.+0x180+line*0x20+team*8 (없으면 +0x190) → 없으면 cache.+0x148.. → nexus +0x170
  PassiveJungle(idx5) => (map_pos_cached(map, kind=jungle@+0xfe3, team=(+0xfc0!=0)) /*16a7af0*/, 150000),
  Epic*(idx10,11) => (map_pos_cached(map, 4, team!=0), 250000),  Serpen*(idx12,13) => (map_pos_cached(map, 5, team!=0), 250000),
  AttackNexus(idx14) => d37040(&out, team, champ) → Some((x,y),250000) | None,  DefenseNexus(idx15) => (cache.nexus[team].pos, 250000),  _ => (None, 0) }
tp.anchor(+0x120 tag,+0x128 x,+0x130 y) = anchor; tp.+0xb50 = r
tp.plan_name(+0x4d8/+0x4e0) = BIGPLAN_NAMES[idx](3a73cf8/3a73d78); tp.sub_plan_name(+0x4e8/+0x4f0) = SUBPLAN_NAMES[sub_plan idx](3a73db8/3a73e48)
702 { _t = ProfTimer::start(57); 703 self.team_plan.update(version, rnd, player, data, debug) /*f18f90 · 신규 에필로그 +0x138..+0x158 finish_race 캐시(w11 §B4)*/; 704 drop(_t) }
705 self.chats(+0x1198..).extend(self.team_plan.chats(+0x2e8..).drain(..))   // e7a790
707 { _t = ProfTimer::start(58); 708 GoalData::update(&mut self.data(+0x0), version, rnd, player, data, debug) /*ff11f0 · v3 undying heal_commit 게이트*/; 709 drop(_t) }

// ── L711 armed + egowave — ★0.6.0 §3-C 확장 (d3e592~)
// (version >= 2)
self.+0x24b4/+0x24b5 (v2_armed/v3_armed) = 1; +0x24d4 (v3_epicops_armed) = 1; +0x24b7..+0x24be = 1; +0x24c0..+0x24c4 = 1
v3 = (version != 2): +0x24b6, +0x24c5, +0x24c7, +0x24c6, +0x24d5(Support 슬롯 제외), +0x24d6(assign 신모드) = v3   // ⇒ version=3 상시 1
// ★0.6.0 포지션 분기: Jungle → §3-E(d40da4) 로 · Support && v3 → §3-D(d3e636) 로 · 그 외 egowave 로
// egowave_check 인라인 (L2269~2285) — ★0.6.0 상수 스케일 변경
match egowave_check(player, bb, ctx) {
    if player.pos == Jungle → None
    r = min(player.roaming(+0x228), 100)*10; e = min(player.ego(+0x238), 100)*10   // ★0.6.0
    my_line = Top→0, Mid→1, _→2; my_line = rule_scope::fallback_line(ctx, my_line) /*line_exists 아니면 valid_lines(tutorial).last() · JungleOnly→Bottom*/
    ms = bb[team].minion_state(my_line); if !(ms.from_mid < -2000 && ms.minion_count < -2) → None
    if player.roaming > 49 → None   // ★0.6.0 (wave_concern = 500−r < 1 과 동치)
    Some((my_line, follow_own = (500 − r)*e/500)) }   // ★0.6.0 = (500−10·roam)·(10·ego)/500 · passive_plan ③ 의 /1000 과 스케일 상이(주의)
  None → self.v2_egowave(+0x24f0) = 0
  Some((line, follow_own)) → if self.v2_egowave != 0 && self.v2_egowave_line(+0x24d8) == line { 유지 } else { self.v2_egowave = if gen_range(0..1000) /*dcaf70*/ < follow_own {2} else {1}; self.v2_egowave_line = line }
// ── ★0.6.0 §3-D 서포터 v3 아군 라인슬롯 (d3e636 · v3 && pos==Support)
for i in 0..4 { slot = &mut self.+0x24dd+i; match (champ[team][i], bb.big_goal[i].tag(+0x300+i*0x20)) { (None,_) => *slot=0xff, (Some(c),0 /*Line*/) => { line = bb.+0x301+i*0x20; if *slot==line || is_near_line(ctx,c.x,c.y,line) /*19903f0*/ { *slot=line } }, (_,1..=4) => *slot=0xff, _ => {} } }
if champ && tutorial∈{0,5,7,8} && tick < first_spawn(ctx+8→+0x8a8) − 30*tps { if efc410(tp,1,data)==2 && efc410(tp,0,data)==2 { … } else { … }   // ★미독: 서포터 라인페이즈 사전플랜 d404d2..d407ad + d44322..d44853 + d4e495..d4e78a(≈2.5KB · efc410×2 · d37650/d37850 · 19903f0) }
else { self.+0xdf8(=tp.+0xd00) = 0; self.+0x24dc = 0xff }
// ── ★0.6.0 §3-E 채팅 발행 (d40da4~d4140d · kind k∈{1,3,4} · prev=+0x24cb/cc/cf · sent=+0x24d0/d1/d2 · armed +0x24b7/b8/b9/ba)
recs = bb[team].vec250
for k in [1,3,4] { now = recs.any(|r| r.kind==k && r.flag[min(my_pos,4)]); was = *prev; *prev = now
  if !recs.any(|r| r.kind==k && r.pos != my_pos) && self.+0x24c5 { *sent = false }
  if now && !was && champ.is_some() && !recs.any(|r| r.kind==k && r.pos==my_pos) && !(self.+0x24c5 && *sent) { *sent = true; self.chats.push(Chat{tag: 0xf, payload: 0}) } }
// ── ★0.6.0 §3-F 넥서스방어 (d4140d~d41684)
if self.+0x24b9 { if recs.any(kind==3 && pos != me) { was = self.+0x24cd; self.+0x24cd = 1
    if !was && tp.objective(+0xcd5) != Defense(2) { n = calculate_nexus_defense_count(player, data) /*d75ad0*/
      if i_am_chosen_defender(player, data, n, 8, 0) /*e759a0*/ { tp.objective = 2
        if plan.is_battle() { self.ff_battle_exit = (src 20, bp.entry_src(+0x213), tick, bp+0x216, bp+0x21e?); latch ← bp+0x21d/+0x21b(u16)/+0x217(u32)/+0x20f; if bp.tower_binary.state(+0xd0) != 2 { self.+0x1ef8..+0x1f18 = bp.+0xb8..+0xd8 } }
        mf_note_swap(23, tick) /*+0x2230/+0x2238*/; self.plan = ForcePassive(tag 2) } } }
  else { self.+0x24cd = 0 } }
// kind 4 (d41684~d41892 · L2363) 동형: recs.any(kind==4 && r.+0x45 != 0xff) → +0x24ce; tp.objective = AttackNexus(4); tp.+0xcd6 = r.+0x45(line); ff src 19; 강제 ForcePassive
// ── ★0.6.0 §3-G ObjContest 표결 동기화 (d418a6~d42145 · L1599)
for k in 0..2 { mode = k==0 ? tp.+0x3e4 : tp.+0x404; kind = 9 + (mode != 2) as u8; table = &mut self.+0x2488+k*10 ([u8;2]×5)
  for r in recs.filter(kind==kind && pos != me) { table[min(pos,4)] = (r.+0x48, r.+0x47); max(+0x30)→A; max(+0x38)→B }
  +0x249e+k / +0x24a0+k 플래그; if (tp.+0xcc5/+0xcc6 게이트 · +0x24c2 · tp.+0x70/+0x80 · 계약 tp.+0xc0(tag)/+0xf0 · get_game_mode==0) { tp.objective = 0xff|0|1; serpen/epic 필드 리셋(tp.+0x3d8=-1, +0x3e0=cnt, +0x3e1=(cnt!=0), +0x3e2..=0 / epic +0x3f8, +0x400..+0x404) }   // ★미독: 정확 조건
  if tp.+0xcc7 { p = map_pos_cached(map, 4+(k==0), team); eta = dist(champ,p)/max(ms,1) + tick; tp.+0x788+k*8 = max(eta, A); tp.+0x778+k*8 = eta + 4tps + sat(eta−tick); tp.+0x798+k*8 = B } }
// ── ★미독: 강제 플랜 배정 d42145..d43c65 (≈7KB · L1774~2034 · ec21b0×4 goal · d38180×4 passive_plan · f1e3c0×2 · d56000 · f1fce0 check_press_tower · d5d700(5.2KB) · eda830 · 2e44af0×4 · vcall 0x1520)
// ── ★미독: 강제 LineGanker 생성 d43c70..d44e64 (≈4.6KB · L2167 · JT 3a74448/3a7445c(포지션 5) · dcaf70 · d37be0 · ecb0f0×3 · f1dd50×2 · d37650×2 · d37850 · d58bd0 · dc1c10 · fa1ec0 · fb1510 · d56740 · dcae80) → `self.plan = LineGanker{phase 7, own_line(+0xac)=JT by my pos{Top→0,Mid→1,Bot/Sup→2}, exposed(+0xa7)=game.is_visible(1−team,my.id), wait_limit=tick+tps*15 …}` d44e69 `[TRAIT_ROAM]`
// ── ★0.6.0 §3-H v3 정글 귀환 상태기 (d44f42~d451ab · L1351 · version>=3 && pos==Jungle && champ)
line_phase = …; strat = replay?(vt +0xe8 is_solorank) player.strategy(+0x568) : game.vt[0x108](team) /*Strategy 24B*/; r12 = (strat.+2 == 0)
match self.+0x1ed0 { 2 => if !fb1200(_, player, data, champ) /*귀환 필요: 우물 rect 안&&hp<max ‖ fb12a0(귀환필요도)>49*/ && (tp.objective==None || r12) { … ★미독: d45b1e … },
                     s => { bl = self.+0x1ed1; if fb1200(..) { counter(+0x1f70?)++; self.+0x1ed0 = 2 } else { ★미독: d45a0f..d45e40 + d4e083..d4e34c(≈1.5KB · d55f00 · d580a0 · d58a80 · f1dda0 · d36270 · d367a0) → d4e101/d4e1b0 } } }

714 self.sanitize_rule_scope(data)
716 if !self.counter_jungle_route_init && player.pos == Jungle && player_count(ctx)==full /*!(tutorial∈1..=7)*/ {
  717 self.counter_jungle_route_init = true
  718 strategy = player.strategy(rnd, game) /*fa2500*/
  719 if strategy.early_jungle == CounterJungle(2) { 720 mf_note_swap(28, tick); 722 p = PassiveJunglePlan::new_counter_jungle(rnd, team, strategy.focused); 721 self.plan = PassiveJungle(p) /*tag 7 · 페이로드 0x88B ★0.6.0(구 104B)*/ } }
728 if !lapse && (!self.received_chats.is_empty() || !self.misunderstood_received_chats.is_empty()) {
  729~735 handled_chats = received_chats.filter(tick <= now).clone(); received_chats.retain(tick > now)   // grow28/grow24 · f44100 retain
  736 for (tick, from, c) in handled_chats { 737 if rule_scope::chat_allowed(ctx, &c) { 738 misunderstood = self.take_misunderstood_received_chat(tick, from, &c) /*d59280*/; 739 self.handle_chat(version, rnd, player, data, from, &c, misunderstood, debug) /*d5b410 → handle_chat_inner d5b980(w7 §3)*/ } }
  742 misunderstood_received_chats.retain(tick > now) }
745 if !lapse && !self.chats_wait.is_empty() { 746~749 for (tick, chat) in chats_wait { if tick <= now && chat_allowed(ctx, chat) { self.chats.push(chat.clone()) } } 754 chats_wait.retain(tick > now) }   // (+debug add_log ★0.6.0)
758 if let Some(chats) = self.plan.chats() /*태그별 Vec 위치 · ★0.6.0 Battle 니치: +0x88*/ { 759 self.chats.extend(chats.drain(..)) }
761 self.sanitize_rule_scope(data)
763 drop(_t_head); 764 _t_np = ProfTimer::start(59)
765 next: Option<BigPlan(0x228)> = self.plan.next_plan(version, rnd, player, data, &self.data, &self.positioning_score(+0x1408), &self.team_plan(+0xf8), debug)   // ec2f70
767 drop(_t_np); 768 _t_tail = ProfTimer::start(78)769 if let Some(plan) = next {
  770 if rule_scope::plan_allowed(ctx, &plan) /*goal = plan.goal()(ec21b0) → goal_allowed: Line→line_exists · Jungle→tutorial∈{0,6,8} · Epic→∉1..6 · Serpen→∈{0,5,7,8} · Nexus/Battle/Recall→true*/ {
    772 if plan.tag==LineGanker(10) && plan.phase(+8+0xad) == WaitResponse(6) { 775 can_transition = tick.saturating_sub(self.gank_cancel_tick(+0x1f40)) > tps*10; 783 if !can_transition → 945 drop(plan) → 947 }
    784 if let Some(chats) = self.plan.chats() { 785 self.chats.extend(chats.drain(..)) }
    789 if player.pos == Jungle {
      789 if self.plan.tag == PassiveJungle(7) { 790 self.last_jungle_lead_action_tick = tick }
      797 if self.plan.tag == LineGanker(10) {
        799 is_transition_to_battle = plan.tag < 2 /*★0.6.0 Battle 니치*/
        801 if is_transition_to_battle { 803 self.active_gank_line = Some(self.plan.LineGanker.line(+0xf58+8+0xab)) }
        else { 806 self.active_gank_line = None
          807 if let Some(last) = self.gank_periods.last_mut() { 808 if last.1 == 0 { 809~811 start=last.0; end=tick; last.1=end
            813 if ctx.trace_level(+0x39) != Off { 815~833 duration/line/kills(d79b70)/deaths(d79c30)/success → pending_trace_events.push(GankResult{..}) } } } } }
      850 if self.plan.tag < 2 /*Battle*/ {
        851 if plan.tag < 2 → 938
        853 if tp.objective(+0xcd5) == ComebackPick(11) { 854~863 got_kill = kill_logs(vt +0x130).rev().any(killer_team==team && tick >= comeback_pick_start_tick); if got_kill { comeback_pick_success_count += 1; outcomes.last.1 = 1 } else { outcomes.last.1 = 6 } }   // (미확인 오프셋)
        866 if tp.objective ∈ {ComebackPick(11), Gank(8), Dive(9)} { 867 tp.mf_note_obj_clear(41, tick) /*+0x500/+0x508*/; 868 tp.objective = None(0xff) }
        870 if let Some(last) = gank_periods.last_mut() { 871 if last.1 == 0 { … 879 line = fallback_line(ctx, self.active_gank_line.unwrap_or(Mid)); 882~899 GankResult push 동일 } }
        909 self.active_gank_line = None }
      916 if plan.tag == LineGanker(10) {
        918 last_start = gank_periods.last().map(.0).unwrap_or(0)
        919 if tick.saturating_sub(last_start) > tps*10 { 920 gank_attempt_count += 1; 922 gank_periods.push((tick, 0)); 925 actual_score = evaluate_gank_opportunity_with_score(rnd, player, data, plan.line, 0) /*fb1510 (노이즈 폭 /20)*/ .1; 927 gank_score_attempts.push((tick, actual_score, 0)) }
        931 if tp.objective == None { 932 tp.objective = Gank(8){line: plan.line}; 933 tp.gank_start_tick = tick } } }
    938 mf_note_swap(20, tick); 939 self.plan = plan   // drop_glue d35010 후 memcpy 0x228
  } else { 942 mf_note_swap(26, tick); 943 self.plan = ForcePassive(tag 2); 945 drop(plan) } }

// ── ★0.6.0 §3-I 리콜커밋 해제 (d46d0d · engage.rs:347 · version>=3 && +0x24aa==1)
if let Some(c) = champ { well = team==0 ? (0,960000) : (960000,0)
  keep = (c.hp < c.max || d601f0(version, player, data, c) /*one_shot*/) && dist(c, well) > 160000 && !e749e0(player, data) /*nexus_last_stand 캐시*/ && eda830(c) /*undying 잔여*/ > tps
  if !keep { self.+0x24aa = 0; self.+0x24ac = 0 } }   // (+0x24aa 를 1 로 세팅하는 곳은 handle_interact_battle P1/P5/§C)

947 self.force_plan_update(version, rnd, player, data, debug, lapse)   // 인라인 1713~1766
  1713 _t = ProfTimer::start(79)
  1714 if !lapse { 1715 self.handle_interact_battle(version, rnd, player, data, debug) /*d606b0*/ } 1717 drop(_t)
  1719 was_battle = self.plan.tag < 2; entry_src = bp.entry_src(+0xf58+0x213)
  1724 r2_saved: Option<BigPlan> = if !was_battle { None } else { Some(self.plan.clone() /*d7d6d0*/) }   // (version<2 가지 사장)
  1726 _t = ProfTimer::start(80)
  1727 if lapse { 1737 before_obj = tp.objective(3B); 1738 before_plan = self.plan.clone(); 1739 before_chats = tp.chats.len(+0x2f8)
    1740 self.team_plan.update_objective(version, rnd, player, data, self.data, &self.plan, debug)   // ★0.6.0 f0f080 = TeamPlan pre-update(r22 b: P 게이트 → hunt_call_pending 소비 → V6 sync-IN → sanitize f105d0 → update_steal f0e5f0 → f10f70 update_objective_after_steal(w7 §1) → V6 sync-OUT)
    1741 cleared = before_obj.is_some() && tp.objective.is_none(); if cleared { drop(before_plan) } else { tp.objective = before_obj; self.plan = before_plan; tp.chats.truncate(before_chats) } }
  else { 1748 self.team_plan.update_objective(…) /*f0f080 2번째 사이트*/ }
  1750 drop(_t)
  1751 if let Some(saved) = r2_saved { if self.plan.tag < 2 { drop(saved) } else { self.plan = saved /*update_objective 가 Battle 을 못 걷어내게 복원*/ } }
  1756 if was_battle && self.plan.tag >= 2 { … ff_battle_exit … }   // v2+ 에선 복원 때문에 도달 불가(사장)
  1763 goal = self.plan.goal()   // 결과 미사용
948 if self.plan.tag == ForcePassive(2) {
  949 (mf_p, mf_src) = self.passive_plan(version, rnd, player, data, debug) /*d38180 · sret 0x228+u8*/
  950 self.v2_apply_assign_commit(version, rnd, player, data, &mut mf_p, &mut mf_src, debug) /*d57b00 · w11 §B9 · r22 c*/
  952 if self.mf_swap.1(+0x2238) != tick { 953 mf_note_swap(mf_src, tick) }
  955 self.plan = mf_p }
960 if !lapse && self.plan.is_passive() /*PassiveLine(3)|SinglePlanLine(4) → true · PassiveJungle(7) → team(+0xfc0)==player_team(+0xfc8) · 그 외 false*/ {
  961 (new, src) = self.passive_plan(version, rnd, player, data, debug)
  969 keep_journey = false; (v3_armed 고정 참) 970 obj_key = tp.objective
  976 if self.v3_dest_obj == obj_key && let Some((dx,dy)) = self.v3_dest { → 977 } else { → 982 }
// ── L977~1002 (배치 C) v3 목적지 게이트 + 커밋
977 (dx,dy) = self.v3_dest.unwrap()
978 arrived = champ.is_some_and(|x| (|x.x−dx|² + |x.y−dy|²) < 64000²+1)
979 if !arrived && closure$9(&self.plan) && closure$9(&new) /*idx ∈ {PassiveLine, SinglePlanLine, PassiveJungle}*/ { 985 drop(new); → 1005 }   // 이동 중 + 구/신 모두 소극 = 교체 생략
982 self.v3_dest = self.v3_plan_dest(player, data, &new) /*d779e0 (sret 24, team, &GameContext, &BigPlan) · 내부 미탐색*/; 983 self.v3_dest_obj = obj_key
988 self.v2_apply_assign_commit(version, rnd, player, data, &mut new, &src, debug)   // d57b00
991 if self.plan.tag == PassiveLine(3) && new.tag == PassiveLine(3) { 992 if old.line(+0x107b) == new.line(+8+0x11b) { 993 new.v46_carry_over(&mut old) /*ec6b80*/ } }
997 if self.mf_swap.1 != tick { 998 mf_note_swap(src, tick) }
1000 self.plan = new
// ★0.6.0 mf src 히스토그램 (d47b3f~d47b8e): self.+0x1fe0[d52970(src)] += 1; +0x24a4/+0x24a5 갱신
// ★미독: 서포터(+0x24b6) 라인페이즈 로밍 트리거 d47bd8..d47e19 (d79580 아군 생존 수 · d79630 적 생존 수 · d356f0 → d493d6/d4e0b1)
// ── ★0.6.0 §3-J v3 강제 ActiveRecall (d47e20~d47f8b)
if version>=3 && self.+0x24aa && self.+0x24ac && self.plan.idx ∈ {0,1,5,8,9,14,15} /*mask 0xc323*/ && champ {
  if idx ∈ {1,2,3,4,5,7,8,9} { self.chats.extend(self.plan.chats().drain(..)) /*e7a790 신규 사이트 d47f3c*/ }
  mf_note_swap(16, tick); self.plan = ActiveRecall(tag 8) }
// ★미독: v3 +0x24aa 후속 부기 d47f92..d48157 (+0x1f70++ · +0x24ab)
// ── L1005~1016
1005 phase = match self.plan.idx { Battle(7)→68, PassiveLine(1)→69, _→70 }; timer = ProfTimer::start(phase)
1010 self.plan.update(version, rnd, player, data, &self.data, &self.team_plan, &self.positioning_score(+0x1408), debug)   // ec2480
1013 drop(timer)
1015 if let Some(chats) = self.plan.chats() { 1016 self.chats.extend(chats.drain(..)) }
// ── L1023~1119 v48/v46 계측 — 동치(오프셋만 · 미확인) : v48 claim 창(sub_plan Battle(0x8000…05) sb.v48_dodge_claim/hold_until) · non_target_hit(player.statistics · 미확인 오프셋) · 시전 분포(skill/skill2/ult effect casting(+0x30) ∈ {Position 1, Direction 2} · nearest 가시 적 · block_move/remain_action_time/range) · PassiveLine v46_pending 접기(+0x650~+0x6e8 → self) · Battle v54_reentry_ticks(bp+0xa0..) append
1121 self.sanitize_rule_scope(data)
// ── L1124~1126 라인 백파이트 지원 진입 (enter_line_backfight_support 599~633 인라인)
timer = ProfTimer::start(76)
if self.plan.tag == PassiveLine(3) { line = plan.line(+0x107b)
  if let Some((ally_id, focus_id)) = utils::line_backfight_support_focus(version, player, data, line) /*de6a80*/ {
    if let Some(focus) = get_entity_by_id(focus_id) { if !is_ignored_battle_enemy(version, player, data, focus, false) /*eecea0? (w4 표: e0bf70→eecea0 is_unreasonable… 라벨 · ★확인 필요: eeb400 이 is_ignored_battle_enemy)*/ {
      battle = BattlePlan::new(version, Support(focus_id) /*tag 1*/, data, player) /*dd8b60*/; battle.entry_src(+0x213) = 6; battle.support_target(+0x00/+0x08) = Some(focus_id); battle.main_objective(+0x208..) = tp.objective(+0xcd5..)
      battle.update(version, rnd, player, data, &self.positioning_score, &self.team_plan, debug) /*dd8e70*/; battle.chats.len(+0x98) = 0
      if battle.sub_goal.tag(+0x78) ∈ {RunAway(3), End(4)} { drop(battle) } else { if ctx.debug { infos push "!v26 line backfight support…" } self.plan = Battle(battle) /*니치 · memcpy 0x228*/ } } } } }
drop(timer)
// ── L1128~1131 종료 판정
plan_ended = !self.plan.is_passive() && self.plan.is_end(version, rnd, player, data, &self.data, &self.team_plan, debug) /*ec22b0*/
if plan_ended {
  // L1133~1173 정글 갱크 결과 트레이스 — 동치(gank_periods · trace_level · kill_logs vt +0x130 · d79b70/d79c30/d72de0)
  1178 if self.plan.is_cancel() { if self.plan.tag == LineGanker(10) { self.gank_cancel_tick(+0x1f40) = tick } }
  // ★0.6.0 §3-K 가필
  if self.plan.tag == LineGanker(10) && plan.phase ∈ {6,8} { self.gank_cancel_tick = tick; self.+0x24d9 = plan.line }
  1189 if self.plan.tag < 2 /*Battle*/ || == LineGanker(10) { 1190 if tp.objective ∈ {Gank(8), Dive(9)} { 1191 tp.mf_note_obj_clear(42, tick); 1192 tp.objective = 0xff } }
  1200 if self.plan.tag < 2 /*Battle · bp = plan+0*/ {
    1204 if (bp.dive_abandoned(+0x200) || bp.with_dive(+0x1f8)) && !bp.dive_entered(+0x1fa) { self.last_dive_abandon_tick(+0x1f50) = tick }
    1211 if bp.main_goal.tag(+0x60) == Response(2) && bp.exit_src(+0x215) ∈ 11..=14 { self.last_response_bail_tick(+0x1f58) = tick }
    1215 if bp.exit_src == 11 { tp.last_resolver_bail_tick(+0x518) = tick }
    1225 if bp.exit_src ∈ {11,12} { if bp.main_goal.tag <= 1 { t = bp.main_goal.0(+0x68); self.last_lost_fight(+0x2050/+0x2058) = (t, tick) } else if bp.sub_goal.tag(+0x78) < 3 { self.last_lost_fight = (bp.sub_goal.focus(+0x80), tick) } }
    // ★0.6.0 v3 전용: 최근 실패 교전 기록 (try_engage 게이트 A · handle_interact_battle §C 게이트 A 의 소스)
    if version > 2 { target = if bp.main_goal.tag <= 1 { bp.+0x68 } else if bp.sub_goal.tag < 3 { bp.+0x80 } else { 0 }; self.+0x2060 = target; self.+0x2068 = tick; self.+0x2070 = bp.exit_src(+0x215) }   // reason 7/13/15/16 = BattlePlan exit_src 값 그대로
    1235 self.ff_battle_exit(+0x2218..) = (bp.exit_src, bp.entry_src(+0x213), tick, bp.exit_sub(+0x21e), bp.ff_wave_obs_open(+0x216))
    1236~1237 self.ff_battle_exit_latch(+0x2228..) = (bp.ff_exit1_cls(+0x21d), ff_wave_open_hp(+0x217), open_pct(+0x218), fire_hp(+0x219), fire_pct(+0x21a), open_danger(+0x21b), fire_danger(+0x21c), dive_abort_src(+0x20f)) }
  1240 (new, src) = self.passive_plan(version, rnd, player, data, debug)
  1241 self.mf_ret25_src[min(src,31)] += 1
  1246 self.v2_apply_assign_commit(version, rnd, player, data, &mut new, &src, debug)
  // ★0.6.0 L1246 뒤: self.v3_dest = self.v3_plan_dest(player, data, &new) (d4a163)
  1247 mf_note_swap(25, tick); 1248 self.plan = new }
// ── L1251 v50_track_dive_episode 인라인 — 동치(오프셋: plan Battle 니치 · with_dive +0x1f8 · dive_tower +0x207 · focus = sub_goal.tag<3 → +0x80 · dive_catch_break +0xd8 · dive_last_race_adv +0x1f0 · model/na/ne +0x210/+0x211/+0x212 · dive_ep_live +0xe30 · abort_src +0x24ec = Battle ? bp.dive_abort_src(+0x20f) : 0)
// ── ★0.6.0 SIM_STATS 계측 d4a52d..d4ac9f (특성키 카운터 aggr0/def1/other2 → 0x502a518[idx] · 챔프 777/778 · 추격 통계 · +0x24b2 플래그만) — 판정 무영향
// ── L1253~1291 (배치 D) 서브플랜 + v3 귀환→패시브 사다리 — 동치
1253 _t = ProfTimer::start(75)
1254 sub: SubPlan = self.plan.sub_plan(version, rnd, player, data, &self.data, &self.team_plan, &self.positioning_score, debug)   // ec2b40 · ★0.6.0 SubPlan 태그 u64 0x8000…+idx · Recall = idx 3
1263 if sub.tag == Recall(0x8000000000000003) && champ.is_some_and(|c| c.hp(+0x670) >= c.stat_cached.hp(+0x628)) /*v3_repair_done*/ {
  1265 self.v3_home_ladder[0] += 1; 1266 plan = self.passive_plan(..).0; swapped_ok = false
  if plan_allowed(ctx, &plan) { 1268 mf_note_swap(29, tick); 1269 self.plan = plan; 1270 sub = self.plan.sub_plan(..); 1272 swapped_ok = (sub.tag != Recall) }
  1272 if !swapped_ok { 1273 v3_home_ladder[1] += 1; 1274 line = fallback_line(ctx, match pos { Top→Top, Bottom|Support→Bottom, _→Mid }); 1279 plan = PassiveLine(PassiveLinePlan::new(line)) /*tag 3 · line@+8+0x11b · v4_split(+0x120)=0*/
    1280 if plan_allowed(ctx, &plan) { 1281 self.plan = plan; 1282 sub = self.plan.sub_plan(..); 1284 if sub.tag == Recall { 1285 v3_home_ladder[2] += 1; 1286 sub = SubPlan::LineWait(line) /*idx 2*/ } }
    else { v3_home_ladder[2] += 1; sub = LineWait(line); drop(plan) } } }
1290 SubPlan::merge(&mut self.sub_plan(+0x13a0), &sub)   // ed3a30 (Hide 승계 규칙 = cb7540.md)
1291 drop(_t)
// ── L1294~1505 trace(트랜지션·EngageDecision·BattleResult) — 동치(get_name ec2660 · +7 디버그 사이트 · 판단 없음)
// ── L1507~1512 디버그 라벨 — 동치
// ── L1520~1622 SIM_STATS 도주 ring · L1629~1670 DIEWIN — 동치(ckdt eda920 신 ABI (0,0,0,None) · v46_flee_gate_check eed7f0(변경: 특성 A/B · v3 *tps) · 신규 콜리 d55be0/d38130)
// ── 꼬리: drop(_t_tail) → ret
```

## changes
- 호출자: update_state f3adb0 → 래퍼 d5a940(update 후 v3 Battle 캐스팅 워치독 · ★미독).
- ★신규 §3-A v3 위치 링(+0x458 VecDeque · tps+1 창) · §3-B 앵커/이름 계산(tp +0x120/+0xb50/+0x4d8~) · §3-C armed 확장(+0x24b6/+0x24c5/c6/c7/+0x24d5/+0x24d6 = version≠2) · §3-D 서포터 아군 라인슬롯(+0x24dd~) · §3-E bb Vec +0x250 kind 1/3/4 → Chat 0xf · §3-F 넥서스방어(d75ad0/e759a0 → objective=2 + ForcePassive) · kind4 → objective=4 · §3-G ObjContest 표결(+0x2488 · tp ETA +0x778/+0x788/+0x798) · §3-H v3 정글 귀환 상태기(+0x1ed0/+0x1ed1 · fb1200) · §3-I 리콜커밋 해제(+0x24aa/+0x24ac) · §3-J 강제 ActiveRecall · §3-K Battle 이탈 시 +0x2060/68/70=(target,tick,exit_src) · gank 취소 +0x24d9 · L1246 뒤 v3_dest 갱신 · mf src 히스토그램.
- egowave_check 상수: `r=min(roam,100)*10 · e=min(ego,100)*10 · roam>49 → None · follow_own=(500−r)*e/500`.
- update_objective → f0f080 pre-update(단일화 · dd9af0+dd90c0+dda220 통합) · TeamPlan::update f18f90(finish_race 캐시 에필로그) · GoalData::update ff11f0(v3 undying) · v2_apply_assign_commit d57b00(신모드 상시) · passive_plan d38180(0x228+u8) · v3_plan_dest d779e0 ×2 · check_kill_die_tick eda920 · v46_flee_gate_check eed7f0 · evaluate_gank fb1510 ×2.
- BigPlan 0x228 · Battle 니치(tag<2 · 페이로드 plan+0) · SubPlan 태그 u64 · LineGanker phase +0xad/line +0xab(+8 기준 · LPH 절대 +0x100b/+0x100d).
- ★미독 6 구역: 강제 플랜 배정 d42145..d43c65 · 강제 LineGanker d43c70..d44e64 · 서포터 사전플랜(≈2.5KB) · v3 정글 귀환 꼬리(≈1.5KB) · 서포터 로밍 트리거 d47bd8..d47e19 · 래퍼 d5a940.

## verified
- capstone 0.6.0 d3d210(15063 명령): 위치 링 `[rbx+0x470]` len · ff4380 grow(d3d74b~d3d82d) ✓ · armed 블록 `cmp version,2; jb skip → word[+0x24b4]=0x101 · +0x24d4=1 · qword[+0x24b7]=0x0101… · dword[+0x24c0] · +0x24c4 · setne +0x24b6/+0x24d5/+0x24c5/+0x24c7/+0x24c6/+0x24d6`(d3e592~d3e606) ✓ · §3-K `if version>2: target = main_goal.tag(+0xfb8)<=1 ? +0xfc0 : sub_goal.tag(+0xfd0)<3 ? +0xfd8 : 0 → [+0x2060]=target · [+0x2068]=tick · [+0x2070]=[+0x116d](bp.exit_src)` 및 last_lost_fight `exit_src−11 ≤ 1 → +0x2050/+0x2058`(d49d22~d49dfd) ✓ · §3-I `+0x24aa=0`(d46e0d) ✓ · d5a940 호출자 존재는 RE w4 근거.
- 확인 못 한 지점: 미독 6 구역의 로직 · §3-G 표결 조건 정확식 · bb 레코드 kind enum 이름 · efc410 반환 1 의미 · 계측 필드(gank_periods/pending_trace_events/prev_plan_name/v48/v46/v3_home_ladder/mf_ret25_src/comeback_pick_*)의 0.6.0 오프셋 · L1124 is_ignored_battle_enemy 콜리 짝(eecea0 vs eeb400) · 구 본체 L1023~1672 는 RE 「동치(오프셋만)」 판정을 옮김.

## confidence
C — 구 본체는 동치이나 Δ+35KB 중 ≈17.5KB 신규 오케스트레이션의 절반(강제 플랜 배정 7KB · 강제 LineGanker 4.6KB · 서포터/정글 꼬리 4KB · 래퍼)이 미독. 판정 영향이 큰 강제 플랜 배정·강제 LineGanker 는 r22 이후 별도 RE 필수.