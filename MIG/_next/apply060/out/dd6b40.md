# dd6b40→f0dc20 v24_objective_setup_lane_pressure_ready

## logic_060
```
fn v24_objective_setup_lane_pressure_ready(self, version, player, data, goal_data, camp, debug) -> bool
  // L13 take_setup_like(camp) 인라인 (team_plan.rs:258) — ★0.6.0 TeamPlan 이중모드(v4_serpen 씬 +0x3c8 / v5_morgard 씬 +0x3e8 · 니치 +0x3e4/+0x404 == 2 이면 씬 None → 레거시 objective +0xcd5/+0xcd6)
  let (obj, phase) = match (self.serpen_scene_none /*+0x3e4==2*/, self.morgard_scene_none /*+0x404==2*/, camp) {   // ★0.6.0
      (true,  true,  Serpen(5))  => { if self.objective_tag /*+0xcd5*/ != 1 { return false } (Serpen(1),  self.objective_phase /*+0xcd6*/) }
      (true,  true,  Morgard(4)) => { if self.objective_tag != 0 { return false } (Morgard(0), self.objective_phase) }
      (true,  false, Morgard(4)) => (Morgard(0), self.morgard_phase /*+0x400*/),
      (false, _,     Serpen(5))  => (Serpen(1),  self.serpen_phase  /*+0x3e0*/),
      _ => return false };
  if phase != Setup(1) { return false }                                                       // ★0.6.0 (구: 0x41f/0x420 직접 비교)
  // ★0.6.0 삭제: `match camp { Morgard(4) => objective==Some(Morgard{Setup}) (0x41f==0 && 0x420==1) … }` 단일 objective 게이트
  if is_object_being_taken_by_enemy(player, data, goal_data, self, obj) → return false     // L23 · 콜리 v3: f88690(동치)
  team = player.info.team                                                                     // +0xa00
  camp_pos = data.context.map.camp_pos(camp, team == 0)                                    // L27 · 1416a7af0 (TLS 메모이즈 camp_pos)
  if !game.is_visible_cell(team, camp_pos.x/32000, camp_pos.y/32000) → return false       // L28 (vtable+0x100)
  if v23_recent_visible_enemies_near_point(player, data, camp_pos.x, camp_pos.y, 180000, 50) != 0 → return false   // L32 · f895f0
  allies = cache.player_champion[team]   (bounds team<2 · cache+0x1e0+team*0x28)
  pc = context.tutorial.player_count()   // context+0x38 · runner.rs:295~301 인라인: TopSolo/MidSolo/JungleOnly→1, First/Bottom→2, MidBottom→3, None/Line/Total→>=4 (아래 min(k,pc) 는 튜토리얼 인덱스 표 143a863e0/86498/864e0 로 접힘)
  gathered = allies.flatten().filter(|c| c.hp*100/c.max_hp > 49 && dist²(c, camp_pos) < 220000²+1).count()   // L36~39 (5회 언롤 · max_hp 0 → div_by_zero 패닉)
  if gathered < min(2, pc) → return false                                                  // L40 (표 143a863e0[tutorial])
  healthy = allies.flatten().filter(|c| c.hp*100/c.max_hp > 49).count()                    // L44 (aux f221d0)
  if healthy < min(3, pc) → return false                                                   // L45 (표 143a864e0[tutorial])
  need = if camp == Morgard { 4 } else { 3 }                                               // L49~52 (표 143a86498 / 143a864e0)
  if gathered >= min(need, pc) {                                                           // L54
     if context.debug(+0x3b) { debug.infos[my_champ.id].push("v24 objective setup gathered ready: {camp:?}") }   // L55~58 (e93590/d72a50/d72d50)
     return true }
  // L63~70 라인 배치 인원 (aux f21fd0)
  placed = allies.flatten().filter(|c| c.hp*100/c.max_hp > 49 &&
              match camp { Morgard => is_top_side(c) || is_near_mid_line(ctx, c.x, c.y),      // L65: x <= height - y
                           Serpen  => is_bottom_side(c) || is_near_mid_line(ctx, c.x, c.y),   // L66: x >= height - y
                           _ => false }).count()
  if placed < min(3, pc) → return false                                                     // L70 (표 143a864e0[tutorial])
  // L74 line_ready = self.v24_objective_setup_relevant_lanes_ready(player, data, camp) — 인라인(objective_discipline.rs:199~202):
  //   L200 lanes: [LineType;2] = match camp { Morgard(4) => [Top, Mid] (143a8525b), Serpen(5) => [Bottom, Mid] (143a8525d), _ => return false }
  //   L202 return v23_objective_setup_pressure_line(player, data, &lanes).is_none()      // f892d0 · -1 = None 니치
  if !line_ready → return false                                                              // L75
  if context.debug { debug.infos[my_champ.id].push("v24 objective setup lane pressure ready: {camp:?}") }   // L79~82
  return true                                                                                // L86
```

## changes
- L13 `take_setup_like(camp)` 게이트만 TeamPlan 이중모드로 재작성(`dd5db0→f0cfe0` L91 과 같은 패턴): serpen 씬 None(+0x3e4==2) && morgard 씬 None(+0x404==2) → 레거시 `+0xcd5`(0 Morgard/1 Serpen)·`+0xcd6` phase / 한쪽만 신씬이면 그 캠프만 허용하고 phase = `+0x400`(morgard) / `+0x3e0`(serpen).
- 이후 전부 동일(집결 220000²+1 · hp>49 · 180000/50 · 임계표 3종 바이트 동일 · 디버그 문자열 2).
- 콜리 RVA: f88690 · f895f0 · f221d0 · f21fd0 · f892d0 · camp_pos 1416a7af0 · debug e93590/d72a50/d72d50. PlayerState team `+0xa00`, position `+0xa90`. version 게이트 없음.

## verified
- Ghidra 0.6.0 `FUN_140f0dc20` 전문 디컴: 진입 게이트(`0x3e4==2 → 0x404==2 → 0xcd6/0xcd5(1|0)` / `0x404!=2 → camp==4 → 0x400` / `0x3e4!=2 → camp==5 → 0x3e0` · phase==1) · `FUN_140f88690` → 0 · vt+0x100 is_visible_cell · `f895f0(…,180000,0x32)` · 5슬롯 언롤 `hp*100/max >= 0x32 && d² < 0xb44dd6401` · 표 `143a863e0/864e0/86498` 인덱스 = `ctx+0x38`(tutorial) · `f221d0`/`f21fd0` · `f892d0(…, 143a8525b|143a8525d, 2) == -1` · 디버그 게이트 `ctx+0x3b`.
- 미확인: 콜리 내부(f88690/f895f0/f892d0 = RE 「skel 동일」 판정 신뢰).

## confidence
A — 전문 디컴이 RE 배치 G §5 와 일치, 변경은 진입 게이트 1곳.
