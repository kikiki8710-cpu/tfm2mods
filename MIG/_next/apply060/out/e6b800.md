# e6b800→d72e70 check_kill

## logic_060
```
// handler.rs:2507 · 0.6.0 d72e70 · sig 동일: fn(version, rnd, player, data, _ps, debug) -> Option<(usize, usize)>  (sret: +0 tag 0/1 · +8 id · +0x10 tick)
// ★0.6.0 오프셋: PlayerState team +0xa00 · pos +0xa90 · id +0x9f8 · aggressive +0x230(0..100 clamp) · +0x208/+0x210(lapse 인자, min 100) · +0x478 · +0x490(신규 judgement) · 특성 aggr +0x49d · def +0x49e · lapse 게이트 +0x49c
//              Entity hp +0x670 · max_hp +0x628 · ms +0x640 · xy +0x660/+0x668 · id +0x5c0 · level +0x5c8 · attack_effect +0x490(casting 태그 +0x4c0 −1=None · range +0x4a0 · growth +0x4a8) · stat_buff.range +0x438 · radius +0x680 · radius_mult +0x470 · Box<dyn Action> +0x570/+0x578(vt+0x90 cooltime) · attack_speed_mult +0x3fc · Effect vt+0x108 range_adjust(구 0xe8)
//              PlayerChampionCache 팀 stride 0x212 qword · 포지션 0x6a qword(구 500/100) · 열: attack +0 · skill +5 · skill2 +0xa · *_per_sec attack +0x32 · skill +0x37 · skill2 +0x3c · (+0x55/+0x5a = skill/skill2 max 후보)

check_kill(version, rnd, player, data, _ps, debug):                                  // L2507
  if player.lapse_enabled(+0x49c) == 1 {                                            // L2509 ★0.6.0 게이트 필드 +0x464→+0x49c (0.5.8 에도 있던 게이트 · 명세 누락분 보강)
    // player_awareness_lapse 인라인 → 17d68b0(seed(vt+0x20), player.id(+0x9f8), tick(vt+0x28), tps, min(+0x208,100), min(+0x210,100), +0x478,
    //                                        ((min(+0x490,100)² * 0xccd) >> 16) + 1000  & 0xffff)   // ★0.6.0 8번째 인자 신설 ≈ 1000 + judgement²/20
    if lapse { return None }                                                          // L2510
  }
  // --- check_kill_v15 인라인 (L2373~2505) ---
  team = player.team (bounds<2); me = cache.player_champion[team][pos].unwrap()      // L2374
  allies  = e0f7a0(...)  = iter_champions(team).filter(|a| dist²(a,me) <= 150000² && a.hp*100/a.max_hp > 39)   // L2377 (콜리 v3: ca2790→e0f7a0 · 동치) ★나 자신 포함
  enemies = e0b750(...)  = iter_champions(1-team).filter(|e| dist²(e,me) <= 150000² && (me.team==Neutral || e.visible_state[my_team]==Visible) && !is_ignored_well_enemy(version, player, e))   // L2379 (술어 d78b60 · 동치)
  ext     = e0f970(...)  // ★0.6.0 신규 v3 확장 후보(술어 d78e70): version>2 && game_mode==Moba && 150000 < dist(me,e) <= 250000 && e.visible[my_team]==Visible && !in_well(e)
                         //   && check_kill_die_tick(version, data, player, focus=e, enemy=[me], towers=[], simple=1, ignore_nuke=0, no_noise=0, None) < tps
  disable_tick = match player_count(tutorial) { 5v5→setting.tower_attack_disable_tick, 2v2→_2v2, 3v3→_3v3 }   // L2383~2386 (표 143a74030[tutorial])
  towers = e114d0(...)   = iter_towers_without_nexus(cache, 1-team).filter(|t| game.tick() <= disable_tick)   // L2389 (동치)

  // 1) 적 전원이 나에게 주는 화력 — ★enemies 만(ext 제외)
  enemy_burst = 0; enemy_dps = 0
  for e in enemies:                                                                  // L2393
    pe = player_by_champion_id(e.id).unwrap()   // 콜리 v3: d31bb0→d72be0 ; cc = champion_cache[pe.team][pe.pos]   // L2395
    b = 0
    if e.can_attack()(164b370) || e.attack_cooldown() is None(ty 점프테이블) || cd <= tps { b = cc.attack[my_pos] }        // L2398~2399
    if e.can_skill()(165d410)  || e.ty!=Champion(13) || e.skill_cooldown(+0xb8)  <= tps { b = max(b, cc.skill[my_pos]) }  // L2402~2403
    if e.can_skill2()(164b650) || e.ty!=Champion     || e.skill2_cooldown(+0xc0) <= tps { b = max(b, cc.skill2[my_pos]) } // L2406~2407
    enemy_dps  += cc.attack_per_sec[my_pos] + cc.skill_per_sec[my_pos] + cc.skill2_per_sec[my_pos]                       // L2410~2412
    enemy_burst += b                                                                 // L2413

  ar = min(player.aggressive(+0x230), 100)                                           // L2417 (★0.6.0 0..100 스케일)
  c1_old = (100-ar)*80/100 + 80                                                      // 항상 계산(아래 aggr 분기용)
  (c1, c2, c3) = if player.aggr(+0x49d) { (0, 67, 32) }                              // ★0.6.0 특성 분기
                 else if player.def(+0x49e) { (240, 67, 32) }                        // ★0.6.0
                 else { (c1_old, ar*45/100 + 45, ar*35/100 + 15) }                   // L2419~2421 (값 동치)

  // 2) 적 하나씩 — 아군 전원의 화력 vs 그 적 — ★enemies ++ ext 순회
  for e in enemies ++ ext:                                                           // L2424 (★0.6.0)
    pe = player_by_champion_id(e.id).unwrap(); epos = pe.pos                         // L2431~2432
    ally_burst = 0; ally_dps = 0
    for a in allies:                                                                 // L2434
      pa = (team,pos) of a via player_champion 10칸 id 스캔 (없으면 unwrap 패닉)      // L2435
      cc = champion_cache[pa.team][pa.pos]                                           // L2440
      ally_dps += cc.attack_per_sec[epos] + cc.skill_per_sec[epos] + cc.skill2_per_sec[epos]   // L2440~2442
      b = 0 ; (공격/스킬/스킬2 가용 판정은 1)과 동일 규칙 · 스킬 가용은 buffs(+0x2c8/+0x2d0, 0x28 stride) 태그 4/5 스캔 + can_move(vt+0x140) 인라인, 대상 열 = epos)   // L2444~2453
      ally_burst += b                                                                // L2456~2457
    my_burst = enemy_burst ; my_dps = enemy_dps
    for t in towers:                                                                 // L2460
      if dist²(t, e) < 22500000001 {                                                 // L2461
        d = estimate_damage_to(t.attack_effect(None→unwrap 패닉), ctx, t, <static>, me)   // L2462 (1643790)
        cool = max(t.action.cooltime(vt+0x90)*100 / max(t.attack_speed_mult(+0x3fc)+100, 1), 3)   // attack_cooltime 인라인(0.5.8 도 동일 산식 · min 3 이라 div0 없음 — 0.5.8 명세 「0 이면 패닉」 정정)
        my_dps  += tps * d / cool                                                    // L2463
        my_burst += d }                                                              // L2464
    K = if version >= 3 { tps } else { 60 }                                          // ★0.6.0 (v3 = tps · 0.5.8 상수 60)
    enemy_die = sat(e.hp - ally_burst) * K / max(ally_dps, 1)                        // L2470
    my_die    = sat(me.hp - my_burst)  * K / max(my_dps, 1)                          // L2471
    if ctx.debug { debug.infos[me.id].push(format!(enemy_die, my_die)) }             // L2473~2474 (fmt 143a711b8)
    if enemy_die > sat(my_die - c1) { continue }                                     // L2477
    if aggr && enemy_die > sat(my_die - c1_old) && eeb5b0(version, data, player, me, e) { continue }   // ★0.6.0 공격형 특성: 구 c1 기준으로도 탈락이면서 적 탈출 가능하면 탈락
    tower_ticks = match towers.min_by_key(|t| dist²(t,e)) { Some(t) => isqrt_dist(e,t)(1660a00) / e.ms (0 이면 div0 패닉), None => 99999999 }   // L2481~2484
    if version >= 3 && game.get_game_mode().tag == Moba(0) && enemy_die < tps && !eeb5b0(version, data, player, me, e) {   // ★0.6.0 v3 즉시 수락(이속/사거리/c2/c3 검사 우회)
      return Some((e.id, max(enemy_die, 60) + game.tick())) }
    if e.ms < me.ms {                                                                // L2489
      ok = enemy_die < sat(tower_ticks - c3)                                         // L2498~2499
    } else {
      range = me.attack_effect.range + (me.level-1)*growth + me.stat_buff.range + range_adjust(vt+0x108)(me,e) + me.radius() + e.radius()   // is_in_range 인라인 (radius() = mult==0 ? radius : radius*(mult+100)/100)
      ok = dist²(me,e) <= range² && enemy_die < c2 && enemy_die < sat(tower_ticks - c3)   // L2491~2494
    }
    if ok { return Some((e.id, max(enemy_die, 60) + game.tick())) }                  // L2502 (60 은 v3 에서도 고정)
  return None                                                                        // L2505
// (텔레메트리: 챔프 id 0x309/0x30a 및 특성별 전역 LOCK 카운터 14502a128/130/530/598/640/678… — 반환값 무관)
// ★v2 사장: K=60 · ext 술어 항상 false · v3 즉시수락 없음

// --- 콜리 eeb5b0 escape_possible(version, data, player, me, e) -> bool (★0.6.0 신규 · RE w1 §2-1) ---
reach = de9900(data, me, e)  // 사용가능 공격/스킬/스킬2(lv≥3)/궁(lv≥5) 중 최대 사거리(TLS 메모 · 본체 deb270: base(+0x4a0)+(lv-1)*+0x4a8 + +0x438 + vt0x108 + a.radius + t.radius)
dist  = isqrt_dist(me, e)
if reach < dist {
  well = (e.team==0 ? (0,960000) : (960000,0)); wd = sat(dist(e,well) - 160000); H = tps * (version>1 ? 2 : 1)
  if wd <= H * e.ms { return true }
  if e.ms < me.ms && wd * (me.ms - e.ms) < sat(dist - reach) * e.ms { return true }
  if e.ms*10 >= me.ms*9 && e7f670(적 타워 중 sat(dist(e,t) - t.reach) <= H*e.ms 존재 · others 연쇄 e6ee90) { return true }
}
false
```

## changes
- L2509 lapse 게이트 필드 `+0x464`→`+0x49c`(==1) · 17d68b0 8번째 인자 `1000+min(+0x490,100)²·0xccd>>16` 추가.
- L2374~2389 후보: `ext = e0f970(...)`(v3 · 150000<dist≤250000 · 가시 · die_tick(simple)<tps) 추가 → 루프2 는 `enemies ++ ext` 순회, 루프1 은 enemies 만.
- L2419~2421: 특성 분기 `aggr → (0,67,32)` · `def → (240,67,32)` · 그 외 구 공식(0..100 스케일: (100-ar)*80/100+80 · ar*45/100+45 · ar*35/100+15 — 값 동치). `c1_old` 는 항상 별도 계산.
- L2470~2471: `*60` → `*K`, `K = version>=3 ? tps : 60`.
- L2477 뒤: `aggr && enemy_die > sat(my_die-c1_old) && eeb5b0(...) → continue` 추가.
- L2484 뒤(v3): `Moba && enemy_die < tps && !eeb5b0(...) → 즉시 Some((e.id, max(enemy_die,60)+tick))` 추가.
- L2462~2463 타워 dps 분모: `cool*100/max(as+100,1), min 3` 인라인 — 0.5.8 도 동일(0.5.8 명세의 「attack_cooltime 0 이면 패닉」은 오기, 패닉 없음).
- 캐시 인덱스 stride 500/100 → 0x212/0x6a qword. Effect vt 0xe8→0x108(range_adjust) · 0x120→0x140(can_move).
- 콜리 v3: player_by_champion_id d31bb0→d72be0 · allies ca2790→e0f7a0 · enemies c9f380→e0b750(d78b60) · towers ca4200→e114d0 · 신규 e0f970(d78e70) · eeb5b0 · de9900/deb270 · 1660a00 isqrt · lapse 16047b0→17d68b0 · estimate 12857f0→1643790.

## verified
- Ghidra 0.6.0 d72e70 디컴 전량 정독: +0x49c 게이트 · 8번째 인자 산식(0xccd>>16+1000) · e0f7a0/e0b750/e0f970/e114d0 호출 순서 · enemies→ext append 후 루프2 순회 · c1/c2/c3 특성 분기(0/0x43/0x20 · 0xf0/0x43/0x20 · 구 공식 +0x230) · c1_old(local_290) 상시 계산 · K=`2<version ? tps : 0x3c` · `enemy_die > sat(my_die-c1) → continue` · aggr&&eeb5b0 분기 · 타워 없음→99999999 · v3 즉시수락(LAB_140d7445d: vt+0x40==0 · tps>enemy_die · !eeb5b0) · e.ms<me.ms 분기 · is_in_range 인라인(vt+0x108) · `max(enemy_die,60)+tick` 반환.
- Ghidra 0.5.8 e6b800 대조: 타워 dps 분모(cool*100/max(as+100,1), min 3) 0.5.8 도 동일 → 「div0 패닉」 정정 · lapse 게이트 +0x464 존재 확인.
- 미확인: eeb5b0/de9900/e0f970 본체는 RE w1 §2-1 의 등가 Rust 채택(이번 스팟체크 미수행).

## confidence
A — 본체 디컴 전량 대조로 분기·상수·오프셋 전부 확인. 신규 콜리 3종(eeb5b0·de9900·e0f970) 내부는 RE 원문 의존.
