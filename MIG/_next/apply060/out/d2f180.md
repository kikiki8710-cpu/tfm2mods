# d2f180→fa47a0 PassiveJunglePlan::next_plan

## logic_060
```
// old/passive_jungle.rs:228 · 0.6.0 fa47a0 · sig 동일: fn(sret, &mut self, version, rnd, player, data, goal_data, _positioning_score, team_plan, debug) -> Option<BigPlan>  (sret +0 태그: −1 None · 8 ActiveRecall · Battle = 니치 0/1 + 0x220B 페이로드)
// ★0.6.0 PassiveJunglePlan 레이아웃: +0x00 chats Vec<Chat>(0x18B: tag u8@0 · 인자 @+1/+8) · +0x18 counter_jungle_route Option(None = u64::MAX · 생성처 미발견 → 항상 None 추정) · +0x48 cj_ambush = Option<(camp u8 @+0x50 (0xff=None), bush_region usize @+0x48, deadline usize @+0x58)> · +0x60 team(구 +0x48) · +0x68 player_team · +0x70 last_lead_action_tick · +0x78 cj_leave_until · +0x80 cj_meet_latch · +0x81 cj_seen_alive · +0x82 cj_meet_decided · +0x83 jungle(구 +0x60)
//   PlayerState team +0xa00 · pos +0xa90 · aggressive +0x230 · ego +0x238 · order +0x220 · items len +0x518 · solorank_strategy +0x568(0.5.8 +0x4f8) · Entity hp +0x670 · max +0x628 · ms +0x640 · xy +0x660/+0x668 · id +0x5c0
//   Strategy 24B(game vt+0x108 strategy(team) 또는 is_solorank(vt+0xe8) 이면 player+0x568): +0x11 focused · +0x12 early_jungle(0 GrowthAndCover · 1 Ganking · 2 CounterJungle)
//   TeamPlan next_respawn_tick +0x668+team*0x20+camp*8(구 +0x378) · cache.region_point +0x23f8+region*4 · map +0x1c98+y*0xf0+x*8 region 격자 · MobaMode 캠프 moba+0x18+slot*0x30 {live len +0x10 · respawn +0x18} · bb stride 0x5c8 · +0x298{valid,camp@+1,team@+8}/+0x318 tick/+0x320{valid,camp@+0x321,team@+0x328} 적 정글러 기록 · setting +0x12f0 · +0x8a8 · +0x12f8 tps · +0x6d70+team*0x20 fountain
//   BattlePlan(0x228): sub_goal +0x78(RunAway=3 · 구 태그 4) · entry_src +0x213 · BigPlan 태그: PassiveLine 3 · ActiveRecall 8 · LineGanker 10 · LineGankCover 11 · Chat: Battle 3 · CoverLine 9 · GankLineCover 0xa · HideLine 0xb · CounterJungle 0x12
// 콜리 v3: BattlePlan::new dd8b60 · update dd8e70 · is_cleared faf060 · is_side_cleared fafd90 · buy_item f27ea0 · upgrade_item f27140(tier vt+0x70) · is_recent_visible 1417d3bc0 · camp_pos 14157b720/16a7af0 · gen_range dcb010(0..=100 i64)/dcae80(0..1000) · 신규 fa1010(딥푸시 갱 아웃라인+f1dd50 게이트) · fa3350(v3 lead) · fa2670(try_setup_counter_jungle_ambush) · fafe50(best_jungle_goal v3 → (camp AL, owner_team RDX)) · fb1200(needs_recall) · fb12a0(recall_need 80/70/0) · fb0b70(enemy_non_jungler_near) · fb0cf0(find_ambush_bush) · fafb20(can_win_duel) · ee0fe0/ee0dd0(격자·부시 경로) · f1dd50/f1f280/f1f3d0(v3 갱 게이트)

fn next_plan(&mut self, version, rnd, player, data, goal_data, _positioning_score, team_plan, debug) -> Option<BigPlan>

[★0.6.0 블록 A · 진입 직후 「매복 유지/해제」 (version>=3 && cj_ambush.is_some())]
 if version >= 3 && let Some((camp, bush, deadline)) = self.cj_ambush {
   champ = player_champion[team][pos] (None → return None)
   alive = moba.is_some() && camp_state(1-team, camp).live.len > 0
   ej = player_champion[1-team][1]                                                  // 적 정글러
   if (분수 사각 안 && champ.hp < max) || fb12a0(version, ctx, champ) > 49 { RESET }
   else {
     if map.region[camp.y/32000][camp.x/32000] != bush {                            // 부시 region 이 캠프 region 과 다르면
       if fb0b70(team, cache, camp.xy) { RESET }                                     // 캠프 150000 내 적 비정글러 → 해제
       else if ee0dd0(ee0fe0(data, player, 1-team), my_cell, bush).is_none() {       // 부시로 가는 격자 경로 소실
         match fb0cf0(player, data, camp.xy, my_cell) { Some(b) => self.cj_ambush.1 = b.region, None => RESET } } }
     if alive {
       if ej.is_none() { self.cj_ambush = None; → 구경로 }
       else if game.is_visible(team, ej.id) && dist²(ej, camp) < 22500000001 {        // 적 정글러가 캠프 150000 내 가시
         self.cj_ambush = None
         if fafb20(ctx, me, ej) {                                                     // 승산
           b = BattlePlan::new(version, TryKill(ej.id, 60), data, player); b.entry_src = 9
           b.update(version, rnd, player, data, _ps, team_plan, debug)
           if b.sub_goal != RunAway(3) { self.chats.push(Battle(ej.id, 0)); self.cj_meet_decided = true; return Some(Battle(b)) } }
         RESET }
       else { rec = (now − bb[1-team].+0x318 < 20 ? bb.+0x298 : bb.+0x320)
              if rec.valid && rec.camp == camp && rec.team == 1-team && now <= deadline { return None }   // 매복 유지 → sub_plan 이 Hide
              else { self.cj_ambush = None; → 구경로 } } }
     else { self.cj_ambush = None
            if ej.is_some() && dist²(ej, camp) < 40000000001 && is_recent_visible(bb[1-team], game, player, ej) && fafb20(ctx, me, ej) { TryKill 동일 }
            RESET-then-구경로 } }
 }
 // RESET = `self.cj_ambush = None; (self.jungle, self.team) = fafe50(version, rnd, player, data, team_plan, camp_hint=0xff, leave_ok=true, debug); 계속 구경로`

[★0.6.0 블록 B · 「매복 시도」 (L390~394 쿨다운 뒤)]
 ego = min(player.ego(+0x238), 100); cooldown = tps * (4 − ego/50)                  // L390~392 (0.5.8 `4 − ego/500` · 값 동치)
 if version >= 3 && self.cj_ambush.is_none()
    && now.saturating_sub(self.last_lead_action_tick(+0x70)) >= cooldown
    && !(tutorial ∈ {0,5,7,8} && now >= sat(setting.first_spawn(+0x8a8) − 30*tps))    // 에픽창 아님
    && strategy(is_solorank ? player.+0x568 : game.strategy(team)).early_jungle(+0x12) == CounterJungle(2) {
   if let Some(c) = champ && !fb1200(version, player, data, c) { fa2670(self, player, data, c) }   // 귀환 불요 → 매복 세팅 시도(유일 세팅처)
   if self.cj_ambush.is_some() { return None } }

[★0.6.0 신규 플래그] if self.team != self.player_team && !self.cj_seen_alive { if camp_state(self.team, self.jungle).live.len > 0 && dist²(me, camp) <= 14400000000 { self.cj_seen_alive = true } }

[절1 · L231~256 카정 근접 교전]
 if self.team != self.player_team {            // is_counter_jungle()
   champ = player_champion[player.team][player.position].unwrap()                     // L232
   hp_ratio = champ.hp*100 / champ.stat_cached.hp                                     // L233 (div0 패닉)
   if hp_ratio > 49 {                                                                 // L235
     team = 1 - player.team                                                           // L236
     // ★0.6.0 v3 전처리(version>=3):
     //   ej = player_champion[team][1]; if ej 150000 이내 && (is_visible ‖ last_seen+120 >= now) { cj_meet_latch = true } else if ej 밖(244949 = √6e10 초과) { cj_meet_latch = false }
     //   if cj_ambush.is_some() {   // 매복 중 별도 경로
     //     if 적 챔프 중 (12f0+32000) 반경 내 최근가시 존재 {
     //       if cj_meet_decided { cj_leave_until = now + cooldown; cj_seen_alive = false; (jungle, team) = fafe50(…, camp_hint=self.jungle, leave_ok=false, …); return None }
     //       else if ej 반경 내 && recent_visible(ej) { cj_meet_decided = true; if fafb20(ctx, me, ej) { TryKill 동일(성공 시 cj_meet_decided=true) } else { 위 leave 처리 } } }
     //     else { cj_meet_decided = false } }
     //   구 절1 진입 조건에 `!(version>=3 && cj_meet_decided)` 추가
     near_enemy = cache.iter_champions(team)                                          // L237~239
        .filter(|x| dist_sq(x, champ) < 14400000001 && bb[team].is_recent_visible(game, player, x))
        .min_by_key(|x| dist_sq(x, champ))
     if let Some(enemy) = near_enemy {                                                // L241
       enemy_hp_ratio = enemy.hp*100/enemy.stat_cached.hp                             // L242
       my_level = player.info.level ; enemy_level = player_by_champion_id(enemy.id).map(|p| p.level).unwrap_or(1)   // L244~246
       if hp_ratio >= enemy_hp_ratio || my_level > enemy_level {                      // L248
         battle = BattlePlan::new(version, TryKill(enemy.id, 60), data, player)      // L249 dd8b60
         battle.entry_src = 9                                                         // L250 (+0x213)
         battle.update(version, rnd, player, data, _positioning_score, team_plan, debug)   // L251 dd8e70
         if battle.sub_goal(+0x78) != RunAway(3) {                                    // L252 ★0.6.0 태그 4→3
           self.chats.push(Chat::Battle(enemy.id, 0)); (v3: cj_meet_decided = true)   // L253
           return Some(BigPlan::Battle(battle)) } } } } }                             // L254

[절2 · L261~300]
 if !is_cleared(faf060)(self.jungle, self.team, version, rnd, player, data, team_plan, 0, debug) { return None }   // L261
 can_buy_item     = buy_item(f27ea0)(version, rnd, player, cache.game, ctx)          // L263
 can_upgrade_item = upgrade_item(f27140)(version, rnd, player, cache.game, ctx)      // L264
 item_score = if let Some((_, item)) = can_upgrade_item { tier = ctx.item_list[item].tier()(vt+0x70); tier==4→100 · 3→70 · 2→50 · else 30 }   // L265~273
   else if can_buy_item.is_some() { player.items.len(+0x518)==0 ? 10 : 0 } else { 0 }   // L278
 if let Some(p) = self.lead_action(…) { return Some(p) }                             // L285~288 (★0.6.0 v3 재구성 · 아래)
 gen = rnd.gen_range(0..=100i64)(dcb010)                                             // L291
 if gen < item_score { Some(BigPlan::ActiveRecall) /*+0=8*/ } else { None /*−1*/ }   // L292

[lead_action · ★0.6.0 v3 재구성 (0.5.8 lead_action_v37 L386~634)]
 (쿨다운 L393~394 는 위 블록 B 앞에서 이미 판정: now − last_lead < cooldown → None)
 champ = player_champion[player.team][player.position]
 // --- 딥푸시 갱 (L401~429) → 아웃라인 fa1010(self, version, rnd, player, data, goal_data, team_plan, debug) ---
 //   본문 = 구 L401~429 그대로: hp_ratio ≥ 60 − min(aggr,100)*20/100 && eplayer 존재 && !bb[team].is_recent_visible(game, eplayer, champ)
 //     → target_lines = [champ.x > height − champ.y ? Bottom : Top, Mid] 순회: has_deep_pushed_enemy(line) && valid_deep_push_gank(line)
 //     ★0.6.0 && f1dd50(version, player, data, line) /* version>1 → f1f280() && f1f3d0(version,player,data,line,0) (★미독: 의미) */
 //     → last_lead = tick; if rnd.gen_range(0..1000)(dcae80) < order(+0x220 · min 100)*6+400 { chats.push(HideLine(line,0)) }
 //     → Some(LineGanker{chats:[], setup_limit: tick+tps, wait_limit: tick+tps*12, line, phase: Setup(7)})   // 태그 10
 if let Some(p) = fa1010(...) { return Some(p) }
 if (분수 안 && hp < max) || fb12a0(version, ctx, champ) > 49 { return None }         // ★0.6.0 v3 귀환 필요 시 리드 없음
 strategy = is_solorank ? player.+0x568 : game.strategy(vt+0x108)(team)             // L443
 gate_window = tutorial ∈ {0,5,7,8} && tick >= sat(first_spawn − 30*tps)             // L439 창(리드행동 금지창)
 // ★0.6.0 v3: strategy match(L445~627) 대신 fa3350 한 번
 return fa3350(self, version, rnd, player, data, team_plan,
               focused = gate_window ? All(2) : strategy.focused(+0x11),
               me.xy,
               flag = (strategy.early_jungle(+0x12) != Ganking(1)) || gate_window,
               debug)
 //   fa3350 요지(★미독 세부): is_cleared(camp) && is_cleared(pair(camp)) (Rhino0↔Bee3 · Mushroom1↔Stump2) → [side(champ), Mid] 를 e04d20(focused.is_focused_line && valid_gank_line(fa1ec0 = 구 L636~683)) 로 필터
 //     → evaluate_gank_opportunity_with_score(fb1510) 최고점 line
 //     → flag==false: score >= −40 && 적 정글러 도착·클리어 추정(ed9a90/dbeae0) 으로 wait_limit 연장
 //        flag==true : f1dda0(player,data) != 0 && score >= 30
 //     → last_lead = now; chats.push(HideLine(0xb, line)); Some(LineGanker{setup_limit: now+tps, wait_limit: now+max(extra+f1dda0, tps), line, phase 7})
 // ★v2 사장: L443~627 strategy match(GrowthAndCover L451~504 · Ganking L508~556 · CounterJungle L560~627) 전체 — 0.6.0 v2 경로에만 잔존(chat CounterJungle 0x12 포함)

[has_deep_pushed_enemy · L305~336 (fa1010 내부 · 동치)]
 t = cache.tower(line, player.team).or(cache.tower2(line, player.team))?
 if !(t.ty == Tower(2) && t.tower.ty >= 5) { return false }
 for ep in 0..5 { Some(rinfo) = goal_data.enemy_region[ep] else continue; if tick.saturating_sub(rinfo.last_known) > tps*3 { continue }
   for idx in 1..=2 { deep = map.line_region(line, team, idx); if map.region_dist[deep][rinfo.region] < 2 { return true } } }
 false
[valid_deep_push_gank · L337~385 (fa1010 내부 · 동치)]
 champ = player_champion[team][pos]? ; if hp_ratio < 50 { return false }
 enemies = iter_champions(1-team).filter(|x| is_near_line(ctx, x.xy, line) && bb[1-team].is_recent_visible(game, player, x)).collect ; if empty → false
 allies = (0..5).filter(|i| bb[team].in_big_line(i, line)).filter_map(player_champion[team][i]).filter(hp_ratio > 40).collect ; if empty → false
 if enemies.len > allies.len + 1 { return false } ; true
```

## changes
- 레이아웃 전면: team +0x48→+0x60 · jungle +0x60→+0x83 · last_lead +0x70 · 신규 +0x18 route · +0x48/+0x50/+0x58 cj_ambush · +0x78 cj_leave_until · +0x80/+0x81/+0x82 cj_meet_latch/seen_alive/meet_decided.
- 진입 직후 블록 A(v3 · cj_ambush Some): 귀환 필요/부시 불일치+적 근접/경로 소실/캠프 사망/적 정글러 가시(승산이면 TryKill Battle)/기록 소멸·deadline 초과 → 해제(RESET = fafe50(0xff, leave_ok=true) 재선정) · 유지 조건이면 즉시 `return None`.
- 쿨다운(L390~394) 을 lead_action 안에서 진입부로 이동 · 산식 `tps*(4 − min(ego,100)/50)`(동치) · 뒤에 블록 B(v3 · 매복 시도 fa2670 · 성공 시 `return None`).
- 신규 플래그 `cj_seen_alive`(카정 캠프 살아있고 120000 이내).
- 절1 v3 전처리: `cj_meet_latch`(150000 내 가시/120틱) · cj_ambush 중 근접 적 → `cj_meet_decided`/`cj_leave_until`/fafe50(camp_hint=self.jungle, leave_ok=false) · TryKill 성공 시 cj_meet_decided=true · RunAway 태그 4→3 · entry_src +0x213.
- lead_action v3: 딥푸시 갱 = fa1010(구 L401~429 + f1dd50 게이트) → `(분수&&hp<max) ‖ fb12a0>49 → None` → strategy match 대신 `fa3350(self, …, focused=(창이면 All), me.xy, flag=(early_jungle!=Ganking ‖ 창), debug)` 반환.
- 절2(L261~292) 동치(item_score · dcb010 · ActiveRecall 8).
- 콜리 v3: 헤더 표. ★미독: fa3350 세부(dbeae0 산식) · fafe50 블록 A/C′ 세부(r22 d 로 블록 B 는 확정) · ee0fe0/ee0dd0 · f1dd50/f1f280/f1f3d0 의미 · counter_jungle_route(+0x18) 생성처(항상 None 추정 · 런타임 확인 필요).

## verified
- capstone 0.6.0 fa47a0 콜·오프셋 지도: fb12a0(fa49e7)→fafe50(fa4a40 → +0x83 저장) = 블록 A RESET · 쿨다운 `now − +0x70 < [rbp+0x2408] → skip B`(fa4b0b) · 창 판정(0x1a1 · +0x8a8 − 30*tps · fa4b3d~fa4b66) · vt+0xe8 is_solorank → +0x568 / vt+0x108 strategy(team) · `+0x12(=+0xa92)==2` · champ → fb1200(fa4d79) → fa2670(fa4d95) · `+0x50 != 0xff → fa6991`(return None) · 절1 진입 `+0x60 == +0x68 → fa4ea5` · +0x81 검사(fa4dc1) · +0x80/+0x82 세팅 지점 · fafb20 ×4 · dd8b60/dd8e70 ×4 · +0x78/+0x81 세팅 후 fafe50(fa5bad/fa66b6) · ee0fe0/ee0dd0/fb0cf0(fa5c62/fa5d02/fa5ea3) · faf060(fa689d) · f27ea0/f27140 · fa1010 ×2(fa6b32/fa6bbf) · dcb010 → ret(fa6c6a) · fb12a0(fa6f06) · fa3350(fa718f).
- 미확인: 블록 A/절1 v3 전처리의 세부 분기 순서는 RE w3 §3 등가 Rust 채택(2966줄 asm 재정독 미수행) · fa3350/fafe50 내부.

## confidence
B — 골격·호출 순서·오프셋은 asm 으로 확인했으나 v3 블록 A/절1 전처리 세부와 fa3350 산식은 RE 원문 의존(★미독 5건).
