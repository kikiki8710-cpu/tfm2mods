# e65b10→d6c2b0 LegacyPlanHandler::get_small_action

## logic_060
```
// sig 동일: fn get_small_action(&mut self LPH, version, rnd, player, data, pre_action: &SmallActionPlay, ignore_action: &Vec<(usize, SmallAction)>, debug) -> (ScoreParameter(0x1508), i64, SmallActionPlay)
// sret: +0 ScoreParameter 0x1508B · +0x1508 score · +0x1510 play(0xb8B · 태그 +0x15c1 = play+0xb1)
// ★0.6.0 LPH 오프셋: sub_plan +0x13a0(구 0x768) · team_plan +0xf8 · plan +0xf58 · positioning_score +0x1408(구 0x990) · pending_global_ult_target +0xde0(구 0x530 · Some 태그 u8 @+0xde0 · target/until 뒤이음) · v3_turnback_scene_prev +0x24d3(구 0x1809)
//   · judge_noise_plan +0xe20(Some)/+0xe28(disc)(구 0x560) · judge_noise_ratio +0x2428..+0x2480 [i64;11](구 0x17a0) · pos_ring +0x458(v3 위치 링 · score 클로저 env[8]) · chats +0x1198.. · pending_trace_events(미확인 오프셋 · 구 0x858) · v48/v3_* 계측 필드(미확인 오프셋 · 구 0x1540/0x1548/0x1804/0x1805/0x1812/0x1813 · 동치 추정)
// ★0.6.0 ScoreParameter: 크기 0x1508 동일 · positioning_score +0x9f0 · v3_turnback_hold +0x14c8(구 0x1500) · +0x14c9 Line 스타일 · wave_snapshot 태그/본체 위치 동일(추정)
// ★0.6.0 SmallActionPlay 태그(+0xb1): RunAway 3 · Recall 4 · Around 5 · AroundRegion 6 · AroundRunAway 7 · Positioning 8 · AroundPosition 0/1/2(dataful · 구멍 9) · AroundPositionBush 10 · AroundBush 11 · LaneMinionPosition 12 · Trace 13 · Attack 14 · Skill 15 · Skill2 16 · Ult 17 · Stop 18 (AroundHide 제거)
// ★0.6.0 SubPlan 태그: u64 0x8000000000000000+idx (LineDefense 0 · LineSafe 1 · LineWait 2 · Recall 3 · Jungle 4 니치 · Battle 5 · … · Hide 7 · ObjContest 0x11) · BigPlan Battle 니치 0/1 · 그 외 idx+2 · BattleSubPlanGoal Trace0/Kiting1/KitingBack2/RunAway3/End4
// player: team +0xa00 · position +0xa90 · champ_id +0x9f0 · hp +0x670 · max_hp +0x628 · move_speed +0x640 · undying +0x488 · level +0x5c8 · ult Option<Effect> +0x538(casting 태그 +0x30 · target +0x28) · attack +0x490(★dispcheck: Entity 동일 · 구 +0x4c0 표기는 attack_effect Option 태그 위치)
// ★v2 사장: `version>1` 게이트는 v3 고정 참 · `version<3` 가지는 없음(신규 블록은 `version>=3` 게이트).

L9  _t_csp = ProfTimer::start(33)
L10 parameter = calculate_score_parameter(version, rnd, player, data, debug)          // ★0.6.0 1000320 · sret 0x1508
L11 self.sub_plan(+0x13a0).calculate_score_parameter_value(version, rnd, player, data, &mut parameter)   // ★0.6.0 디스패처 ed10b0
L12 self.positioning_score(+0x1408) = parameter.positioning_score(+0x9f0)              // memcpy 2760B
L13 drop(_t_csp)
L16 _t_ws = ProfTimer::start(35)
L17~19 parameter.wave_snapshot = Some(build_minion_wave_snapshot(player, data, player.param.last_hit_prediction_depth(), last_hit_source_quality()))   // ★0.6.0 de3e80
L20 drop(_t_ws)
L22 team = player.team(+0xa00) (bounds<2) ; champ = cache.player_champion[team][player.position(+0xa90)] (+0x1e0+team*0x28+pos*8) .unwrap()

L29 // (version>1 고정)
    if game.get_game_mode()(vt +0x40).tag != DeathMatch(2) {
L30   base_exempt = champ.undying(+0x488) || nexus_final_stand(player, data) /*db0160 LAST_STAND_MEMO `eax&0x100`*/ || cache.nexus[team](+0x170+team*8).is_some_and(|n| champ.distance(n) < 180001)
L33   sh: SoloHuntObs = solo_hunt_obs(game, team, champ)   // ★0.6.0 eb2e10(sret 16B)
L34   scene_now = if base_exempt || sh.ally_ctx > 1 { false } else if sh.al120 + 1 < sh.en150 && sh.chasers != 0 { sh.aa_safe } else { false }
L35   parameter.v3_turnback_hold(+0x14c8) = scene_now || self.v3_turnback_scene_prev(+0x24d3)
L36   self.v3_turnback_scene_prev = scene_now
L37   if ctx.debug(+0x3b) { debug.add_log("TBHOLD-EVAL …") } }
L50 if v3_survival_incoming(player, data, champ) /*1008c60*/ >= champ.hp && !nexus_final_stand(player, data) {
L52   return (parameter, 99999, RunAway(SmallActionRunAway::new(data, player, 5)))   // 인라인 페이로드 {+0x1510 tick, +0x1518/+0x1520 = 우물 좌표(team0:(32000,928000)/team1:(928000,32000)), +0x1528=5, +0x1538=-1} · 태그 3 · sret+0x1508=0x1869f }

L56 if let Some((target_id, until_tick)) = self.pending_global_ult_target(+0xde0) {
L58   if game.tick()(vt +0x28) > until_tick → L88
L61   if !champ.can_ult() /*165b800*/ → L88
L64   target = game.get_entity_by_id(target_id)(vt +0x1f0) ; None → L88
L66   eff = champ.ult_effect()  // level(+0x5c8) > 4 ? &champ.ult(+0x538) : &NONE(143a6fb70)
L67   if eff.tag(+0x30) == -1 → L88
L70~72 visible_enemies = Σ_{i<5} cache.player_champion[1-team][i].is_some_and(|e| is_recent_visible(e) /*★0.6.0 인라인: vt[0xf8] is_visible(team,id) ‖ (vt[0x150](id)→pos → bb[1-team].last_seen(+0x3e8+pos*8)+120 ≥ tick) · bb stride 0x5c8*/ && distance_sq(champ, e) < 22500000001)
L73   if visible_enemies == 0 && CastingTarget::check(&eff.target(+0x28), champ, target) /*165e7a0*/ {
L75     return (parameter, 99999, Ult(SmallActionUlt::new(data, target_id)))   // 페이로드 {+0x1510 tick, +0x1518 target, +0x1520 0} · 태그 17(★0.6.0 구 18) }
L88   self.pending_global_ult_target = None }
// ★0.6.0 L88′ (v3 · 아군 대상 궁 → 위기 아군에게 즉시)
if version >= 3 && champ.can_ult() && champ.level(+0x5c8) >= 5 && champ.ult(+0x538).tag != -1 {
  eff = &champ.ult ; info = eff.vt[0xc0](sret, eff, ctx, champ, &STATIC 143a70098)   // ★0.6.0 Effect vt 0xc0(구 0xa0 expected_buff_deep 계열 · 의미 추정 「스킬 정보」)
  if info.+0x48 != -1 && info.+0x118 & 1 {                                              // 아군 대상 가능 플래그(추정)
    for ally in cache.player_champion[team][0..5] { if ally.is_none() || ally.id(+0x5c0) == champ.id { continue }
      if cache.player_by_champion_id(ally.id) /*d72be0*/ == 0 { continue }
      if !CastingTarget::check(&eff.target(+0x28), champ, ally) { continue }
      if !1416432c0(eff, champ, ally) { continue }                                        // ★미독: 1416432c0 (사거리/유효성 검사 추정)
      if defensive_crisis(version, ?, player, data, ally, extra_tick=0, debug) /*e82c00 6-arg*/ & 1 {
        return (parameter, 99999, Ult{tick, target: ally.id}) /*태그 17*/ } } } }

L92 _t_ac = ProfTimer::start(36)
L93 candidates: bumpalo Vec<SmallActionPlay> = self.sub_plan.action_candidates(version, rnd, player, data, &parameter, &self.team_plan(+0xf8), debug)   // ★0.6.0 ed0b50(sret 0x2b70 스택 · JT 호스트)
L94 drop(_t_ac)
L99 if game.get_game_mode().tag == DeathMatch(2) {   // (Moba 에선 사장)
L100  move_speed = champ.stat_cached.move_speed(+0x640)
L101~115 filtered = candidates.filter(closure$2).cloned().collect_in(pool)   // closure$2: 태그 ∈ 14..=17 → true ; merged = pre_action.clone(); merged.merge(version, champ, a); match merged.get_input(version, &mut rnd.clone(), player, data, &parameter.positioning_score, debug) /*ebf5b0*/ { None → false, Some(Move{x,y}) → max(|x−champ.x|,|y−champ.y|) >= move_speed, Some(_) → true }
L116  if filtered.len()==0 { combat = self.sub_plan.combat_fallback(version, rnd, player, data); candidates = if combat.len()==0 { candidates } else { combat } } else { candidates = filtered } }
L125 self.v3_last_stand  = nexus_last_stand(player, data)     // (미확인 오프셋 · 구 0x1804)
L126 self.v3_final_stand = nexus_final_stand(player, data)    // (구 0x1805)
L127 self.v3_bail_goal = if self.plan(+0xf58).tag < 2 /*★0.6.0 Battle 니치*/ { match plan.sub_goal.tag(+0xf58+0x78) { Trace0→0, Kiting1→1, KitingBack2→2, RunAway3→3, End4→6 } /*★0.6.0 재번호 · Assassin/Protect 자리 소멸(4/5 미생성)*/ } else { 7 }   // (구 0x1813 · 미확인)
L139 self.v3_cand_src = match self.sub_plan.tag { DefenseNexus → match last_gate { 1→1, 2→2, _→3 } ; Battle(0x8000…05) → if last_bail_gate ∈ 1..=3 { +3 } else if candidates.len()==1 { 7 } else { 8 } ; _ → 0 }   // (구 0x1812 · 서브필드 오프셋 미확인)
L146 if self.sub_plan.tag == Battle(0x8000000000000005) && candidates.len()==1 {
L147   match candidates[0].get_action() { RunAway(태그 3,4,7) → self.v48_dodge_flee_picks += 1 ; AroundPosition(태그 6,10,11 + 0/1/2) → self.v48_dodge_step_picks += 1 ; _ → {} } }   // (미확인 오프셋 · 동치 추정)
L154 judge_accuracy = 100 + 9*min(player.[+0x480]*[+0x218]/1000, 100)   // 인라인
L157 judge_noise_ratio_now = if game.get_game_mode().tag == 2 { 0 } else { (900 − 9*min(x,100)) >> 1 }   // ★0.6.0 asm d6d6ed~d6d70f (= (1000−judge_accuracy)>>1 동치)
L158 lo = 1000 − ratio ; L159 hi = 1000 + ratio
L165 disc = if self.plan.tag >= 2 { tag − 2 } else { 7 }   // ★0.6.0 (Battle 니치 → idx 7 · 구 「tag>1 ? tag-2 : 4」)
L166 if self.judge_noise_plan(+0xe20 Some / +0xe28) != Some(disc) {
L167   self.judge_noise_plan = Some(disc)
L168   for i in 0..11 { self.judge_noise_ratio[i](+0x2428+8i) = rnd.gen_range(lo..=hi) /*dcad10*/ }   // ★rnd 소비 11회 }
L174 _t_sl = ProfTimer::start(37)
L175 judge_noise_ratio = self.judge_noise_ratio(+0x2428..+0x2480)   // 지역 복사 [i64;11]
L177~185 with_score: bumpalo Vec<(i64, SmallActionPlay)> = candidates.into_iter().map(closure$3).collect_in(pool)   // ★0.6.0 e13c10 (ca6700.md 참조) · env = {&sub_plan(+0x13a0), &pos_ring(+0x458), &version, &parameter, rnd, player, data, debug}
        closure$3(a): score = self.sub_plan.score(version, &parameter, rnd, player, data, &a, debug) /*ed3dd0*/
                      // ★0.6.0 v3 veto(e13c10 내부): f71610(version, player, data, &a, &pos_ring, debug) 이면 score = −9_999_999 (TurnBack veto · r22 a)
                      k = a.get_action() 판별자 (RunAway0 · Positioning1 · Around2 · AroundPosition3 · Trace4 · Attack6 · Skill7 · Skill2 8 · Ult9 · Stop10)
                      if |score| < 6 { score } else { judge_noise_ratio[k] * score / 1000 }   // 가중치표 tag3..9 [0,0,2,3,0,1,3] (AroundHide 제거 · 동치)
                      (score, a)
L186 drop(_t_sl)
L189 if ctx.trace_level(+0x39) != Off(0) {
L190   if with_score.any(|(_,a)| a.tag==RunAway(3) && a.with_ult(+0x81)) {
L192     hp_ratio = champ.hp*100 / champ.stat_cached.hp(+0x628)   // 0 → div_by_zero
L193~196 visible_enemies = Σ 적 5명: is_some && is_recent_visible && dist² < 22500000001
L199     nearby_allies = Σ 아군 5명: is_some && e.id != champ.id && dist² < 22500000001
L201     self.pending_trace_events.push(PendingTraceEvent{ UltEscape{hp_ratio, visible_enemies, nearby_allies, ult_used:false}, tick }) } }

// ── L215~242 TowerEngage 트레이스 (trace_level != Off 안) — 동치
L215 if self.plan.tag < 2 /*Battle*/ && plan.sub_goal.tag(+0x78) == RunAway(3) {
L217~219 nearest = cache.iter_towers_without_nexus(team).filter(|t| t.can_target(+0x6b9) && t.block_target_tick(+0x6a0)==0).min_by_key(dist_sq(champ))
L221   if let Some(tower) = nearest { d2 = tower.distance_sq(champ)
L223     if d2 < 2500000001 {
L224~226 tower_range = tower.attack_effect.map(|e| e.range(+0x4a0) + e.growth_range(+0x4a8)*(tower.level−1) + tower.stat_buff_cached.range(+0x438) + tower.radius()).unwrap_or(0)
L228~231 enemy_in_tower_range = cache.player_champion[1-team].flatten().any(|e| is_recent_visible(e) && e.distance_sq(tower) <= (tower_range + e.radius())²)
L234     hp_ratio = champ.hp*100 / champ.stat_cached.hp
L235~237 enemy_count = player_champion[1-team].flatten().filter(is_recent_visible).count()
L239~242 self.pending_trace_events.push({ TowerEngage{ tower_distance: sqrt(d2), hp_ratio, enemy_count, enemy_in_tower_range }, tick }) } } }
// ── L254~256 디버그 후보 덤프 · L260~261 후보 0 개 println! — 동치
// ── L269~290 「음수 점수 + 아군 대상 캐스트」 배제 — 동치 (태그 15..=17 Skill/Skill2/Ult · play+8 target · team eq)
L277 any_bad = with_score.any(|(s,p)| s < 0 && p.tag ∈ 15..=17 && game.get_entity_by_id(p.target(+8)).is_some_and(|e| e.team == champ.team))
L280~288 if any_bad { filtered = with_score.filter(!bad).cloned().collect(); if !filtered.is_empty() { with_score = filtered } }
L291 max_score = with_score.max_by_key(|x| x.0).unwrap().0   // 비어 있으면 unwrap_failed
// ── L300~307 도주 대체 후보 (version>1 고정)
L300 if max_score < −8999999 && game.get_game_mode().tag != 2 {
L302   run = RunAway(SmallActionRunAway::new(data, player, 5)) /*태그 3*/
L303   score = self.sub_plan.score(version, &parameter, rnd, player, data, &run, debug)   // ★rnd 소비 가능
L304   if score > max_score { return (parameter, score, run) } drop(run) }
// ── L309~326 최고점 후보 선택 — 동치 (choose = e59c80 · clone = d7c3d0)
L309 max_score_actions = with_score.filter(|x| x.0 == max_score).map(clone).collect()
L313 if ignore_action.len()==0 { result = (max_score, max_score_actions.choose(rnd).unwrap().clone()) }   // ★rnd ①
     else { without = max_score_actions.filter(|a| !ignore_action.any(|(_,ia)| *ia == a.get_action())).collect();
            result = if without.is_empty() { (max_score, max_score_actions.choose(rnd).unwrap().clone()) /*②*/ } else { (max_score, without.choose(rnd).unwrap().clone()) /*③*/ } }
// ★0.6.0 L326′ 계측(추정 · 판정 무영향): if player.champ_id(+0x9f0) ∈ {777,778} && (¬serpen_map ‖ tick ≥ first_spawn−30tps) && champ.attack.tag != -1 { 적 5명 가시 별 attack.vt[0x108](eff,champ,enemy) → lock inc [145029ba8 + k*32 + cat*8] }
// ── L329~339 디버그 로그(BUFFPICK / TBHOLD-PICK) — 동치 (add_log = e5d400 · 태그 판정 tag−15<3)
// ── L342~352 TBPICK 로그 — ★0.6.0 삭제
// ── L356~357
L356 if result.1.tag == LaneMinionPosition(12) { return (parameter, result.0, result.1) }
// ── L360 3갈래 (switch on tag−3 · ★0.6.0 재번호)
L360 match result.1 {
       Trace(13)                                                        → 갈래 A
       Around(5)|AroundRegion(6)|AroundPosition(0/1/2)|AroundPositionBush(10)|AroundBush(11)|LaneMinionPosition(12)   /*switch 값 {2,3,6,7,8,9} · bt 0x3cc*/ → 갈래 B
       _ (RunAway·Recall·AroundRunAway·Positioning·Attack·Skill·Skill2·Ult·Stop) → return (parameter, result.0, result.1) }
// 갈래 A (Trace): 같은 대상을 겨눈 Attack/Skill/Skill2/Ult 후보로 갈아타기
L360   target = t.target(+0x60)
L361   if self.sub_plan.tag == Battle(0x8000…05) { if let Some(t) = game.get_entity_by_id(target) { r = support_min_action_range(champ, t) /*★0.6.0 ebb2f0*/ + 25000; if dist²(t, champ) > r*r { return (parameter, result.0, result.1) } } }
L370   cast = with_score.filter(|a| a.1.tag ∈ 14..=17 && a.1.target(+8) == target).collect_in(pool)
L378   if cast.is_empty() { return (parameter, result.0, result.1) }
L382   max_score = cast.max_by_key(|a| a.0).unwrap().0 ; best = cast.filter(|a| a.0 == max_score).map(clone).collect()
L385   if max_score >= 0 || (SmallAction::from(&result.1) == SmallAction::from(pre_action) && pre_action.move_near_complete(champ)) { picked = best.choose(rnd).unwrap().clone() /*★rnd 1회*/; return (parameter, max_score, picked) }
       else { return (parameter, result.0, result.1) }
// 갈래 B (Around 계열): 대상 무관 Attack/Skill/Skill2 양수 후보로 갈아타기
L393   atk = with_score.filter(|(s,a)| a.tag ∈ 14..=16 /*Ult 제외*/ && *s > 0).collect_in(pool)
L399   if atk.is_empty() { return (parameter, result.0, result.1) }
L403   max_score = atk.max_by_key(..).0 ; best = atk.filter(== max).map(clone).collect()
L407   if max_score >= 0 || (SmallAction::from(&result.1) == SmallAction::from(pre_action) && pre_action.move_near_complete(champ)) { picked = best.choose(rnd).unwrap().clone(); return (parameter, max_score, picked) }
       else { return (parameter, result.0, result.1) }
// SmallActionPlay → SmallAction 변환표(★0.6.0 태그): RunAway3|Recall4|AroundRunAway7 → RunAway(0) · Positioning8 → Positioning{+8,+0x10}(1) · Around5|LaneMinionPosition12 → Around{+8}(2) · AroundRegion6 → AroundPosition{+0x10,+0x18}(3) · AroundPosition(0/1/2) → AroundPosition{+0x30,+0x38}(3) · AroundPositionBush10 → {+8,+0x10}(3) · AroundBush11 → {+0x18,+0x20}(3) · Trace13 → Trace{+0x60}(4) · Attack14 → Attack{+8}(6) · Skill15 → 7 · Skill2 16 → 8 · Ult17 → 9 · Stop18 → Stop(10)
// rnd 소비 순서: L11 → L93 → [L119 DM] → [L168 ×11] → L178 ×len(각 score 내부) → [L303] → L314/322/324 choose → [L386|L408]. ★0.6.0 신규 v3 궁 블록·계측 블록은 rnd 미소비 → 순서 불변.
```

## changes
- 오프셋: sub_plan +0x768→+0x13a0 · pending_global_ult +0x530→+0xde0 · 래치 +0x1809→+0x24d3 · positioning_score +0x990→+0x1408 · judge_noise_plan +0x560→+0xe20/+0xe28 · judge_noise_ratio +0x17a0→+0x2428 · parameter.v3_turnback_hold +0x1500→+0x14c8 · player team/pos/id +0xa00/+0xa90/+0x9f0.
- 재번호: Ult 태그 18→17 · Trace 14→13 · Attack/Skill/Skill2 15/16/17→14/15/16 · LaneMinionPosition 13→12 · 갈래 B switch 집합 {2,3,4,7,8,9,10}→{2,3,6,7,8,9} · sub_plan Battle 태그 7→0x8000000000000005 · plan Battle 9→니치(tag<2) · sub_goal 8→5(v3_bail_goal 매핑 4/5 미생성) · disc 기본 4→7.
- ★신규 L88′(v3): 아군 대상 궁 — `can_ult && level≥5 && ult.tag≠-1 && vt[0xc0] info.+0x48≠-1 && info.+0x118&1` → 아군(≠me · 플레이어 존재 · CastingTarget::check · 1416432c0 · defensive_crisis(ally, extra_tick 0)) 첫 매치에 `Ult{ally} 99999` 즉시 반환.
- ★신규 L326′ 계측(챔프 777/778 전용 전역 카운터 · 판정 무영향).
- L342~352 TBPICK 로그 블록 삭제.
- 콜리 v3: calculate_score_parameter 1000320 · cspv ed10b0 · snapshot de3e80 · solo_hunt_obs eb2e10 · incoming 1008c60 · final_stand db0160 · CastingTarget 165e7a0 · can_ult 165b800 · action_candidates ed0b50 · get_input ebf5b0 · score ed3dd0 · score 클로저 e13c10(f71610 veto 포함) · choose e59c80 · clone d7c3d0 · add_log e5d400 · support_min_action_range ebb2f0 · gen_range dcad10 · defensive_crisis e82c00(6-arg) · player_by_champion_id d72be0.

## verified
- capstone 0.6.0 d6c2b0(3300+줄): 래치 `+0x24d3`/`+0x14c8`(d6c86e~d6c886) ✓ · pending_global_ult `+0xde0`(d6cbc2/d6ce72) ✓ · v3 궁 블록 `cmp version,2; jbe skip → 165b800 can_ult → level(+0x5c8)≥5 → ult(+0x538).tag≠-1 → call [vt+0xc0] → [+0x1518]≠-1 && [+0x15e8]&1 → 아군 5슬롯 루프(id≠me · d72be0 · 165e7a0 · 1416432c0 · e82c00 6-arg extra 0) → Ult{tick,ally} · 태그 0x11 · 0x1869f`(d6ce7d~d6d0a6) ✓ · judge_noise: `(900−9·min(x,100))>>1 · DM→0 · disc = tag≥2 ? tag−2 : 7 · +0xe20/+0xe28 · [+0x2428..+0x2480] gen_range(dcad10) 11회`(d6d6ed~d6d7bc) ✓ · score 클로저 env `{+0x13a0, +0x458, &version, …}`(d6d86a~) ✓.
- 확인 못 한 지점: v48/v3_cand_src/v3_bail_goal 등 계측 필드의 0.6.0 오프셋(동치 추정) · info.+0x48/+0x118 의미 · 1416432c0 정체 · L326′ 계측 블록 정체(추정) · 배치 F/G 갈래 A/B 는 RE w6 「재번호만」 판정을 그대로 옮김.

## confidence
A — 본체 동치 + 신규 v3 블록이 asm 직독으로 확정. 잔여 미확인은 계측 필드 오프셋·vt 0xc0 info 필드 의미(판정 조건은 확정).