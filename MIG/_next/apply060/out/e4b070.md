# e4b070→d57b00 v2_apply_assign_commit

## logic_060
```
fn v2_apply_assign_commit(&mut self /*LPH*/, version, player, data, plan: &mut BigPlan, src: &mut u8)   // ★0.6.0 ABI: rnd·debug 탈락(6-arg) · rcx self · rdx version · r8 player · r9 data · [rsp+0xb0] plan · [rsp+0xb8] src (Ghidra 미정의 · capstone)
// 콜러: update_on_dead d51a70(d51d9e · `self.+0x24d6` 게이트) 등. ★+0x24d6 = LPH::update d3d210 `setne(version≠2)` → v3 상시 1 (r22 c §6) ⟹ 아래 「신모드」 만 살아있음.
// tp = self.team_plan(+0xf8) ; ctx = data+8 ; tut = ctx+0x38 ; cache = data+0 ; bbs = data+0x10 ; team = player.+0xa00 ; my_pos = player.+0xa90

if version < 2 { return }                                            // L520 (v3: 항상 통과)
s = *src
if (s − 11) as u8 <= 1 /* s ∈ {11 Serpen, 12 Morgard/Epic} */ {         // L524
  key = mf2_obj_key()                                                 // L525 ★0.6.0 이중모드(씬 니치 우선 → objective 폴백)
        = if tp.v4_serpen.take_born(+0x3e4 = self+0x4dc) != 2 { 0x50 | tp.v4_serpen.phase(+0x3e0 = self+0x4d8) }
          else if tp.v5_morgard.take_born(+0x404 = self+0x4fc) != 2 { 0x60 | tp.v5_morgard.phase(+0x400 = self+0x4f8) }
          else { match tp.objective.tag(+0xcd5 = self+0xdcd) { 0 Morgard => 0x60 | phase(+0xcd6 = self+0xdce), 1 Serpen => 0x50 | phase, _ => return } }
  alive = if s == 11 { serpen_exists(tut) = (0x1a1 >> tut) & 1 /*tut∈{0,5,7,8}*/ } else { morgard_exists(tut) = tut ∉ 1..=6 /*(tut−7) as u8 < 0xfa*/ }   // L526
  if plan.tag(u32 @+0) & !3 == 12 /* ∈ {12,13,14,15} Epic/Serpen HuntAndPoke/Battle */ {   // L527
    self.v2_obj_part(+0x24a2 tag / +0x24a3 key) = Some(key)          // L529 ★0.6.0 +0x1800/+0x1801 → +0x24a2/+0x24a3
    return
  }
  if self.v2_obj_part.is_none() { return }
  cur = self.+0x24a3
  if cur == key && alive {                                            // L530
    if v2_obj_restore_safe(version, player, data) {                   // L531 콜리 v3: d56570 3-arg(version, player, data) — ★RE 「(version, rnd, player)」 는 오기(레지스터: rcx←rdx version · rdx←r8 player · r8←r9 data · 본체가 [rdx+0xa00] team 읽음)
      drop(*plan) /*d35010*/; *plan = if s == 11 { SerpenHuntAndPoke(기본) } else { EpicHuntAndPoke(기본) }   // L533 · 레이아웃 {tag 14/12 @+0, +8=0, +0x10=8, +0x18=0, dword +0x1f=0}
    }
    return                                                            // (restore_safe 실패 시에도 return · v2_obj_part 유지)
  }
  if cur == key && !alive { self.v2_obj_part = None(+0x24a2=0) }      // L535~538 (cur≠key 이면 아무것도 안 함)
  return
}
// s ∉ {11,12}
if self.v2_obj_part.is_some() && self.+0x24a3 != mf2_obj_key() {      // L545 ★0.6.0 같은 이중모드 재계산(씬 니치 → objective · 둘 다 None 이면 「다름」)
  self.v2_obj_part = None                                             // L546
}
if s ∉ {17,18,19} { goto TAIL }                                       // L548 ★0.6.0 구 return → 신규 꼬리로
if let Some((csrc, cline)) = self.v2_assign(+0x24a6 csrc / +0x24a7 cline · 0xff=None) {   // L551 ★0.6.0 +0x1802/+0x1803 → +0x24a6/+0x24a7
  ok = match csrc {                                                   // L552
    19 => if self.+0x24d6 /*v3 상시 1*/ { d753e0(cache, tut, team, cline) && d750f0(cache, ctx, team, cline) == Some(my_pos) }   // L553 ★0.6.0 신모드 헬퍼화(아래)
          else { d75840(team, my_pos, data, cline, self.+0x24d5) },   // ★v2 사장(= 구 mf2_cover_need_excluding_me + Support 제외 플래그 · v3 dead)
    17 => d756b0(team, cache, tut, cline),                            // L554 ★0.6.0 헬퍼화 = 구 mf2_epic_line_valid 비트 동치
    _  => false }
  if ok {                                                             // L557
    drop(*plan); *plan = PassiveLine(PassiveLinePlan::기본(line=cline))   // L558 · 레이아웃 {tag 3 @+0, +8=0, Vec×5 {cap 0, ptr 8, len 0} @+0x20/+0x38/+0x50/+0x68/+0x80, +0x90..+0x11f=0, dword +0x11f=0, +0x123=line, word +0x124=0xffff}
    *src = csrc                                                       // L559
    return
  }
  self.v2_assign = None(+0x24a7=0xff)                                 // L562
}
match s { 17 | 19 => /* (s|2)==0x13 */                                // L564
  if plan.tag == 3 PassiveLine { self.v2_assign = Some((s, plan.line(+0x123))) }   // L565~566 (+0x24a6=s, +0x24a7=line)
  _ => {} }
return

TAIL:  // ★0.6.0 신규 꼬리(s ∉ {17,18,19} · +0x24d6 게이트 = v3 상시)
if !self.+0x24d6 { return }
match s {
  13 | 15 => {                                                        // s13 Line / s15 (Epic 라인 계열)
    if plan.tag != 3 PassiveLine || plan.v4_split(+0x120) != 0 { return }   // v4_split = PassiveLinePlan 페이로드 +0x118(r22 c)
    line = plan.line(+0x123)
    idx = (s != 13) as usize ; latch = &mut self.+0x24e3[idx]         // +0x24e3 (s13) / +0x24e4 (s15) · 초기값 0xff(LPH::new d593e0)
    if *latch == 0xff { *latch = line; return }
    ok = if s == 13 { d75780(team, tut, bbs, *latch) /*라인 밀림*/ } else { d756b0(team, cache, tut, *latch) /*에픽 라인 유효*/ }
    if !ok { *latch = line; return }
    if *latch != line { drop(*plan); *plan = PassiveLine(PassiveLinePlan::기본(line=*latch)) }   // src 불변
  }
  29 => { if plan.tag == 3 && plan.+0x120 == 0 { self.+0x24e5 = plan.line(+0x123) } }   // 기록만
  _ => {}
}
return

--- d753e0(cache, tut:u8, team, line) -> bool  [신모드 커버 필요 · 구 mf2_cover_need_excluding_me 에서 n==0 항 삭제] ---
if !line_exists(tut, line) { return false }                          // 마스크 Top 0x185 / Mid 0x1b1 / Bottom 0x1ab · tut>8 → false
pos = [0,2,3][line]
if cache.lead[line][team] (+0x23a0 + line*0x10 + team*8) > 2 { return false }   // lead < 3
return cache.player_champion[team][pos] (+0x1e0+team*0x28+pos*8).is_null()

--- d750f0(cache, ctx, team, line) -> Option<Position>  [신규 · 지정 커버러 · RAX=is_some, RDX=pos] ---
for l in [Mid1, Top0, Bottom2] { if l == line { continue }
  pos = [0,2,3][l]; if cache.player_champion[team][pos].is_null() { continue }
  if !line_exists(ctx.tutorial, l) || cache.lead[l][team] >= 3 { return Some(pos) } }
None

--- d756b0(team, cache, tut, line) -> bool  [= 구 mf2_epic_line_valid 비트 동치] ---
line_exists(tut, line) 아니면 false
(tag, moba) = game.get_game_mode() /*vt+0x40*/ ; tag != 0 || moba.is_null() → false
moba.epic_minion_buff_time[team] (+0x240+team*8) == 0 → false
e = 1 − team ; return cache.tower[line][0][e] != 0 || cache.tower[line][1][e] != 0   // +0x180 + line*0x20 + tier*0x10 + e*8

--- d75780(team, tut, bbs, line) -> bool  [신규 · 라인이 우리 쪽으로 밀림] ---
line_exists(tut, line) 아니면 false
ms = bbs[team](stride 0x5c8).minion_state[line] (+0x0/+0x28/+0x50)
return ms.from_mid(+0x10) < −3000 && ms.minion_count(+0x20) < −3

// 부수효과: self +0x24a2/+0x24a3/+0x24a6/+0x24a7/+0x24e3/+0x24e4/+0x24e5 · *plan(교체 시 drop d35010 + 새 레이아웃) · *src. 헬퍼는 읽기만.
```

## changes
- ABI: (self, version, rnd, player, data, plan, src, debug) → (self, version, player, data, plan, src) — rnd·debug 탈락. restore_safe 도 (version, player, data).
- 필드 이동: v2_obj_part +0x1800/1 → +0x24a2/3 · v2_assign +0x1802/3 → +0x24a6/7.
- [L525]/[L545] mf2_obj_key 이중모드: TeamPlan 씬 니치(serpen +0x3e4/+0x3e0 · morgard +0x404/+0x400) 우선 → objective(+0xcd5/+0xcd6) 폴백.
- [L553] csrc 19 = 신모드 `d753e0 && d750f0==Some(my_pos)`(v3 상시) · 구모드 d75840(+Support 제외 플래그) 는 dead.
- [L554] csrc 17 → d756b0 헬퍼(동치).
- [L548] s∉{17,18,19} 는 return 대신 신규 꼬리: s∈{13,15} && PassiveLine && !v4_split → 래치 +0x24e3/+0x24e4 로 라인 복원(d75780/d756b0 검증) · s==29 → +0x24e5 기록.
- PassiveLine 기본 레이아웃 0.6.0(+0x123 line · +0x124 0xffff · +0x120 v4_split=0).
- 콜리 v3: `d56570` restore_safe · `d753e0`/`d750f0`(신규) · `d756b0`(동치) · `d75780`(신규) · `d75840`(dead) · `d35010` drop.

## verified
- capstone d57b00 전량(d57b00~d5809b): version<2 return · s−11≤1 · 씬 니치 +0x4dc/+0x4fc → 0x50|+0x4d8 / 0x60|+0x4f8 · objective +0xdcd/+0xdce · alive 마스크 0x1a1 / (tut−7)<0xfa · plan tag&~3==0xc → +0x24a2=1,+0x24a3=key · cur==key&&alive → d56570(rcx=version, rdx=player, r8=data) → plan={0xc/0xe,…} · +0x24a2=0 조건 · s−17<3 · +0x24a7==0xff · csrc 0x11→d756b0 / 0x13→(+0x24d6 ? d753e0→d750f0==rdx vs +0xa90 : d75840(+0x24d5)) · PassiveLine 레이아웃 · *src=csrc · +0x24a7=0xff · (s|2)==0x13 래치 · 꼬리 s∈{0xd,0xf,0x1d} · +0x120==0 · +0x24e3+idx · d75780/d756b0 · +0x24e5.
- Ghidra 0.6.0 d56570 디컴: (version, player, data) → 내 챔프 None → true ; 근접 적(e0b8b0) 없음 → true ; else `check_kill_die_tick eda920(version, data, player, me, &enemies, &[], 0,0,0, &None) > 2·tps`.
- 헬퍼 5 본체 = RE r22 c 전재(이번 스팟체크 미수행).

## confidence
A — 본체 전량 디스어셈 대조 · 헬퍼는 r22 c 의존(B).
