# de92d0→fb5ff0 epic_passive_plan

## logic_060
```
// 인자: (sret Option<BigPlan>, version, rnd, player, data, phase: ObjectPhase, team_plan, depart_anchor: Option<(u64,u64)> {tag@0,x@8,y@0x10}, debug)
// 반환 인코딩: None = tag -1 · EpicHuntAndPoke(0xc) = {tag 0xc, +8 0, +0x10 8, +0x18 0, +0x1f u32 0}(빈 Vec + 플래그 4개 false) · PassiveLine(3) = 0x128B 페이로드(+0x120 v4_split · +0x123 line · +0x124/+0x125 0xff) · ★0.6.0 ActiveRecall = tag 8(페이로드 없음)
ctx = data.context; if !spawn_epic(ctx.tutorial(+0x38)) [(tag−1) <u 6 ⇔ 태그∈1..=6] → return None            [L318-319]
team = player.info.team(+0xa00) (bounds 2); pos = player.info.position(+0xa90)
champ = cache.player_champion[team][pos].unwrap()   (cache+0x1e0+team*0x28+pos*8)                            [L322]
// ★0.6.0 phase 분기 재작성 (구: Hunt(3)→EHP · Setup(1)→본문 · 그 외 None)
match phase {
  p if p > 2  => if !tp.v6_obj(+0xcc7) { return EpicHuntAndPoke }  else { CONTEST }      // cc7 = (version≥2) 상시 1 ⟹ 실효: Hunt(3)/그 이상 → CONTEST   // ★0.6.0
  2           => if !tp.cc7 { return None } else { CONTEST }                             // ★0.6.0 (구: None)
  1 (Setup)   => SETUP
  _           => return None                                                             [L497]
}

// ── SETUP ── ★0.6.0 tp.v10(+0xcc0)=(version≥2) 상시 1 → 구 L327~L457(strategy/Split/Flexible/Gather 표) 전체 사장. 아래가 대체 본문(fb62b0~fb6b50)
SETUP:
  // ★v2 사장: if !tp.cc0 { L327 strategy … L457 } — 0.5.8 logic 전문의 Split/Flexible/Gather 트리(top/mid lead +0x23a0/+0x23b0 · bb minion_count top +0x20 mid +0x48 bottom +0x70 · Split → PassiveLine{Bottom, v4_split=1 (+0x120=0x2000001)} 은 바이너리에 남아 있으나 도달 불가)
  camp = map_pos_cached(Morgard(4), team)                                                  // 1416a7af0 (TLS 메모이즈, 구 157b720 camp_pos 와 값 동일)
  moba = game.get_game_mode()(vt+0x40).as_moba().unwrap()                                  // tag 0 아니면 unwrap 패닉
  remain = moba.jungle_runner.epic.next_respawn_tick(moba+0x1b0).saturating_sub(tick = game.tick()(vt+0x28))   // ★0.6.0 캠프 상태 moba+0x18+8*0x30+0x18
  (px,py) = depart_anchor.unwrap_or((champ.x(+0x660), champ.y(+0x668)))                    [L399]
  travel = distance(px,py, camp) / max(champ.move_speed(+0x640), 1)                        // ★0.6.0 ms 0 → 1 (구: 패닉)
  if travel + tps*2 < remain → return None                                                 [L403-404 동형]
  since = tick.saturating_sub(tp.obj_spawn.epic_camp_last_visible_tick(+0xa0))             // ★0.6.0 +0x80→+0xa0
  top_lead = cache.top_lead[team](+0x23a0+team*8); mid_lead = cache.mid_lead[team](+0x23b0+team*8)   // ★0.6.0 +0x21c0/+0x21d0→+0x23a0/+0x23b0
  if top_lead < 3 {
     if since <= tps*3 { return if camp_has_visible_enemy() { EHP } else { None } }        // f904b0
     else { goto F }
  } else {
     if since <= tps*3 { return if mid_lead > 2 || camp_has_visible_enemy() { EHP } else { None } }
     else if mid_lead > 2 { return EHP }
     else { goto F }
  }
  F: near = team_plan.can_near_enemies_range(version, rnd, player, data, camp.x, camp.y, 150000, debug)   // f10c60 → Vec (drop) · 콜리 v3: dd9f30→f10c60 본체 동치
     return if near.len() > 2 { EHP } else { None }
  // camp_has_visible_enemy() = FUN_140f904b0: cache.player_champion[1-team].iter().flatten().any(|e|
  //     (game.is_visible(team, e.id)(vt+0xf8) || game.get_player_by_id(e.id)(vt+0x150).is_some_and(|ps| tick <= bb[1-team].last_seen[ps.position(+0xa90)](bb+0x3e8+p*8) + 120))
  //     && dist²(e, camp) < 150000²+1)

// ── CONTEST ── (fb60cf~fb6300 · serpen 판과 동형, slot 1 · 마스크 ca5/ca3 · 씬 +0x404/+0x403 · ETA +0x790 · k-랭킹 없음)
CONTEST:
  // v3 블록 (version > 2 — 상시)
  bit = (1u8 << pos) & tp.v6_assemble_released[1](+0xca5)          // 부적격→귀환 비트
  d3 = match phase {
        2 => bit == 0 && contest_eligible(1, version, pos, player, data),                                     // ef5f20
        3 => if bit != 0 { false } else if tp.morgard_scene_none(+0x404) != 2 && tp.morgard_stance_grind(+0x403) & 1 == 0 { contest_eligible(1, version, pos, player, data) } else { true },
        _ => bit == 0,
      };
  bf = !d3;
  if d3 && !contest_can_hold(tp, 1, pos, player, data) {                                                        // ef6da0
      camp = map.camp_pos(Morgard, team==0) (157b720); d2 = dist²(champ, camp); hp = champ.hp*100 / max(champ.stat_cached.hp, 1);
      n = healthy_allies_except_me(pos, cache, player)                                                         // fbb470: p≠me · 생존 · hp%≥50 카운트
      bf = if hp < 50 { phase != 3 || d2 > 250000² } else if phase == 3 { d2 > 250000² && n > 2 } else { n > 2 };
  }
  if bf { return if recall_fits_window(tp, 1, player, data) { tp.c38 += 1; Some(ActiveRecall(8)) } else { None } }   // ef57f0 · +0xc38 lock inc
  // 공통
  if phase != 2 {
      camp = map_pos_cached(4, team); travel = distance(champ, camp) / max(ms,1); tick = game.tick();
      if tick + travel + tps*2 < tp.v6_obj_arrive_at[1](+0x790) { return None }     // 팀 도착 예정 틱
      return EHP
  } else {
      if (tp.v6_assemble_departed[1](+0xca3) >> pos) & 1 != 0 {
          camp = map_pos_cached(4, team);
          if dist²(champ, camp) > 220000² { return EHP }
      }
      return None
  }
```

## changes
- 진입 match: `phase>2 && cc7 → CONTEST`(cc7==0 이면 구 EHP) · `phase==2 && cc7 → CONTEST`(구 None) 추가.
- Setup 본문: `tp.cc0`(=v≥2, 상시) 이면 구 L327~L457(strategy·v23 압박·Split·Flexible·Gather 표) 전체 사장 → 신 본문 = 리스폰 잔여(epic next_respawn moba+0x1b0) vs 이동+2tps → since(tick−+0xa0) · top/mid lead(+0x23a0/+0x23b0) 표 → f904b0(캠프 150000 내 가시/최근가시 적) / f10c60(150000).len()>2.
- CONTEST 블록 신규(ObjContest v6): ca5 비트 · contest_eligible ef5f20(slot 1) · contest_can_hold ef6da0 · hp<50/phase 3/건강 아군 n>2(fbb470) → ActiveRecall(recall_fits_window ef57f0, +0xc38++) 또는 None · phase 2: ca3 비트 && dist²>220000² → EHP else None · phase≠2: tick+이동+2tps < +0x790 → None else EHP.
- 오프셋: TeamPlan camp_last_visible `+0x80→+0xa0` · lead `+0x21c0/d0→+0x23a0/b0` · PlayerState +0xa00/+0xa90 · BigPlan ActiveRecall 태그 8 · PassiveLine v4_split +0x120(사장 경로만 1).
- 콜리: camp_pos 157b720(비캐시)/1416a7af0(캐시) · f87c30/f892d0/f89d70/fbb190/fbb560/f909c0(사장 경로) · f904b0 · f10c60 · ef5f20/ef57f0/ef6da0/fbb470 · distance 1660a00.
- version<3 분기: CONTEST 의 v3 블록만 `version>2` 게이트(상시 참) — logic 에서 게이트 표기 생략.

## verified
- Ghidra 0.6.0 `FUN_140fb5ff0` 전문 디컴: tutorial 게이트 · `2<phase → cc7==0 ? EHP : CONTEST` · `cc7 && phase==2 → CONTEST` · `cc0 != 0` Setup 신본문(vt+0x40 tag 0 · `moba+0x1b0` · anchor tag → x/y · `1660a00/max(ms,1)` · `travel + 2tps < remain → None` · `tp+0xa0` since · `+0x23a0/+0x23b0` 비교 3 · `f904b0` · `f10c60(…,150000)` len>2) · CONTEST(`ca5` 비트 · phase 2/3 분기 · `0x404!=2 && !(0x403&1)` · ef5f20(1,…) · ef6da0(tp,1,…) · 157b720 camp · `hp*100/max(max,1) < 0x32` · fbb470 · 62500000000 · ef57f0 → `lock inc +0xc38` → tag 8) · 공통(`+0x790` · `ca3` 비트 · 48400000000).
- `FUN_140f904b0` 디컴으로 의미 확정(가시 vt+0xf8 ‖ get_player vt+0x150 → bb[1−team]+0x3e8+pos*8 +120 ≤ tick · dist² < 0x53d1ac101).
- 미확인: 사장 경로(L327~L457)의 줄단위 대조(RE w9 「비트동일」 신뢰) · ef5f20/ef6da0/ef57f0/fbb470/f10c60 내부(RE w9 §1 정체표 신뢰).

## confidence
A — 전문 디컴이 RE w9 §3 과 일치하며 f904b0 의미를 추가 확정.
