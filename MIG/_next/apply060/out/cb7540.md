# cb7540→f356b0 HideSubPlan::action_candidates

## logic_060
```
// sig 동일: fn action_candidates(&mut self HideSubPlan, version, rnd, player, data, _parameter, _debug) -> bumpalo Vec<SmallActionPlay>
// ★0.6.0 HideSubPlan(SubPlan 페이로드 +8 기준 · 구 16B → 0x25B): +0x00..+0x17 ambush_cell(Option 계열 · 본 함수 미소비) · +0x18 bush(usize) · +0x20 check_move · +0x21 enemy_spotted_me · +0x22 direct(미소비) · ★+0x23 stealth · +0x24 out_line
//   생성지(0.6.0 실행 경로) = fa3980 PassiveJungle::sub_plan 매복 1곳: {ambush None, bush, check_move 0, spotted 0, direct 0, stealth 1, out_line 1} ⇒ 런타임 Hide 는 stealth=true 전용(구 캠프접근 L34~55·배치 M 은 실질 사장 · 추정)
//   승계 ed3a30 case Hide(0x8000000000000007): old.bush==new.bush && old.out_line==new.out_line → ambush_cell=new · check_move/enemy_spotted_me 는 old 유지 · direct/stealth=new&1 · 아니면 전체 교체
// ★0.6.0 태그: AroundPosition 0/1/2(dataful) · RunAway 3 · AroundBush 11 · Trace 13 · Attack 14 · Skill 15 · Skill2 16 · Stop 18(0x12)
// ★0.6.0 콜리: can1v1win de7220(rnd,player,data,champ,enemy) · battle_action ed6300(&out,version,rnd,player,data,5) · attack_summon_action ed8550(&out,player,data) · AroundBush::new_with_target ecfff0(data,enemy,bush,out_line) / new_with_out_line ed0240(rnd,data,player,bush,out_line) · camp_pos 16a7af0(캐시) · estimate 1643790 · can_attack 164b370 · can_skill 165d410 · can_skill2 164b650 · CastingTarget::check 165e7a0 · ★신규 ee0fe0/ee0dd0/ee1820/ecdb90
// player: team +0xa00 · position +0xa90 · bb stride 0x5c8 · last_seen +0x3e8+pos*8 · map = ctx+0x20 · map.region/bushes 그리드 +0x1c98 [y][x] (row stride 0xf0 · 30×30)

fn action_candidates(self, version, rnd, player, data, _parameter, _debug) -> Vec<SmallActionPlay>
 v3flag = version >= 3   // [rbp+0x254c] ★0.6.0
 res = Vec::new_in(ctx.pool)
 team = player.team(+0xa00) (bounds<2) ; champ = cache.player_champion[team][player.position(+0xa90)].unwrap()
 is_visible_to_enemy = game.is_visible(1-team, champ.id)   // vt +0xf8
 if is_visible_to_enemy { self.enemy_spotted_me(+0x21) = true } else if self.check_move(+0x20) { self.enemy_spotted_me = false }   // (쓰기만 · 읽는 곳 없음)

 // ★0.6.0 stealth 이동 블록 (L34 앞 · v3 && stealth && !check_move)
 if v3flag && self.stealth(+0x23) && !self.check_move {
   (cx, cy) = (min(champ.x/32000, 29), min(champ.y/32000, 29))
   if map.region[cy][cx] (map+0x1c98 + cy*0xf0 + cx*8) == self.bush(+0x18) { self.check_move = true; → L57 }
   else {
     g = ee0fe0(data, player, 1-team)   // ★미독: 격자 묶음 0x11e4B(g0/g1/g2/g3/meta/extra)
     match ee0dd0(&g.g1, &g.g0, &g.g2, &g.g3, cx, cy, min(self.bush, 0xffff) as u16) {   // ★미독: 셀→부시 경로 Option<Vec<(u8,u8)>>
       Some(r) if r.len()==0 → self.check_move = true; → L57
       Some(r) → res.push(AroundPosition(SmallActionAroundPosition::new_ex(rnd, data, r[0].0*32000+16000, r[0].1*32000+16000, end_delay 5, out_line 1, radius 16000)))   // ecdb90
       None → if g.g0[cy*30+cx] != 0 { if let Some((nx,ny)) = ee1820(&g.g1, &g.g0, &g.meta, g.extra, cx, cy) /*★미독: 폴백 다음 셀*/ { res.push(AroundPosition::new_ex(rnd, data, nx*32000+16000, ny*32000+16000, 5, 1, 16000)) } else { res.push(Stop /*태그 18*/) } }
              else { res.push(Stop) } }
     → 꼬리(배치 M 건너뜀) } }

 // L34 — ★0.6.0 게이트: `!check_move && !(v3 && stealth)` (stealth 모드에선 구 캠프 접근 블록 사장)
 if !self.check_move && !(v3flag && self.stealth) {
   (x,y) = BUSH_POSITIONS(71셀 · 143a87830).filter(|&(x,y)| map.bushes[y][x] == self.bush).next().unwrap()
   bx = x*32000+16000; by = y*32000+16000
   if is_top_side(ctx, bx, by) /* setting.height(+0x12c0) − by >= bx */ {
     (ex,ey) = map.camp_pos(Morgard(4), team==0); if champ.distance_sq(ex,ey) > 100000² { res.push(AroundPosition(new_with_out_line(rnd, data, ex, ey, 5, Outline(1)) /*인라인 · 반경 60000 · +0x58=80000 · 태그 1*/)) } else { self.check_move = true } }
   else { (ex,ey) = map.camp_pos(Serpen(5), team==0); if champ.distance_sq(ex,ey) > 100000² { res.push(AroundPosition(..)) } else { self.check_move = true } } }
 // L57
 if self.check_move {
   (x,y) = BUSH_POSITIONS.filter(map.bushes[y][x]==self.bush).next().unwrap(); bx = x*32000+16000; by = y*32000+16000
   nearest_enemy = iter_champions(cache.player_champion[1-team]).filter(|e| is_recent_visible(bb[1-team], e) /*last_seen+120 ≥ tick*/ && e.distance_sq(bx,by) < 250000²+1).min_by_key(|e| e.distance_sq(champ))   // 동점 앞 원소
   if is_visible_to_enemy {
     if let Some(enemy) = nearest_enemy {
       nearby_allies  = iter_champions(player_champion[team]).filter(|e| e.id != champ.id && e.distance_sq(champ) < 150000²+1).count()
       nearby_enemies = iter_champions(player_champion[1-team]).filter(|e| e.is_visible_from(champ) /*champ.team Neutral→true · Player(t): e.visible_state[t](+0x38+16t).tag==0*/ && e.distance_sq(champ) < 150000²+1).count()
       dominated = nearby_allies < nearby_enemies
       can_fight = !dominated && can1v1win(rnd, player, data, champ, enemy)   // de7220 (dominated 면 호출 생략)
       if can_fight { res.extend(battle_action(version, rnd, player, data, 5)); res.push(Trace(SmallActionTrace::new(data, enemy.id, 5)) /*태그 13 · goal=(t.x,t.y) or (0,0) · margin 15000*/) }
       else { res.push(RunAway(SmallActionRunAway::new_with_skill(data, player, 5, false)) /*태그 3*/); if !dominated { res.extend(battle_action(..)) } } }
     else { res.push(RunAway(new_with_skill(data, player, 5, false))) } }
   else {
     if let Some(enemy) = nearest_enemy { res.push(AroundBush(SmallActionAroundBush::new_with_target(data, enemy, self.bush, self.out_line(+0x24))) /*ecfff0 · 태그 11*/) }
     else { res.push(AroundBush(new_with_out_line(rnd, data, player, self.bush, self.out_line)) /*ed0240*/) }
     res.extend(self.steal_jungle_action(rnd, player, data))   // 인라인 본체 ↓
     // ===== steal_jungle_action 인라인 (동치) =====
     // steal_res = Vec::new_in(bump); steal_range = 150000; champ 재로드
     // (bx,by) = BUSH_POSITIONS.filter(bushes[y][x]==self.bush).next().map(|(x,y)| (x*32000+16000, y*32000+16000)).unwrap_or(champ.xy)
     // for jungle in cache.jungles (Vec +0xd0 ptr / +0xe8 len) {
     //   if !(jungle.ty(+0x68)==4 && jungle.ty.Jungle.info.camp_type.0(+0x98)==1-team) { continue }
     //   if jungle.distance_sq(bx,by) > 150000² { continue }
     //   move_speed = champ.stat_cached.move_speed(+0x640)
     //   if champ.can_attack() { if let Some(atk) = &champ.attack(+0x490 · 태그 +0x4c0 != -1) { expected = atk.expected_damage_target(ctx, champ, jungle) /*1643790*/; range = atk.range(champ) /*+0x4a0 + (level−1)*+0x4a8 + +0x438*/ + atk.range_adjust(champ, jungle) /*vt +0x108 ★0.6.0 구 +0xe8*/ + champ.radius() + jungle.radius(); dist_sq = jungle.distance_sq(champ); max_dist = range + move_speed*20; if jungle.hp <= expected && dist_sq <= max_dist² { steal_res.push(Attack(SmallActionAttack::new(data, jungle.id)) /*태그 14*/) } } }
     //   if champ.can_skill() { if let Some(skill) = &champ.skill(+0x4c8 · 태그 +0x4f8) { if skill.target(+0x4f0).check(champ, jungle) { … 동형 … steal_res.push(Skill(..) /*태그 15*/) } } }
     //   if champ.can_skill2() { if let Some(skill2) = champ.skill2_effect() /*level>2 ? +0x500 : NONE · 태그 +0x530*/ { if skill2.target(+0x528).check(champ, jungle) { … steal_res.push(Skill2(..) /*태그 16*/) } } } }
     // ===== 끝 =====
     → 꼬리(배치 M 건너뜀) } }
 // 배치 M — ★0.6.0 게이트: is_visible_to_enemy && !check_move && !(v3 && stealth)
 else if is_visible_to_enemy && !self.check_move && !(v3flag && self.stealth) {
   nearest_enemy = iter_champions(1-team).filter(|x| x.is_visible_from(champ)).min_by_key(dist_sq(champ))   // 동률 앞 원소
   if let Some(enemy) = nearest_enemy {
     nearby_allies  = iter_champions(team).filter(|x| x.id != champ.id && x.distance_sq(champ) < 22500000001).count()   // 기준점 champ
     nearby_enemies = iter_champions(1-team).filter(|x| x.is_visible_from(champ) && x.distance_sq(champ) < 22500000001).count()
     dominated = nearby_allies < nearby_enemies ; can_fight = !dominated && can1v1win(rnd, player, data, champ, enemy)
     if can_fight { res.extend(battle_action(version, rnd, player, data, 5)) }
     else { res.push(RunAway(new_with_skill(data, player, 5, false))); if !dominated { res.extend(battle_action(..)) } } }
   else { res.push(RunAway(new_with_skill(data, player, 5, false))); res.extend(battle_action(..)) } }
 // 꼬리
 res.extend(attack_summon_action(player, data))   // ed8550
 if v3flag && self.stealth && res.is_empty() { res.push(Stop /*태그 18*/) }   // ★0.6.0 신규 (f373eb)
 return res
```

## changes
- 레이아웃: HideSubPlan 16B → 0x25B(+0x18 bush · +0x20 check_move · +0x21 enemy_spotted_me · +0x22 direct · +0x23 stealth · +0x24 out_line · +0x0 ambush_cell) — 본 함수는 +0x18/+0x20/+0x21/+0x23/+0x24 만 읽음.
- ★신규 stealth 이동 블록(v3 && stealth && !check_move): 현재 셀 region==bush → check_move=true · 아니면 ee0fe0/ee0dd0 격자 경로 → AroundPosition::new_ex(out_line 1, r 16000) · 폴백 ee1820 · 없으면 Stop · 배치 M 건너뜀.
- L34 게이트 `!check_move` → `!check_move && !(v3 && stealth)` · L107 게이트 `is_visible && !check_move` → `… && !(v3 && stealth)`.
- 꼬리: `v3 && stealth && res.is_empty() → push Stop`.
- 태그 재번호(RunAway 3 유지 · AroundBush 12→11 · Trace 14→13 · Attack/Skill/Skill2 15/16/17→14/15/16 · Stop 19→18) · range_adjust vt +0xe8→+0x108.
- 콜리 v3: can1v1win→de7220 · battle_action→ed6300 · attack_summon(cada80/eb8b80)→ed8550 · AroundBush 생성자 ecfff0/ed0240 · camp_pos 16a7af0 · estimate 1643790 · can_* 164b370/165d410/164b650 · CastingTarget 165e7a0 · 신규 ee0fe0/ee0dd0/ee1820/ecdb90/dc05a0(reserve)/f26680(extend).
- 오프셋: player team/pos +0xa00/+0xa90 · bb stride 0x5c8 · last_seen +0x3e8 · BUSH_POSITIONS 143a87830 · 그리드 map+0x1c98(row 0xf0).

## verified
- capstone 0.6.0 f356b0(1400+줄): `cmp version,3; setae [rbp+0x254c]` · `[r12+0x23] & v3 · !check_move` 게이트(f35830~f35899) ✓ · 셀 계산 `x/32000(0x20c49ba5e353f7d) · min 29 · [map+0x20]+cy*0xf0+cx*8+0x1c98 == [r12+0x18]` → check_move=1(f358a6~f3590d) ✓ · ee0fe0 → ee0dd0(7-arg · u16 bush) → r.len==0 분기 / new_ex(ecdb90 · x*0x7d00+0x3e80 · [rsp+0x28]=5 · [rsp+0x30]=1 · [rsp+0x38]=0x3e80) / ee1820 폴백 / Stop 0x12(f35965~f35dac) ✓ · L107 게이트 `cmp [rbp+0x254c],0`(f36824) ✓ · 꼬리 Stop 0x12(f373eb~f37409) ✓.
- 확인 못 한 지점: ee0fe0/ee0dd0/ee1820 내부(격자 의미·경로탐색) · direct/ambush_cell 소비처 · L57~101/배치 M 은 RE w8 「그대로」 판정을 옮김.

## confidence
B — 게이트·신규 블록·태그는 asm 확정. 재현 완전성은 미독 격자 헬퍼 3종(ee0fe0/ee0dd0/ee1820) RE 에 달림(w3 JungleSubPlan 과 공유).