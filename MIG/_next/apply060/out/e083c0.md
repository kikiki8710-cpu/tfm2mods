# e083c0→ee8450 resolve_fight_uncached

## logic_060
```
// fight_model.rs:0~464 (배치 K)
// ★0.6.0 인자 15→16: version, data(&OperationData → data.cache · data.context), champ, near_allies(&[&Entity]), near_enemies(&[&Entity]), committed_dir(i8), tower(Option<&Entity>), judge_accuracy, ★bias: i8(신규 · judge_accuracy 뒤 · [rsp+0x3b0] movsx), arrivals(&[i64]), baseline(i64) → sret FightPrediction(64B · 레이아웃 동일)
// 콜리 계약(자식 명세 별도): self_sustain_in_window(version,&GameContext,&Entity,horizon)->i64 · 콜리 v3: ed9f40(구 eba0c0 · Effect vt 0x40/0x48→0x58/0x60) · available_cc_in_window(version,&Entity,horizon)->i64 · 콜리 v3: ed8db0(구 eb93e0 · vt 0x88→0xa8) · fight_dps(version,&GameContext,att,tgt)->i64 · 콜리 v3: ee0800(구 ebe220) · expected_dps(&GameContext,tower,tgt)->i64 · 콜리 v3: ed5fd0(구 eb5dd0) · error_ratio_noise(&mut NoiseRng,j)->i64 (e=(1000-j)/20(udiv) · state+=0x9E3779B97F4A7C15 · splitmix64 믹스 · mulhi(mix, 2e+1) - e + 100 ⇒ [100-e, 100+e] 균등 정수 백분율)

L379: let context = data.context;
L380: if near_allies.is_empty() || near_enemies.is_empty() {
L381:   return FightPrediction { focus_target: None, soaker: None, rescue_ally: None, net_value: 0, line: Hold(3), line_absolute: Hold(3) };  // 0x0/0x10/0x20 ← 0, 0x30 ← 0, 0x38/0x39 ← 3
}

// ── 시드 (L392~403) ──
L392: let seed: u64 = if version > 1 {                                       // v3 ⇒ 항상 이 가지
L394:   let mut set_h: u64 = 0;
L394:   for a in near_allies.iter().take(5) {
L395:     set_h ^= a.id.wrapping_mul(0x9E3779B97F4A7C15);                    // Entity+0x5c0
        }
L397:   for e in near_enemies.iter().take(5) {
L398:     set_h ^= e.id.wrapping_mul(0x517CC1B727220A95);
        }
L400:   game.seed() ^ set_h.rotate_left(17) ^ champ.id                        // vtable+0x20 seed() · fshl 17 · champ+0x5c0 (xor 순서: (seed ^ rotl) ^ id)
      } else { /* ★v2 사장: version≤1 틱 버킷 시드 champ.id ^ ((tick / max(tps*2,1)) << 40) */ unreachable };
L392: let mut jrng = NoiseRng(seed);                                          // seed 그대로(믹스 없음)

// ── 오판·DPS 클로저 (L405~412) ──
L405: let misjudge = |v: i64| -> i64 {
L406:   if judge_accuracy > 999 { v } else { v * error_ratio_noise(&mut jrng, judge_accuracy) / 100 }   // sdiv 100 · 호출마다 jrng 전진
      };
L411: let dps_of = |att: &Entity, tgt: &Entity| -> i64 {
L412:   fight_dps(version, context, att, tgt)                                  // 콜리 v3: ee0800
      };

// ── 창·아군 표 (L415~425) ──
L415: let horizon_ticks: i64 = tps * 6;                                       // GameSetting+0x12f8
L416: let mut our_hp = [0i64; 5]; let mut our_dps = [0i64; 5]; let mut our_alive = [false; 5]; let mut our_n = 0usize; let mut our_cc_sum = 0i64;
L418: let mut our_arrive = [0i64; 5];
L419: for (ai, a) in near_allies.iter().copied().take(5).enumerate() {
L420:   our_arrive[ai] = arrivals.get(ai).copied().unwrap_or(0);
L421:   let tgt = near_enemies.iter().copied().min_by_key(|e| { let dx=|a.x-e.x|; let dy=|a.y-e.y|; dx*dx+dy*dy });   // 첫 원소 next 뒤 fold · 동률=먼저 것
L422:   let dps = tgt.map(|t| dps_of(a, t)).unwrap_or(0);
L423:   let ehp = a.hp + self_sustain_in_window(version, context, a, horizon_ticks);   // Entity+0x670 · 콜리 v3: ed9f40
L424:   our_cc_sum += available_cc_in_window(version, a, horizon_ticks);      // 콜리 v3: ed8db0
L425:   our_hp[our_n] = misjudge(ehp * 1000); our_dps[our_n] = misjudge(dps); our_alive[our_n] = true; our_n += 1;   // 노이즈 호출 순서: hp → dps
      }                                                                       // our_n == min(len_a, 5)

// ── 적 표 (L427~435) ──
L427: let mut their_hp = [0i64; 5]; let mut their_dps = [0i64; 5]; let mut their_alive = [false; 5]; let mut their_n = 0usize;
L428: let mut their_id = [0usize; 5]; let mut their_cc_sum = 0i64;
L430: for e in near_enemies.iter().take(5) {
L431:   let nearest_a = near_allies.iter().copied().min_by_key(|a| dist2(a, e)).unwrap_or(champ);
L432:   let ehp = e.hp + self_sustain_in_window(version, context, e, horizon_ticks);
L433:   their_cc_sum += available_cc_in_window(version, e, horizon_ticks);
L434:   their_hp[their_n] = misjudge(ehp * 1000); their_dps[their_n] = misjudge(dps_of(e, nearest_a));   // 노이즈 순서: hp → dps
L435:   their_alive[their_n] = true; their_id[their_n] = e.id; their_n += 1;
      }                                                                       // their_n == min(len_e, 5) ≥ 1
// ⇒ jrng 소비 순서(judge_accuracy ≤ 999 일 때만): 아군 i=0..our_n (hp, dps) → 적 j=0..their_n (hp, dps) · 최대 20회

// ── CC 로 유효시간 보정 (L437~443) ──
L437: if horizon_ticks != 0 {
L440:   let their_eff = max(h - min(h / 2, our_cc_sum), 1);                   // h = horizon_ticks · smin/smax(i64) · ashr 1
L441:   let our_eff   = max(h - min(h / 2, their_cc_sum), 1);
L442:   for j in 0..their_n { their_dps[j] = their_dps[j] * their_eff / h; }    // sdiv · 5칸 언롤
L443:   for i in 0..our_n   { our_dps[i]   = our_dps[i]   * our_eff   / h; }    // our_n==0 이면 통째 생략
      }

// ── 타워·소커 (L449~455) ──
L449: let tower_dps: i64 = tower.map(|t| {
L450:     (0..our_n).max_by_key(|&i| near_allies[i].stat_cached.hp)            // Entity+0x628 · 동률=뒤의 것 · our_n==0 → None
L451:       .map(|i| expected_dps(context, t, near_allies[i]))                 // 콜리 v3: ed5fd0
          }).flatten().unwrap_or(0);
L454: let soaker: Option<usize> = (0..our_n).filter(|&i| our_alive[i]).max_by_key(|&i| near_allies[i].stat_cached.hp);
L455: let soaker_id: Option<usize> = soaker.map(|i| near_allies[i].id);

// ── 틱 전개 준비 (L459~464) ──
L459: let mut t: i64 = 0; let mut our_dead_v: i64 = 0; let mut their_dead_v: i64 = 0; let mut first_focus: Option<usize> = None;
L461: let horizon = horizon_ticks;
L461: loop {
L463:   let arrived = |i: usize| -> bool { our_arrive[i] <= t };
L464:   let our_dps_sum: i64 = (0..our_n).map(|i| if our_alive[i] && arrived(i) { our_dps[i] } else { 0 }).sum();
        // → 배치 L(줄 465): their_dps_sum 등 이어짐
      }

// fight_model.rs:465~521 (배치 L)
// ── 전제(배치 K 산출) ── our_alive/our_arrive/our_dps/our_hp · their_alive/their_dps/their_hp/their_id · our_n · their_n · horizon · t · tower_dps · soaker · soaker_id
// arrived(i) := !(our_arrive[i] > t)
// our_total = Σ our_dps[i] (i<our_n, our_alive[i] && arrived(i))   (464)

// 465: their_total = (0..their_n).filter(|i| their_alive[i]).map(|i| their_dps[i]).sum()
// 466: te = (0..their_n).filter(|i| their_alive[i]).min_by_key(|i| their_hp[i])   (동률이면 앞 인덱스)
//   → 생존 적 없음 → 508 줄로 break(전멸 = 승리 정산)
// 468: ta = (0..our_n).filter(|i| our_alive[i] && arrived(i)).min_by_key(|i| our_hp[i])
//   our_n==0 경로: net = 0 - baseline, our_unit = 0 으로 곧장 515 로
//   → 도착한 생존 아군 없음 → 471 로
// 471 (ta 없음 분기): next = (0..our_n).filter(|i| our_alive[i] && our_arrive[i] > t).map(|i| our_arrive[i]).min()
//   → 후보 없음 → 508 줄로 break(net 정산)
//   → Some(next): if next < horizon { 472: t = next; continue } else → 508 로 break
// 476: if t >= horizon || (our_total == 0 && their_total == 0) → break → 508
// 477: if first_focus.is_none() { first_focus = Some(their_id[te]) }
// 480: soak: Option<usize> = soaker.filter(|&s| our_alive[s] && arrived(s))
// 481: ta_incoming = their_total + (if soak == Some(ta) { tower_dps } else { 0 })
// 484: t_te = if our_total > 0 { (their_hp[te] + our_total - 1) / our_total } else { INF }   (INF = 2305843009213693951 · ceil 나눗셈, sdiv)
// 485: t_ta = if ta_incoming > 0 { (our_hp[ta] + ta_incoming - 1) / ta_incoming } else { INF }
// 486: t_soak = if let Some(s) = soak && s != ta && tower_dps > 0 { (our_hp[s] + tower_dps - 1) / tower_dps } else { INF }
// 487: dt = max(1, min(horizon - t, min(t_soak, min(t_ta, t_te))))
// 489: their_hp[te] -= dt * our_total
// 490: our_hp[ta]   -= dt * ta_incoming
// 491: if let Some(s) = soak && s != ta { our_hp[s] -= dt * tower_dps }   (★tower_dps>0 검사 없음 — 0 이면 0 차감)
// 492: t += dt
// 493: if their_hp[te] < 1 { their_alive[te] = false; their_dead_v += their_dps[te] }   (dead_v 는 죽은 유닛의 dps 합)
// 494: if our_hp[ta] < 1 {
// 495:   our_alive[ta] = false; our_dead_v += our_dps[ta]
// 496:   if soaker == Some(ta) { soaker = (0..our_n).filter(|i| our_alive[i]).max_by_key(|i| near_allies[i].stat_cached.hp) }   (동률이면 뒤 인덱스 · 후보 없으면 None) · 콜리 v3: 재선정 fold dbcec0/dbd050/dbd1e0(구 e33570/e33700)
//      }
// 498: if let Some(s) = soak && s != ta {
// 499:   if our_hp[s] < 1 {
// 500:     our_alive[s] = false; our_dead_v += our_dps[s]
// 501:     if soaker == Some(s) { soaker = 위 496 과 동일 재선정 }
//      } }
// → 루프 머리로 back-edge · 464 로

// ── 루프 탈출 후 ──
// 508: net = their_dead_v - (our_dead_v + baseline)
// 510: our_unit = if our_n > 0 { (Σ_{i<our_n} our_dps[i]) / our_n } else { 0 }   (생존/도착 무관 전원 합)
// 515: match committed_dir {                                                                    // ★0.6.0 bias 반영(asm eead67~eeae2a)
// 516:   1  => line = if net < -our_unit { Disengage } else if net > -1 /*net>=0*/ { Commit } else { Hold }                      // 동일(bias 무관)
// 517:   -1 => { let thr = our_unit * (1 - bias as i64);                                                                        // ★0.6.0 (구 thr = our_unit)
//               line = if net > thr { Commit } else if net < 1 /*net<=0*/ { Disengage } else { Hold } }
// 518:   _  => { let thr = our_unit * (1 - min(bias as i64, 0));                                                                // ★0.6.0 (구 thr = our_unit) · bias≥0 이면 구와 동일
//               line = if net > thr { Commit } else if net < -our_unit { Disengage } else { Hold } }
//      }                                                                                        // bias == 0 이면 세 arm 모두 0.5.8 과 완전 동치(현 호출자 resolve_join_stake ee4e70 은 0 전달)
// 520: *sret = FightPrediction { focus_target: first_focus @+0/+8, soaker: soaker_id @+0x10/+0x18 (K 초기값 — 루프 중 교체분 아님), rescue_ally: None @+0x20, net_value: net @+0x30, line @+0x38, line_absolute: line @+0x39 }
// 521: ret
```

## changes
- 시그니처: judge_accuracy 뒤 `bias: i8` 신설(param 13 · [rsp+0x3b0] movsx).
- L517 committed_dir == -1: Commit 임계 `our_unit` → `our_unit * (1 - bias)`.
- L518 그 외(0): Commit 임계 `our_unit` → `our_unit * (1 - min(bias, 0))`.
- L516 committed_dir == 1: 변화 없음.
- 시드 L392: v3 ⇒ 집합 해시 경로 고정(틱 버킷 가지 사장).
- 콜리 v3: self_sustain eba0c0→ed9f40 · available_cc eb93e0→ed8db0 · expected_dps eb5dd0→ed5fd0 · fight_dps ebe220→ee0800 · 소커 재선정 fold e33570/e33700→dbcec0/dbd050/dbd1e0.

## verified
- capstone eead4b~eeae66: `cmp r8b,1` → 구 arm(neg unit · net<-unit → 2 · sets*3) / `cmp edx,0xff` → `rdx = 1 - bias; imul rax,rdx; cmp r11,rax; jg → 0` else `setle → xor 3`(net≤0 → 2 · else 3) / 그 외 `rdx = bias & (bias>>63)`(=min(bias,0)) · `rcx = (1-rdx)*unit` · `cmp r11,rcx; jle` → `neg unit; setl; xor 3` · sret +0x38/+0x39 line · +0x30 net · +0/+8/+0x10/+0x18 · +0x20 ← 0.
- Ghidra ee8450 디컴은 「Backward normalization」 류로 미시도(RE 배치 E 가 ee8450_dec.c 로 대조 완료). 시드/노이즈/표/CC/틱 전개는 RE 배치 E 「상수집합·콜리 동일」 판정 채택 — 명령 단위 전수 대조 미수행(RE 와 동일한 한계).
- bias 실제 호출값(0 이외 존재 여부) 미조사(RE 불확실 항목 그대로).

## confidence
B — 변경점(bias 임계) asm 확인 · 나머지 본문은 RE 의 상수/콜리 동치 판정에 의존(명령 단위 대조 없음).