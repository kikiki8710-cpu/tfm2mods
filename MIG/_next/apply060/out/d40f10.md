# d40f10→fb1510 evaluate_gank_opportunity_with_score

## logic_060
```
fn evaluate_gank_opportunity_with_score(rnd, player, data, line, required_score) -> (ok, evaluated, actual)   // passive_jungle.rs:693 · fb1510
  self = data.cache; team = player.info.team(+0xa00); enemy_team = 1 - team  (L702, bounds<2)   // ★0.6.0 +0x930→+0xa00
  // L701~705 적 라이너 수집
  line_enemies = self.iter_champions(enemy_team)            // player_champion[+0x1e0][enemy_team][0..5] 의 Some 만 · 콜리 v3: collect 헬퍼 e10490(구 ca31c0)
      .filter(|e| is_near_line(data.context, e.x, e.y, line)          // L703
               && data.blackboard[enemy_team](stride 0x5c8).is_recent_visible(game, player, e))  // L704 — 인덱스가 1-team 임에 주의   // ★0.6.0 bb stride 0x2e8→0x5c8 · last_seen +0x3e8
      .collect()
  if line_enemies.is_empty() → return (false, 0, 0)          // L708~709
  // L713~716 아군 라이너 수집
  line_allies = (0..5)
      .filter(|pos| data.blackboard[team].in_big_line(pos, line))   // L714: big_goal[pos](+0x2f8+pos*0x20, tag@+8) == Line{line}   // ★0.6.0 big_goal +0xf8→+0x2f8+i*0x20
      .filter_map(|pos| self.player_champion[team][pos])           // L715 · 콜리 v3: collect 헬퍼 e0ad00(구 c9e660)
      .filter(|a| a.hp(+0x670)*100 / a.stat_cached.hp(+0x628) > 40)   // L716  ※ 자기 자신 제외 없음
      .collect()
  if line_allies.is_empty() → return (false, 0, 0)           // L720
  if line_enemies.len() > line_allies.len() + 1 → return (false, 0, 0)   // L724 (정글러 합류 감안)
  jungler_champ = self.player_champion[team][player.info.position(+0xa90)] else return (false,0,0)  // L728   // ★0.6.0 +0x9c0→+0xa90
  target_enemy = line_enemies.iter().min_by_key(|e| (|e.x-j.x|)² + (|e.y-j.y|)²)  else return  // L733 (정글러에게 가장 가까운 적 · 동점=앞)
  nearest_ally = line_allies.iter().min_by_key(|a| dist²(a, target_enemy)) else return           // L738
  // L747~755 적 체력
  ehp = (target_enemy.hp*100 / target_enemy.stat_cached.hp) as i32     // stat_cached.hp==0 → unwrap 패닉(div 아님 · 게이트)
  score = if ehp > 79 { -40 } else if ehp > 59 { (80-ehp) >> 1 } else if ehp > 39 { 80-ehp } else { 90-ehp }
  // L759~772 적 타워 거리
  enemy_tower = tower(line)[enemy_team].or(tower2(line)[enemy_team])   // cache+0x180+line*0x20+enemy_team*8 / +0x190+…
  match enemy_tower {
    Some(t) => { d = utils::distance(target_enemy.xy, t.xy);           // 콜리 v3: 1660a00(구 12a07d0)
                 if d < 130000 { score -= 60 } else if d < 160000 { /* 변화 없음 */ } else if d < 200000 { score += 20 } else { score += 40 } }
    None    => score += 35 }
  // L776~777 아군 체력
  ahp = (nearest_ally.hp*100 / nearest_ally.stat_cached.hp) as i32   // stat_cached.hp==0 → unwrap 패닉
  if ahp < 50 { score -= 30 } else if ahp > 69 { score += 10 }
  // L784~785 아군↔대상 거리
  ad = utils::distance(nearest_ally.xy, target_enemy.xy)               // 콜리 v3: 1660a00
  if ad > 120000 { score -= 25 } else if ad < 80000 { score += 15 }
  // L792 수적 우위
  if line_allies.len() + 1 > line_enemies.len() { score += 20 }
  actual_score = score
  // L799~801 판단력 노이즈 — ★0.6.0 헬퍼 de2fa0(rnd, player.stat.judgement(+0x218)) 로 아웃라인(산식 동일)
  error = (1000 - player.stat.judgement) / 20            // judgement 1000 → 0, 0 → 50   (de2fa0: (1000-judge)/0x14)
  pct = 100 + rnd.gen_range(-error ..= error)             // Lemire 32-bit(폭 2*error+1 · 거절 마스크) · assert low<=high (error<0 이면 "cannot sample empty range" 패닉)
  evaluated_score = actual_score * pct / 100               // i32 sdiv (0 방향 절사)
  return (evaluated_score >= required_score, evaluated_score, actual_score)   // L803 · sret {i32 evaluated @0, bool ok @+4, i32 actual @+8}
```
// ★v2 사장: 없음(version 인자 없는 함수).

## changes
- 로직 변경 **없음**(정정): 브리핑·spec_patch §B 의 「노이즈 폭 /10 → /20」 은 **오판** — 0.5.8 `d40f10` 디컴(ghidra_beta)도 `(1000U - judge) / 0x14` 로 이미 /20 이었다(RE 가 magic `0xcccccccccccccccd >>4` 를 /10 으로 잘못 읽음 — `>>3` 이 /10, `>>4` 가 /20). 0.5.8 logic 의 `/20` 이 맞다.
- 노이즈 산식 인라인 → 헬퍼 `de2fa0(rnd, judgement) -> i64`(= 100 + gen_range(-k..=k), k=(1000-judge)/20) 아웃라인. RNG 소비 패턴(Lemire 거절 마스크·폭)은 동일 → 난수열 갈림 없음.
- 오프셋: PlayerState team +0x930→+0xa00 · position +0x9c0→+0xa90 · judgement +0x218 불변 · Blackboard stride 0x2e8→0x5c8(is_recent_visible/in_big_line 클로저 내부).
- 콜리 RVA: utils::distance 12a07d0→1660a00 · line_enemies collect ca31c0→e10490 · line_allies collect c9e660→e0ad00.

## verified
- Ghidra 0.6.0 `fb1510` 전문 디컴: 흐름·상수(0x50/0x3c/0x28 · 130000/160000/200000 · +0x23/-0x3c/+0x14/+0x28 · 0x46/0x32 · 80000/120000 · +0x14) 전부 0.5.8 logic 과 일치 · `de2fa0(rnd, [player+0x218])` 호출 · `(evaluated*pct)/100` · `setge` 반환.
- Ghidra 0.6.0 `de2fa0` 전문 디컴: `(1000U-judge)/0x14` · Lemire · `return (roll - k) + 100`.
- Ghidra 0.5.8 `d40f10`(ghidra_beta) 디컴: 인라인 노이즈 `(1000U - *(player+0x218)) / 0x14` — **/20 확인**(RE §7 의 「구 /10」 반증).
- 미확인: is_near_line / is_recent_visible 클로저 본체(수집 헬퍼 e10490/e0ad00 내부)는 미디컴(0.5.8 동치 가정 · bb 오프셋만 갱신).

## confidence
A — 본체·헬퍼 전문 디컴 대조. 유일한 「변경」 주장(/10→/20)이 반증돼 실질 동치(헬퍼 아웃라인+오프셋).
