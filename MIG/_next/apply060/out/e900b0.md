# e900b0→f3da50 AgentVerHamster::get_input

## logic_060
```
// lib.rs:0~1004 (배치 M) · 0.6.0 f3da50
// 시그니처: fn get_input(&mut self, rnd:&mut StdRng, player:&PlayerState, data:&OperationData) -> (Option<Input>, bumpalo::Vec<TurnEvent>)
// ★0.6.0 AgentVerHamster 오프셋: version +0x3608 · small_action +0x3550(태그 +0x3601 = +0xb1) · positioning_score +0x2a88 · next_input_tick +0x3638 · last_lapse +0x36c2 · plan_system +0x530(LPH: plan +0xf58 → self+0x1488 · sub_plan self+0x18d0 · pos_ring self+0x988~ · +0x5f0 → self+0xb20)
// ★0.6.0 SmallActionPlay 태그: RunAway 3 · Recall 4 · Around 5 · AroundRegion 6 · AroundRunAway 7 · Positioning 8 · AroundPosition 0/1/2 · AroundPositionBush 10 · AroundBush 11 · LaneMinionPosition 12 · Trace 13 · Attack 14 · Skill 15 · Skill2 16 · Ult 17 · Stop 18

// ★0.6.0 신규 prelude(브리핑 외 · 디컴 발견): v3 위치 링 갱신 — 입력 지연 게이트보다 먼저, 매 호출 실행
let game = data.cache.game;
if self.version(+0x3608) >= 3 && game.get_game_mode(vt+0x40).tag == 0 /*Moba*/ {
  let tick = game.tick();
  if let Some(champ) = data.cache.player_champion[player.team(+0xa00)][player.position(+0xa90)] {
    let ring = &mut self.plan_system.pos_ring;     // LPH+0x458 VecDeque<(tick,x,y)> {cap +0x988, ptr +0x990, head +0x998, len +0x9a0} (self 기준)
    if !(ring.len != 0 && ring.back().tick == tick) {   // 같은 틱 중복 push 방지
      ring.push_back((tick, champ.x, champ.y))        // len==cap → grow(ff4380)
    }
    while ring.len != 0 && tick.saturating_sub(ring.front().tick) > tps + 1 { ring.pop_front() }   // tps+1 틱 창 유지
  }
}

// ── L844 입력 지연 게이트
let tick = game.tick();                          // vtable+0x28
if tick < self.next_input_tick {                 // self+0x3638 · ★0.6.0 +0x2940→+0x3638
  // L848  조기반환: (None, Vec::new_in(data.context.pool))
  sret+0 = -1(i64) ; sret+32 = 8(dangling ptr) ; sret+40 = context.pool ; sret+48..64 = 0 (cap,len)
  return;
}
// L851  입력 지연 롤 — 유일의 rnd 직접 소비 사이트
let a = player.+0x200 (AthleteParameter 값 · 0.6.0 디컴: input_delay 인라인)
let delay = rnd.gen_range((400 + 3*(100 - min(a,100))) ..= (1200 - 4*min(a,100)))[dcad10] / 100;   // RangeInclusive<usize> · udiv 100 (0.5.8 `input_delay_min()..=input_delay_max()` 의 인라인 형태 · 0.5.8 오라클 700..=1200 = a==0 과 정합)
// L852
self.next_input_tick = game.tick() + delay;      // store self+0x3638
// L854
let mut turn_event: bumpalo::Vec<TurnEvent> = self.update_state(rnd, player, data);   // sret 32B · 콜리 v3: e8d6f0→f3adb0(변경 · spec_patch §B)
// L855
let _t_sai = ProfTimer::new(26);                 // prof::ENABLED(atomic i8)==0 → None / 아니면 Instant::now, {26, secs, nanos}
// L856  1차 소액션 입력
let mut input: Option<Input> = self.small_action.get_input(self.version, rnd, player, data, &self.positioning_score, &mut self.debug);
//        인자: (&mut self+0x3550, self+0x3608 load, rnd, player, data, &self+0x2a88, &mut self+0x0) · sret 32B · 콜리 v3: e5e0e0

// ── L865  version>=2 이고 입력이 없으면 소액션 갱신 후 재시도 (v3 상시)
if self.version > 1 && input.is_none() {         // is_none = tag == -1
  // L869
  self.update_small_action(rnd, player, data);   // 콜리 v3: f3bde0
  // L870
  input = self.small_action.get_input(self.version, rnd, player, data, &self.positioning_score, &mut self.debug);   // 2차
  // L876  lapse(인지 공백) 중 무입력 → 플랜 폴백
  if self.version > 1 && input.is_none() && self.last_lapse {   // self+0x36c2 bool · ★0.6.0 +0x29ca→+0x36c2
    // L877
    self.plan_system.v3_lapse_noinput_rescues += 1;   // self+0x2798 · ★0.6.0 +0x1b60→+0x2798
    // L878
    self.plan_system.v3_fall_back_to_passive(self.version, rnd, player, data, &mut self.debug);   // &mut plan_system(self+0x530) · 콜리 v3: d582a0
    // L879
    self.update_small_action(rnd, player, data);
    // L880
    input = self.small_action.get_input(...);   // 3차
    // L888  그래도 없으면 도주 강제 검토
    if input.is_none() {
      // L889
      let team = player.info.team;               // player+0xa00 · bounds 2
      if let Some(champ) = data.cache.player_champion[team][player.info.position() as usize] {   // cache+0x1e0 [2][5] · position = player+0xa90 i32
        // L890
        let (flx, fly, frx, fry) = data.context.map.fountains[team];   // MapDef+0x6d70 + team*32
        // L895~897  분수 안이고 (v1 이거나 아직 풀피 아님) 이면 강제 안 함
        let in_rect = flx <= champ.x && champ.x <= frx && fly <= champ.y && champ.y <= fry;   // Entity+0x660/+0x668
        let force = if in_rect { let in_fountain = champ.hp(+0x670) < champ.stat_cached.hp(+0x628); !in_fountain } else { true };   // ★v2 사장: version<=1 → false
        if force {
          // L898
          self.plan_system.v3_lapse_move_forces += 1;   // self+0x27a0 · ★0.6.0 +0x1b68→+0x27a0
          // L899
          self.small_action = SmallActionPlay::RunAway(SmallActionRunAway::new(data, player, 5));   // drop_in_place(old) → 필드 기록(+0x3550.. · end_delay 5 · path_finder None 태그 +0x35cd=2 · +0x35d0=1) → tag@+0x3601 = 3
          // L900
          input = self.small_action.get_input(...);   // 4차
        }
      }
    }
  }
}
// ★0.6.0 신규(v3 폴백 · 위치 = 재시도 체인 전체가 끝난 합류점, L906 직전): 도주계 소액션이 끝내 None 이면 우물 방향 Move 강제
if self.version >= 3 && input.is_none() && game.get_game_mode(vt+0x40).tag == 0 /*Moba*/ {
  let idx = if self.small_action.tag(+0x3601) >= 3 { tag - 3 } else { 6 };
  if idx <= 4 && (0x13 >> idx) & 1 != 0 {            // idx ∈ {0 RunAway, 1 Recall, 4 AroundRunAway}
    if let Some(champ) = data.cache.player_champion[team][position] {
      let in_fountain = fountains[team] rect 안(champ.x/y · MapDef+0x6d70+team*32)
      let near = data.cache.player_champion[1-team].iter().flatten().any(|e|
          (game.is_visible(vt+0xf8)(team, e.id) || game.get_player(vt+0x150)(e.id).map_or(false, |p| blackboard[1-team](stride 0x5c8).last_seen(+0x3e8)[p.position]+120 >= game.tick()))
          && dist²(e, champ) < 200000² + 1 /*0x9502f9001*/);
      if near && !in_fountain && (champ.ty(+0x68) != Champion(13) || champ.action_state.tag(+0x70) != 1) {   // action_state 1 = 캐스팅(RE 추정)
        input = Some(Input::Move{ x: team==0 ? 32000 : 928000, y: team==0 ? 928000 : 32000 });   // tag 0 · healp
      }
    }
  }
}
// L906
drop(_t_sai);   // Option<ProfTimer>: phase(+16) != -1 이면 PHASE_NANOS[phase] += elapsed_ns · PHASE_CALLS[phase] += 1 (bounds 132 · atomicrmw)

// ── L909  전투 액션이면 마지막 전투 틱 갱신
let sa_tag = self.small_action.tag();            // self+0x3601 i8
if (14..=17).contains(&sa_tag) {                 // Attack/Skill/Skill2/Ult · ★0.6.0 15..=18→14..=17
  // L911
  self.last_combat_tick = game.tick();           // self+0x3698 · ★0.6.0 +0x29a0→+0x3698
}

// ── L914  「집(분수)에서 풀피로 서서 안 사는」 진단 (stay_home_full_nobuy)
let team = player.info.team;                     // bounds 2
let champ_opt = data.cache.player_champion[team][player.info.position() as usize];
if let Some(champ) = champ_opt {
  // L915  입력이 제자리인가
  let stays = match input {
    Some(Input::Move{x, y}) => {                 // tag 0 · x@+8 y@+16
      // L917~919
      let (ax, ay) = Game::adjust_position(data.context.map, data.context.setting, x, y);   // 콜리 v3: ec2660 · context+0x20, +0x8
      dist2(champ.pos, (ax, ay)) < 4000001       // 2000² +1
    }
    None => true,                                // tag -1 → 바로 L923
    _ => sa_tag == 18,                           // L921  Return/Attack/Skill*/Ult 는 small_action==Stop 일 때만 · ★0.6.0 19→18
  };
  // L923
  if stays && !(champ.hp < champ.stat_cached.hp) {   // 풀피
    // L924~925
    let (flx, fly, frx, fry) = data.context.map.fountains[team];
    if flx <= champ.x && champ.x <= frx && fly <= champ.y && champ.y <= fry {
      // L926~927  구매 가능성 — rnd 는 clone 으로 전달(원본 스트림 비소비)
      let can_buy = buy_item(self.version, &mut rnd.clone(), player, game, data.context).is_some()   // 콜리 v3: e7b640→f27ea0(변경 · 활성템 >3 게이트 · spec_patch §B)
                 || upgrade_item(self.version, &mut rnd.clone(), player, game, data.context).is_some();   // 콜리 v3: f27140
      // L928
      if !can_buy {
        // L929
        self.stay_home_full_nobuy += 1;          // self+0x36a0 · ★0.6.0 +0x29a8→+0x36a0
        // L930
        self.stay_home_full_nobuy_plan[BigPlan::freeze_plan_cls(&self.plan_system.plan)] += 1;   // self+0x4d0 [usize;6] · plan = self+0x1488 · ★0.6.0 +0xb18→+0x1488 · cls 콜리 f29410
        // L932  (앞에 rnd.clone() 1회 실행되나 결과 미사용)
        let rd: bool = self.plan_system.v3_repair_done(self.version, player, data);   // 실제 인자 (version, team, position i32, cache)
        // L933~935  적 위협 수 (closure#1 · count)
        let threat: usize = data.cache.player_champion[1 - team].iter_champions()
            .filter(|e| data.blackboard[1 - team].is_recent_visible(game, player, e)   // ★적팀 bb · stride 0x5c8 · last_seen +0x3e8
                     && dist2(e.pos, champ.pos) < 62500000001)   // 250000²+1
            .count();
        // L936~939  키 문자열
        let cut = |s: String| s.chars().take_while(|c| *c != ' ' && *c != '(').collect::<String>();   // closure#0 · 마스크 (c & 0x1FFFF7)==32
        let key = format!("rd={} lapse={} 적{} | {} | {} | {}", rd, self.last_lapse, threat,
                          self.plan_system.plan.debug_label(),                      // L937 String
                          cut(format!("{:?}", self.plan_system.sub_plan)),         // L938 self+0x18d0 · SubPlan::fmt(f4f6e0) · ★0.6.0 +0xc98→+0x18d0
                          cut(format!("{:?}", self.small_action.get_action())));   // L939 SmallAction::fmt
        // L940
        *self.stay_home_full_nobuy_stack.entry(key).or_insert(0) += 1;   // self+0x2a0 · rustc_entry
      }
    }
  }
}

// ── L948  데스매치 idle 입력 통계 (관측 전용 전역)
if game.get_game_mode().tag == 2 /*DeathMatch*/ {   // vtable+0x40 · {i64,ptr}
  // L949
  if let BigPlan::DeathMatchBattle(b) = &self.plan_system.plan {   // ★0.6.0 plan.tag(self+0x1488 dword) == 6 (idx 4 + 2 · 구 `tag<2` 니치)
    // L950
    if b.idle_spec_tick == game.tick() {         // self+0x15d0 · ★0.6.0 +0xc58→+0x15d0
      // L952/961
      CNT_DM_IDLE_INPUT[input.tag + 1].fetch_add(1);   // [7] · None→0 Move→1 … Ult→6 · 0.6.0 @145029ea8
    }
  }
}
// ★0.6.0 신규(브리핑 외 · 디컴 발견): 공격 입력 틱 기록
if input.tag >= 2 /*Attack|Skill|Skill2|Ult*/ { self.plan_system.+0x5f0 = game.tick(); }   // self+0xb20 · 필드명 미상(last_attack_input_tick 추정) · 0.5.8 e900b0 에 없음(대조 확인)

// ── L967  입력 기회 카운터
self.input_chances += 1;                         // self+0x3650 · ★0.6.0 +0x2958→+0x3650
// L968
if self.last_lapse { self.lapse_chances += 1; }  // self+0x3690 · ★0.6.0 +0x2998→+0x3690
// L969
let target_dead = self.small_action.is_premise_lost(data);   // bool
// L970
if input.is_none() {
  // L971
  self.noinput_bucket[((self.last_lapse as usize) << 1) | target_dead as usize] += 1;   // self+0x410 [4]
  // L972
  if self.last_lapse {
    // L973
    self.noinput_lapse_action[cls(sa_tag)] += 1;   // self+0x500 [6] · cls = {Trace:0, RunAway|Recall|AroundRunAway:1, Around|AroundRegion|Positioning|AroundPosition|AroundPositionBush|AroundBush:2, Attack|Skill|Skill2|Ult:3, LaneMinionPosition:4, Stop:5}   // ★0.6.0 AroundHide 제거
    // L974
    if let Some(champ) = champ_opt {
      // L975~977
      let (flx, fly, frx, fry) = data.context.map.fountains[team];
      let band = if in_rect(champ) { 0 }
                 else { match data.cache.nexus[team] {   // cache+0x170 [2] · closure#2
                          Some(n) => if dist2(champ.pos, n.pos) < 67600000001 { 1 } else { 2 },   // 260000²+1
                          None => 2 } };
      self.noinput_lapse_pos[band] += 1;         // self+0x36a8 [3] · ★0.6.0 +0x29b0→+0x36a8
    }
  }
}

// ── L983  StayEvent 기록 (디버그 컨텍스트 전용)
if champ_opt.is_some() && data.context.debug {  // context+0x3b bool
  let champ = champ_opt.unwrap();                // L984 재로드
  // L985~993
  let (stays2, kind): (bool, u8) = match input {
    Some(Input::Move{x,y}) => { let (ax,ay) = Game::adjust_position(map, setting, x, y);   // L988~989
                                let s = dist2(champ.pos,(ax,ay)) < 4000001; (s, if s {1} else {2}) }   // L990
    None => (true, 0),                           // kind 0
    _ => (false, 2),
  };
  // L993~994
  if stays2 || sa_tag == 18 /*Stop*/ {           // ★0.6.0 19→18
    // L995~996
    let (flx, fly, frx, fry) = data.context.map.fountains[team];
    let (can_buy, pos_band): (u8, u8) = if in_rect(champ) {
      // L1002~1003  분수 안: 구매 가능성(둘 다 rnd.clone())
      let cb = if buy_item(version, &mut rnd.clone(), player, game, ctx).is_some() { 1 } else { upgrade_item(version, &mut rnd.clone(), player, game, ctx).tag as u8 };
      (cb, 0)
    } else {
      // L998~999
      (0, match data.cache.nexus[team] { Some(n) => if dist2(champ.pos,n.pos) < 67600000001 {1} else {2}, None => 2 })
    };
    // L1004  push (필드값은 배치 N 이 1005~1043 에서 계산)
    self.stay_events.push(StayEvent{ tick, team, pos:(champ.x, champ.y), position: player.info.position(), kind, pos_band, can_buy: can_buy!=0, /* + 배치 N 값들 */ });
    //   → 208B 조립 후 len==cap 이면 grow_one(self+0x2a70) → memcpy(ptr(+0x2a78)+len*208) → len+1 (self+0x2a80) · ★0.6.0 +0x1d78/+0x1d88→+0x2a70/+0x2a80
  }
}
// 이후(배치 N): L1048~1075 freeze 감시 · L1080 trace_escape_* · L1082 sret = (input, turn_event) · L1083 ret

// lib.rs:1005~1083 (배치 N)
// ─── [A] StayEvent 필드 (루트 1005~1043 · 값만 내 범위, push 는 배치 M L1004)
tick     = game.tick()                                   // L1005
plan_label  = self.plan_system.plan.debug_label()        // L1009 · String · (&BigPlan) -> String
plan_detail = self.plan_system.plan.get_name()           // L1010
sub_plan = short(format!("{:?}", self.plan_system.sub_plan))   // L1011 · closure#5(lib.rs:1000) 인라인: short(s) = s[..idx] where idx = 첫 c==' ' || c=='(' 의 바이트 오프셋(없으면 s.len()) · self+0x18d0
action   = format!("{:?}", self.small_action.get_action())       // L1012 · get_action(small_action.rs:308 인라인) 태그별(★0.6.0): RunAway(3)/Recall(4)/AroundRunAway(7) → SmallAction::RunAway(0) · Around(5)/LaneMinionPosition(12) → Around(2){target=+0x8} · AroundRegion(6) → AroundPosition(3){+0x10,+0x18} · Positioning(8) → Positioning(1){+0x8,+0x10} · AroundPosition(0/1/2) → AroundPosition(3){+0x30,+0x38} · AroundPositionBush(10) → AroundPosition(3){+0x8,+0x10} · AroundBush(11) → AroundPosition(3){+0x18,+0x20} · Trace(13) → Trace(4){+0x60} · Attack(14)→Attack(6){+0x8} · Skill(15)→Skill(7) · Skill2(16)→Skill2(8) · Ult(17)→Ult(9) · Stop(18)→Stop(10)
goal     = self.small_action.target_position()          // L1013 · small_action.rs:229 인라인 → Option<(u64,u64)>: RunAway/Positioning/AroundPosition/AroundPositionBush → (+0x8,+0x10) · Recall → (+0x50,+0x58) · Around/LaneMinionPosition → (+0x10,+0x18) · AroundBush → (+0x18,+0x20) · Trace → (+0x68,+0x70) · AroundRegion/AroundRunAway/Attack/Skill/Skill2/Ult/Stop → None
pos      = (champ.x, champ.y)                                        // L996/L1014
hp_pct   = champ.hp * 100 / max(champ.stat_cached.hp, 1)          // L1016
enemy_row = data.cache.player_champion[1 - team]                    // L1017
bb       = &data.blackboard[1 - team]                              // L1018 · ★인덱스 = 적팀 · stride 0x5c8
near_enemy = enemy_row.iter().filter(|e| e.is_some() && bb.is_recent_visible(game, player, e) && dist²(e, champ) < 150000²+1).count()   // L1019 · 5회 루프
near_ally  = ally_row.iter().filter(|e| e.is_some() && e.id != champ.id && dist²(e, champ) < 150000²+1).count()   // L1021 · 5회 언롤 · 가시성 검사 없음 · 자기 자신 제외
in_lapse = self.last_lapse                                          // L1023 · +0x36c2
since_combat = if self.last_combat_tick == usize::MAX { usize::MAX } else { game.tick().saturating_sub(self.last_combat_tick) }   // L1024~1025 · +0x3698
target_kind: &str = match self.small_action.get_action() {           // L1026~1040
   Around(id)|Trace(id)|Attack(id)|Skill(id)|Skill2(id)|Ult(id) => {              // Trace → +0x60, 나머지 → +0x8
      match game.get_entity_by_id(id) {                                            // vtable+0x1f0
         None => "소멸",
         Some(e) => match e.ty /*+0x68*/ {
            Champion(13) => "챔피언" · Epic(5) => "에픽" · Serpen(6) => "세르펜"
            Jungle(4) if e.ty.info.camp_type.0 /*+0x98*/ < 2 => "정글몹"
            Tower(2) => "타워" · Nexus(3) => "넥서스"
            _ (Minion/Ghoul/…/Jungle camp_type.0>=2) => "미니언"
         } } }
   _ => ""
}
// → StayEvent 208B 조립·push (배치 M L1004) → L994 로 복귀 후 L1048

// ─── [B] 정지(freeze) 에피소드 계측 (루트 1048~1075) · ★0.6.0 오프셋 = 디스어셈 귀속
if let Some(champ) = data.cache.player_champion[team][position] {
   tick  = game.tick()                                                          // L1049
   moved = utils::distance(champ.x, champ.y, self.freeze_anchor.0, self.freeze_anchor.1) > 8000   // L1050 · anchor = self+0x430/+0x438 · (`>= 0x1f41`)
   if moved || input.is_some() || self.freeze_since == 0 {                        // L1051 · freeze_since = self+0x3658
      self.freeze_since = tick; self.freeze_anchor = (champ.x, champ.y); self.freeze_fired = false   // L1052~1054 · freeze_fired = self+0x36c3
   } else if self.freeze_fired {                                                 // L1055
      self.freeze_ticks += 1                                                     // L1056 · self+0x3670
   } else if tick.saturating_sub(self.freeze_since) >= data.context.setting.tick_per_second {   // L1057 · 1초 게이트
      self.freeze_fired = true                                                   // L1058
      cls = small_action_cls(&self.small_action)                                 // L1059 · lib.rs:481 인라인: Trace→0 · RunAway/Recall/AroundRunAway→1 · Around/AroundRegion/Positioning/AroundPosition/AroundPositionBush/AroundBush→2 · Attack/Skill/Skill2/Ult→3 · LaneMinionPosition→4 · Stop→5   // ★0.6.0 AroundHide 제거
      self.freeze_eps[cls] += 1                                                  // L1060 · self+0x440[6]
      plan_cls = self.plan_system.plan.freeze_plan_cls()                         // L1061 · types.rs:121 인라인(BigPlan idx 기준 · 16 불변): SinglePlanBattle(idx3)/DeathMatchBattle(4)/Battle(7)→0 · PassiveLine(1)/SinglePlanLine(2)→1 · PassiveJungle(5)→2 · ForcePassive(0)/ActiveRecall(6)→3 · LineGanker(8)/LineGankCover(9)→4 · 그 외(idx10~15)→5 · ★0.6.0 태그 인코딩: Battle(idx7) 니치 0/1 · 나머지 idx+2 · 콜리 f29410
      self.freeze_plan[plan_cls] += 1                                            // L1061 · self+0x470[6]
      (flx, fly, frx, fry) = data.context.map.fountains[team]                    // L1063 · MapDef+0x6d70 + team*32
      pos_cls = if 우물 rect 안 { 0 }                                             // L1064
                else { match data.cache.nexus[team] { None => 2, Some(n) => if dist²(champ, n) > 260000² { 2 } else { 1 } } }   // L1066~1067
      self.freeze_pos[pos_cls] += 1                                              // L1072 · [+0x3678, +0x3680, +0x3688]
      if pos_cls == 2 { self.freeze_field_action[cls] += 1 }                     // L1073 · self+0x4a0[6] (넥서스 None 경로도 포함)
      if self.last_lapse { self.freeze_eps_lapse += 1 }                          // L1074 · self+0x3660
      if premise_lost { self.freeze_eps_dead_target += 1 }                       // L1075 · self+0x3668
   }
}

// ─── [C] self.update_trace_escape_abandon(data) (L1080 · lib.rs:1088~1108 인라인)
tick = data.cache.game.tick()                                                    // L1089
abandon_threshold = data.context.setting.tick_per_second * 2                     // L1090
if let Trace(t) = &mut self.small_action /*tag(+0x3601)==13 · L1091*/ {          // ★0.6.0 14→13
   tid = t.target /*+0x60 (self+0x35b0) · L1092*/
   if t.last_escape == Some(true) /*+0x95 & 1 (self+0x35e5) · L1093 · 0.5.8 명세의 +0x8d 는 오기(0.5.8 도 +0x95)*/ {
      if self.trace_escape_target(+0x3620) == tid && tick >= self.trace_escape_last_tick(+0x3630) {            // L1094
         self.trace_escape_ticks(+0x3628) = (tick - self.trace_escape_last_tick) + self.trace_escape_ticks   // L1095
      } else { self.trace_escape_target = tid; self.trace_escape_ticks = 0 }                  // L1097
      self.trace_escape_last_tick = tick                                                     // L1100
      if !(self.trace_escape_ticks < abandon_threshold) && !t.abandoned { t.abandoned = true }   // L1101~1102 · +0x92 (self+0x35e2) ← 1
   } else if self.trace_escape_target == tid {                                               // L1104
      dec = tick.saturating_sub(self.trace_escape_last_tick)                                 // L1106
      self.trace_escape_ticks = self.trace_escape_ticks.saturating_sub(dec)                  // L1107
      self.trace_escape_last_tick = tick                                                     // L1108
   }
}
// ─── [D] 반환 (L1082~1083)
ret.1(+32..+64) = turn_event ; ret.0(+0..+32) = input ; ret void
// 언와인드: drop Option<ProfTimer> → drop bumpalo Vec<TurnEvent> · 문자열은 각 cleanuppad 에서 drop
```
// ★v2 사장: L865 `version>1` 게이트·L895 `version>1` 풀피 판정은 v3 에서 항상 참 가지.

## changes
- (신규·브리핑 외) 함수 선두 v3 prelude: Moba 이면 pos_ring(LPH+0x458 = self+0x988~+0x9a0 VecDeque<(tick,x,y)>) 에 같은 틱이 아니면 push_back((tick,x,y)), `tick - front.tick > tps+1` 이면 pop_front — 입력 지연 게이트보다 먼저 실행(r22 a RE 「매 틱 push · tps+1 창」 과 정합 · 세팅처가 이 함수임을 확인).
- L851 입력 지연 범위가 `(400+3*(100-min(a,100))) ..= (1200-4*min(a,100))`, a = player+0x200 으로 인라인(0.5.8 AthleteParameter 메서드의 인라인 형태로 추정 · 로직 변경 아님).
- (신규·v3 폴백) L903 뒤(합류점, L906 직전): input None && small_action ∈ {RunAway,Recall,AroundRunAway} && Moba && !우물 && 최근가시 적 200000 내 && (champ.ty!=13 || action_state.tag!=1) → input = Some(Move(healp)). RE §12 의 「1차 get_input 직후」 는 위치 오기 — 실제는 재시도 체인(L865~L903) 전부 지난 뒤.
- (신규·브리핑 외) L961 뒤: `input.tag >= 2`(Attack~Ult) 이면 `LPH+0x5f0(self+0xb20) = game.tick()` — 0.5.8 e900b0 에 없음(디스어셈 대조) · 필드명 미상.
- 태그 재번호: L909 15..=18→14..=17 · L921/L994 Stop 19→18 · L1091 Trace 14→13 · get_action/target_position/cls 표에서 AroundHide 제거 · DeathMatchBattle 판정 `tag<2`→`tag==6`.
- 오프셋: version +0x3608 · small_action +0x3550(태그 +0x3601) · positioning_score +0x2a88 · next_input_tick +0x3638 · last_lapse +0x36c2 · v3_lapse_noinput_rescues +0x2798 · v3_lapse_move_forces +0x27a0 · last_combat_tick +0x3698 · stay_home_full_nobuy +0x36a0(+plan +0x4d0 · stack +0x2a0) · plan +0x1488 · idle_spec_tick +0x15d0 · sub_plan +0x18d0 · input_chances +0x3650 · lapse_chances +0x3690 · noinput_bucket +0x410 · noinput_lapse_action +0x500 · noinput_lapse_pos +0x36a8 · stay_events +0x2a70/+0x2a78/+0x2a80 · freeze_anchor +0x430/+0x438 · freeze_since +0x3658 · freeze_fired +0x36c3 · freeze_ticks +0x3670 · freeze_eps +0x440 · freeze_plan +0x470 · freeze_pos +0x3678/+0x3680/+0x3688 · freeze_field_action +0x4a0 · freeze_eps_lapse +0x3660 · freeze_eps_dead_target +0x3668 · trace_escape_target/ticks/last_tick +0x3620/+0x3628/+0x3630.
- 콜리 v3: update_state f3adb0(변경) · small_action.get_input e5e0e0 · update_small_action f3bde0 · v3_fall_back_to_passive d582a0 · buy_item f27ea0(변경) · upgrade_item f27140 · adjust_position ec2660 · freeze_plan_cls f29410 · gen_range dcad10.

## verified
- capstone f3da50 전체 선형 디스어셈(140f3da50~140f3fed8): prelude(version>=3 · vt+0x40 · +0x988/+0x990/+0x998/+0x9a0 · back.tick==tick 스킵 · tps+1 pop 루프) · 지연 게이트(+0x3638 · +0x200 산식 · dcad10 · /100) · f3adb0 · e5e0e0(rdx=+0x3550 · r8=+0x3608 · +0x30=+0x2a88 · +0x38=self) · 재시도 체인(+0x36c2 · +0x2798 · d582a0 · +0x27a0 · RunAway 필드 기록 +0x3550..+0x35d0) · v3 폴백(140f3e14c: version>=3 · tag -1 · vt+0x40==0 → 140f3e777: idx<=4 · BT 0x13 · 우물 rect · vt+0xf8/+0x150/+0x3e8 · 0x9502f9001 · ty 13/+0x70!=1 · Move(0x7d00/0xe2900)) · L909(+0x3601 · +0x3698) · DM 블록(+0x1488==6 · +0x15d0 · @145029ea8) · +0xb20←tick(tag-2 unsigned 비교 → tag>=2) · 카운터 +0x3650/+0x3690/+0x410/+0x500/+0x36a8/+0x36a0/+0x4d0/+0x2a0 · stay_events +0x2a70/78/80 · freeze 블록(+0x430/+0x438 · 0x1f41 · +0x3658 · +0x36c3 · +0x3670 · +0x440 · +0x470 · +0x3678/80/88 · +0x4a0 · +0x3660 · +0x3668) · trace_escape(tag 0xd · +0x35b0 · +0x35e5&1 · +0x3620/28/30 · +0x35e2).
- 0.5.8 e900b0 capstone 대조: `+0xb20←tick` 상당 쓰기 없음(DM 블록 직후 곧장 input_chances++) · Trace last_escape 는 0.5.8 도 +0x95(0x28ed-0x2858) → 명세 +0x8d 오기 정정.
- 미확인: +0x5f0 필드명·독자 · action_state tag 1 의 의미(캐스팅 = RE 추정) · +0x200 파라미터 이름 · StayEvent 필드 순서(배치 N 본체 미대조 · RE 「동일」 인용).

## confidence
A(v3 폴백·prelude·+0x5f0·오프셋 표) / B(StayEvent·freeze 세부 = 0.5.8 명세 + 오프셋 귀속) — 신규 2건(prelude·+0x5f0)은 브리핑·RE 에 없던 발견이라 상위 문서 정정 필요.
