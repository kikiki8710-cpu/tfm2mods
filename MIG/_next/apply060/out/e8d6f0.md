# e8d6f0→f3adb0 AgentVerHamster::update_state

## logic_060
```
fn update_state(&mut self, rnd, player, data) -> Vec<TurnEvent>   // lib.rs:556~610 · 0.6.0 f3adb0 · sret {cap 8, bump=ctx.pool, ptr 0, len 0}
  _t_ev = ProfTimer::start(23)                                  // L558 (ENABLED 원자 145029209 이 0 이면 None · 0.6.0 정적 주소)
  self.update_event(rnd, player, data.cache, data.context)      // L559 (콜리 f3a420 ★0.6.0 RVA · 명세 밖)
  drop(_t_ev)                                                   // L560 → PHASE_NANOS[23](1450292c8)+=ns, PHASE_CALLS[23](1450296e8)+=1
  _t_plan = ProfTimer::start(24)                                // L562
  lapse = utils::player_awareness_lapse(player, data)           // L566 — 0.6.0 인라인:
      //   if player.+0x49c /*★0.6.0(구 +0x464) · 인지공백 활성 바이트*/ != 1 { false }
      //   else { m = 1000 + (min(player.+0x490 /*★0.6.0 신규 u64 스탯(이름 미확정)*/, 100)² * 0xccd >> 16) /*≈ 1000 + x²/20*/ & 0xffff;
      //          r = 17d68b0(game.seed(vt+0x20), player.+0x9f8 /*★0.6.0(구 +0x928)*/, tick(vt+0x28), tps, min(player.+0x208,100), min(player.+0x210,100), player.+0x478 /*★0.6.0(구 +0x448)*/, m /*★0.6.0 8번째 인자 신설*/);   // 콜리 v3: 17d68b0 — 내부 elapsed=sat_sub(tick,tps*210) → 구 `elapsed*6000/(tps*810)` 가 `((elapsed*max(m,1))/1000)*6000/(tps*810)` 로(min 6750 캡 등 나머지 동일)
      //          r.0 | r.1 }   // (bool,bool) 반환을 OR — 0.5.8 도 `or al,dl` 동일
  self.last_lapse = lapse                                       // +0x36c2 ★0.6.0(구 +0x29ca) 쓰기
  // ★v2 사장: `if self.version > 1`(+0x3608 ★0.6.0(구 +0x2910)) 상시 참 · v≤1 경로(lapse 면 update 생략 / update_plan(false)) 는 사장
  self.v3_recent_flag = sat_sub(tick, self.+0x3698 /*틱 · 이름 미확정*/) <= tps*2     // ★0.6.0 신설 · agent+0x29dd(bool) 기록 · 소비처 = LPH::update(d606b0) 카운터 +0x1f98 증분(통계)
  res = self.update_plan_lapse(rnd, player, data, lapse)        // L570 → lib.rs:689 인라인: plan_system(agent+0x530).update(version, rnd, player, data, &mut self.debug(agent), lapse); Vec::new_in(pool)   // 콜리 v3: d5a940 ★0.6.0 RVA(구 e4c5c0 · 7인자 동일)
  d52c00(plan_system, player, data, agent)                      // ★0.6.0 신설 콜 — `[TRAIT_AUD] gank_linger_*/join_came_to_me/gank_hit_before_reach` ChampTrait 감사(11KB) · LPH 필드 다수 기록(+0xea8..+0xf42 · +0x11c0/+0x11d8 · +0x20a0..+0x20b0 · +0x23d0 · +0x24a8/+0x24a9 · +0x24ea/+0x24eb) · 판단 재소비 여부 미확정 → 별도 명세 대상
  drop(_t_plan)                                                 // L576 → [24](1450292d0/1450296f0)
  _t_sa = ProfTimer::start(25)                                  // L577
  { _t_sus = ProfTimer::start(28)                               // L579
    self.small_action.update_state(rnd, player, data, &mut self.debug)   // L580 · small_action = agent+0x3550 ★0.6.0(구 +0x2858) · 0.6.0 인라인: 태그(+0x3601)==0xb(AroundBush · idx 8) 일 때만 ecfdd0(small_action, rnd, player, data, agent) 호출, 그 외 no-op
    drop(_t_sus) }                                              // L581 → [28](1450292f0/145029710)
  need_new = self.small_action.is_premise_lost(data)            // L589 — v3 상시 평가 · 0.6.0 인라인(★태그 재번호):
      //   idx = tag(+0x3601) < 3 ? 6 /*AroundPosition 니치 0/1/2*/ : tag-3
      //   if idx ∈ {0,1,3,4,5,6,7,8,15} /*마스크 0x81fb ★0.6.0(구 0x103f3 · AroundHide 비트 제거)*/ → false
      //   else id = idx ∈ {2,9,11,12,13,14} /*마스크 0x7a04 ★0.6.0(구 0xf40c)*/ ? payload+0x8 : payload+0x60 /*Trace(idx 10) 등*/ ; game.get_entity_by_id(id)(vt+0x1f0) == None → true(전제 상실)
             || self.small_action.is_end(rnd, self.version, player, data)   // L590 · 콜리 e5da70 ★0.6.0 RVA — premise_lost 가 true 면 is_end 미호출
  if need_new:
    // premise_lost 경로는 can_skip_eval 을 건너뛰고 바로 재평가. is_end 경로만 아래 검사
    if premise_lost || !self.can_skip_eval(player, data):        // L591 (lib.rs:796~824 인라인)
      _t_usa = ProfTimer::start(29)                              // L594
      self.update_small_action(rnd, player, data)                // L595 · 콜리 f3bde0 ★0.6.0 RVA(별도 명세)
      drop(_t_usa)                                               // L596 → [29](1450292f8/145029718)
    else:
      self.small_action.extend_action(game.tick())               // L592 → small_action.rs:445: idx(위 규칙)별 start_tick = tick — idx 0·2~9 → +0x0(agent+0x3550) · idx 1(Recall) → +0x48(+0x3598) · idx 10(Trace) → +0x58(+0x35a8) · idx ≥11(캐스트/Stop) 무시
  drop(_t_sa)                                                   // L598 → [25](1450292d8/1450296f8)
  if data.context.debug {                                       // L600 (ctx+0x3b)
    self.debug.merge(self.big_debug.clone())                    // L601 (agent+0xe0 · f53870 clone → 13ef9a0 merge → 33c2c0 drop)
    self.debug.merge(self.small_debug.clone())                  // L602 (agent+0x1c0)
    if let Some(pf) = self.small_action.path_finder() {         // L604 (idx 별 Option<PathFinder> 슬롯: 0/2/4 → +0x35cd 태그·+0x3588 / 1 → +0x3595 / 3·9 → +0x35c5·+0x3580 / 5·8 → +0x35bd·+0x3578 / 6 → +0x35fd·+0x35b8 / 7 → +0x35ad·+0x3568 / 10 → +0x35a5·+0x3560 · 태그 2 = None)
      pf.draw_debug(player, data, &mut self.debug) } }          // L605 · e5b900
  return res                                                    // L609 (빈 Vec)

---- can_skip_eval(&self, player, data) -> bool  (lib.rs:796~824 · 인라인 · L591 문맥) ----
  tick = game.tick()                                            // L797 (vtable+0x28)
  if game.get_game_mode().tag == 2 (DeathMatch): return false   // L801 (vtable+0x40)
  if !(tick < self.last_eval_tick + 10): return false           // L806 (+0x3640 ★0.6.0(구 +0x2948)) — 재평가 예산 10틱
  if self.small_action.is_action_complete(): return false       // L811 → small_action.rs:463: tag(+0x3601)∈{14,15,16,17} ★0.6.0(구 15~18 · Attack/Skill/Skill2/Ult idx 11~14) && payload.is_act(+0x10 → agent+0x3560)
  champ = data.cache.player_champion[player.info.team /*+0xa00*/][player.info.position /*+0xa90*/].unwrap()   // L816 (team ult 2 bounds · None→unwrap_failed)
  if champ.hp(+0x670) < self.last_eval_hp: return false         // L817 (+0x3648 ★0.6.0(구 +0x2950)) — 피해 입었으면 재평가
  current_enemies = count_nearby_enemies(champ, player, data)   // L822 (u16 마스크) · 콜리 f3d140(champ.x, champ.y, team, cache, blackboard) ★0.6.0 RVA
  return current_enemies == self.last_eval_nearby_enemies       // L823 (+0x36c0 ★0.6.0(구 +0x29c8)) — 근접 적 구성 불변이면 생략 가능

※ 분기 극성: premise_lost → 재평가(f3b4d7) / is_end 거짓 → 아무것도 안 함 / can_skip 참 → extend.
※ 개발자 주석(_docs game_ai.txt:9~11): is_premise_lost 는 '재평가 예산(can_skip_eval)으로 연장해서는 안 되는 유일한 조건' — 0.6.0 도 동일.
```

## changes
- L566 awareness_lapse: 콜리 17d68b0 에 8번째 인자 `m = 1000 + min(player.+0x490,100)²/20`(정확히 `(x*x*0xccd)>>16 + 1000`, u16 절삭) 신설 · 게이트 바이트 +0x464 → +0x49c · +0x928 → +0x9f8 · +0x448 → +0x478.
- ★신설: `agent.+0x29dd = sat_sub(tick, agent.+0x3698) <= tps*2` (플랜 update 직전).
- ★신설: 플랜 update(d5a940) 직후 `d52c00(plan_system, player, data, agent)` 호출(v≤1 경로에도 있음).
- 필드: version +0x2910 → +0x3608 · last_lapse +0x29ca → +0x36c2 · small_action +0x2858 → +0x3550(태그 +0x3601) · last_eval_tick +0x2948 → +0x3640 · last_eval_hp +0x2950 → +0x3648 · last_eval_nearby +0x29c8 → +0x36c0 · plan_system agent+0x530.
- 태그 재번호: AroundBush 0xc → 0xb · premise_lost 마스크 0x103f3/0xf40c → 0x81fb/0x7a04(idx=tag-3 · tag<3 → 6) · is_action_complete `tag-0xf` → `tag-0xe`(14~17).
- 콜리: update_event f3a420 · plan update d5a940 · is_end e5da70 · update_small_action f3bde0 · count_nearby f3d140 · AroundBush update ecfdd0.
- v2 사장: version>1 게이트(L569·L589) 상시 참 · v≤1 분기 제거.

## verified
- Ghidra 0.6.0 f3adb0 디컴+디스어셈 전체 확인: +0x49c==1 게이트 · 17d68b0 8인자(rsp+0x38 = ((min(+0x490,100)²*0xccd)>>16)+0x3e8 movzx r8w) · `or sil,dl` · +0x36c2 · +0x3608<2 분기 · +0x3698 sat_sub ≤ tps*2 → setbe [+0x29dd] · d5a940 7인자(lapse 바이트 [rsp+0x30]) · d52c00 4인자(양 경로) · +0x3601==0xb → ecfdd0 · 마스크 0x81fb/0x7a04 · +8/+0x60 → vt+0x1f0 · e5da70 · vt+0x40!=2 · +0x3640+10 · (tag-0xe)≤3 && +0x3560 · +0x3648 · f3d140 → cmp +0x36c0 · extend switch(+0x3550/+0x3598/+0x35a8) · 디버그 블록 · sret {8,pool,0,0}.
- 17d68b0 디컴으로 m 적용식 `((elapsed*max(m,1))/1000)*6000/max(tps*810,1)` · 6750 캡 확인. 0.5.8 e8d6f0 디스어셈으로 `or al,dl` 동일 확인(반환 쌍 OR 은 변경 아님).
- d52c00: 디스어셈 스캔으로 LPH 쓰기 오프셋 목록만 확보 — 본문 의미·판단 재소비 여부 미확인(11KB · 별도 명세 필요).
- 미확인: +0x490 스탯 이름 · +0x3698 틱의 갱신처.

## confidence
B — 본 함수의 변경점 3건은 전부 디컴 확인. 단 d52c00 이 LPH 판단 상태를 바꾸는지 미확정이라 '재현 완결' 은 별도 명세 후에야 성립.