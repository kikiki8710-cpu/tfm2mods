# cc4260→eafb50 JungleSubPlan::action_candidates

## logic_060
```
// sub_plan/jungle.rs:107 · 0.6.0 eafb50 · ★0.6.0 인자 7→6: fn(sret, &mut self, version, rnd, player, data) — `parameter:&ScoreParameter` 삭제(스택 슬롯 읽기 0회)
// ★0.6.0 JungleSubPlan(0x5c B · SubPlan 니치 dataful): +0x0 route Vec<(u8 x,u8 y)>(cap +0/ptr +8/len +0x10 · cap 이 SubPlan 태그 자리) · +0x18/+0x30 Option<String> 디버그(−1=None) · +0x48 team · +0x50 route_idx · +0x58 check_move · +0x59 camp · +0x5a/+0x5b 디버그 플래그
//   PlayerState team +0xa00 · pos +0xa90 · Entity ty +0x68 · action_state +0x70 · level +0x5c8 · skill +0x4c8(casting +0x4f8) · skill2 +0x500(+0x530) · ult +0x538(+0x568) · attack +0x490(casting +0x4c0 · range +0x4a0 · growth +0x4a8) · stat_buff.range +0x438 · radius +0x680 · mult +0x470 · ms +0x640 · xy +0x660/+0x668 · id +0x5c0
//   setting +0x12f0(시야거리 추정) · +0x12b8 width · +0x12c0 height · ctx.map +0x20 · ctx.debug +0x3b · bb stride 0x5c8 · last_seen +0x3e8+pos*8 · cache.jungles ptr +0x130+team'*0x20(idx 0x1a) / len +0x148
//   MobaMode 캠프 상태 = moba+0x18+slot*0x30 (slot = team*4+camp · Morgard=8 → +0x198) {live cap +0 · ptr +8 · len +0x10 · respawn +0x18}
//   SmallActionPlay 0xb8B · 태그 +0xb1: RunAway 3 · AroundPosition 6 · Attack 0xe · Skill 0xf · Skill2 0x10 (구 15/16/17)
// 콜리 v3: nontarget_windup_perceived de6080 · position_score_at_position ff5ec0(version,player,data,cx,cy,11) · battle_action ed6300 · attack_summon_action ed8550 · camp_pos memo 16a7af0 · Vec grow/extend dc05a0/e920f0 · AroundPosition::new 인라인(dde330 내부 new · v2 경로) / ecde50(v3 · 반경 60000) · new_with_out_line 인라인(v2) / ecdb90(v3 · out_line·radius 인자) · ★신규 e7f810(적 챔프·미니언이 나를 사거리에 둠) · ee0fe0(격자 생성) · ee2310(셀→셀 경로) · 텔레메트리 14502a550/a5b0

fn action_candidates(&mut self, version, rnd, player, data) -> Vec<SmallActionPlay>
L108 bump = data.context.pool; res = Vec::new_in(bump)
L111 team = player.info.team (bounds<2); champ = cache.player_champion[team][player.info.position].unwrap()
L114 enemy_team = 1 - team
     has_non_target_action_range = cache.player_champion[enemy_team].iter().flatten().any(|c|
L115   nontarget_windup_perceived(version, player, data, c) && c.ty==Champion(13) &&
L116   match c.action_state { Skill(4) => e = c.skill_effect (casting −1 → unwrap_failed · 1|2 만 통과) ,
L118                          Skill2(5) => e = (if c.level>2 {c.skill2_effect} else {NONE 표 143a7e940}).unwrap() ,
L120                          Ult(6)    => e = (if c.level>4 {c.ult_effect} else {NONE}).unwrap() , _ => false }
       && matches!(e.casting, Position(1)|Direction(2)) && e.is_in_range(caster=c, target=champ))   // range+(lv-1)*growth+stat_buff.range+range_adjust(vt+0x108)+c.radius()+champ.radius() 제곱 비교
L127 ps = position_score_at_position(version, player, data, clamp(champ.x/32000,29)*32000+16000, clamp(champ.y/32000,29)*32000+16000, Objective(11))   // ★0.6.0 셀 중심 6인자 · parameter 제거
L129 if ps.on_trajectory || has_non_target_action_range || ps.on_periodic_trajectory {
       // ★0.6.0 관측용: if ctx.debug && self.team != player.team { self.dbg2(+0x30) = format!(fmt 143a7f928, team, pos, tick, ps, has_non..); self.+0x5b = 0 }
L131   res.push(RunAway(new_with_skill(data, player, end_delay=5, with_skill=true)))   // 태그 3
L132   return res }
L136 res.extend(battle_action(version, rnd, player, data, 5))
L137 res.extend(attack_summon_action(player, data))
L140 camp_pos = map.camp_pos(self.camp(+0x59), is_blue_side = self.team(+0x48)==0)   // 16a7af0
L142 push_runaway = dist²(champ, camp_pos) < 22500000001 || cache.game.is_visible(vt+0xf8)(enemy_team, champ.id)
L143    || cache.jungles.iter().any(|e| dist²(e, champ) < 22500000001)   // 단락 순서: 거리 → is_visible → jungles
     if push_runaway {
       // ★0.6.0 v3 재판정(카정일 때만): 구 조건이 참이어도 「적 챔프 근접 ‖ 나를 사거리에 둔 적 존재」 로 대체
       if version >= 3 && self.team != player.team {
         r = (setting.+0x12f0 + 32000)²
         near = player_champion[enemy_team].iter().flatten().any(|a|
                  (game.is_visible(team, a.id) || (player_by_champion_id(vt+0x150)(a.id) is Some(p) && bb[enemy_team].last_seen[p.pos] + 15 >= game.tick()))
                  && dist²(champ, a) <= r)
         push_runaway = near || e7f810(&{ jungles/others 슬라이스 · cache · champ })   // (텔레메트리 a550/a5b0 LOCK inc)
       }
       // ★0.6.0 관측용: if self.team != player.team && ctx.debug { self.dbg2(+0x30) = format!(fmt 143a7f966, … 최근접 적 dist/1000 …); self.+0x5b=0 }
       if push_runaway {
L144     res.push(RunAway(SmallActionRunAway::new(data, player, 5))) }   // 태그 3
     }
L147 res.extend(attack_jungle_action(data, champ):   // jungle.rs:55~105 인라인 · 캠프 상태 = moba+0x18+(self.team*4+self.camp)*0x30 (camp 4 → +0x198 · team∉{0,1} → unreachable 패닉) · game_mode 태그≠0 → unwrap_failed
  L55   champ = player_champion[team][pos].unwrap()
  L56   camp_state = moba.jungle_runner.get_camp_state(self.team, self.camp)
  L59   sub = Vec::new_in(bump)
  L60   move_speed = champ.stat_cached.move_speed
  L62   for id in camp_state.live_list.iter() { target = cache.game.get_entity_by_id(vt+0x1f0)(id) (None → continue)
  L63     if champ.can_attack()(164b370) {
  L64       atk = champ.attack_effect.unwrap()   (casting −1 → unwrap_failed)
  L66       max = atk.range + move_speed*30 + champ.stat_buff_cached.range + (champ.level-1)*atk.growth_range + atk.range_adjust(vt+0x108)(champ, target) + champ.radius()
  L70       max += target.radius()
  L71       if dist²(target, champ) <= max² { L72 sub.push(Attack(SmallActionAttack::new(data, target.id))) } }   // ★0.6.0 태그 0xe(구 15) · tick · target id
  L76     if let Some(skill) = &champ.skill_effect (casting != −1) {
  L77       if champ.can_skill()(165d410) && skill.target.check(165e7a0)(champ, target) {
  L78         max = skill.range + move_speed*30 + stat_buff.range + (level-1)*skill.growth_range + skill.range_adjust(champ,target) + champ.radius()
  L82         max += target.radius()
  L84         if dist² <= max² { L85 sub.push(Skill(SmallActionSkill::new(data, target.id))) } } }   // ★0.6.0 태그 0xf(구 16)
  L90     skill2 = if champ.level>2 {&champ.skill2_effect} else {NONE}; if skill2.casting != −1 {
  L91       if champ.can_skill2()(164b650) && skill2.target.check(champ, target) {
  L92         max = skill2.range + move_speed*30 + (level-1)*skill2.growth_range + stat_buff.range + range_adjust + champ.radius()
  L96         max += target.radius()
  L97         if dist² <= max² { L98 sub.push(Skill2(SmallActionSkill2::new(data, target.id))) } } }   // ★0.6.0 태그 0x10(구 17)
        }
  L105  sub )
L148 res.push(self.move_action(rnd, data):   // jungle.rs:18~51 인라인
  L20   camp = map.camp_pos(self.camp, self.team==0)
       // ★v2 사장: version<3 = 구 L22~51 그대로(아래 「구 경로」)
       // ★0.6.0 v3 카정 격자 경로 (version>=3 && player.team != self.team && !self.check_move):
       if version >= 3 && self.team != player.team && !self.check_move {
         my_cell = (clamp(champ.x/32000,29), clamp(champ.y/32000,29)); camp_cell = (clamp(camp.x/32000,29), clamp(camp.y/32000,29))
         if my_cell == camp_cell || dist²(champ, camp) < 14400000001 {          // 캠프 120000 이내
           self.check_move = true; self.route.len = 0; AroundPosition::new(ecde50)(rnd, data, camp.0, camp.1, 5)   // 태그 6 · 반경 60000
         } else {
           idx = self.route_idx; len = self.route.len
           for j in idx+1..=len { if self.route[j-1] == my_cell { self.route_idx = j; idx = j; break } }   // 현 셀을 지났으면 인덱스 전진
           if idx >= len || cheb(self.route[idx], my_cell) > 1 {                 // 경로 없음/이탈 → 재계산
             grids = ee0fe0(data, player, enemy_team)                             // 900B×3 격자 (★미독: ee0fe0 본체)
             self.route = ee2310(data, player, enemy_team, my_cell, camp_cell).unwrap_or(empty); self.route_idx = 0   // ★미독: ee2310 본체(Option<Vec<(u8,u8)>>)
             (ctx.debug: ASCII 맵 문자열 → self.+0x18)
             if self.route.len > 0 { ecdb90(rnd, data, route[0].x*32000+16000, route[0].y*32000+16000, 5, out_line=1, radius=16000) }   // 태그 = out_line(1)
             else { AroundPosition::new(ecde50)(rnd, data, camp.0, camp.1, 5) }
           } else { ecdb90(rnd, data, route[idx].x*32000+16000, route[idx].y*32000+16000, 5, 1, 16000) }
         }
       } else {  // 구 경로(아군 캠프이거나 check_move 이거나 v2)
  L22   if player.team == self.team || self.check_move { L51 AroundPosition::new(rnd, data, camp.0, camp.1, 5) }   // 태그 6 · 60000/80000 필드
        else {
  L25     champ = player_champion[team][pos].unwrap()
  L26     if !is_enemy_side(team, champ.x, champ.y) {     // ((x−y+height) > width) != (team==0) — 아군 진영일 때만 경유지 검사
  L29       if (self.camp as u8 - 1) < 2 {      // Mushroom(1)|Stump(2) → Morgard 쪽
  L31         (ex,ey) = map.camp_pos(Morgard(4), team==0)
  L33         if dist²(champ,(ex,ey)) > 10000000000 { L34 return AroundPosition::new_with_out_line(rnd, data, ex, ey, 5, Outline(1)) }   // 태그 +0xb1 = 6 · out_line 필드 1(0x106)
            } else {                            // Rhino(0)|Bee(3)|… → Serpen 쪽
  L40         (ex,ey) = map.camp_pos(Serpen(5), team==0)
  L41         if dist² > 10000000000 { L42 return new_with_out_line(rnd, data, ex, ey, 5, Outline(1)) } }
          }
          self.check_move = true   // 적 진영이거나, 아군 진영이면서 경유지 100000 이내
  L51     AroundPosition::new(rnd, data, camp.0, camp.1, 5) } } )
L149 return res
```

## changes
- 인자 7→6(`parameter` 삭제) · L127 position_score 는 ff5ec0 6인자 셀 중심 좌표(clamp 29 · *32000+16000).
- L142~144: 구 조건 참일 때 `version>=3 && self.team != player.team` 이면 `near(적 챔프 (12f0+32000)² 내 · 가시 ‖ last_seen+15 ≥ tick) ‖ e7f810(...)` 로 push 여부를 대체. 아군 캠프면 구 동작.
- L18~51 move_action: v3 && 카정 && !check_move 분기 신설 — 캠프 셀 도달/120000 이내면 check_move=true + AroundPosition(camp) · 아니면 route(+0x0/+0x8/+0x10)·route_idx(+0x50) 를 따라 ecdb90(셀 중심 · out_line 1 · radius 16000) · 이탈/없음이면 ee0fe0/ee2310 재계산. 아군 캠프·check_move·v2 는 구 경로.
- 태그: Attack/Skill/Skill2 15/16/17 → 0xe/0xf/0x10. RunAway 3 · AroundPosition 6 유지.
- 캠프 상태 접근 = moba+0x18+slot*0x30(camp 4 → +0x198) · 디버그 문자열 2곳(+0x30) 은 ctx.debug 게이트(로직 무관).
- 콜리 v3: ff5ec0 · ed6300 · ed8550 · 16a7af0 · dc05a0/e920f0 · ecde50/ecdb90 · 신규 e7f810 · ee0fe0 · ee2310.
- ★미독: ee0fe0(격자 생성 2112B) · ee2310(셀→셀 경로 3176B) · e7f810 내부(534B · RE 요지만).

## verified
- Ghidra 0.6.0 eafb50 디컴 전량 정독: 인자 6(param_2=self · param_3=version · param_5=player · param_6=data) · 논타겟 판정 · ff5ec0 clamp 0x1d 셀 중심 · RunAway 필드(태그 3) · ed6300/ed8550 · 구 조건(`0x53d1ac100 < d²` && !is_visible && jungles 루프 `22500000000 < d²`) → `local_160=1` · v3 블록(`self.+0x48 != player.team && version>2` · +0x12f0+32000 제곱 · vt+0xf8/vt+0x150 · +0x3e8 last_seen(0x7d*8) `+0xf >= tick` · 5슬롯 언롤 · e7f810 · `bVar14 | e7f810` · LOCK a550/a5b0) · attack_jungle(slot 점프테이블 +3/+9/+0xf/+0x15/+0x33 qword · 태그 0xe/0xf/0x10 · move_speed*0x1e) · move_action v2(`version<3` · +0x58 · is_enemy_side · camp−1<2 → 4/5 · 1e10 · dde330 60000 · 0x106) · v3(cell clamp 0x1d · `< 0x35a4e9001` · route 스캔 +0x50/+0x10/+0x8 · cheb>1 → ee0fe0 · ecdb90(…,5,1,16000) / ecde50(camp,5)).
- 미확인: ee2310 반환 후의 route 대입·route_idx=0·디버그 맵 문자열은 디컴 절단(memcpy 아티팩트) — RE w3 §1 채택.

## confidence
A(본체) / B(v3 경로 재계산 꼬리·격자 콜리 3종 내부 미독) — 재현 시 ee0fe0/ee2310 은 별도 RE 필요.
