# dda220→f10f70 TeamPlan::update_objective_after_steal

## logic_060
```
// team_plan.rs:910 · 0.6.0 f10f70(30.9KB) · sig 동일: fn(&mut TeamPlan, version, rnd, player, data, goal_data, plan:&mut BigPlan, debug)  — 콜러 = f0f080(TeamPlan pre-update · r22 b: P 게이트 → hunt_call_pending 소비 → V6 sync-IN → f105d0 sanitize → f0e5f0 update_steal → 본 함수 → V6 sync-OUT)
// ★0.6.0 TeamPlan 오프셋(r21 w7 §0-b 전수표): objective +0xcd5/+0xcd6/+0xcd7 · chats +0x2e8/+0x2f0/+0x2f8 · mf_obj_clear +0x500(code)/+0x508(tick) · last_battle_tick +0x510 · vision last_visible_pos +0x520+p*16 · last_checked +0x5c0+p*8 · gank_start +0x640 · press_tower_start +0x648 · comeback_pick_start +0x650 · success_count +0x660 · comeback_pick_outcomes +0x300(ptr +0x308 · len +0x310) · next_respawn_tick +0x668 · obj_spawn epic_giveup +0x70/+0x78 · serpen_giveup +0x80/+0x88 · camp_last_visible +0xa0/+0xa8 · objective_misunderstanding +0x160(tag i64 −1=None · opposite_group_start +0x160/+0x168 · solo_hunt_start +0x170/+0x178 · accepted_tick +0x180 · target +0x188) · objective_discipline +0x3a8(wait_pos +0x3a8/+0x3b0 · until_tick +0x3b8 · target +0x3c0 · kind +0x3c1 · 2=None) · steal_action +0xc9a · v4_nexus_basis +0x0(Option<(u64,u64,u64)> tag@0) · v4_repair_low +0xcb8[5] · v3_press_chat_line +0xcd4
//   씬: v4_serpen +0x3c8{entry_dead +0x3c8 · entry_enemies +0x3d0 · assemble_due +0x3d8 · phase +0x3e0 · with_battle +0x3e1 · follower +0x3e2 · stance_grind +0x3e3 · take_born +0x3e4(니치 2=None)} · v5_morgard +0x3e8{… phase +0x400 · +0x401 · +0x402 · +0x403 · +0x404 니치} · locks v4_serpen_lock +0xc0{tag i32 · tick +0xc8 · dead +0xd0 · near +0xd8 · obj_hp +0xe0 · why +0xe8} · v5_morgard_lock +0xf0(동형) · armed v4_obj +0xcc5 · v5_obj +0xcc6 · v6_obj +0xcc7(★v≥2 상시 1) · v6_sync_follower +0xcd2 · v6_track_seen +0xc90[2] · arrived +0xc92 · acted +0xc94 · promoted +0xc96 · hunt_call_pending +0xc98 · window_intent +0xc9e[2] · assemble_arrived +0xca0 · departed +0xca2 · released +0xca4 · v6_obj_deadline +0x778[2] · arrive_at +0x788 · quorum_k +0x798 · scene_flow +0x7a8[3] · enemy_gather_since +0x7c0[2](−1 None) · intent_flips +0x7d0 · intent_ticks +0x7d8[2][4] · pred_dpt +0x818[2] · pred_n +0x828 · pred_idle_since +0x838 · commit_at +0xc40 · last_commit +0xc50 · v4_lock_sets +0xc68 · v4_lock_opens +0xc78 · v4_standoff_resumes +0xc88 · v6_audit +0x848[8](nopromote_gone +0xa28 · hopeless/unfit_hover/asm_conceded 등 텔레메트리)
//   BigPlan(plan+0): ForcePassive 2 · PassiveJungle 7(jungle +0x83 · team +0x60) · ActiveRecall 8 · Battle 니치 0/1(BattlePlan plan+8 · sub_goal +0x78 · RunAway 3) · LineGanker 10 · goal() = ec21b0(Line 0 · Epic 2 · Serpen 3 · Battle 5)
//   PlayerState team +0xa00 · pos +0xa90 · Entity hp +0x670 · max +0x628 · ms +0x640 · xy +0x660/+0x668 · id +0x5c0 · ty +0x68 · visible_state +0x38+t*0x18 · Tower.nearest_enemy +0x88/+0x98 · attack_effect +0x490(casting +0x4c0) · Blackboard stride 0x5c8 · call-vec +0x250/+0x258(0x50 · kind +0x46 · flags +0x40+pos) · big_goal +0x2f8+p*0x20{update_tick@0 · tag@+8} · prev +0x280+p*0x18 · minion_state top/mid/bottom +0x0/+0x28/+0x50{from_mid +0x10 · minion_count +0x20} · MobaMode epic live +0x1a8 · respawn +0x1b0 · serpen +0x1d8/+0x1e0 · buff_time +0x240+team*8 · cache twin_towers +0x130+team*0x20 · towers +0x180/+0x1a0/+0x1c0(+8*team) · tower2 +0x190/+0x1b0/+0x1d0 · nexus +0x170 · lead 카운터 +0x478/+0x23a0/+0x23b0(+team*8 · 정체 미확정) · goal_data epic_ally_tick +0x98 · serpen +0xd0 · epic_enemy_killed +0x90 · epic_ally_killed +0xa0 · serpen_enemy_killed +0xc8 · serpen_ally_killed +0xd8
// 콜리 v3: efa190 track 종료 정산 · e077c0 near-ally 리스트 클로저 · 100b8d0 DPS · f88690 obj 최근가시 · ef6ac0 enemy_committed · efb980 도착 정렬 · f87000/f87600 obj_helpers · f1e3c0 (dead,near,obj_hp%) · eff930 handle_morgard · efeeb0 handle_serpen · ef7680 · ef7350 hopeless · ef5a30 hunt 가능 · efa880 gathered · efafc0 door · efb0c0 t_full · efbb70 (ally_mass,enemy_mass) · efb250 stance_grind · ef7520 conceded · f10ac0/f10be0 lock 설정 · eda830 undying 잔여 · fe1ad0 finish_race · ef3cf0 · f27590 should_recall_to_shop · f024d0 handle_none_or_gank · efe020 handle_dive · efea40 handle_nexus · f01e40 handle_defense_line · eff8b0 handle_defense · f00450 handle_press_epic · f00e80 handle_split_epic · f01380 handle_press_tower · f082d0 handle_comeback_pick · fb7b20 v3_epicops_repair_need · efedd0 handle_repair(v1) · 1013290 nexus basis 3-튜플 · f882e0 v23_healthy_allies_near_point · f86f50 objective_is_damaged · ede160 should_disengage_object_hunt · fb1b70 check_epic_hunt · f0dc20/f18bf0 · required_group 표 143a864e0[tutorial]

// 공통 매크로: clear(N) = mf_note_obj_clear(N, tick) → self.mf_obj_clear=(N @+0x500, tick @+0x508) 후 self.objective=None(0xff @+0xcd5) / FP = drop(*plan); *plan=ForcePassive(2) / chat(T,..) = self.chats(+0x2e8).push(Chat{태그 T,..}) / eff(유효 objective) = v4_serpen Some→Serpen(5) · v5_morgard Some→Morgard(4) · 레거시 0→4 · 1→5 · phase 는 씬이면 +0x3e0/+0x400 아니면 +0xcd6

// ===== [A] team_plan.rs:911 — 규율 갱신 / V6 track 감시
if !self.v6_obj_armed(+0xcc7) {
  // ★사장(v≥2 에서 armed 상시 1 → 실행 불가): v27 objective discipline(objective_discipline.rs:334~481 · 0.5.8 logic D336~468 전문 · target=eff · 0.6.0 오프셋 +0x3a8..+0x3c1 · vision +0x520/+0x5c0 · goal_data +0x98/+0xd0)
} else {
  self.objective_discipline = None (store 2 @+0x3c1)
  for idx in [0,1] {                                                              // ★0.6.0 V6 track 종료 감시
    if scene[idx].is_none() && self.v6_track_seen[idx](+0xc90+idx) {
      if ctx.debug && !self.v6_sync_follower(+0xcd2) { log V6SCENE-END }
      gone = moba.is_some() ? (self.v6_track_arrived[idx](+0xc92+idx) && live_list[idx].is_empty()) : self.v6_track_arrived[idx]
      if gone && !self.v6_track_promoted[idx](+0xc96+idx) { self.v6_audit_scene_nopromote_gone(+0xa28) += 1 }
      efa190(self, idx)                                                            // 카운터 +0x948..+0xa08 누적 후 track_* [idx] 0 · +0x8f8/+0x990 [idx][5] 0
    }
  }
  self.v6_track_seen[0] = self.v4_serpen.is_some(); self.v6_track_seen[1] = self.v5_morgard.is_some()
}
// ===== [B] team_plan.rs:920~939 — 오브젝트 사망(장기 리스폰) 시 목표 해제
if version >= 3 && eff != Nexus(4) { self.v4_nexus_basis(+0x0) = None }               // ★0.6.0 v3 (사망 검사 전)
if let Some(moba) = game.get_game_mode().as_moba() {                                   // (version>1 게이트 · 항상 참)
  now = game.tick(); limit = tps*15
  dead = match eff { Morgard(4) => epic.live_list.len(+0x1a8)==0 && epic.next_respawn_tick(+0x1b0).saturating_sub(now) > limit,
                     Serpen(5)  => serpen.live_list.len(+0x1d8)==0 && serpen.next_respawn_tick(+0x1e0).saturating_sub(now) > limit, _ => false }
  if dead { clear(36); self.v4_serpen = None(+0x3e4=2); self.v5_morgard = None(+0x404=2);      // ★0.6.0 씬도 함께 드롭
            if matches!(plan.goal(), Epic(2) | Serpen(3)) { FP } }
}
// ===== [C] ★0.6.0 신규 V6 pre-pass (armed)
if armed {
  if moba { if serpen.live_list.is_empty() { self.v4_serpen_lock(+0xc0) = None }; if epic.live_list.is_empty() { self.v5_morgard_lock(+0xf0) = None } }
  for idx in [0 /*serpen · big_goal 태그 3*/, 1 /*epic · 태그 2*/] {
    // ② enemy_gather_since
    n = #적 챔프 p: HP%(p) >= 30 && (tick − bb[enemy].big_goal[p].update_tick(+0x2f8+p*0x20) > 19 ? bb[enemy].prev[p].tag(+0x280+p*0x18) : bb[enemy].big_goal[p].tag(+0x300+p*0x20)) == (idx==0 ? 3 : 2)
    if n >= 3 { if self.enemy_gather_since[idx](+0x7c0+idx*8) == −1 { = tick } } else { = −1 }
    // ③ pred_dpt / pred_n
    if 목표 살아있음 { list = e077c0(...)(캠프 근처 아군 리스트); dps = 100b8d0(list, obj); self.pred_idle_since[idx](+0x838) = 0
      if self.pred_dpt[idx](+0x818) == 0 { pred_dpt = dps; pred_n[idx](+0x828) = list.len }
      else { if pred_n < list.len { v6_audit[0](+0x848) += 1; pred_n = list.len }; if pred_dpt < dps { pred_dpt = dps } } }
    else if pred_dpt != 0 { if pred_idle_since == 0 { pred_idle_since = tick } else if pred_idle_since + tps*12 < tick { pred_dpt = pred_n = pred_idle_since = 0 } }
    // ④ commit 창 감사
    if self.commit_at[idx](+0xc40) != 0 && commit_at + tps*4 <= tick { if popcount(self.assemble_departed[idx](+0xca2+idx)) <= 1 { v6_audit[3](+0x860) += 1 }; commit_at = 0 }
    // ⑤ window_intent 산출 (0 없음 · 1 · 2 · 3)
    live = live_list[idx].len; remain = (next_respawn[idx] − tick).sat
    new = if !moba { 0 } else if live == 0 && remain >= tps*15 { 0 } else if scene[idx].is_some() { 3 }
          else if live != 0 {
            if f88690(player, data, goal_data, self, idx==0) { idx==0 ? (efb980 len>2 && (pred_dpt 처치틱 < arr[2]+tps*2 ? 2 : 3)) : 3 }
            else { n = ef6ac0(self, kind); if n > 2 || pred_dpt != 0 { 위 3-경로 }
                   else if idx==1 { cache.lead[+0x23a0+team*8] >= 3 ? (cache.lead[+0x23b0+team*8] > 2 ? 3 : 1) : 1 }
                   else { f87000(...) ? (version>=2 && f87600(...) ? 2 : lock 검사(f1e3c0(5) vs lock) … efb980 len>2 && arr[2] <= tps*20 → e088d0/100b8d0/efd2f0 예측 → cnt < v+2 ? 2 : (cache[+0x478+team*8] > 2 ? (cache[+0x23b0] > 2 ? 3 : 1) : 1)) : (cache[+0x478..] 기준 1/3) } } }
          else { /*live==0 && remain<tps*15*/ cache 카운터 기준 1/3 }                    // ★미독 세부(RE 요지)
    old = self.window_intent[idx](+0xc9e+idx); if old != new { if old != 0 && new != 0 { self.v6_intent_flips(+0x7d0) += 1 }; log V6INTENT; window_intent[idx] = new }; self.v6_intent_ticks[idx][new](+0x7d8+idx*0x20+new*8) += 1
  }
}
// ===== [D] ★0.6.0 신규 씬↔레거시 충돌 정리(V6KILL r1)
if self.v4_serpen.is_some() || self.v5_morgard.is_some() {
  if self.objective ∈ {Gank 8, ComebackPick 11} { clear(48) }
  else if self.objective.is_some() {                                                   // 레거시 objective 가 씬을 이김
    for scene in [v4_serpen, v5_morgard] { if scene.is_some() { if scene.phase == 2 { self.scene_flow[2](+0x7b8) += 1 }; log V6KILL(r1); scene = None } }
    clear(48)
  }
}
// ===== [E] team_plan.rs:945~947 — if let Some(Gank{line}) = self.objective(태그 8) { if self.handle_gank_preprocess(version, player, data, plan, line) { return } }
// G1134: if !line_exists(ctx, line) → clear(38); if plan.tag==LineGanker(10) → FP; return true
// G1143: if tick.saturating_sub(self.gank_start_tick(+0x640)) > tps*20 → clear(39); if plan==LineGanker → FP; return true
// G1152: target_laner_pos = match line { Top→0, Mid→2, Bottom→3 }
// G1158~1160: laner = cache.player_champion[team][target_laner_pos]; cancel_reason = laner None → 2 / HP%(laner) < 25 → 1
//   취소 시 (G1166~1170): clear(40); chat(Cancel 0x11, byte1=cancel_reason); if plan==LineGanker → FP; return true
// G1175: enemy_tower = cache.{top,mid,bottom}_tower[enemy](+0x180+0x20*line+8*enemy).or(tower2(+0x190+…))
// G1177~1180: tower_target_minion = enemy_tower.and_then(|t| t.ty==Tower(2) && t.Tower.nearest_enemy(+0x88 Some) → get_entity_by_id(+0x98)).filter(|m| m.ty==Minion(1) && m.team==Player(team)) — 없으면 return false
// G1193: tower_attack = enemy_tower.attack_effect(+0x490 · casting +0x4c0 −1 → return false)
// G1197~1199: ally_minions_in_range = cache.iter_minions(team).filter(|m| tower_attack.is_in_range(enemy_tower, m)).count(); tower_damage = tower_attack.expected_damage_target(ctx, enemy_tower, minion)
// G1200: if ally_minions_in_range < 2 && !(tower_target_minion.hp > tower_damage) → return false
// G1205: near_allies  = (0..5).filter(|p| champ[team][p].is_some_and(|c| is_near_line(ctx,c.x,c.y,line) && HP%(c) > 39)).count()
// G1212: near_enemies = iter_champions(enemy).filter(|e| is_near_line(ctx,e.x,e.y,line) && bb[team].is_recent_visible(game,ctx,player,e)).count()
// G1218: if near_allies > 1 && near_allies > near_enemies && (version < 3 || plan.tag == LineGanker(10)) → self.objective = Dive(9){line}; chat(GankDive 0x2c{line,0}); return true   // ★0.6.0 v3: plan 이 LineGanker 일 때만
//   else return false
// ===== [F] team_plan.rs:949~950 — if self.repair_misunderstood_objective(player, data, plan, debug) { return }   [인라인 · team_plan.rs:603~720 · 동치 · 오프셋만]
// state = self.objective_misunderstanding(+0x160 · tag −1 → None) ; None → 계속(false)
// target = match self.objective(레거시 +0xcd5) { Morgard→4 / Serpen→5 / 그 외 → +0x160 = −1; false }   // R607~609 (★새 objective 는 레거시 슬롯에 씀)
// if state.target(+0x188) != target → +0x160 = −1; false
// phase = +0xcd6; opposite = Morgard?5:4
// opposite_available = (Morgard → serpen_exists(tutorial∈{0,5,7,8}) && serpen.live_list.len!=0 / Serpen → morgard_exists(tutorial∈{0,7,8}) && epic.live_list.len!=0)
// if !opposite_available → state.opposite_group_start = None(+0x160=0), state.solo_hunt_start = None(+0x170=0); false
// tick = game.tick(); marker_age = tick.saturating_sub(state.accepted_tick(+0x180)) / max(tps,1)
// target_camp = camp_pos(map, target, team==0); opposite_camp = camp_pos(map, opposite, team==0)
// target_allies = v23_healthy_allies_near_point(f882e0)(player, data, target_camp, 180000, 40); opposite_allies = …(opposite_camp, 200000, 40)
// required_group = 표 143a864e0[tutorial] ({1,3}→2 · {2,4,6}→1 · {0,5,7,8}→3)
// opposite_group_start = (opposite_allies >= required_group && !(target_allies > 1 && opposite_allies < target_allies+2)) ? Some(기존 유지 or tick) : None
// target_started = objective_is_damaged(f86f50)(data, target)
// my_near_target = champ 존재 && dist_sq(champ, target_camp) < 36100000001
// solo_hunt_start = (phase==3 && target_started && my_near_target && target_allies < 2) ? Some(기존 유지 or tick) : None
// opposite_group_elapsed = opposite_group_start.is_some_and(|s| tick − s >= tps*4); solo_hunt_elapsed = solo_hunt_start.is_some_and(|s| tick − s >= tps*6)
// if !(opposite_group_elapsed || solo_hunt_elapsed) → +0x160..+0x17f 갱신 저장; false
// [수리 발동] new_phase = (opposite_allies >= required_group || objective_is_damaged(data, opposite)) ? Hunt(3) : Setup(1)
// self.objective = opposite(Morgard→Serpen(1) / Serpen→Morgard(0)) with {phase=new_phase @+0xcd6, with_battle=1 @+0xcd7}; +0x160 = −1
// if plan.goal() != (새 목표 Morgard ? Epic(2) : Serpen(3)) → FP
// if champ 존재 && ctx.debug { debug.infos[champ.id].push("objective misunderstanding repaired: {:?} -> {:?} by {}" …); push(" objective misunderstanding age: {}s") }
// return (true → ret)
// ===== [G] team_plan.rs:956~972 — 귀환 판정
// if plan.tag == ActiveRecall(8) → [H] / else if plan.goal() == Battle(5) → [H] / else if eff ∈ {Defense 2, DefenseLine 3, Nexus 4, Dive 9} → [H]
// ★0.6.0 v2+: else if bb[team].call_vec.iter().any(|e| e.kind(+0x46)==1 && e.flags[+0x40+my_pos] != 0) && HP%(me) > 24 → [H]   (콜 대기 중 귀환 억제)
// ★0.6.0 v3: else if me.is_some() && eda830(me) > tps → [H]   (언다잉 잔여)
//            else if fe1ad0(finish_race)(sret, self+0x138?, data).tag == 1 && ef3cf0(&{tick, ctx, game, tps}, out.+8, out.+0x10) → [H]   (마무리 경주 중)
// else if should_recall_to_shop(f27590)(version, rnd, player, data, goal_data) {
//   if plan.tag == PassiveJungle(7) { camp_live = get_jungle_live_list(moba.jungle_runner, plan.jungle(+0x83), plan.team(+0x60)==0); camp_pos = camp_pos(map, jungle, team==0)
//     skip_for_camp = !camp_live.is_empty() && champ.is_some_and(|c| dist_sq(c, camp_pos) < 14400000001); drop(camp_live); if skip_for_camp → [H] }
//   drop(*plan); *plan = ActiveRecall(8); return }
// ===== [H] team_plan.rs:977~1022 — 분기 (★0.6.0: 양 씬이 모두 None 일 때만 레거시 switch · 씬이 있으면 [I]/[J])
if self.v4_serpen.is_none() && self.v5_morgard.is_none() {
  match self.objective(+0xcd5) {
    None(0xff) | Gank(8)  → self.handle_none_or_gank_objective(f024d0)(version, rnd, player, data, goal_data, plan, debug); return
    Repair(7)  → [K]
    Serpen(1)  → efeeb0(self, version, rnd, player, data, goal_data, plan, debug, phase, is_born=false)   // §S (eff930 대칭 · ★미독)
    Morgard(0) → eff930(self, version, rnd, player, data, goal_data, plan, debug, phase, is_born=false)   // §M
    PressEpic(5) → f00450(…) §PE(★미독) · SplitEpic(6) → f00e80(…) §SE(★미독) · Defense(2) → eff8b0 §D · Nexus(4) → efea40 §N · DefenseLine(3) → f01e40 §DL · Dive(9) → efe020 §DV · PressTower(10) → f01380 §PT(★미독) · ComebackPick(11) → f082d0 §CB(★미독)
  }   // 모든 arm 은 return
} else { [I] (v5_morgard Some) / [J] (v4_serpen Some) }
// ===== [I]/[J] ★0.6.0 신규 씬 핸들러 (I = morgard idx1 kind4 · J = serpen idx0 kind5 · 대칭)
s = scene.take()   // +0x404(또는 +0x3e4) = 2 임시 None
legacy = {phase: s.phase==4 ? 1 : s.phase, with_battle: s.with_battle} → self.objective = Morgard(0)/Serpen(1) with legacy   // 레거시 슬롯에 임시 기록
(I: eff930 / J: efeeb0)(self, version, rnd, player, data, goal_data, plan, debug, legacy.phase, is_born = (s.phase==4))
if self.objective != Morgard(0)/Serpen(1) {                                            // 핸들러가 해제/전환(V6KILL hv)
  if s.phase == 2 { scene_flow[2] += 1 }; if assemble_arrived[idx] != 0 && 목표 사망 { v6_audit_asm_conceded += 1 }
  scene = None
  if lock.is_some() && lock.tick == tick { lock = Some{tick, f1e3c0(kind), why=1}; v4_lock_sets(+0xc68) += 1 }   // 같은 틱 재기록
  return
}
phase = +0xcd6; wb = +0xcd7; stats = f1e3c0(kind)
if phase ∈ {2,3} && armed { ef7680(self, idx, version, phase, (!s.stance_grind) | (phase==2), player, data, debug) }   // ★미독(8.6KB)
if phase == 3 && armed && ef7350(self, idx, team, data, goal_data) /*hopeless*/ { v6_audit_hopeless += 1; if version >= 3 { scene = None; self.objective = None; return } }
if phase ∈ {2,3} && armed { v6_audit_unfit_hover += #아군(HP%<50 && dist_sq(camp(kind)) < 250000²+1) }
match phase {
  3 (Hunt): if wb != 1 → 유지; if ef5a30(self, idx, version, player, data, goal_data) → 유지;
            else { if version >= 3 && plan.goal() == (Epic|Serpen) { FP }; if !sync_follower { chat(MorgardSetup 0x22 / SerpenSetup 0x19) }
                   if ef7520(idx, team, data) /*conceded*/ { scene = None; self.objective = None; scene_flow[1] += 1; return }
                   assemble_departed[idx] = 0; phase = 2 }
  2 (Assemble): gathered = efa880(self, idx, version, player, data); ok = ef5a30(...) || efafc0(idx, min_dead, min_enemy, player, data)   (debug: V6ASM)
            if gathered && ok { scene_flow[0] += 1; arr = efb980(kind); k = min(quorum_k[idx], arr.len); arrive_at[idx] = tick + arr[k−1]; chat(MorgardHunt 0x26 / SerpenHunt 0x1d); phase = 3 }
            else { if gathered && (assemble_arrived[idx] & assemble_departed[idx]) != 0 { v6_audit[2] += 1 }
                   if deadline[idx] < tick && (assemble_due < tick || deadline < assemble_due) { scene = None; if !ef7520(...) { f10ac0/f10be0(self, player, data, why=1) }; self.objective = None; return } }
  4 (take_born 대기): efb0c0 / goal.+0x88/+0x90(스폰틱) / efbb70 로 'k met' 판정 → phase 3 + chat 0x26/0x1d (+ v4_standoff_resumes(+0xc88) += 1) 또는 유지   // ★미독 세부
}
assemble_due = if phase==2 && armed { (cache 카운터 < 3 || !ef5a30) ? tick + clamp(efb0c0, …, tps*600) − tps*4 : tick } else { s.assemble_due }
assemble_departed[idx] 비트: 아군 p 의 tick+travel+tps*2 < due 이면 (이미 set && +tps*14 도 미달이면 clear & v6_audit[1] += 1) else set     // ★미독 세부
scene = Some{ entry_dead: stats.dead, entry_enemies: stats.near, assemble_due, phase, with_battle: wb, follower: 0, stance_grind: efb250(self, idx, player, data), take_born: (s.phase==4) }
self.objective = None                                                                   // 레거시 슬롯 비움
if version >= 3 && !sync_follower && (phase, wb) 불변 { self.chats.truncate(entry_len) }   // 이번 틱 push 된 챗 폐기
return
// ===== [K] Repair(7) (team_plan.rs:982~988)
// ★v2 사장: v1 → efedd0(handle_repair_objective) / v2 → fb7b20(player,data,plan)==0 → clear(46)+None+v4_repair_low=0
// v3: for p in 0..5 { self.v4_repair_low[p](+0xcb8+p) = champ[team][p].is_some_and(|c| HP%(c) < 41 && c.hp < c.max_hp) }
//     if v4_repair_low.iter().any(|&b| b != 0) { return }
//     if fb7b20(v3_epicops_repair_need)(team, cache, plan) != 0 { return }
//     clear(46); (v4_repair_low 전부 0); return
// ===== §M eff930 handle_morgard_objective(self, version, rnd, player, data, goal_data, plan, debug, phase, is_born)  [objective_handlers.rs:685~829 · 0.6.0 별도 함수 2843B]
// M686: if !morgard_exists(ctx) → clear(35); FP; return
// M697: moba(unwrap); if epic.live_list.len==0 && epic.next_respawn_tick.saturating_sub(tick) > tps*15 → clear(36); if plan.goal()==Epic(2) → FP; return   (version>1 게이트 · 상시)
// M710: if let Some(line) = handle_nexus_attack(efdbf0)(…) → objective = Nexus(line); (v3 call_vec kind 4 억제) chat(AttackNexus 0x2a{line,0}); return
// M718: if need_defense_nexus(e74b40) { need_count = d75ad0(…); if objective_defense_role(e76100)(version, player, data, Morgard(4), need_count) == GoDefend(1) → objective=Defense(2); FP; (v3 kind 3 억제) chat(DefenseNexus 0x2b); return }
// M728 Hunt 블록 — ★0.6.0 교체(armed 상시):
//   if phase == Hunt(3) {
//     k = min(goal_data.epic_enemy_killed_tick(+0x90), epic_ally_killed_tick(+0xa0))
//     recent = k <= tps*4 || (self.last_battle_tick(+0x510) != 0 && tick <= last_battle_tick + tps*4)
//     if recent { if epic 살아있고 HP%(epic) > 30 { (a,e) = efbb70(4, team, data); if self.v6_obj_deadline[1](+0x780) < tick && phase==3 && a < e → SETUP복귀 } }
//     else if phase==3 && tick > deadline[1] → SETUP복귀
//     SETUP복귀 = if (0..5).any(|p| bb[team].big_goal[p].tag == Epic(2)) { if k > tps*2 && (armed ? a < e : should_disengage_object_hunt(ede160)(version, player, data, camp_pos(Morgard), 150000)) → objective = Morgard{Setup(1), with_battle 1}; (version>=3 && armed → return); if goal==Epic → FP; chat(MorgardSetup 0x22) }
//                else { objective = Morgard{Setup(1),1}; (version>=3 && armed → return); if goal==Epic → FP }
//   }
//   ★v2 사장(!armed): 구 M729(in_epic 전원 아님 → Setup) · M741~753(min(killed) > tps*2 && should_disengage → Setup + chat 0x22)
// M759: moba; if epic.live_list.len==0 { if epic.next_respawn_tick.saturating_sub(tick) > tps*15 → clear(36); if goal==Epic → FP; return  else return }
// M770 Setup(1):
//   ★0.6.0 armed 면 check_epic_giveup 검사 생략(GIVEUP 경로는 !armed 전용 → ★사장): [GIVEUP M772~798: clear(37); obj_spawn.epic_giveup_tick(+0x70/+0x78)=Some(tick); if plan Battle && !BattlePlan::is_end(plan+8) → plan.battle.sub_goal(+0x78)=RunAway(3); if goal==Epic → FP; if epic 살아있음 → chat(MorgardGiveUp 0x28); if serpen_exists && serpen.live_list.len!=0 && serpen_giveup_tick(+0x80).is_none() → return; if let Some(press_line)=f1fce0(…, Some(Bottom)(2), …) → objective=PressTower(line); press_tower_start(+0x648)=tick; chat(PressTower 0x2d); FP; return]
//   if phase == 1 && !is_born {
//     M806: v24_setup_ready = self.v24_objective_setup_lane_pressure_ready(version, player, data, goal_data, Morgard(4), debug)[f0dc20/f18bf0 계열]; delay = should_delay_morgard_for_wave_priority(f18ad0)(self, player, data, goal_data)
//     M807~810: if delay && !v24_setup_ready { if goal==Epic → FP; return }
//     M814: if v24_setup_ready || check_epic_hunt(fb1b70)(version, rnd, player, data, goal_data, plan, self, debug) → objective = Morgard{Hunt(3), with_battle 1}; if goal != Epic → FP; chat(MorgardHunt 0x26); return
//   }
//   return
// } else if phase == Hunt(3) { ★0.6.0 armed 면 giveup 검사 없음 → return }   (★v2 사장: check_epic_giveup → GIVEUP)
// else return (Assemble/None)
// ===== §S efeeb0 handle_serpen_objective(…, phase, is_born)  [objective_handlers.rs:558~684 · 2555B · ★미독 — eff930 대칭 추정 · 0.5.8 본문 유지]
// S559: if !serpen_exists(ctx) → clear(32); FP; return
// S566: if let Some(line) = handle_nexus_attack(…) → objective=Nexus(line); chat(0x2a); return
// S574: if need_defense_nexus { need_count; if objective_defense_role(version, player, data, Serpen(5), need_count)==GoDefend(1) → objective=Defense(2); FP; chat(0x2b); return }
// S584: moba; if serpen.live_list.len(+0x1d8)==0 { if serpen.next_respawn_tick(+0x1e0).saturating_sub(tick) > tps*15 → clear(33); if goal==Serpen(3) → FP; return  else return }
// S595: match phase { Hunt(3) → S596.. / Setup(1) → S661.. / 그 외 → return }
// [Hunt] S596: (armed 시 M728 과 같은 교체가 있을 것으로 추정 · ★미독) 0.5.8: if (0..5).all(|p| !bb[team].in_serpen(p)) → objective = Serpen{Setup(1), 1}; if goal==Serpen → FP; return
//   S607: if min(goal_data.serpen_enemy_killed_tick(+0xc8), serpen_ally_killed_tick(+0xd8)) > tps*2 { camp_pos = camp_pos(map, Serpen(5), team==0); if should_disengage_object_hunt(version, player, data, camp_pos, 150000) → objective=Serpen{Setup,1}; if goal==Serpen → FP; chat(SerpenSetup 0x19); return }
//   S626: if check_serpen_giveup(1010060)(…) → GIVEUP-S; else return
// [Setup] S626: if check_serpen_giveup(…) → GIVEUP-S
//   else S661: v24_setup_ready = v24_objective_setup_lane_pressure_ready(self, version, player, data, goal_data, Serpen(5), debug); delay = should_delay_serpen_for_wave_priority(f18a80)(self, player, data, goal_data)
//   S662~665: if delay && !ready { if goal==Serpen → FP; return }
//   S669~677: if ready || check_serpen_hunt(version, rnd, player, data, goal_data, plan, self, debug) → objective = Serpen{Hunt(3), 1}; if goal != Serpen → FP; chat(SerpenHunt 0x1d); return   else return
// [GIVEUP-S] S627: clear(34); S629: obj_spawn.serpen_giveup_tick(+0x80/+0x88) = Some(tick); S631: if goal==Serpen → FP; S635~637: if plan Battle && !is_end → sub_goal(+0x78)=RunAway(3)
//   S641: reason = serpen_giveup_chat_reason(player, data)(Option<u8> · 2=None); if reason != None → chat(SerpenGiveUp 0x1f{reason})
//   S645: if morgard_exists && epic.live_list.len!=0 && epic_giveup_tick(+0x70).is_none() → return
//   S648~652: if let Some(press_line) = f1fce0(…, Some(Top)(0), …) → objective=PressTower(line); press_tower_start=tick; chat(0x2d); FP; return   else return
// ===== §D eff8b0 handle_defense(self, version, rnd, player, data, plan, debug)  [objective_handlers.rs:16~27 · 116B · 동치]
// if need_defense_nexus(…) → return; if plan.tag > 1 (≠Battle) { clear(2); FP }; return
// ===== §N efea40 handle_nexus(self, version, rnd, player, data, plan, line, debug)  [objective_handlers.rs:29~44 · 909B · ★0.6.0 v3 다건]
// N30: if !line_exists(ctx, line) → clear(3); FP; return
// ★v2 사장: v<3 = if !end_check(version, rnd, player, data, line, debug) → clear(4); FP; return
// ★0.6.0 v3:
//   if need_defense_nexus(…) && i_am_chosen_defender(…, calculate_nexus_defense_count(…), &[]) → objective=Defense(2); (kind 3 억제) chat(0x2b); FP; return
//   if end_check(…) { self.v4_nexus_basis(+0x0) = Some(1013290(player, data, line)) /*3-튜플 · 정체 미확정*/ ; return }
//   if (0..5).any(|p| bb[team].big_goal[p].tag == Battle(5)) → return (유지)
//   basis 비교: 현재 3-튜플이 v4_nexus_basis 의 각 항 이상이면 유지, 아니면 v4_nexus_basis=None; clear(4); FP; return   // ★미독 세부(항 의미)
// ===== §DL f01e40 handle_defense_line(self, version, rnd, player, data, plan, line, debug)  [objective_handlers.rs:46~87 · 1497B · 동치 + v3 챗 억제]
// DL47~49: if !line_exists(ctx, line) → clear(5); FP; return
// DL54: if need_defense_nexus(…) && i_am_chosen_defender(player, data, calculate_nexus_defense_count(…), &[]) → objective=Defense(2); (v3: call_vec kind 3 타인 존재 시 생략) chat(0x2b); FP; return
// DL61: tower = cache.{top,mid,bottom}_tower[team](+0x180/+0x1a0/+0x1c0 + 8*team).or(tower2[team](+0x190/+0x1b0/+0x1d0))
// DL62~67: if tower None → tower = cache.twin_towers[team](+0x130+0x20*team · ptr +0 · len +0x18).iter().min_by_key(|t| dist_sq(t, line.get_start_position(setting, team)))
// DL80~81: if tower 여전히 None → clear(7); FP; return
// DL70~71: has_near_enemy_champion = cache.champions(enemy, pool).iter().any(|e| bb[team].is_recent_visible(game, ctx, player, e) && dist_sq(e, tower) < 90000000001)
// DL73~75: if !has_near_enemy_champion → clear(6); FP; return   else return
// ===== §PE f00450 handle_press_epic(self, version, rnd, player, data, goal_data, plan, line, debug)  [objective_handlers.rs:253~333 · 2596B · ★미독(clear 20/21 로 정체 확정) — 0.5.8 본문 유지]
// PE254~257: if !morgard_exists || !line_exists(ctx, line) → clear(20); FP; return
// PE261~263: if end_check(…) → objective=Nexus(line); chat(0x2a); return
// PE267~269: if need_defense_nexus && i_am_chosen_defender(…, &[]) → objective=Defense; FP; chat(0x2b); return
// PE274~275: goal = plan.goal(); if goal == Line(0){now_line} && now_line != line → FP
// PE280: if goal != Battle(5) { low_hp_allies = #(champ[team] 존재 && HP%<41); live_allies = #존재; live_enemies = #(champ[enemy] 존재); if low_hp_allies > 1 && live_allies <= live_enemies → objective=Repair(7); chat(Repair 0x17); return }
// PE292~294: if serpen_exists && check_serpen_setup(100f210)(…) { if should_delay_serpen_for_wave_priority(…) → return; if check_serpen_giveup(…) → serpen_giveup_tick=Some(tick); if goal==Serpen → FP; return; objective = Serpen{Setup,1}; chat(SerpenSetup 0x19); if goal != Serpen → FP; return }
// PE317~319: moba; if epic_minion_buff_time[team](+0x240+8*team) == 0 → clear(21); return
// PE321~325: live_ally_count = #(HP%>29); live_enemy_count = #존재; if live_ally_count < live_enemy_count → objective=Repair(7); return
// PE329: else handle_epic_line_change(version, rnd, player, data, debug, line); return
// ===== §SE f00e80 handle_split_epic(…, line, debug)  [objective_handlers.rs:334~392 · 1275B · ★미독(clear 22/23) — 0.5.8 본문 유지]
// SE335~338: if !morgard_exists || !line_exists → clear(22); FP; return
// SE342~344: if end_check → objective=Nexus(line); chat(0x2a); return
// SE348~350: if need_defense_nexus && i_am_chosen_defender → objective=Defense; FP; chat(0x2b); return
// SE355~356: if plan.goal()==Line{now_line} && now_line != line → FP
// SE361~381: if serpen_exists && check_serpen_setup { delay → return; giveup → serpen_giveup_tick=Some(tick); if goal==Serpen → FP; return; objective=Serpen{Setup,1}; chat(0x19); if goal != Serpen → FP; return }
// SE386~388: moba; if epic_minion_buff_time[team]==0 → clear(23); return   else return
// ===== §DV efe020 handle_dive(self, version, rnd, player, data, plan, line, debug)  [objective_handlers.rs:88~140 · 2580B]
// DV89~92: if !line_exists(ctx, line) → clear(8); FP; return
// DV96~98: if let Some(nexus_line) = handle_nexus_attack(…) → objective=Nexus(nexus_line); (v3 억제) chat(0x2a); return
// DV101~104: if need_defense_nexus && i_am_chosen_defender(…, calculate_nexus_defense_count, &[]) → objective=Defense; FP; (v3 억제) chat(0x2b); return
// DV108~111: if tick.saturating_sub(self.gank_start_tick(+0x640)) > tps*30 → clear(9); FP; return
// ★0.6.0 v3: return (Dive 유지 · 아래 near_allies/near_enemies 비교 생략)
// ★v2 사장: DV116~134 near_allies = #(champ[team] 존재 && is_near_line && HP%>39); near_enemies = #(champ[enemy] 존재 && is_near_line && is_recent_visible); if near_allies > near_enemies → return; if tick − gank_start > tps*20 → clear(10); FP; return else objective = Gank(8){line}; return
// ===== §PT f01380 handle_press_tower(self, version, rnd, player, data, goal_data, plan, line, debug)  [objective_handlers.rs:141~252 · 2747B · ★미독(clear 11~19) — 0.5.8 본문 유지]
// PT142~145: if !line_exists → clear(11); FP; return
// PT149~151: if let Some(nexus_line) = handle_nexus_attack → objective=Nexus(nexus_line); chat(0x2a); return   (FP 없음)
// PT154~156: if need_defense_nexus && i_am_chosen_defender → objective=Defense; FP; chat(0x2b); return
// PT161~163: if cache.{top,mid,bottom}_tower[enemy].is_none() && tower2[enemy].is_none() → clear(12); FP; return
// PT168~170: if tick.saturating_sub(self.press_tower_start_tick(+0x648)) > tps*30 → clear(13); FP; return
// PT175~191: minion_state = bb[team].minion_state(line); if minion_state.from_mid(+0x10) < −1000 { engaged_at_tower = cache.tower(line, enemy).is_some_and(|t| cache.champions(team).any(dist_sq(c,t) < 90000000001) || cache.minions(team).any(dist_sq(m,t) < 40000000001)); if !engaged_at_tower → clear(14); FP; return }
// PT197~202: for check_line in valid_lines(tutorial) { if check_line != line { ms = minion_state(check_line); if ms.from_mid < −2999 && ms.minion_count(+0x20) < −2 → clear(15); FP; return } }
// PT208~213: healthy_allies = #(HP%>39); live_enemies = #(champ[enemy] 존재); if !(healthy_allies > live_enemies) → clear(16); FP; return
// PT219~227: near_enemies = iter_champions(enemy).filter(is_recent_visible && is_near_line).count(); if > 1 → clear(17); FP; return
// PT232~234: if serpen_exists && check_serpen_setup(…) → clear(18); FP; return
// PT238~240: if morgard_exists && check_epic_setup(fb34e0)(…) → clear(19); FP; return
// PT245~246: if plan.goal()==Line{now_line} && now_line != line → FP; return
// ===== §CB f082d0 handle_comeback_pick(self, version, rnd, player, data, goal_data, plan, line, ready, debug)  [objective_handlers.rs:393~557 · 5507B · ★미독(clear 24~31) — 0.5.8 본문 유지]
// outcome(V) = self.comeback_pick_outcomes(+0x300 · len +0x310 · ptr +0x308).last_mut().map(|l| l.1(@+4 u8) = V)
// CB394~396: if !line_exists → clear(24); FP; return
// CB401~404: if let Some(nexus_line) = handle_nexus_attack → if last.1==0 { outcome(6) }; objective=Nexus(nexus_line); chat(0x2a); return
// CB407~411: if need_defense_nexus && i_am_chosen_defender → if last.1==0 { outcome(6) }; objective=Defense; FP; chat(0x2b); return
// CB415: pick_start = self.comeback_pick_start_tick(+0x650)
// CB416~431: my_kill_on_line = kill_logs.rev().any(|log| log.killer_team==team && log.tick >= pick_start && tick − log.tick <= tps*8 && (log.killed_position==Jungle(1) || line_of(killed_position)==line)); enemy_kill_on_line = 동일 술어 killer_team != team
// CB439~452: if my_kill_on_line → self.comeback_pick_success_count(+0x660) += 1; outcome(1); clear(26); FP; → CB454
// CB440~443: else if enemy_kill_on_line → outcome(5); clear(25); FP; return
// CB479~484: timeout_s = ready ? 20 : 15; if tick − pick_start > tps*timeout_s → outcome(2); clear(27); FP; return
// CB488~492: elapsed = tick − pick_start; grace_period = elapsed < tps*5; in_battle = plan is Battle
// CB493/498: visible_enemies_in_line = iter_champions(enemy).filter(is_recent_visible && is_near_line).count(); near_allies = (0..5).filter(|p| champ[team][p].is_some_and(|c| HP%>39 && (is_near_line || side(line, c)))).count()
// CB510~513: if visible_enemies_in_line > 2 → outcome(3); clear(28); FP; return
// CB518: if !(grace_period || in_battle) { if visible==2 && near_allies < 3 → outcome(3); clear(29); FP; return ; if visible==0 → outcome(3); clear(30); FP; return ; if near_allies < 2 → outcome(4); clear(31); FP; return }
// CB544: if ready → return
// CB545~552: allies_on_line = (0..5).filter(|p| champ[team][p].is_some_and(|c| HP%>39 && is_near_line)).count(); if visible != 0 && allies_on_line > 1 → objective = ComebackPick{line, ready:true}(+0xcd7=1); return   else return
// [CB454~474 · my_kill 성공 후] epic_unavailable = epic.live_list.len==0 || epic_giveup_tick.is_some(); serpen_available = serpen.live_list.len!=0 && serpen_giveup_tick.is_none()
//   if !epic_unavailable → objective = Morgard{Hunt(3),1}; chat(MorgardHunt 0x26); return   else if serpen_available → objective = Serpen{Hunt(3),1}; chat(SerpenHunt 0x1d); return
//   else if let Some(press_line) = f1fce0(…, None(−1), …) → objective=PressTower(line); press_tower_start=tick; chat(0x2d); return   else return
```

## changes
- [A] `!v6_obj_armed` 일 때만 v27 discipline(armed 상시 1 → 사실상 사장) · armed 면 discipline=None 강제 + V6 track 종료 감시(efa190 · track_seen 갱신).
- [B] 사망 판정 대상 = 유효 objective(씬 우선) · 발동 시 씬 2개도 None · v3: objective≠Nexus 이면 v4_nexus_basis(+0x0)=None.
- [C] 신규 V6 pre-pass(armed): lock 해제 · enemy_gather_since · pred_dpt/n/idle · commit 창 감사 · window_intent 0..3 산출.
- [D] 신규 V6KILL: 씬 있을 때 레거시 objective(Gank/ComebackPick → clear(48) · 그 외 Some → 양 씬 드롭 + clear(48)).
- [E] G1218 Dive 전환에 v3 `plan.tag == LineGanker(10)` 조건 추가.
- [F] repair_misunderstood 동치(오프셋 +0x160 블록 · 표 143a864e0).
- [G] 귀환 억제 2종: v2+ `call_vec kind 1 && flags[my_pos] && HP%>24` · v3 `eda830(me) > tps` / `finish_race(fe1ad0).tag==1 && ef3cf0(...)`. PassiveJungle 필드 +0x83/+0x60.
- [H] switch 는 양 씬 None 일 때만 · 핸들러 12 = 별도 함수(RVA 표) · Serpen/Morgard 핸들러에 (phase, is_born) 인자.
- [I]/[J] 신규 씬 핸들러(take → legacy 기록 → eff930/efeeb0 → 재구성 · hopeless(v3 해제)/conceded/deadline · phase 2↔3↔4 · assemble_due/departed · chats.truncate).
- [K] Repair v3: v4_repair_low[5](+0xcb8) 갱신 → 하나라도 low 면 유지 → fb7b20 → clear(46).
- §M eff930: M728 Hunt 블록 교체(armed: recent(k≤4tps ‖ last_battle+4tps) && efbb70 a<e && deadline 경과 → SETUP 복귀 · v3&&armed 면 FP 없이 return) · M770 armed 면 giveup 검사 생략 · Hunt 에서 giveup 없음 · `phase==1 && !is_born` 일 때만 Hunt 전이 검사 · RunAway 태그 3.
- §N efea40 v3: 수비 선점 → end_check 시 v4_nexus_basis=Some(1013290) → 아군 Battle 있으면 유지 → basis 비교.
- §DV efe020 v3: gank_start+30s 초과 clear(9) 까지만, near 비교 생략(Dive 유지).
- §DL f01e40 · §D eff8b0 동치(+v3 챗 억제). §S/§PE/§SE/§PT/§CB 는 ★미독(정체는 clear 코드·크기·소스행으로 확정 · 0.5.8 본문 유지).
- ★v2 사장: [A] v27 discipline · [K] v1/v2 가지 · §M !armed Hunt/GIVEUP · §N v<3 · §DV v<3 near 비교.

## verified
- capstone 0.6.0 f10f70 콜·게이트 지도: +0xcc7 분기(f10fb2 · f114ac · f1172e) · +0xcd2 · efa190 ×2 · e077c0 ×2 · f88690 · ef6ac0 · efb980 ×2 · f87000/f87600 · f1e3c0 ×6 · eff930 ×2(f12dce · f131e5) · efeeb0 ×2(f12ea9 · f14cfd) · ef7680 ×2 · ef7350 ×2 · efa880 ×2 · ef5a30 ×5 · ef7520 ×3 · efb0c0 ×3 · efbb70 ×4 · efafc0 ×2 · efb250 ×2 · f10ac0/f10be0 · f024d0(f13cef) · 핸들러 표 9종(f082d0 · f01e40 · efe020 · eff8b0 · f00e80 · efea40 · f00450 · f01380 · efedd0) · 귀환 블록(f180e2~f181e2: `HP%>24` · `version>=3` · eda830 > tps · fe1ad0 tag==1 → ef3cf0 · f27590 · plan==7 캠프 예외) · Repair(+0xcb8/+0xcb9 저장 · +0xcb8 dword 0 초기화).
- RE w7 §1(디컴 4235줄 정독) 의 블록 지도·등가 Rust 를 채택. Ghidra 재디컴은 미수행(크기 30.9KB).
- 미확인: [C] window_intent 세부 분기·cache 카운터 +0x478/+0x23a0/+0x23b0 정체 · [I]/[J] phase 4 · assemble_due/departed 세부 · ef7680/efa880/efd2f0 내부 · §S efeeb0 armed 교체 유무 · §PE/§SE/§PT/§CB 본체 · 1013290 3-튜플 의미 · goal_data 필드 오프셋(0.5.8 값 유지).

## confidence
B — 골격(A~K)·콜 시퀀스·오프셋은 asm+RE 로 확인, 세부 산식은 RE 의존 · 핸들러 5종(§S/§PE/§SE/§PT/§CB) 은 0.5.8 본문 유지(★미독).
