# df0e90→dcbe00 SerpenHuntAndPokePlan::sub_plan

## logic_060
```
// serpen/hunt_and_poke.rs:30~92 (+ check_recall :99~159 인라인). Epic 판(defcd0→fd7f70)과 같은 골격 — ★표시가 차이.
// SubPlan 태그(★0.6.0 u64 인코딩 0x8000000000000000+idx): Steal ..10 · SerpenCheck ..0b · SerpenHunt ..0c · SerpenPoke ..0d · Recall ..03 · LineDefense ..00 · ★0.6.0 ObjContest ..11 {slot u64 @+8, poke bool @+0x10}
t = player.info.team(+0xa00); pos = player.info.position(+0xa90); champ = cache.player_champion[t][pos].unwrap()   // :32
// A. 스틸 (:35~44)
if self.focus_serpen_only || self.vision_only {
   serpen = as_moba().jungle_runner.serpen.live_list.first().and_then(get_entity_by_id)   // :36~37 (moba+0x1d0 ptr/+0x1d8 len · vt+0x1f0)
   Some → Steal{ last_vision_tick:0, target:Some(Serpen) ★, commit:focus_serpen_only }   // :39
   None → SerpenCheck{false}                                                            // :44
}
// B. 일반
hp_ratio = hp*100/max_hp                                                                 // :47 (max 0 → 패닉)
serpen = live_list.first().and_then(get_entity_by_id); None → SerpenCheck{false}         // :48~50
(x0,y0,x1,y1) = map.fountains[t] (map+0x6d70+t*0x20); is_in_heal_area = x0<=x<=x1 && y0<=y<=y1   // :55~56
can_upgrade_item = upgrade_item(version, rnd, player, game, ctx).is_some()               // :57 · 콜리 v3: e7a8c0→f27140(items +0x510/+0x518 · gold +0xa68 · 결과 동치)
// ★0.6.0 :58~59 v3 재작성 (v<3 = 구식 `serpen.hp==max && (hp_ratio<51 || can_upgrade || (in_heal && hp<max)) → Recall` 사장)
if serpen.hp(+0x670) == serpen.stat_cached.hp(+0x628) && (can_upgrade_item || (is_in_heal_area && champ.hp < champ.max_hp)) {   // ★0.6.0 hp_ratio<51 항 삭제
   if tp.v6_obj(+0xcc7) != 1 → Recall                                                    // ★0.6.0 (cc7 상시 1 → 실효 없음)
   if !finish_before_enemy(tp, 0, player, data) → Recall                                 // ★0.6.0 ef7590: team_kill_feasible(ef9de0).kill <= min_p enemy_arrival_eta(ef65a0)
   tp.c30(+0xc30) += 1   // 귀환 보류 카운터 · 계속(귀환 안 함)                             // ★0.6.0
}
// C. 오브젝트 게이트
if tp.serpen_scene_none(+0x3e4) == 2 && !(tp.morgard_scene_none(+0x404) == 2 && tp.objective_tag(+0xcd5) == 1) → Recall   // :63 ★0.6.0 `objective != Some(Serpen{..})` 이중모드 치환 (구 0x41f != 1)
if tp.cc7 && bt(tp.v6_assemble_released[0](+0xca4) as u32, pos) { tp.c38(+0xc38) += 1; return Recall }              // ★0.6.0 v3 부적격→귀환 비트 (version>2 게이트 · 상시)
phase = if tp.+0x3e4 == 2 { tp.objective_phase(+0xcd6) } else { tp.serpen_phase(+0x3e0) }                             // ★0.6.0
if tp.cc7 && phase == 2 (Assemble) → return ObjContest{ slot:0, poke: serpen.hp < serpen.max_hp }                      // ★0.6.0 신규
if phase == Hunt(3) {                                                                                                  // :64
   strategy = if game.is_solorank()(vt+0xe8) { player.strategy(+0x568..+0x580) } else { game.strategy(team)(vt+0x108) }   // :65 (구 player.strategy(rnd, game) 인라인)
   if strategy.object_buildup == Split(p) (tag<5) && p == pos && tp.v4_buildup(+0xcc2) == 0 && v25_objective_splitter_can_stay(version, player, data, Serpen ★5)   // :66~68 ★0.6.0 cc2==0 조건 추가(cc2 = v>98 이라 상시 0 = 실효 동치) · 콜리 v3: →f88b30
      → LineDefense{ style:Aggressive(0), line:fallback_line(ctx, Top ★0) (튜토리얼 점프테이블 143a7a8b0 → 143a87388 등), minion_action_type:Push(2) }   // :70~73
   // ★0.6.0 신규 (구 :77 `else → SerpenHunt{false}` 앞에 삽입)
   if tp.cc7 && !contest_can_hold(tp, 0, pos, player, data) && min(goal_data.serpen.epic_enemy_tick(+0xc0), goal_data.serpen.epic_ally_tick(+0xd0)) > tps*5 { tp.c38 += 1; return Recall }   // ★0.6.0 v3 (v<=2 판 `cc7 && hp_ratio<50 && min>5tps → Recall` 은 사장)
   if !tp.cc7 || tp.+0x3e4 == 2 || tp.serpen_stance_grind(+0x3e3) & 1 != 0 → SerpenHunt{ need_recall:false }   // :77 ★0.6.0 조건부
   else → ObjContest{ slot:0, poke:true }                                                                        // ★0.6.0
}
// D. check_recall (:99~159) — 그대로
   heal_area(t).contains(champ) && hp<max → true                                          // :103~104 (팀0 x<=64000&&896000<=y<=960000 / 팀1 892000<=x<=960000&&y<=64000)
   hp_ratio = hp*100/max                                                                  // :108
   min(goal_data.serpen.epic_enemy_tick(+0xc0), epic_ally_tick(+0xd0)) <= tps*5 → false   // :111
   if self.v46_flee {                                                                     // :119
      if self.v46_flee_threats.iter().any(|id| game.get_entity_by_id(id).is_some_and(|e| dist_sq(e,champ) < 40000000001)) → true   // :120~121 (200000²+1)
      self.v46_flee=false; self.v46_flee_threats.clear()                                   // :126
   } else {
      (code, threats) = v46_flee_gate_check(version, player, data, champ)                  // :129 · 콜리 v3: d3b2a0→eed7f0 (A = aggr(+0x49d)?0 : def(+0x49e)?240 : 구 · B = 특성?67 : 구 · v3 `*60→*tps`)
      if code == 0 { self.v46_flee=true; self.v46_flee_threats = threats.to_vec(); if ctx.debug(+0x3b) { debug.add_log(...) } return true }   // :131~135
   }
   ★serpen = game.jungle_runner()(vt+0xe0).serpen.live_list.first().and_then(get_entity_by_id)   // :142~143
   ★if serpen.is_none() → true(귀환)                                                       // :143
   ★enemy_cnt = player_champion[1-t] 중 blackboard[1-t](bb+(1-t)*0x5c8).is_recent_visible(game, player, e) && dist_sq(e, serpen) < 22500000001 인 수   // :148~151
   ★ally_cnt  = player_champion[t]   중 blackboard[t].is_recent_visible   && dist_sq(a, serpen) < 22500000001 인 수   // :153~156
   return hp_ratio < 21 && ally_cnt < enemy_cnt                                            // :159
if check_recall → Recall                                                                  // :79~80
// E. 꼬리
if team_plan.v24_objective_setup_should_check_camp(version, player, data, goal_data, Serpen ★5) → SerpenCheck{false}   // :82~83 · 콜리 v3: dd5db0→f0cfe0(L91 게이트 이중모드)
★else → SerpenPoke (거리 게이트 없음)                                                       // :86
```

## changes
- B(:58): v3 조건 = `serpen.hp==max && (can_upgrade || (in_heal && hp<max))`(hp_ratio<51 삭제) → `cc7!=1 || !ef7590(tp,0,..) ? Recall : {c30++; 계속}`.
- C(:63): `objective != Serpen` → `3e4==2 && !(404==2 && cd5==1) → Recall` · 신규 `cc7 && bt(ca4,pos) → {c38++; Recall}` · `phase = (3e4==2) ? cd6 : 3e0` · 신규 `cc7 && phase==2 → ObjContest{0, poke: serpen.hp<max}`.
- Hunt(:66~77): Split 조건에 `cc2==0`(상시 참) 추가 · strategy 인라인(is_solorank vt+0xe8 → player+0x568 / vt+0x108) · 신규 `cc7 && !ef6da0(tp,0,..) && min(gd.c0,gd.d0)>5tps → {c38++; Recall}` · `!cc7 || 3e4==2 || 3e3&1 → SerpenHunt{false} else ObjContest{0,true}`.
- D/E 그대로(상수 전부 동일). 콜리: upgrade_item f27140 · v46 eed7f0 · v25 f88b30(…,5) · v24 f0cfe0 · 신규 ef7590/ef6da0. SubPlan 태그 u64 인코딩 · ObjContest 0x11.
- v<3 분기(구식 B 조건 · Hunt 의 `hp_ratio<50 && min>5tps`)는 사장.

## verified
- capstone 0.6.0 `dcbe00..dccde0`(949줄) 정독: B(`f27140` · `cmp version,3; jae` · v3 경로 `serpen.hp!=max → C` / `can_upgrade → cc7` / `!(in_heal && hp<max) → C` / `cc7!=1 → Recall` / `ef7590(tp,0,player,data)` false → Recall / `lock inc +0xc30`) · C(`3e4==2 → 404!=2 or cd5!=1 → Recall` · `version>2 && cc7 → bt [+0xca4], pos → lock inc +0xc38 → Recall` · phase ptr `cd6`/`3e0` · `==2 → ObjContest{[+8]=0, [+0x10]=serpen.hp<max}` 태그 ..11 · `==3 → vt+0xe8 → +0x568 / vt+0x108` · `tag<5 && ==pos && cc2==0 → f88b30(…,5)` → 점프테이블 143a7a8b0 → LineDefense · `version>2: cc7==0 → 65b; ef6da0 → 65b; min(gd+0xc0,gd+0xd0) > 5tps → c38++ Recall` · `65b: cc7==0 or 3e4==2 or 3e3&1 → SerpenHunt(..0c) else ObjContest{0,1}`) · D(`0x9502f9001` · `eed7f0` · `0x53d1ac101 ×9` · `cmp hp_ratio,0x14`) · E(`f0cfe0` → SerpenCheck(..0b) · SerpenPoke(..0d)).
- Ghidra 디컴은 타임아웃(RE 와 동일). 미확인: D 의 :142 `vt+0xe0 jungle_runner` 경로·is_recent_visible 인라인 줄단위(RE w9 「그대로」) · ef7590/ef6da0/f27140/eed7f0/f0cfe0 내부.

## confidence
A- — 변경 지점 전부 asm 로 확인, 불변 D/E 는 상수 매칭 + RE 판정.
