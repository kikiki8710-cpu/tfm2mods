# d2e500→fa3980 PassiveJunglePlan::sub_plan

## logic_060
```
// 반환형 = 진짜 SubPlan sret(0x60B+): Recall = `[sret]=0x8000000000000003` · Jungle = 니치 dataful(JungleSubPlan 0x5c B) · ★0.6.0 Hide = `0x8000000000000007`(HideSubPlan payload sret+8)
// self 레이아웃(0.6.0 · w3 §0): +0x48 cj_ambush = Option<(camp u8 @+0x50 (0xff=None), bush_region usize @+0x48, deadline @+0x58)> · +0x60 team · +0x83 jungle   // ★0.6.0 team +0x48→+0x60 · jungle +0x60→+0x83
sub_plan(L135~140) = if self.check_recall(version,rnd,player,data,debug) { Recall } else { ambush_or_jungle() }   // ★0.6.0 Jungle 귀결 전부가 ambush_or_jungle 로 감
check_recall 전체가 인라인(passive_jungle.rs:143~206):
L143: team = player.info.team(+0xa00); champ = cache.player_champion[team][player.info.position(+0xa90)].unwrap()
L145: (lx,ly,rx,ry) = context.map.fountains[team]   (map+0x6d70+team*0x20)
L146: is_in_heal_area = lx<=champ.x<=rx && ly<=champ.y<=ry
L147: if is_in_heal_area && champ.hp(+0x670) < champ.stat_cached.hp(+0x628) { return true(Recall) }   // 분수 안에서 회복 중이면 계속 귀환
// ★0.6.0 신규(L147 뒤 · fa3a67~fa3b5b): undying 게이트 — 귀환 검사 전부 skip 하고 매복 블록 직행
if version >= 3 && champ.stat_buff_cached.undying(+0x488) == 1 {
  remain = champ.stat_buff(ptr +0x2e0, len +0x2e8, stride 0x120).iter().filter(|b| b.undying(+0x118)==1)
             .map(|b| match b.duration(+0x48) { Permanent(0) => usize::MAX, Time(1){tick @+0x50} => tick, WithShield(2) => 0 }).max();   // 빈 경우(undying 버프 없음) → 아래 구경로
  if let Some(r) = remain && r > tps(setting+0x12f8) { return ambush_or_jungle() }   // → fa4361
}
L154: moba = cache.game.get_game_mode()(vt+0x40).as_moba().unwrap(); camp_live = moba.jungle_runner.get_jungle_live_list(self.jungle, self.team == 0)   // camp_state = moba+0x18+(side*4+camp)*0x30 · live_list {cap,ptr@+8,len@+0x10}
L155: if !camp_live.is_empty() {
  L156: camp_pos = map.camp_pos(self.jungle, self.team == 0)   // 1416a7af0 TLS 메모이즈
  L157: if champ.distance_sq(camp_pos) < 14400000001 {
    L159~162: edpt = camp_live.iter().filter_map(|id| get_entity_by_id(id)).map(|e| e.attack_effect(+0x490).as_ref().map(|ae| ae.expected_damage_target(ctx, e, champ) * 1000 / e.attack_cooltime()).unwrap_or(0)).sum()   (cooltime 0 → div_by_zero panic · attack_cooltime = Action vt+0x90 cool * 100 / max(attack_speed_mult(+0x3fc)+100, 1), min 3 인라인 — 동치 추정 · expected_damage = 1643790 estimate_damage_to)
    L164: die_tick = if edpt == 0 { usize::MAX } else { champ.hp * 1000 / edpt }
    L165: if die_tick > tps { return ambush_or_jungle() }   // ★0.6.0 구 "return false(Jungle)" 자리 → fa4357
  } }
L172: can_heal = … (★0.6.0 삭제: 인라인 vamp/skill/skill2 힐 검사 — 헬퍼 fb12a0 로 이동, 아래 L205~206 참조)
L180: is_in_fight = get_jungle_live_list(self.jungle, self.team==0).iter().any(|id| L181 get_entity_by_id(id).is_some_and(|e| L182 e.ty(+0x68) == Jungle(4) && L184 e.ty.info.focused(+0x88 tag==1, +0x90) == Some(champ.id(+0x5c0))))
L191: if is_in_fight {
  L192: camp_live = get_jungle_live_list(…)  (3번째 호출)
  L193~195: edpt = max(camp_live.iter().filter_map(get_entity_by_id).map(|e| e.attack_effect.as_ref().unwrap().expected_damage_target(ctx,e,champ)*1000 / e.attack_cooltime()).sum(), 1)   (★attack_effect None 이면 unwrap 패닉 — L162 와 달리 unwrap_or 없음)
  L197: die_tick = champ.hp * 1000 / edpt
  L199: return die_tick <= tps   // 1초 안에 죽으면 Recall(fa40e1), 아니면 ambush_or_jungle()(fa4357)
} else {
  L205~206: return recall_need(version, ctx, champ) > 49   // ★0.6.0 콜리 교체: FUN_140fb12a0 → {80,70,0}: can_heal = vamp(+0x438 stat_buff_cached.vamp)>0 || skill(+0x4c8)/skill2(+0x500, level>2) 의 expected_heal(Effect vt+0x58)>0 || effect_buff_target(vt+0xc0).vamp>0 ; can_heal ? (hp%<11→80, <21→70, else 0) : (hp%<21→80, <41→70, else 0). `>49` 이므로 구 `can_heal ? hp<21 : hp<41` 과 값 동치
}

// ★0.6.0 신규 블록 ambush_or_jungle() (fa4357~fa4640) — 구 "Jungle{team, camp, check_move:false}" 귀결 전부를 대체
fn ambush_or_jungle() -> SubPlan {
  if version >= 3 {
    if let Some((camp, bush_region, _deadline)) = self.cj_ambush /*+0x50 != 0xff*/ {
      let hide = Hide{ ambush_cell: None (+0 tag 0), bush: bush_region /*self+0x48 → sret+0x20*/, check_move:false, enemy_spotted_me:false, direct:false, stealth:true /*sret+0x2b*/, out_line:1 /*sret+0x2c*/ };   // 태그 0x8000000000000007
      let Some(me) = cache.player_champion[team][pos] else { return hide };
      let Some(ej) = cache.player_champion[1-team][Jungle(1)] else { return hide };          // 적 정글러
      if game.get_game_mode().tag != Moba(0) { return hide };
      live = moba.camp_state(side = 1-team /*적 측 표 143a931d4 (team==1) / 143a931c0*/, camp).live_list;
      let Some(mob) = live.iter().filter_map(get_entity_by_id).min_by_key(|m| m.hp) /*db75f0 · 동점=앞*/ else { return hide };
      dps = dps_x1e5(ctx, ej, mob);   // de7750: Σ attack/skill/skill2(lv>2) estimate_damage(1643790)·1e5 / max(cool·100/(as+100),3)
      if dps == 0 { return hide }
      kill_ticks = mob.hp(+0x670) * 100000 / dps;
      travel = free_dist_ticks(data, me.x, me.y, mob.x, mob.y, max(me.move_speed(+0x640), 1));   // fafc10: 30×30 u16 표 ×6400/speed · 미도달 MAX
      if kill_ticks > travel + tps/2 { return hide }   // 적 정글러가 몹을 못 끝내는 동안 도착 가능 → 매복
      // else fallthrough → Jungle
    }
  }
  Jungle{ route: vec![] (+0 cap 0,+8 ptr 1,+0x10 len 0), dbg1: None(+0x18=-1), dbg2: None(+0x30=-1), team: self.team(+0x60) → +0x48, route_idx: 0 (+0x50), check_move: false (+0x58), camp: self.jungle(+0x83) → +0x59, +0x5a/+0x5b: 0 }
}
```

## changes
- 반환 인코딩: 소형 enum(5 Recall/6 Jungle) → SubPlan 본체(Recall 0x8000000000000003 · Jungle 니치 · **Hide 0x8000000000000007 신규**). JungleSubPlan 0x5c B(route Vec · 디버그 String 2 · team +0x48 · route_idx +0x50 · check_move +0x58 · camp +0x59).
- self: team `+0x48→+0x60` · jungle `+0x60→+0x83` · 신규 `cj_ambush` (+0x48 bush_region · +0x50 camp/0xff · +0x58 deadline) — 세팅처는 next_plan 블록 B(fa2670) 뿐.
- L147 뒤 v3 undying 게이트(잔여 > tps → 귀환 검사 skip → ambush_or_jungle).
- L165 / L199 거짓 / L206 거짓 의 Jungle 귀결이 전부 ambush_or_jungle() 로: cj_ambush Some 이면 `kill_ticks(mob.hp·1e5/dps(ej→mob)) > travel(me→mob)+tps/2 → Hide{bush,stealth,out_line 1}` 아니면 Jungle.
- L172~178 can_heal 인라인 + L205~206 → 헬퍼 `fb12a0 recall_need > 49`(값 동치).
- Effect vt 슬롯: expected_heal 0x40→0x58 · expected_buff_deep 0xa0→0xc0 (fb12a0 안).
- 새 콜리: fb12a0 · db75f0 · de7750 · fafc10. 캠프 상태 = `moba+0x18+slot*0x30`. PlayerState +0xa00/+0xa90.

## verified
- capstone 0.6.0 `fa3980..fa46d3`(832줄): `fa3a53 hp<max → Recall` · `fa3a67 cmp version,2; jbe` · `[champ+0x488]==1` · stat_buff 순회(0x2e0/0x2e8 · stride 0x120 · +0xd0 → 0x118 undying · duration 태그 0/1/2 → MAX/+8/0 · max 누적 cmovbe) · `cmp remain, [setting+0x12f8]; ja fa4361` · 캠프 근접 `0x35a4e9000` · `fb12a0 → cmp eax,0x31; jbe fa4357` · `fa4357 cmp version,2; jbe Jungle(fa43ed)` · `fa4361 [self+0x50]==0xff → Jungle` · `player_champion[1-team][1]` null → Hide · vt+0x40 tag → Hide · 표 143a931d4 · `db75f0` rdx==0 → Hide · `de7750`==0 → Hide · `mob.hp*0x186a0/dps` · `fafc10(data, me.x, me.y, mob.x, mob.y, max(ms,1))` · `kill > travel + tps>>1 → Hide` · Hide payload `[+8]=0,[+0x20]=self+0x48,[+0x28]=0x01000000,[+0x2c]=1` · Jungle payload `{0,1,0,-1,-1,+0x48=self+0x60,+0x50=0,+0x58=0,+0x59=self+0x83,+0x5a=0}`.
- Ghidra 디컴은 310KB(점프테이블 오염)로 미사용. 미확인: L154~199 본문 줄단위(RE w3 「그대로」 신뢰 · cooltime 인라인 산식은 추정) · fb12a0/de7750/fafc10/db75f0 내부는 RE w3 정체표 신뢰.

## confidence
A- — 변경 지점 전부 asm 로 확인, 불변 구간(L154~199)은 RE 판정 의존.
