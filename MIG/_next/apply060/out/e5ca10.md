# e5ca10→d5f3c0 try_engage

## logic_060
```
try_engage(&self, version, rnd, player, data, target_id, debug) -> Option<BattlePlan>   // d5f3c0 · LPH 오프셋 = 0.6.0
  // ── 게이트 (version >= 2 전제 · v3 확정)
  if target_id == self.last_lost_fight.0(+0x2050) {                            // L43 · ★0.6.0 +0x1490→+0x2050
    if game.tick() <= self.last_lost_fight.1(+0x2058) + tps*3 { return None } }   // L44~45 · ★0.6.0 +0x1498→+0x2058
  v3 = false
  // ★0.6.0 신규 게이트 A(v3 · target_id != 0): 최근 실패 교전 대상 재교전 3초 금지 (reason 필터)
  if version != 2 && target_id != 0 {
    if self.last_fail.target(+0x2060) == target_id && self.last_fail.reason(+0x2070 u8) < 0x11
       && (0x1a080 >> reason) & 1 != 0                                          // reason ∈ {7, 13, 15, 16} (enum 이름 미상)
       && game.tick() <= self.last_fail.tick(+0x2068) + tps*3
       && (reason != 15 || game.get_entity_by_id(target_id).map_or(false, |t| ee8090(data, player, t)))   // ee8090 = 대상이 적 타워 사거리+8000 안(아래)
    { return None }
  }
  // ★0.6.0 신규 게이트 B(v3): 팀 블랙보드 타워 상태 — 대상 챔피언이 그 타워 250000 안이면 교전 금지
  if version != 2 {
    bb = data.blackboard[team(+0xa00)]  (stride 0x5c8 · team>=2 → panic)
    st = bb.+0x278 (u8)
    if st != 2 && (st & 1) == 0 {
      t  = game.get_entity_by_id(target_id)
      if t.is_some() && t.ty(+0x68) == Champion(13) {
        tw = game.get_entity_by_id(bb.+0x260 /*u64 tower id*/)
        if tw.is_some() && tw.ty == Tower(2) && tw.hp(+0x670) != 0 && distance(tw, t) /*1660a00 usqrt*/ <= 250000 { return None } } }
    v3 = true
  }
  // ── 본체 (0.5.8 동일)
  target = game.get_entity_by_id(target_id)                                  // L52
  in_tower = target.map_or(false, |t| engage_requires_dive(player, data, t)) // L52 closure$0 (e7f810 · iter_towers_without_nexus(1-team) 상태 6칸+슬라이스)
  battle = if in_tower {                                                     // L53
    t = get_entity_by_id(target_id)?  (None → return None)                   // L54
    if game.tick() <= self.last_dive_abandon_tick(+0x1f50) + 1 + tps*4 { return None }   // L57 (dive_rejoin_cd) · ★0.6.0 +0x1480→+0x1f50
    if version > 1 {                                                         // L63
      if let Some(me) = cache.player_champion[team][pos(+0xa90)] {           // L64
        if open_chase_race_hopeless(version, data, player, me, t) { return None } } }   // L65 · 콜리 v3: e0ad90→eeb5b0
    if !tower_dive_is_viable(version, rnd, player, data, &self.team_plan(+0xf8), t, true, debug) { return None }   // L70 · 콜리 v3: e07430→ee6d40(변경: v3 near_enemies 클로저·pen 산식 — spec_patch §B)
    bp = BattlePlan::new_dive(version, &TryKill(target_id, 60), data, player) // L73 · 콜리 v3: dfdd50→ddc9f0
    bp.entry_src(+0x213) = 2                                                 // L74
    tower = iter_towers_without_nexus(cache, 1-team).min_by_key(|tw| dist_sq(tw, t))   // L75 closure$1 (첫 최소 · 6칸 인라인 + e7e1e0 fold)
    bp.dive_tower(+0x207) = tower.and_then(|tw| if tw.ty==Tower(2) { Some(tw.info.ty(+0x128) as u8) } else { None })   // L76~77 closure$2 · None = 0xff
    bp
  } else {
    if version > 1 {                                                         // L81
      if let (Some(me), Some(t)) = (player_champion[team][pos], get_entity_by_id(target_id)) {   // L82~83
        if open_chase_race_hopeless(version, data, player, me, t) { return None } } }   // L84 · 콜리 v3: eeb5b0
    bp = BattlePlan::new(version, &TryKill(target_id, 60), data, player)      // L89 · 콜리 v3: dfb5d0→dd8b60
    bp.entry_src(+0x213) = 1                                                 // L90
    bp }
  if version > 1 { bp.main_objective(+0x208 u16, +0x20a u8) = (self.+0xdcd, self.+0xdcf) }   // L96~97 set_main_objective · ★0.6.0 LPH +0x517/+0x519→+0xdcd/+0xdcf · bp +0xff→+0x208/+0x20a
  bp.screening(+0x204) = true                                                // ★0.6.0 신규: update 직전 세팅
  bp.update(version, rnd, player, data, &self.positioning_score(+0x1408), &self.team_plan(+0xf8), debug)   // L99 · 콜리 v3: dfb840→dd8e70 · ★0.6.0 +0x990→+0x1408 (positioning_score) · team_plan +0xf8
  if bp.sub_goal.tag(+0x78) >= 2 /*KitingBack(2)|RunAway(3)|End(4)*/ { bp.screening=false; drop(bp); return None }   // L100~101 · ★0.6.0 재번호(구 {3,4,7})
  // ★0.6.0 신규(v3): 두 번째 update 로 재확인 · +0x98 은 두 값 중 min 유지
  if v3 {
    pre = bp.+0x98 (u64)
    bp.update(version, rnd, player, data, &self.positioning_score(+0x1408), &self.team_plan(+0xf8), debug)   // 2회째
    bp.+0x98 = min(pre, bp.+0x98)
    CNT[0x145029f60] += 1   (텔레메트리)
    if bp.sub_goal.tag == End(4) { CNT[0x14502a310] += 1; drop(bp); return None }   // 2회째는 End 만 거부(KitingBack/RunAway 는 통과)
  }
  bp.screening(+0x204) = false                                               // ★0.6.0 신규
  return Some(bp)                                                            // L102 (0x228B memcpy)

ee8090(data, player, t) -> bool   // ★0.6.0 신규 헬퍼 (RE §3 인용 · 디컴 미대조)
  = t.team == Player(1-team) && t.ty == Champion(13)
    && iter_towers_without_nexus(1-team).any(|tw| tw.can_target(+0x6b9)==1 && tw.block_target_tick(+0x6a0)==0
         && distance(tw, t) <= tw.attack.range(+0x4a0)+tw.stat_buff.range(+0x438)+(tw.level-1)*growth(+0x4a8) (attack None 이면 0) + 8000 + t.radius + tw.radius)
```
// ★v2 사장: `version == 2` 가지(게이트 A/B 생략 · v3=false · update 1회) 및 `version < 2` 가지(게이트 전무 · set_main_objective 생략)는 v3 런타임에서 도달 불가.

## changes
- 게이트 A(신규·v3·target_id≠0): LPH `+0x2060`(대상 id)/`+0x2068`(tick)/`+0x2070`(reason u8) — reason∈{7,13,15,16} 이고 tick ≤ +0x2068+3tps 이면 None(reason 15 는 추가로 ee8090 참일 때만).
- 게이트 B(신규·v3): bb[team]+0x278(u8) 이 2 도 홀수도 아니면 bb+0x260 의 타워가 살아있고 대상 챔피언과 distance ≤ 250000 → None.
- L74/L90 entry_src 저장처 bp+0x213 · L76 dive_tower bp+0x207 · L96 main_objective 복사 LPH+0xdcd/+0xdcf → bp+0x208/+0x20a(구 +0x517/+0x519 → +0xff).
- update 직전 `bp.screening(+0x204)=true`, 반환 직전 false(신규).
- L100 sub_goal 거부 집합 {3,4,7} → `tag>=2`(재번호 동치).
- v3 이면 update 2회째 + `+0x98=min(pre,new)` + End(4) 만 거부(신규).
- 오프셋: last_lost_fight +0x1490/0x1498→+0x2050/0x2058 · last_dive_abandon +0x1480→+0x1f50 · positioning_score +0x990→+0x1408 · team_plan +0xf8.
- 콜리 v3: new dd8b60 · new_dive ddc9f0 · update dd8e70 · open_chase_race_hopeless eeb5b0 · tower_dive_is_viable ee6d40(변경) · 신규 ee8090.

## verified
- Ghidra 0.6.0 d5f3c0 디컴 + 디스어셈 전체: 게이트 A(0x2060/0x2070<0x11/BT 0x1a080/0x2068+3tps/reason 0xf→ee8090) · 게이트 B(0x5c8 stride · +0x278 `!=2 && (st&1)==0` · +0x260 · ty 13/2 · hp≠0 · 1660a00 ≤ 0x3d090) · 다이브 경로(+0x1f50+4tps+1 · eeb5b0 · ee6d40(…,+0xf8,t,1,debug) · ddc9f0 · +0x1d3←2 · +0x1c7←tower.+0x128) · 일반 경로(dd8b60 · +0x1d3←1).
- memcpy 뒤 갭(Ghidra noreturn 오판)을 capstone 으로 확인: `version>1 → bp+0x208/+0x20a ← LPH+0xdcd/+0xdcf` · `bp+0x204←1` · dd8e70(bp,version,rnd,player,data,LPH+0x1408,LPH+0xf8,debug) · `cmp [bp+0x78],1; jbe` · v3 플래그 → `rdi=bp+0x98; update; cmp/저장(min)` · `cmp [bp+0x78],4` → None · `bp+0x204←0` → memcpy 0x228 → Some.
- 미확인: ee8090 내부(RE §3 인용) · +0x2070 reason enum 이름 · bb+0x260/+0x278 필드명 · +0x98 의미(RE 도 미해소).

## confidence
A(본체) / B(ee8090·필드 의미) — 본체는 디스어셈까지 전량 대조, 신규 필드의 의미는 미상.
