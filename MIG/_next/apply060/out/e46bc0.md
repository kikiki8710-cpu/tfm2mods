# e46bc0→d38180 passive_plan

## logic_060
```
// sig 동일 (sret, &self LPH, version, rnd, player, data, debug) -> (BigPlan, u8)
// ★0.6.0 반환 슬롯: BigPlan 384B→0x228(552B) · 발원코드 sret+0x228(구 +0x180). 본문은 로컬 BigPlan + 코드 → 단일 에필로그(memcpy 0x228 + code) · 단 에필로그 직전 「후처리 E」가 sret 에 직접 쓰는 경로 있음.
// ★0.6.0 LPH 오프셋: team_plan +0xf8(tp.X = LPH+0xf8+X) → objective +0xdcd/dce/dcf(tp+0xcd5/6/7) · steal_action tag +0xd92(0 None/1 Lurk/2 Commit)·target +0xd93(0 Epic/≠0 Serpen) · cc7 +0xdbf · cc2 +0xdba · wave_priority_clear_line +0xdcb(tp+0xcd3 i8) · c9e/c9f +0xd96/+0xd97 · epic_giveup_tick +0x168/+0x170(Option) · eo_cover_picks = tp+0x720(LPH+0x818, lock inc)
//   plan +0xf58(PassiveJungle 페이로드 +0xf60 · pj.jungle +0xfe3 · pj.team +0xfc0 · PassiveLine.line +0x107b) · v2_egowave +0x24f0 · v2_egowave_line +0x24d8 · v2_armed +0x24b4 · v3_armed +0x24b5 · v2_obj_part +0x24a2/+0x24a3 · v3_epicops_armed +0x24d4 · last_jungle_lead_action_tick(미확인 오프셋 · pj.+0x448 관련)
//   ★신규 LPH 플래그: +0x24b6(Support 후처리) · +0x24bd(ambient) · +0x24c5(ComebackPick LineGanker) · +0x24c6/+0x24c7(Support 후처리) · +0x24d5(Support 슬롯 제외 · ⚠「exclude_jungler」아님) · +0x24d6(assign 신모드) · +0x24dc(i8 line) · +0x24e5(i8 line 래치 s29) · +0x1ed0(정글 상태 u8 · 2=정상) · +0x1ed1(정글 대체 라인) · +0x20d8(tick · best_jungle_goal allow_invade) · +0x24af(u8)
//   ⇒ version=3 런타임에서 +0x24b6/+0x24c5/c6/c7/+0x24d5/+0x24d6 은 LPH::update 가 `setne(version≠2)` 로 상시 1(r22 c §6).
// ★0.6.0 PassiveLine(BigPlan tag 3) 꼬리 필드: +0x11f u8 · +0x120 mode(0 기본 · 1 = 웨이브우선/SplitEpic/d56ac0 결과 → 후처리 면제 = PassiveLinePlan.v4_split) · +0x121 · +0x122 · +0x123 line · +0x124 i8(-1) · +0x125 i8(ambient 서브코드 · 기본 -1)
// ★0.6.0 BigPlan 태그: Battle = 니치 0/1 · 나머지 idx+2 (PassiveLine 3 · PassiveJungle 7 · ActiveRecall 8 · LineGanker 10 · EpicHuntAndPoke 12 · SerpenHuntAndPoke 14 · AttackNexus 16 · DefenseNexus 17)
// player: team +0xa00 · position +0xa90 · roaming_ratio +0x228 · ego_ratio +0x238 (스케일 0~100 · min(·,100) 클램프)
// legacy_objective(tp) 인라인(6곳 · 확정): if tp.3e4==2 && tp.404==2 { (tp.cd5, tp.cd6, tp.cd7) } else if tp.3e4!=2 { (Serpen(1), phase=tp.3e0, tp.3e1) } else { (Morgard(0), phase=tp.400, tp.401) }
// ★v2 사장: version<2 분기(Repair 의 `version<2 → ActiveRecall`) 및 `+0x24d6==0` 가지(⑫ 구식 B_x · d75840) 는 v3 에서 도달 불가.

fn passive_plan(&self, version, rnd, player, data, debug) -> (BigPlan, u8)
// ① 정글러 스틸 (:1858~1872) — ★0.6.0 필드 교체만
is_jungle = (position == Jungle(1))
if is_jungle {
  match (tp.steal_action.tag(+0xd92), target(+0xd93)) {
    (Lurk(1), Serpen) if serpen_exists(tutorial∈{0,5,7,8}) → return (SerpenHuntAndPoke{focus:false, vision_only:true, flee:false}(tag 14), 1)
    (Lurk(1), Epic)   if morgard_exists(tutorial∉1..=6)  → return (EpicHuntAndPoke{false,true,false}(tag 12), 1)
    (Commit(2), Serpen) if serpen_exists → return (SerpenHuntAndPoke{focus_serpen_only:true, vision_only:false}, 1)
    (Commit(2), Epic)   if morgard_exists → return (EpicHuntAndPoke{focus_epic_only:true, vision_only:false}, 1)
    _ → 계속 } }
// ② 팀 목표 직결 (:1878~1892) — ★0.6.0 objective 태그 JT 재배치(tag-2): 2 DefenseNexus · 3 DefenseLine · 4 AttackNexus · 9 Dive · 10 PressTower · 11 ComebackPick · 5 PressEpic · 6 SplitEpic · 7 Repair · 0 Morgard{phase} · 1 Serpen{phase} · 0xff None
match tp.objective(+0xdcd) {
  4 AttackNexus{line} → return (AttackNexus{team: 1-my_team, line}(tag 16), 2)      // ★0.6.0 구 Nexus(line)
  2 Defense           → return (DefenseNexus{team}(tag 17), 3)
  3 DefenseLine{line} → return (PassiveLine::new(line), 4)
  9 Dive{line}        → return (PassiveLine::new(line), 5)
  _ → 계속 }
// ③ 에고웨이브 (:1897~1920) — ★0.6.0 상수·산식 변경
match self.v2_egowave(+0x24f0) {
  2 → return (PassiveLine::new(self.v2_egowave_line(+0x24d8)), 6)
  0 → if !is_jungle {
        roam = min(player.roaming_ratio(+0x228), 100); ego = min(player.ego_ratio(+0x238), 100)   // ★0.6.0 클램프
        my_line = Top→Top, Mid→Mid, else Bottom ; line = fallback_line(ctx, my_line)
        ms = bb[team].minion_state(line)   (+0/+0x28/+0x50 · from_mid +0x10 · minion_count +0x20)
        if ms.from_mid < -2000 && roam <= 49 /*★0.6.0 roam>49 → skip*/ && ms.minion_count < -2 {
          p = (500 - 10*roam) * (10*ego) / 1000                                          // ★0.6.0 (구 (500-roam)*ego/500 · base<1 불참 조건 소멸)
          if rnd.gen_range(0..1000) /*dcaf70*/ < p { return (PassiveLine::new(line), 6) } } }
  _ → 계속 }
// ④ PressTower (:1928~1939) — ★0.6.0 상수 변경
if tp.objective == PressTower(10){line} {
  if let Some((x,y)) = self.v3_assign_anchor(player, data) /*d55830*/ {
    if is_near_line(ctx,x,y,line) /*19903f0*/ || (player.roaming_ratio(+0x228) >= 40 /*★0.6.0 구 >399*/ && side(line,x,y)) { return (PassiveLine::new(line), 7) } } }
// ⑤ ComebackPick (:1946~1960) — ★0.6.0 정글 LineGanker 조건 추가
if tp.objective == ComebackPick(11){line} {
  if is_jungle {
    ok = if self.+0x24c5 == 1 /*v3 상시*/ { hp_ok = champ.hp*100/max(champ.max_hp,1) >= 41; r = ff0c10(self, line, map) /*★미독: LPH+0x00..+0x78 [Option<usize>;5] 중 하나라도 적측 lane 의 region 3개와 map 거리행렬(map+0x54d8, 27×27)<2*/; hp_ok && r && f1dd50(3, player, data, &tp, line) /*v≥2: f1f280() && f1f3d0(version,player,data,line,0) · ★미독*/ }
         else { f1dd50(3, …) }
    if ok { return (LineGanker{chats:[], setup_limit:0, wait_limit(+0x20): tick+tps*15, line(+0xab), phase(+0xad): Setup(7), own_line(+0xac): None, exposed(+0xa7): false}(tag 10), 8) } }
  else if let Some((x,y)) = v3_assign_anchor(..) { if is_near_line(..) || side(line,x,y) { return (PassiveLine::new(line), 8) } } }
// ⑥ 웨이브 우선 정리 (:1967~1970) — ★0.6.0 재작성
r15 = f18b10(&tp, player, data, self)   // ★0.6.0 should_delay_object_setup_for_wave_priority 래퍼: tp.3e4/404 모드·cd5/cd6·cc7·튜토리얼 검사 후 !f18bf0() · ★미독: f18bf0
if r15 || (moba && moba.epic_minion_buff_time[team](+0x240+team*8) != 0) {
  hint = tp.wave_priority_clear_line(+0xcd3 i8)
  if hint != -1 && wave_priority_clearer_position(player, data, hint, exclude=false) /*f88880 → (bool,pos)*/ == (true, my_pos) { return (PassiveLine::new(hint){mode(+0x120)=1}, 9) } }
// 6b ★0.6.0 신규
if tp.cc7(+0xdbf) && tp.+0xc9e(window_intent) == 2 && f87c30(player, data, 5 /*serpen*/) /*오브젝트 존재·팀 가시·hp<max·f882e0(…,180000,40) ≤ f895f0(…) ≠0 · ★미독 f882e0/f895f0*/ {
  l = f892d0(player, data, &[Top, Mid])   // pick_wave_line: 후보 중 (lead<3 ‖ from_mid<2000 ‖ count<2) 인 것 중 count*1000+lead*10000+from_mid 최소 · -1 없음
  if l != -1 && f88880(player, data, l, exclude=self.+0x24d5) == (true, my_pos) { return (PassiveLine::new(l), 9) } }
// 6c ★0.6.0 신규
if tp.cc7 {
  obj = legacy_objective(tp)
  if obj == Serpen{phase 2} { if line_exists(Bottom) && bottom_lead(+0x23c0+team*8) <= 2 && f88880(player,data,Bottom,excl)==(true,me) { return (PassiveLine(Bottom), 9) } else if Mid 동형 { return (PassiveLine(Mid), 9) } }
  if obj == Morgard{phase 2} || tp.c9f(+0xd97) == 1 { Top 먼저 동형 → (Top, 9) … else Mid 동형 → (Mid, 9) } }
if r15 → goto ⑨ (⑦⑧ 건너뜀)   // 0.5.8 동일
// ⑦ 정글러 손상 캠프 (:1974~1995) — ★0.6.0 objective = legacy_objective(tp)
if is_jungle && legacy_objective(tp) ∈ {Morgard,Serpen} && phase == Setup(1) && self.plan(+0xf58).tag == PassiveJungle(7) {
  me = player_champion[team][Jungle]; if None → ⑧
  pj = &self.plan.payload(+0xf60); camp_live = as_moba().map(|m| m.jungle_runner.get_jungle_live_list(pj.jungle(+0xfe3), pj.team(+0xfc0)==0)).unwrap_or_default()
  if !camp_live.is_empty() { (cx,cy) = map.camp_pos(pj.jungle, pj.team==0)
    if distance_sq(me,(cx,cy)) < 14400000001 && camp_live.any(|id| get_entity_by_id(id).map_or(false,|e| e.hp < e.stat_cached.hp)) { return (PassiveJungle(pj.clone() /*d78920 0x88B*/), 10) } } }
// ⑧ 오브젝트 전용 패시브 (:2000~2034) — ★0.6.0 Some 폐기 조건에 `tp.cc7==0` 추가(cc7 은 v≥2 상시 1 ⇒ 폐기 분기 사장)
if player_champion[team][position].is_some() {
  if legacy_objective(tp) is Serpen{phase} {
    anchor = v3_depart_anchor(v3_armed, team, position, ctx) /*d55a80*/
    match serpen_passive_plan(version, rnd, player, data, &tp, phase, anchor, debug) /*1011d20*/ {
      Some(p) → if v2_armed && tp.cc7==0 && (p.tag&30)==14 && !v2_obj_restore_safe(d56570: version,player,data) { drop → ⑧′ } else { return (p, 11) }
      None → if v2_armed && v2_obj_part(+0x24a2)==1 && +0x24a3 == 0x50|phase && serpen_exists && restore_safe { return (SerpenHuntAndPoke{false,false,false}, 11) } } }
  if legacy_objective(tp) is Morgard{phase} {
    anchor = v3_depart_anchor(..); match epic_passive_plan(version, rnd, player, data, phase, &tp, &anchor, debug) /*fb5ff0*/ {
      Some(p) → if v2_armed && tp.cc7==0 && (p.tag&30)==12 && !restore_safe { drop → ⑧′ } else { return (p, 12) }
      None → if v2_armed && +0x24a2==1 && +0x24a3 == 0x60|phase && morgard_exists && restore_safe { return (EpicHuntAndPoke{false,false,false}, 12) } } } }
// ⑧′ ★0.6.0 신규 ambient (objective Some 일 때)
if tp.objective(+0xdcd) != 0xff {
  (otag, phase) = legacy_objective(tp); setup_like = otag ∈ {0,1} && phase == 1
  go_ambient = false
  if tp.cc7 == 1 && serpen_map(tutorial∈{0,5,7,8}) && tick >= first_spawn(+0x8a8) - 30*tps {
    a = efc410(&tp, 0, data); b = efc410(&tp, 1, data)   // 3상태: 0 임박(deadline-tick ≤ 15tps)/1 진행중/2 없음 · ★미독: 내부 필드(tp+0x70/+0x80/+0x1a8/+0x1b0/+0x1d8/+0x1e0)
    if !(a==2 && b==2) { if self.+0x24bd { go_ambient = true } else { goto ⑩ } } }
  if !go_ambient && setup_like && self.+0x24bd { go_ambient = true }
  if go_ambient {
    match fb8cc0(player, data, &tp, u16{lo=-1, hi=phase}) /*→ fb8da0 3534B · ★미독: 본문(콜리 f892d0×2·f87c30×2·f88880·f89d70·f44580·17d3bc0×5·1660a00×5)*/ {
      Some((p, sub)) → if p is PassiveLine {
            if sub == 3 && self.+0x24d6 && self.+0x24e5 != -1 { p.line(+0x123) = line_exists(+0x24e5) ? +0x24e5 : p.line; p.+0x125 = 3 } else { p.+0x125 = sub } }
          return (p, 29)
      None → if self.+0x24d6 && tp.cc2(+0xdba)==0 && +0x24e5 != -1 && line_exists(+0x24e5) { return (PassiveLine::new(+0x24e5), 29) } else { goto ⑩ } }
  } else { goto ⑩ } }// ⑨ 에픽옵스 커버 라인 (:2040~2107) — 동치 (objective None 일 때만)
if tp.objective == 0xff {
  enemy_buff = as_moba().map_or(false,|m| m.epic_minion_buff_time[1-team]!=0)
  recent_giveup = self.epic_giveup_tick(+0x168 Some/+0x170).is_some_and(|t| tick.saturating_sub(t) <= tps*10)
  our_buff = self.v3_epicops_armed(+0x24d4) && as_moba().map_or(false,|m| m.epic_minion_buff_time[team]!=0)
  if (enemy_buff || recent_giveup || our_buff) && player_champion[team][position].is_some() {
    best=None; best_score=0
    for line in valid_lines(tutorial) /* {0,7,8}:[T,M,B] 5:[M,B] 4:[M] 2:[T] {1,3}:[B] 6:[] */ {
      ms = bb[team].minion_state(line); if ms.from_mid > -3001 || ms.minion_count > -4 → continue
      if d79a70(아군 i≠me 살아있고 is_near_line(ctx,e.pos,line) 카운트 · ★0.6.0 i==4(Support) 슬롯은 +0x24d5 이면 skip) != 0 → continue
      if enemy_buff || recent_giveup || !our_buff { (ax,ay) = v3_assign_anchor(..).unwrap_or(me.pos); ok = is_near_line(ctx,ax,ay,line) || side(line,ax,ay) }
      else { ok = (line 의 포지션 == my position) }
      if ok { score = -ms.minion_count*1000 - ms.from_mid; if score > best_score { best_score=score; best=line } } }
    if let Some(l) = best { if !(enemy_buff || recent_giveup || !our_buff) { tp.eo_cover_picks(+0x720).fetch_add(1) } return (PassiveLine::new(l), 13) } } }
// ⑩ 초반 라인 페이즈 (:2113~2126) — ★0.6.0 정글 분기 변경
if is_line_phase(ctx,tick) /* tutorial∉{0,5,7,8} || tick < first_spawn(+0x8a8) - tps*30 */ {
  match position { Top→(PassiveLine(Top),14) · Mid→(PassiveLine(Mid),14) · Bottom→(PassiveLine(Bottom),14) · Support→(PassiveLine(Bottom),14)
    Jungle → if self.+0x1ed0 == 2 { pj = best_jungle_goal(version, rnd, player, data, &tp, camp_hint=-1, allow_invade = tick >= self.+0x20d8, debug) /*★0.6.0 fafe50 · r22 d: 반환 (camp, owner_team)*/;
                                    pj.+0x448 = (fafe50.al << 24) | (self.+0x24af << 16) | …; pj.last_lead_action_tick = self.last_jungle_lead_action_tick; return (PassiveJungle(pj), 27) }
              else { return (PassiveLine::new(self.+0x1ed1), 14) } } }   // ★0.6.0 정글 귀환 상태기(LPH::update §3-H) 가 +0x1ed0≠2 로 두면 라인으로 대체
// ⑪ 우리 에픽 버프 창 (:2128~2186) — ★0.6.0 분기 변경
if let Some(m) = as_moba() { if m.epic_minion_buff_time[team] != 0 {
    if self.v3_epicops_armed(+0x24d4) && tp.objective != Repair(7) { f = fb74d0(rnd, player, data) /*v3_epic_formation 래퍼 → fb78b0(strategy(player+0x568..),pos,player,data) → (tag: 2=None, line) · ★미독 fb78b0*/; if f.tag != 2 { return (PassiveLine::new(f.line), 15) } }
    if tp.objective == PressEpic(5){l} { return (PassiveLine::new(l), 15) }
    if tp.objective == SplitEpic(6){l} { return (PassiveLine::new(l){mode(+0x120)=1}, 15) }   // ★0.6.0 mode=1
    if tp.objective == Repair(7) {
      // ★v2 사장: version<2 → ActiveRecall 16
      champ = player_champion[team][position]; if None { return (ActiveRecall(tag 8), 16) }
      if champ.hp < champ.stat_cached.hp { if eda830(champ) /*undying 버프 잔여 최대 · undying 아니면 0*/ > tps { return (ActiveRecall, 16) } }   // ★0.6.0 v3: undying 잔여>tps 면 귀환 · 아니면 cand 로 (구 !v3_repair_done → 16)
      // (hp==max 이면 cand 로) }
    cand = [Top if line_exists(Top)/*tutorial∈{0,2,7,8}*/ && enemy(1-team) top_tower|top_tower2 살아있음(+0x180+line*0x20+tier*0x10+e*8), Mid if line_exists(Mid)/*{0,4,5,7,8}*/ && mid_tower*, Bottom if line_exists(Bottom)/*{0,1,3,5,7,8}*/ && bottom_tower*]
    if !cand.is_empty() && player_champion[team][position].is_some() {
      l = cand.iter().min_by_key(d35e00: |line| distance_sq(me.pos, map.region_centers(map+0x6ba0)[map.line_region(line, team, lead[team])])).unwrap()
      return (PassiveLine::new(l), 17) } } }
// ⑪′ ★0.6.0 신규 cc2 플래너 — ★런타임 실측 tp.cc2(+0xdba)=0 상시(spec_patch §A version 행) ⇒ 사장
if tp.cc2 { if let Some((p,c)) = d56ac0(self, player, data) /*4159B · c∈{32,33,34} · ★미독: `champ 생존 → my_line → f1efc0(player,data)==(true,my_pos) → PassiveLine(my_line){mode=1} 32` 이후 pos 별 +0x24a5/+0x24a4/plan 비교·f1da00×24·f1f9c0 → 33/34*/ { return (p, c) } }
// ⑫ 라인 리드 기반 최종 폴백 (:2189~2260) — ★0.6.0 +0x24d6 신모드(v3 상시)
top_lead/mid_lead/bottom_lead = cache.lead[team] (+0x23a0/+0x23b0/+0x23c0 + team*8)   // ★0.6.0 구 +0x21c0/d0/e0
x_cnt = d37ad0(bb[team], x): big_goal 슬롯 i 의 {tag(+0x300+i*0x20)==0(Line), line(+0x301+i*0x20)==x} 카운트 · ★0.6.0 5번째 슬롯(+0x380) 은 +0x24d5==0 일 때만 계수(v3: 제외)
A_x = x_lead < 3
B′_x = d753e0(cache, tutorial, team, x) /*line_exists && lead<3 && champion[pos(x)].is_none() — 구 B_x 에서 x_cnt==0 항 삭제*/ && d750f0(cache, ctx, team, x) == Some(my_pos) /*지정 커버러: Mid→Top→Bottom(x 제외) 중 「챔프 존재 && (라인 없음 ‖ lead≥3)」 첫 포지션*/   // ★0.6.0 (r22 c) · ★v2 사장: +0x24d6==0 → 구 B_x(d75840)
match position {
  Top:    A_top → (Line Top,18) ; else B′_mid → (Mid,19) ; else B′_bottom && champion[Mid] 없음 → (Bottom,19) ; else (Top,18)
  Jungle: if champion[Top] 없음 && champion[Mid] 없음 && d79cc0(살아있는 아군 캠프 수 · 환경 143a6ff30/34) < 3 && champion[Bottom] 없음 {
            B′_mid → (Mid,19) ; else B′_top → (Top,19) ; else B′_bottom → (Bottom,19) ; else (Mid,19) }
          else { if self.+0x1ed0 == 2 { pj = best_jungle_goal(… camp_hint=-1, allow_invade=tick>=+0x20d8 …); pj.last_lead_action_tick = …; (PassiveJungle(pj), 27) } else { (PassiveLine(+0x1ed1), 14) } }   // ★0.6.0 ⑩ 과 동일 대체
  Mid:    A_mid → (Mid,18) ; else B′_top → (Top,19) ; else B′_bottom → (Bottom,19) ; else (Mid,18)
  Bottom: A_bottom → (Bottom,18) ; else B′_top && champion[Mid] 없음 → (Top,19) ; else B′_mid && champion[Top] 없음 → (Mid,19) ; else (Bottom,18)
  Support: (PassiveLine(fallback_line(tutorial, Mid)), 18) }
// 코드 18 경로 계측(★0.6.0 · 판정 무영향): if tp.cc7 && efc410(&tp,0,data)==1 && (tp+0xca2&1 ‖ tp+0xca0&1) → tp+0x888 += 1 (Top) / (tp+0xca3&8 ‖ tp+0xca1&8) (Mid 계열)

// ★0.6.0 후처리 E (에필로그 직전 · 모든 반환 경로 공통 · 로컬 BigPlan 완성 후)
plan = 로컬 BigPlan; code = 발원코드
if position == Support(4) && self.+0x24b6 /*v3 상시*/ {
  if serpen_map && tick >= first_spawn-30tps && code ∈ {4,6,7,8,9,11,12,13,15,17,18,19,29} && plan.tag==3 && plan.mode(+0x120)&1==0 && champ[Support].is_some() && 적 챔프 생존 ≥2 {
    if 아군 Top/Jg/Mid/Bot 전원 None { worst = d55d60(tutorial, bb, team) /*라인별 from_mid 최솟값(≤-2000) 의 최댓값 라인*/; if let Some(l)=worst { sret = PassiveLine{+0x11f=0, line: l}; code = (mode<2 ? code : 31) } }
    else {
      ms = bb.minion_state(plan.line(+0x123))
      if ms.from_mid <= -2000 && ms.minion_count < 0 { /*그대로*/ }
      else if self.+0x24dc != -1 && d358f0(아군(≠me) 중 big_goal Line{+0x24dc} 있음) { sret = PassiveLine(+0x24dc){+0x11f=0} }
      else if self.+0x24c6 { l = d754d0(data, team) /*밀린 라인 발견 후 e6bad0 선택 · ★미독*/; if l != -1 { sret = PassiveLine(l){+0x11f=…}; code = (mode<2 ? code : 31) }
                             else { l = d751f0(team, data) /*bb Line 목표 ‖ 물리근접 기준 가장 가까운 아군 라이너의 라인*/; … 동형 } }
      else if self.+0x24c7 { d55d60 best 라인 }
      else { 코드 30 경로 }   // ★미독: 30/31 분기 세부(부분독) } } }
if position ∈ {Top,Mid,Bottom} && self.+0x24d5 /*v3 상시*/ {
  my_line = position→line
  if serpen_map && tick>=first_spawn-30tps && code ∈ {6,13,19,29} && plan.tag==3 && plan.mode&1==0 && plan.+0x122 != my_line && line_exists(my_line) && champ[me].is_some()
     && d35530(다른 라이너 슬롯{0,2,3} 중 big_goal Line{plan.line} 있음) && !d357f0(슬롯 0..4\{4,me} 중 big_goal Line{my_line} 있음) {
    sret = PassiveLine(my_line){+0x11f=0}; code = 18 } }   // 내가 고른 타라인이 이미 점유됐고 내 라인이 비면 내 라인 복귀
return (plan, code)

// 발원 코드 표(ret.1): 1 스틸 포크 · 2 AttackNexus · 3 DefenseNexus · 4 DefenseLine · 5 Dive 라인 · 6 에고웨이브 · 7 PressTower 앵커 · 8 ComebackPick · 9 웨이브우선 라인(+6b/6c) · 10 손상캠프 유지 · 11 serpen 패시브 · 12 epic 패시브 · 13 에픽옵스 커버 · 14 초반 기본 라인(+정글 대체) · 15 버프창 라인 · 16 Repair 귀환 · 17 버프창 최근접 라인 · 18 리드 폴백(자기/기본 라인 · 후처리 E 복귀) · 19 리드 폴백(다른 라인) · 27 best_jungle_goal 정글 · ★29 ambient · ★30/31 Support 후처리 · ★32/33/34 cc2 플래너(사장)
```

## changes
- 반환 슬롯: BigPlan 0x228 · 코드 sret+0x228.
- ① steal_action 필드 → LPH+0xd92/+0xd93. ② objective 태그 JT 재배치(AttackNexus 4 · DefenseNexus 2 · DefenseLine 3 · Dive 9 · PressTower 10 · ComebackPick 11 · PressEpic 5 · SplitEpic 6 · Repair 7).
- ③ 에고웨이브: `roam>49 → skip` · `p=(500−10·min(roam,100))·(10·min(ego,100))/1000`(구 `(500−roam)·ego/500`) · v2_egowave +0x24f0/+0x24d8.
- ④ PressTower `roaming ≥ 40`(구 >399). ⑤ ComebackPick 정글: `+0x24c5 → hp≥41 && ff0c10 && f1dd50` 게이트 추가.
- ⑥ 웨이브 우선: f18b10 게이트 + `wave_priority_clearer_position(…,exclude)` 4-arg · PassiveLine{mode=1} · 신규 6b(cc7·window_intent==2·f87c30·f892d0) · 6c(legacy_objective phase 2 별 라인).
- ⑦ objective → legacy_objective(tp) 인라인. ⑧ 폐기 조건에 `tp.cc7==0`(상시 1 → 사장). ⑧′ 신규 ambient(efc410·fb8cc0 · 코드 29 · +0x24bd/+0x24d6/+0x24e5).
- ⑩/⑫ 정글: `+0x1ed0==2 ? best_jungle_goal(fafe50, allow_invade=tick≥+0x20d8) : PassiveLine(+0x1ed1)`. ⑪ Repair v3: undying 잔여>tps 면 ActiveRecall · SplitEpic mode=1 · v3_epic_formation fb74d0. ⑪′ cc2 플래너 d56ac0(코드 32~34 · 사장).
- ⑫ B′_x = d753e0 && d750f0==Some(me)(구 B_x 의 x_cnt==0 항 삭제 · 지정 커버러 규칙) · lead 오프셋 +0x23a0.. · d37ad0 Support 슬롯 제외.
- ★신규 후처리 E(Support +0x24b6 · 라이너 +0x24d5 내 라인 복귀 18 · 코드 30/31).
- 콜리 v3: e4aec0→d56570 · e4a780→d55830 · e4a9d0→d55a80 · ec9de0→f88880(4-arg) · d40b20→fafe50(7-arg) · d65620→1011d20 · de92d0→fb5ff0 · de03d0→f18b10 · dea420→fb74d0 · e705a0→d79a70 · 1453260→19903f0 · 131f300→dcaf70 · e45e50→ff0c10 · 신규 efc410/d56ac0/fb8cc0/f87c30/f892d0/f1dd50/eda830/d78920/d35e00/d37ad0/d753e0/d750f0/d55d60/d754d0/d751f0/d35530/d357f0/d358f0/d79cc0.

## verified
- capstone 0.6.0 d38180(3852줄): ③ `roam=min([r15+0x228],100)·ego=min([+0x238],100)`(d38a5c~) · `cmp [rcx+0x10],-0x7d1; jg skip` · `cmp r9,0x31; ja skip` · `cmp dword [rcx+0x20],-3; jg skip` · `(500−10r)·(10e)` imul → `/1000`(0x10624dd3 shr 0x25) · dcaf70(0,1000) · 코드 6(d390b7~d39195) ✓ · sret+0x228 코드 저장(d389c3/d38d94/d3a0b4) ✓.
- 확인 못 한 지점: ④~⑫·후처리 E 는 RE w6 §1-2/§1-3 + r22 c 를 그대로 옮김(스팟체크 생략) · 미독 헬퍼: ff0c10 · f1dd50(f1f280/f1f3d0) · f18b10(f18bf0) · f882e0/f895f0 · fb8da0 ambient 본문 · d56ac0 후반 · efc410 필드 · d754d0→e6bad0 · fb78b0 · 후처리 E 코드 30/31 세부 · last_jungle_lead_action_tick 0.6.0 오프셋.

## confidence
B — 단계 구조·조건·상수·콜리는 확정(asm 직독 근거), 신규 헬퍼 5~6 개 본문이 미독이라 ⑤/⑥/⑧′/후처리 E 의 완전 재현은 후속 RE 필요.