# e7b640→f27ea0 buy_item

## logic_060
```
fn buy_item(_version, rnd, player, _game, context) -> Option<usize>   // lib.rs:1477 · f27ea0
// L1480
let has_low_tier_item = player.info.items(+0x510/+0x518).iter().any(|x| x.tier() < 4);   // vtable+0x70   // ★0.6.0 items +0x4a0/+0x4a8→+0x510/+0x518
if has_low_tier_item { return None; }
// L1487~1491
let pool = context.pool; let item_list = context.item_list(+0x30 → ptr@+8/len@+0x10);
let mut candidate: bumpalo Vec<usize> = Vec::new_in(pool);
// L1493
let tag = player.info.champion.category();                      // ChampionCategory (Box<dyn> +0x580/+0x588 · vtable+0x20)   // ★0.6.0 +0x510/+0x518→+0x580/+0x588
// ★0.6.0 신규(구 L1496 `items.len()>2 → continue` 대체): 활성 보유템 수 선검사
if !player.info.items.is_empty() && !item_list.is_empty() {
  let active_owned = player.info.items.iter()
      .filter(|it| item_list.iter().any(|li| li.is_active()/*vt+0x50*/ && li.name()/*vt+0x58 → &String{ptr@+8,len@+0x10}*/ == it.name()))   // len 동일 && memcmp==0
      .count();
  if active_owned > 3 { return None; }                          // ★0.6.0 (구: 보유 ≤2 일 때만 구매 → 신: 활성 보유 ≤3 이면 구매 가능)
}
// L1495
for (i, item) in item_list.iter().enumerate() {
  // ★0.6.0 삭제: L1496 `if player.info.items.len() > 2 { continue; }` (위 선검사로 대체)
  if !item.is_active() { continue; }                             // L1500 vtable+0x50
  if item.price() > player.info.gold { continue; }               // L1504 vtable+0x68 vs +0xa68   // ★0.6.0 gold +0x998→+0xa68
  if item.tier() != 0 { continue; }                              // L1508 vtable+0x70
  let category = item.category();                                // L1512 vtable+0xa0 (ItemCategory)
  let is_defensive = matches!(category, Defense(2) | MagicResistance(3) | Hp(5));   // L1514
  let is_magic = category == Magic(4);                           // L1515
  let n = player.info.items.len();
  let ok = match tag {                                           // L1518 (0.6.0 점프테이블 143a8d4a4 — Ghidra 미복원 · 0.5.8 동일 구조 인용)
    Melee(0) => {                                                // L1523
      let hp = player.info.champion.stat().hp;                   // vtable+0x30, EntityStat+0x10
      if hp < 1550 { match n { 0 | 2 => category < 2, _ => is_defensive } }        // L1524~1531
      else if player.info.champion.stat().hp < 1800 {                              // L1536 (stat() 재호출 — 0.5.8 주석 그대로)
        if n == 2 { category < 2 } else { is_defensive } }                          // L1537~1544
      else { is_defensive }                                                         // L1551
    }
    Assassin(4) => match n { 0 | 2 => category < 2, _ => is_defensive },           // L1557~1564
    Range(1) => category < 2,                                                       // L1570
    Magician(2) => is_magic,                                                        // L1575
    Util(3) => { let hp = player.info.champion.stat().hp;                           // L1582
                 if hp < 1550 { is_magic } else { is_defensive } }                  // L1583/1586
    _ => unreachable,
  };
  if ok { candidate.push(i); }                                   // L1592
}
// L1595
if candidate.is_empty() { return None; }
// L1596
Some(candidate[rnd.gen_range(0..candidate.len())])
```
// ★v2 사장: 없음(version 미사용).

## changes
- L1496 `if items.len() > 2 { continue }`(루프 불변 → 사실상 「보유 3개 이상이면 후보 0」) **삭제**.
- L1493 뒤 신규: `active_owned = items.iter().filter(|it| item_list.iter().any(|li| li.is_active() && li.name()==it.name())).count(); if active_owned > 3 { return None }` — 보유템 중 「활성 목록에 같은 이름이 있는 것」만 세어 4개 이상이면 구매 안 함(비활성 템은 카운트 제외 · 구 규칙과 임계도 2→3 으로 완화).
- 신규 검사는 `items.len()!=0 && item_list.len()!=0` 일 때만 수행(둘 중 하나가 비면 카운트 0 취급).
- 오프셋: items +0x510/+0x518 · champion Box<dyn> +0x580/+0x588 · gold +0xa68.
- 같은 술어의 헬퍼 `16f2f40(player, list_ptr, list_len) -> usize`(should_recall_to_shop 이 사용)와 동일 술어이나 buy_item 은 인라인.

## verified
- Ghidra 0.6.0 `f27ea0` 디컴: tier<4 조기 None · category(vt+0x20) · 이중 루프 `is_active(vt+0x50) && name(vt+0x58) len==·memcmp==0` → `3 < cnt → return 0` · 본 루프에 len>2 검사 없음 · `is_active && price(vt+0x68)<=gold(+0xa68) && tier(vt+0x70)==0 → category(vt+0xa0)` · 이후 점프테이블(Ghidra "Too many branches" 미복원).
- Ghidra 0.6.0 `16f2f40` 디컴: 동일 술어 확인(참고).
- 미확인: 챔피언 카테고리 switch 본체(점프테이블) — 0.5.8 명세 인용(RE §8 「구와 동일」).

## confidence
A — 변경 지점(선검사·len>2 삭제) 디컴 직접 확인. switch 본체만 0.5.8 인용.
