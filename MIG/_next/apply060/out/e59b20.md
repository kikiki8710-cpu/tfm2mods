# e59b20→d5b980 handle_chat_inner

## logic_060
```
// handler/chat.rs:41 · 0.6.0 d5b980 · sig 동일: fn(&mut LegacyPlanHandler, version, rnd, player, data, from:Position, chat:Chat, misunderstood:bool, debug)
// ★0.6.0 LPH 오프셋: plan +0xf58(태그 u64 · Battle = 니치 0/1 · ForcePassive 2 · PassiveLine 3 · ActiveRecall 8 · LineGanker 10) · team_plan +0xf8(TeamPlan 인라인: objective +0xcd5(+0xdcd) · phase/line +0xcd6 · with_battle/ready +0xcd7 · chats +0x2e8/+0x2f0/+0x2f8 · mf_obj_clear +0x500(+0x5f8)/+0x508(+0x600) · allies +0x488 · gank_start +0x640 · press_tower_start +0x648 · comeback_pick_start +0x650 · comeback_pick_outcomes +0x300 · obj_spawn serpen_spawn_call +0x98 · epic_spawn_call +0x90 · serpen_giveup +0x80/+0x88 · epic_giveup +0x70/+0x78 · ally_battle_stop +0x20+i*0x10 · vision.mia_call +0x570+p*8 · objective_misunderstanding +0x160(tag i64 −1 None · accepted_tick +0x180 · target +0x188) · v4_serpen 씬 +0x3c8(니치 +0x3e4) · v5_morgard +0x3e8(니치 +0x404)) · self.chats cap +0x1198 / ptr +0x11a0 / len +0x11a8(원소 0x18B: tag u8@0 · byte1 · u64@8) · mf_note_swap +0x2230(code)/+0x2238(tick) · ff_note_battle_swap = d56000(self, code, tick) · positioning_score +0x1408 · gank_cancel_tick(0.5.8 +0x1470 · 0.6.0 재확인 필요) · pending_global_ult_target(0.5.8 +0x530.. · d5d700 내부로 이동)
//   챗 무시 게이트 바이트(★0.6.0 신설 · 0 이어야 처리): +0x24b8 GankDive/Cancel-Dive · +0x24b9 DefenseNexus · +0x24ba AttackNexus · +0x24bc DefenseLine/Repair/ComebackPick · +0x24be Serpen/Morgard 계열 · +0x24c1 Mia/BattleStop/HideLine/Cancel/Prepare · +0x24c2 GiveUp · +0x24c3 PressTower · +0x24c4 Battle · +0x24d4 Press/PressChange
//   Chat 태그: Mia 1 · Battle 3 · BattleDive 4 · BattleHelp 6 · BattleStop 7 · HideLine 0xb · HideLineToo 0xc · DefenseLine 0xe · Ok 0xf · Cancel 0x11 · Press 0x15 · PressChange 0x16 · Repair 0x17 · SerpenPrepare 0x18 · SerpenSetup 0x19 · SerpenEnemyHunt 0x1b · SerpenAssemble 0x1c · SerpenHunt 0x1d · SerpenBattle 0x1e · SerpenGiveUp 0x1f · MorgardPrepare 0x21 · MorgardSetup 0x22 · MorgardEnemyHunt 0x24 · MorgardAssemble 0x25 · MorgardHunt 0x26 · MorgardBattle 0x27 · MorgardGiveUp 0x28 · AttackNexus 0x2a · DefenseNexus 0x2b · GankDive 0x2c · PressTower 0x2d · ComebackPick 0x2e
//   MainObjective 태그: Morgard 0 · Serpen 1 · Defense 2 · DefenseLine 3 · Nexus 4 · PressEpic 5 · SplitEpic 6 · Repair 7 · Gank 8 · Dive 9 · PressTower 10 · ComebackPick 11 · None 0xff
// 콜리 v3: ★d5d700(Battle 3종 아웃라인) · d35010 drop BigPlan · d38180 passive_plan(sret 0x180) · d56000 ff_note_battle_swap · ★d56740(에피소드 기록 추정) · d75ad0 calculate_nexus_defense_count · d779e0 MainObjective::eq · dcaf70 gen_range(0..1000) · e753e0 handle_line_defense · e759a0 i_am_chosen_defender(player,data,n,&[]) · ec21b0 BigPlan::goal · ecb0f0 PassiveLinePlan::has_lead · eda830 undying_remaining_ticks · ★f1dd50 v3 갱 게이트 · fb1510 evaluate_gank_opportunity_with_score · ★fb9b70/d35630/d37d10/d37c50(HideLine v3 사전검증 · 정체 미확정)

// chat.rs:41~43 진입
objective_target_before = self.team_plan.objective_target()   // ★0.6.0 씬 우선: v4_serpen(+0x3c8) Some→Serpen(5) · v5_morgard(+0x3e8) Some→Morgard(4) · 레거시 objective 0→4 · 1→5 · 그 외 None(0xff)
chat_target = objective_chat_target(&chat)   // 0x19,0x1b,0x1c,0x1d → Serpen(5) · 0x22,0x24,0x25,0x26 → Morgard(4) · 그 외 None (마스크 0x3a000000/0x7400000000 · 동치)

// chat.rs:45 match chat  (처리 후 대부분 공통 꼬리로 합류)

// ---- Battle(3)/BattleDive(4)/BattleHelp(6) {target_id, _} : chat.rs:139~284 ---- ★0.6.0 전체 아웃라인
if self.ignore_battle(+0x24c4) == 0 {
  kind = chat==BattleDive ? 3 : (chat==BattleHelp ? 2 : 1)
  d5d700(self, version, rnd, player, data, from, kind, chat.target_id, debug)      // ★미독: d5d700(5228B) — 0.5.8 :139~284 본문(is_ignored_battle_enemy · Battle 중 support_target/with_dive · 합류 stake · 글로벌 궁 pending) 이 이동. 내용 대조 미수행 → 재현 시 0.5.8 logic :139~284 를 기준으로 하되 d5d700 별도 RE 필요
}
→ 공통 꼬리(chat_target None 이라 무동작)

// ---- HideLine(0xb)/HideLineToo(0xc) {line, _} : chat.rs:46~108 ----
if +0x24c1 != 0 → 무시(꼬리)
self.team_plan.allies[from] = Some(AllyRegionInfo{last_tick: tick, region: line*2 (Top0/Mid2/Bottom4)})   // :47~68 (+0x488+from*0x10)
if self.plan is PassiveLine(3) {                                                     // :70
  me = player_champion[team][position]; None → :98
  hp_th = 70 − min(roaming(+0x228),100)*20/100                                        // :74~75 (값 동치)
  if line.is_adj_line(p.line) /* Top↔Mid, Mid↔(Top|Bottom), Bottom↔Mid */ && p.has_lead(ecb0f0)(plan+8, version, rnd, player, data, debug) && me.hp*100/me.max_hp >= hp_th {   // :76
    (_,_,score) = evaluate_gank_opportunity_with_score(fb1510)(rnd, player, data, line, 0)   // :78
    go = score >= 0 || rnd.gen_range(0..1000)(dcaf70) >= min(ego(+0x238),100)*7   // :80~83
    // ★0.6.0 v3 사전검증(version>=3):
    //   pre = { x = fb9b70(line, player, data); tmpl = 0x470B copy of 143a70460; out = d35630(&out, &{0, 0x47}, &{tmpl, 0x47, seed(vt+0x20), &x}); out.tag==1 ? d37d10(self, tps, out.+8, out.+0x10) : false }
    //   if from > 2 (Bottom/Support 발신) { partner = player_champion[team][from==3 ? 4 : 3]; if partner None → 꼬리; ok = d37c50(&{tmpl, blackboards, player, &idx, &line_out}); go = go && pre && ok; line = line_out(+0x6df) }   // 라인 재지정
    //   else { go = go && pre }
    //   (★미독: fb9b70/d35630/d37d10/d37c50 정체 — 「갱 콜 사전검증」 추정)
    if go { self.chats.push(HideLineToo{0xc, line, 0}); mf_note_swap(21=0x15, tick); self.plan = LineGanker{tag 10, chats: [], setup_limit: 0, wait_limit: tick+tps*10, line, phase: Setup(7)};   // :88~91
           d56740(self, line, 0, tick, me.x, me.y, player, data, debug) }   // ★0.6.0 신규 호출(에피소드 기록 추정)
  }
}
if self.team_plan.objective(+0xdcd) == None(0xff) {                                  // :98
  my_line = position Top(0)→Top(0), Mid(2)→Mid(1), Bottom(3)→Bottom(2), else skip   // :99~102
  if my_line == line { objective = Gank(8){line}; team_plan.gank_start_tick(+0x640) = tick }   // :106~108
}

// ---- Cancel(0x11) : chat.rs:113~136 ----
if +0x24c1 != 0 → 무시
self.team_plan.allies[from] = None (+0x488+from*0x10+8 = 0xff)
if position==Jungle(1) && self.plan is LineGanker(10) && (from 의 라인 == plan.line) { plan.phase = Cancel(8); self.gank_cancel_tick = tick }   // :115~126
if objective == ComebackPick(11) { comeback_pick_outcomes.last_mut() 의 .1 이 0 이면 6 }   // :131~132
if objective ∈ {Gank 8, ComebackPick 11} || (objective == Dive 9 && +0x24b8 == 0) { mf_note_obj_clear(43=0x2b, tick) → +0x5f8=0x2b/+0x600=tick; objective = None }   // :134~136 (★0.6.0 Dive 는 +0x24b8 게이트 추가)

// ---- Press(0x15)/PressChange(0x16) {line,_} : chat.rs:287~321 ----
if +0x24d4 != 0 → 무시
// ★0.6.0 삭제: `if self.v3_epicops_armed → 무시`(:290)
if objective != None(0xff) && d779e0(&objective, &PressEpic{5, line}) → 무시          // :291 (동치 · eq 호출로 교체)
strategy = is_solorank(vt+0xe8) ? player.+0x568 : game.strategy(vt+0x108)(team); match strategy.morgard_use(+0x14):   // :292~295
  Gather(0): objective = PressEpic(line); if 내 챔피언 살아있음 { chats.push(Ok(0xf)) }              // :297~299
  Split14(1){position}: if position != my position { objective = PressEpic(line); if 살아있음 push(Ok) }   // :305~308
  Split131(2){p1,p2}: if my position ∉ {p1,p2} { objective = PressEpic(line); if 살아있음 push(Ok) }   // :313~316

// ---- Repair(0x17) : chat.rs:324~333 ----
if +0x24bc != 0 → 무시
if objective != Repair(7) {
  objective = Repair; if 살아있음 push(Ok)
  // ★0.6.0 조건 교체 (0.5.8: !(version>1 && plan is Battle))
  if version < 2 || (plan !is Battle(tag<2) && (version == 2 || me.is_none() || eda830(me) <= tps)) {   // ★v3: 언다잉 잔여 > tps 면 전환 안 함 · ★v2 사장: version==2 는 무조건
    ff_note_battle_swap(17=0x11, tick); mf_note_swap(22=0x16, tick); drop(plan); self.plan = ActiveRecall(8) } }

// ---- Mia(1){pos,_} :340 team_plan.vision.mia_call_ticks[pos](+0x570+pos*8) = tick   (+0x24c1 게이트)
// ---- BattleStop(7) :338 team_plan.ally_battle_stop_tick[from](+0x20+from*0x10) = Some(tick)   (+0x24c1 게이트)
// ---- SerpenPrepare(0x18) :487 obj_spawn.serpen_spawn_call_tick(+0x98) = tick ; MorgardPrepare(0x21) :490 epic_spawn_call_tick(+0x90) = tick   (+0x24c1 게이트)

// ---- Serpen 계열(+0x24be 게이트 · objective 가 Serpen(1) 이 아닐 때만 처리) ----
// 공통 「플랜 교체」 = if plan.goal()(ec21b0) != BigGoal::Serpen(3) && !(version>1 && plan is Battle(tag<2)) { ff_note_battle_swap(18=0x12, tick); (p,_) = passive_plan(d38180)(self, version, rnd, player, data, debug); mf_note_swap(23=0x17, tick); drop(plan); self.plan = p }
SerpenSetup(0x19) :345 objective = Serpen{1, Setup(1), false}; :350 플랜 교체; :359 if 살아있음 push(Ok)
SerpenEnemyHunt(0x1b)/SerpenAssemble(0x1c) :366 objective = Serpen{Assemble(2), false}; :371~376 플랜 교체; :380 push(Ok) (생존 체크 없음)
SerpenHunt(0x1d) :385 objective = Serpen{Hunt(3), false}; :389~394 플랜 교체; :398 if 살아있음 push(Ok)
SerpenBattle(0x1e) (objective 가 Serpen 일 때만) :406 objective.with_battle(+0xdcf) = true; :407 push(Ok)
SerpenGiveUp(0x1f) (+0x24c2 게이트 · objective 가 Serpen 일 때만) :413 obj_spawn.serpen_giveup_tick(+0x80/+0x88) = Some(tick); :414 mf_note_obj_clear(44=0x2c); :415 objective = None; :417 if plan.goal()==Serpen(3) { (p,_) = passive_plan(); mf_note_swap(23); plan = p } (Battle 보호 없음); :423 if 살아있음 push(Ok)
// ---- Morgard 계열(+0x24be 게이트 · objective 가 Morgard(0) 가 아닐 때만) ----
MorgardSetup(0x22)/EnemyHunt(0x24)/Assemble(0x25) :430 objective = Morgard{0, Setup(1), false} (3종 모두 Setup); :435 if plan.goal()!=Epic(2) && !(v>1&&Battle) { ff_note_battle_swap(18)[인라인 · Battle 이면 +0x15f8.. 스냅샷]; passive_plan; mf_note_swap(23); plan=p }; :444 push(Ok) (생존 체크 없음)
MorgardHunt(0x26) :449 objective = Morgard{Hunt(3), false}; :453~458 동일 교체; :461 push(Ok)
MorgardBattle(0x27) (Morgard 일 때만) :467 with_battle = true; :468 push(Ok)
MorgardGiveUp(0x28) (+0x24c2 게이트 · Morgard 일 때만) :474 epic_giveup_tick(+0x70/+0x78) = Some(tick); :475 mf_note_obj_clear(45=0x2d); :476 objective=None; :478 if goal()==Epic { passive_plan; mf_note_swap(23); plan=p }; :483 push(Ok)

// ---- AttackNexus(0x2a){line,_} (+0x24ba 게이트 · objective != Nexus(4) 일 때) :494 objective = Nexus(line); :495 if !(v>1&&Battle) { ff_note_battle_swap(19=0x13); mf_note_swap(23); drop; plan = ForcePassive(2) }; :500 if 살아있음 push(Ok)
// ---- DefenseNexus(0x2b) (+0x24b9 게이트 · objective != Defense(2) 일 때) :507 n = calculate_nexus_defense_count(d75ad0)(player,data); :509 if i_am_chosen_defender(e759a0)(player,data,n,&[]) { :512 objective=Defense; :513 if !(v>1&&Battle){ ff_note_battle_swap(20=0x14); mf_note_swap(23); plan=ForcePassive }; :518 if 살아있음 push(Ok) }
// ---- DefenseLine(0xe){line,_} (+0x24bc 게이트) :525 if objective ∈ {Defense 2, DefenseLine 3} → 무시; else :526 if handle_line_defense(e753e0)(version,rnd,player,data,line,debug) { :529 objective = DefenseLine(line); :530 if !(v>1&&Battle){ ff_note_battle_swap(20); mf_note_swap(23); plan=ForcePassive } } else { return (꼬리 생략) }
// ---- GankDive(0x2c){line,_} (+0x24b8 게이트) :542 objective = Dive(9){line}; :543 if 살아있음 push(Ok)
// ---- PressTower(0x2d){line,_} (+0x24c3 게이트) :549 objective = PressTower(10){line}; :550 team_plan.press_tower_start_tick(+0x648) = tick; :551 if !(v>1&&Battle){ ff_note_battle_swap(19)[인라인 · Battle 스냅샷]; mf_note_swap(23); plan=ForcePassive }; :556 if 살아있음 push(Ok)
// ---- ComebackPick(0x2e){line,_} (+0x24bc 게이트) :562 if objective ∈ {None 0xff, Gank 8} { :563 objective = ComebackPick{11, line, ready:false}; :564 comeback_pick_start_tick(+0x650) = tick }
//   :567 match position (JT 143a74848 · 미복구 · 구조 동일 추정) {
//     Jungle: ★0.6.0 `if !f1dd50(3 /*version 상수*/, player, data, &team_plan, line) → 무시` 게이트 추가; :568 if !(v>1&&Battle){ ff_note_battle_swap(21=0x15); mf_note_swap(21=0x15); drop; plan = LineGanker{10, chats:[] (+0xf60 cap 0/ptr 8/len 0), setup_limit: 0, wait_limit(+0xf80): tick+tps*15, line, phase: Setup(7)} }
//     Top: line∈{Top,Mid} 이면 계속 / Mid: 항상 / Bottom(3)·Support(4): line!=Top 이면 계속 → :583 if !(v>1&&Battle){ ff_note_battle_swap(21); mf_note_swap(23); plan=ForcePassive } }

// ---- 공통 꼬리 chat.rs:595~607 ----
if let Some(t) = chat_target {                                                       // :595
  after = self.team_plan.objective_target()   // ★0.6.0 씬 우선(v4_serpen→5 · v5_morgard→4) 후 레거시   // :596
  if after == Some(t) {                                                              // :597
    if misunderstood { if objective_target_before != Some(t) { team_plan.mark_objective_misunderstanding(t, tick) /* (t & 6)==4 이면 +0x160 = Some{None(0), None(0), accepted_tick(+0x180)=tick, target(+0x188)=t} */ } }   // :598~600
    else { team_plan.clear_objective_misunderstanding() /* +0x160 = −1 */ }          // :603
  }
}
return
```

## changes
- Battle/BattleDive/BattleHelp(:139~284) 처리 전체가 `d5d700(self, version, rnd, player, data, from, kind(1 Battle/2 Help/3 Dive), target_id, debug)` 로 아웃라인 · 진입 게이트 `+0x24c4==0`. ★미독(내용 대조 안 됨 · 0.5.8 본문 기준으로 두되 별도 RE 필요).
- 챗 종류별 무시 게이트 바이트(LPH +0x24b8~+0x24d4) 신설 — 0 이어야 처리.
- Press/PressChange: `v3_epicops_armed → 무시` 삭제 → `objective.is_some() && objective == PressEpic{line} → 무시`(d779e0).
- HideLine(:76~91) v3: `go` 산출 뒤 `fb9b70→d35630→(tag1 ? d37d10)` 사전검증(pre) 로 AND · 발신자 Bottom/Support 면 파트너 슬롯 존재 시 d37c50 로 재검증 + 라인 재지정 · LineGanker 생성 후 `d56740(self,line,0,tick,me.x,me.y,player,data,debug)` 호출 추가.
- Repair(:324): 플랜 교체 조건 `!(v>1 && Battle)` → `v<2 ‖ (plan≠Battle && (v==2 ‖ me.is_none() ‖ eda830(me)<=tps))`.
- Cancel(:134): Dive(9) 해제에 `+0x24b8==0` 게이트.
- ComebackPick Jungle 가지: `f1dd50(3, player, data, &team_plan, line)` 게이트(false → 무시) 추가.
- 공통 꼬리 objective_target(): 씬(v4_serpen/v5_morgard) 우선 → 레거시. mark 조건 `(t & 6)==4` 동일.
- 오프셋: 헤더 표(objective +0xdcd · self.chats +0x1198/+0x11a0/+0x11a8 · mf_note_swap +0x2230/+0x2238 · mf_obj_clear +0x5f8/+0x600 · plan +0xf58 · positioning_score +0x1408 · strategy +0x568/vt+0x108 · morgard_use +0x14 · Ok 태그 0xf).
- 콜리 v3: 헤더 표(d35010 · d38180 · d56000 · d75ad0 · d779e0 · dcaf70 · e753e0 · e759a0 · ec21b0 · ecb0f0 · eda830 · fb1510 · 신규 d5d700 · d56740 · f1dd50 · fb9b70 · d35630 · d37d10 · d37c50).
- ★v2 사장: Repair 의 `version==2` 무조건 전환 가지.

## verified
- capstone 0.6.0 d5b980 선형 디스어셈: Battle 가지(+0x24c4 · kind 산출 `cl==6→2 · cl==4→3 · else 1` · d5d700 9인자 · 꼬리 점프) · 게이트 바이트 11종 위치 전부 · Press(+0x24d4 · +0xdcd==0xff ‖ !d779e0(&obj,&{5,line}) · vt+0xe8 → +0x568/+0x578 복사) · Repair(+0x11a8/+0x1198/+0x11a0 push 0xf · `version<=1 → 교체 · plan<2 → 무시 · version==2 → 교체 · me None → 교체 · eda830(me) > tps → 무시` · d56000(0x11) · +0x2230=0x16 · d35010 · +0xf58=8) · Cancel(+0xdcd ∈ {8,0xb} ‖ (9 && +0x24b8==0) → +0x5f8=0x2b/+0x600=tick/+0xdcd=0xff) · AttackNexus(+0x24ba · +0xdcd==4 skip · d56000(0x13) · +0x2230=0x17 · plan=2 · 생존 시 Ok) · HideLine(ecb0f0 · fb1510 · dcaf70 vs ego*7 · `version<3 → 구 go` · fb9b70 · 0x470B memcpy 143a70460 · d35630 · tag==1 → d37d10 else 0 · from>2 → 파트너 슬롯 `[from==3 ? 4 : 3]` · d37c50 · `go & pre & ok` · line=+0x6df · d56740 9인자) · ComebackPick Jungle(f1dd50(ecx=3) · d56000(0x15) · +0x2230=0x15 · plan=0xa · +0xf80 = tick+tps*15).
- Ghidra 0.5.8 e59b20 디컴 대조: Chat::Ok 태그 0xf · 종류 태그 표 · ff_note/mf_note 코드값(17/18/19/20/21/22/23 · clear 43/44/45) 동일.
- 미확인: d5d700 본문 · ComebackPick 포지션 점프테이블(143a74848) 각 가지 · HideLine v3 콜리 4종 정체 · gank_cancel_tick 0.6.0 오프셋.

## confidence
B — 57종 분기·게이트·오프셋은 asm 으로 확인. Battle 3종은 d5d700 미독(0.5.8 본문을 잠정 기준), HideLine v3 사전검증 콜리 정체 미확정.
