# dc2960→ed0400 SmallActionAroundBush::get_input

## logic_060
```
SmallActionAroundBush::get_input(&mut self, version, rnd, player, data, ps, debug) -> Option<Input>   [around.rs:1195 · ed0400]
 entity = data.cache.player_champion[player.info.team(+0xa00)][player.info.position(+0xa90)].unwrap()   (L1196; team<2 아니면 panic_bounds_check 2 · None 이면 unwrap 패닉)   // ★0.6.0 +0x930/+0x9c0→+0xa00/+0xa90
 tcx = min(self.target_x(+0x18)/32000, 29) ; tcy = min(self.target_y(+0x20)/32000, 29)             (L1197~1198; 지역 변수, 클로저에 &참조)
 tower_dodge = TowerDodgeContext::new_tower_avoid_v3(version, player, data, self.target_x, self.target_y)   (L1199; 416B 지역) · 콜리 e609e0
 env = { &version, player, ps, data, &tower_dodge, &self.out_line(+0x70), &tcx, &tcy }   (클로저 환경 8칸 · 순서 0.5.8 과 동일)
 // ★0.6.0 신규: 다른 소액션이 남긴 PathFinder(이름 != "around_bush") 는 폐기
 if self.path_finder.is_some() (태그 +0x6d != 2)
    && !(self.path_finder.name.len(+0x30) == 11 && name[0..8] == "around_b" && name[3..11] == "und_bush"):   // ★0.6.0 (name ptr = +0x28 · movabs 8B×2 겹침 비교)
     drop(self.path_finder)   // ★0.6.0 = HeapFree(+0x50 버퍼) · HeapFree(+0x58 버퍼) · 태그 +0x6d = 2 (name 은 &'static str 라 해제 없음)
 if self.path_finder.is_none():                                                        (L1200)
     self.path_finder = PathFinder::new_target(rnd, data.context(data+8), version, "around_bush"(len 0xb · 0x143a82420), entity.x(+0x660), entity.y(+0x668), self.target_x, self.target_y, &env,
         |sx,sy,nx,ny| check_cell(version, player, ps, data, &tower_dodge, self.out_line, sx,sy,nx,ny, tcx, tcy))   (L1201) · 콜리 e1f610 (반환 후 구 값 태그 != 2 면 +0x50/+0x58 HeapFree 뒤 +0x28..+0x6d 통째 덮어씀 — 위 폐기 경로에선 태그가 이미 2 라 이중 해제 없음)
     if self.path_finder.is_none(): return None (sret 태그 = -1)                        (L1206 — new_target 결과 None 가능)
 pf = self.path_finder.as_mut()  (Some 확정)
 pf.update_path(rnd, data.context, entity.x, entity.y, self.target_x, self.target_y, &env /*같은 클로저(s_0)*/)   (L1206) · 콜리 e2d650
 if self.path_finder.is_none(): return None                                            (L1210 — update_path 뒤 재검사)
 input = pf.get_input(player, data, SafeMoveWithSkill::Safe(1), false)                 (L1210) · 콜리 e5bb90
 if data.context.debug(ctx+0x3b == 1) && input is Move{x:mx, y:my} (태그 0):           (L1211~1212)
     debug.infos(debug+0xa0 · hashbrown<entity_id, Vec<String>>).entry(entity.id(+0x5c0)).or_insert(Vec::new())
          .push(format!("region: {}", map(ctx+0x20).regions(+0x38b8)[entity.y/32000][entity.x/32000]))   (L1213; ex,ey ≥ 960000 이면 panic_bounds_check 30 · [[i64;30];30] stride 0xf0 · fmt 14006ec10)
     debug.lines(debug+0x18 · Vec<0x30B>).push({entity.x, entity.y, mx, my, Color(1,0,1,1)})   (L1214; 색 = 0x143a7b950)
 return Some(input)                                                                    (L1218; sret 4×8B 복사)

클로저 계약(0.5.8 과 동일 · 본 배치는 콜리 내부 대조 안 함): check_cell 12인자를 위 순서로 호출, 반환 PathVerdict 태그로 switch(0 Allow / 1 Soft / 2·3·4 위반 처리 — path_finder 계층). out_line 은 self.out_line(+0x70) 을 매 호출 로드.
```
// ★v2 사장: 없음(이 함수는 version 분기 없음 · version 은 new_tower_avoid_v3 / new_target / 클로저 환경에 값으로만 전달).

## changes
- L1200 `if is_none` 앞 **신규 삽입**: `if self.path_finder.as_ref().is_some_and(|pf| pf.name != "around_bush") { self.path_finder = None; }` — 구현 = `cmp [self+0x30],0xb` · `[name+0]=="around_b"` · `[name+3]=="und_bush"` 셋 중 하나라도 실패 → HeapFree(+0x50) · HeapFree(+0x58) · `mov byte [self+0x6d],2` → new_target 경로로 합류. 효과: 0.5.8 은 다른 소액션(Around/AroundPositionBush "dodge_tower_cell" 등)이 남긴 PathFinder 를 그대로 update_path 했으나 0.6.0 은 새로 생성 → 경로·rnd 소비(new_target 이 rnd 사용)가 달라짐. 재현 시 필수 반영.
- PlayerState `team +0x930→+0xa00` · `position +0x9c0→+0xa90`.
- **self 레이아웃 불변**: +0x18 target_x · +0x20 target_y · +0x28 path_finder(name ptr +0x28 · len +0x30 · 버퍼 +0x50/+0x58 · 태그 +0x6d) · +0x70 out_line — 0.5.8 dc2960 과 동일. 브리핑의 dispcheck 변위 짝 `0x18→0x50 · 0x20→0x18 · 0x50→0x58` 은 삽입 블록(HeapFree 인자 +0x50/+0x58 적재 · target_x/y 재적재 +0x18/+0x20)이 시퀀스를 밀어 생긴 **오배열** — 실제 필드 대응 변화 없음.
- 그 외 본체(29 clamp · 32000 · 960000 · new_tower_avoid_v3 · env 8칸 · get_input(Safe=1,false) · debug region 로그 · add_line 색 [1,0,1,1]) 동일.
- 콜리(0.6.0): new_tower_avoid_v3 **e609e0** · new_target **e1f610**(단일 호출자 = ed0400) · update_path **e2d650**(단일 호출자) · pf get_input **e5bb90** · hashbrown reserve 14386d660 · Vec<String> grow 143960dc0 · lines grow 143821560 · panic_bounds_check 143821813 · unwrap None 143821770.

## verified
- Ghidra 0.6.0 ed0400 디컴 전문: +0xa00/+0xa90 · min(/32000, 0x1d) ×2 · e609e0(version, player, data, tx, ty) · env 8칸 {&version, player, ps, data, &tower_dodge, self+0x70, &tcx, &tcy} · 태그 +0x6d==2 → e1f610(rnd, ctx, version, DAT_143a82420, 0xb, ex, ey, tx, ty, &env) · else 분기의 `+0x30 != 0xb || [name+3] != 0x687375625f646e75 || [name] != 0x625f646e756f7261` → HeapFree(+0x50)·HeapFree(+0x58)·`+0x6d = 2`·goto new_target · e2d650(pf, rnd, ctx, ex, ey, tx, ty, &env) · e5bb90(sret, pf, player, data, 1, false) · ctx+0x3b · Input 태그 0 · id +0x5c0 · debug+0xa0 hashbrown · map+0x38b8 stride 0xf0 · 960000 guard · debug+0x18 push 0x30B · 색 DAT_143a7b950.
- exe .rdata 실바이트: 0x143a82420 = `around_bush` · 0x143a8242b = `region: ` fmt piece · 0x143a7b950 = `[1.0, 0.0, 1.0, 1.0]` f32×4.
- Ghidra 0.5.8 dc2960 디컴 대조: 이름 검사 블록 없음 · 그 외 명령 흐름 1:1(self 오프셋 전부 동일).
- xref: e1f610 · e2d650 코드 호출자 = ed0400 뿐(모노모프 확정). ed0400 자체는 코드 xref 없음(e5e0e0 디스패처 점프테이블 arm 8 — RE r19 표).
- 미확인: 콜리 내부(e1f610/e2d650 의 check_cell 클로저 · e5bb90) 본체는 본 배치 범위 밖(브리핑 callee_changed 없음 = 동치 전제). 런타임 미검증(정적 확정).

## confidence
A — 본체 전문 디컴 대조 · 변경 블록 바이트/문자열 실측 · self 레이아웃 불변을 양 버전 디컴으로 확정(dispcheck 짝은 오배열).