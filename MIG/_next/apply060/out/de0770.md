# de0770→f18f90 TeamPlan::update

## logic_060
```
fn update(&mut self, version, rnd, player, data, debug)
// 인자 레지스터: rcx self(TeamPlan · LPH+0xf8) · rdx version · r8 rnd · r9 data · [rsp+0x28] player · [rsp+0x30] debug
// 콜러: LPH::update ×3(d3d3e1/d3dc4f/d3e3c6) + update_on_dead d521c0. ★정정: +0xcc7 v6_obj 세팅·hunt_call_pending(+0xc98) 정리는 별도 pre-update f0f080(LPH::update 만 호출) — 이 함수엔 없음.
// ★v2 사장: 이 함수에 version 분기 없음(감사 프롤로그가 +0xa88 에 version 을 기록만).

// ── ★0.6.0 신규 프롤로그(f18f90~f1a5a3 ≈5.6KB) = v6 감사 텔레메트리 — 결정 영향 없음(추정 · 소비처 미검증) ──
//   self.+0xa80 = seed ; self.+0xa88 = version
//   챔프 생사 플래그 +0xccb/+0xccd 갱신
//   교전 에피소드: in_fight(+0xcc8) = (적 300000² 내 && bb[team].big_goal[pos].tag(+0x300+pos*0x20) == 5 Battle) ‖ (오브젝트 캠프 250000² 국지전)
//     → +0xa48 누적 · +0xa58 · +0xa60 시작위치 · +0xa70 최근위치 · +0xcca(hp≤10%) · +0xcc9 · plan-tag 별 +0xb90/+0xb98/+0xba0/+0xba8+k*8
//     종료 = f20f60(self, kind, tps)(+0xcc8/9/a=0 · +0xa48/a58=0 · +0x340=0 · debug 시 stderr)
//   이동/idle 감사: +0xa98 last_tick · +0xaa0/aa8/ab0 · +0xa90 · +0xcce/ccf · +0xab8/ac0/ac8 ; plan 이름 문자열(+0x4d8/+0x4e0 · "Line/Force/Battle/Jungle/AtkNex/DefNex/EpicHP/EpicHB/SerpHP/SerpHB/Ganker/GankCover") 분류 → 종료 = f21490(self, kind, tps)
//   디버그(ctx+0x3b ‖ 전역 144f3e15c) 시에만 check_kill_die_tick eda920 ×4 · f263a0 · e0e840 로그(+0x330/+0x350 Vec 레코드)
//   ※ 재현 시: 이 블록의 write 는 전부 self 의 감사 필드(+0x330~+0x350 · +0xa48~+0xbxx · +0xcc8~+0xccf) 이며 아래 레거시 본체·다른 판단함수가 읽는 필드는 없음(추정 · 미검증)

// ── 레거시 본체(f1a5a3~f1d3da) = 0.5.8 L295~L442 동치 · 오프셋만 0.6.0 ──
[L295] self.sanitize_rule_scope(context)   // 콜리 v3: f105d0(self)
[L296] if !self.is_init(+0xcc4) {  // ── init 인라인 (team_plan.rs:281~289)   // ★0.6.0 +0x41c → +0xcc4
[L282]   self.is_init = true; team = player.info.team(+0xa00)
[L285]   if game.get_game_mode() /*vt+0x40*/ is Moba(m) {           // 태그 0 아니면 건너뜀
[L288]     for (idx, jungle) in [(0,Rhino0),(1,Mushroom1),(2,Bee3),(3,Stump2)] {
             self.next_respawn_tick[team][idx]   = m.camps[team*4+jungle].next_respawn(+0x18)     // ★0.6.0 self+0x668 + team*0x20 + idx*8 (구 +0x378) · camps = moba+0x18 stride 0x30
[L289]       self.next_respawn_tick[1-team][idx] = m.camps[(1-team)*4+jungle].next_respawn
           } } }
[L301] if let Some(st) = self.objective_discipline (+0x3c1 != 2) { if st.until_tick(+0x3b8) <= tick { [L302] self.objective_discipline = None(+0x3c1=2) } }   // ★0.6.0 +0x140/+0x149 → +0x3b8/+0x3c1

// ── 정글캠프 리스폰 추적 (L305~320) — team ∈ {0,1} (내 팀 먼저, 그 다음 상대) × idx ∈ [Rhino,Mushroom,Bee,Stump]
[L308] (x,y) = camp_pos_memo(map(ctx+0x20), jungle, side = t != 0)   // ★0.6.0 TLS 메모이즈 1416a7af0 · (xi,yi) = (x/32000, y/32000)
[L311] if game.is_visible_cell(my_team, xi, yi) {                    // vt+0x100 · 인자 = player.info.team
[L314]   if tick >= self.next_respawn_tick[t][idx] {                 // self+0x668+t*0x20+idx*8
[L317]     if !(mode is Moba && m.camps[t*4+jungle].live_list.len(+0x10) != 0) {
[L320]       info = JungleType::get_info(&jungle, setting)   // 콜리 v3: 16febd0 · 272B · respawn_tick @+0x100 · 힙 버퍼 있으면 HeapFree
             self.next_respawn_tick[t][idx] = tick + info.respawn_tick(+0x100)
           } } }

// ── 타임아웃 정리 (L325~346)   // ★0.6.0 allies +0x488+i*0x10{last_tick@0, tag@+8(0xff=None)} · ally_battle_stop +0x20+i*0x10{tag i32@0(1=Some), tick@+8}
[L326] for i in 0..5 { if allies[i].is_some() && allies[i].last_tick + tps*10 < tick { allies[i] = None(0xff) } }   // 각 i 뒤에
[L331]   if let Some(t) = ally_battle_stop_tick[i] { if t + tps*6 < tick { = None(0) } }   (i 별 교대로 실행)
[L338] if let Some(t) = obj_spawn.serpen_giveup_tick(tag +0x80 / tick +0x88) { [L339] if t + tps*20 < tick { [L340] = None } }   // ★0.6.0 구 +0x60 → +0x80/+0x88
[L344] if let Some(t) = obj_spawn.epic_giveup_tick(tag +0x70 / tick +0x78)   { [L345] if t + tps*20 < tick { [L346] = None } }   // ★0.6.0 구 +0x50 → +0x70/+0x78
       // 두 검사 모두 항상 실행(0.5.8 과 동일)

// ── 웨이브 우선 클리어 라인 (L350 → update_wave_priority_clear_line 540~559 인라인)   // ★0.6.0 self.wave_priority_clear_line = +0xcd3(0xff=None) (구 +0x41d)
[L541] minion_wave = if game.vt+0xe8(game) /*is_solorank(r22 d)*/ { player.strategy(+0x568).minion_wave(+0x10) }   // ★0.6.0 Strategy +0x4f8 → +0x568
                     else { game.strategy(team) /*vt+0x108 · 24B*/ .wav(+0x10) }                                        // ★0.6.0 신규 분기: 팀 전략 우선
       if minion_wave == WavePriority(0) {
[L546]   if let Some(line) = self.wave_priority_clear_line(+0xcd3) {
[L547]     if rule_scope::line_exists(tut, line) {                    // 마스크 Top 0x185 / Mid 0x1b1 / Bottom 0x1ab (tut>8 → false)
[L551]       if blackboard[team].<line>_minion_state.minion_count(+0x20 Top / +0x48 Mid / +0x70 Bottom) > 0 {   // ★0.6.0 bb stride 0x5c8
[L552]         self.wave_priority_clear_line = None; [L558] = find_wave_priority_clear_line(player, data) }   // 콜리 v3: f880e0
             else { 유지 }
           } else { self.wave_priority_clear_line = None }
[L558]   } else { self.wave_priority_clear_line = find_wave_priority_clear_line(player, data) }
       } else { self.wave_priority_clear_line = None }

[L351] position = player.info.position(+0xa90)
       if blackboard[team].big_goal[position].tag(+0x300+pos*0x20) == 5 Battle && .focus(+0x308+pos*0x20) != 0 { [L352] self.last_battle_tick(+0x510) = tick }   // ★0.6.0 +0x220 → +0x510 · bb 레코드 +0x2f8+i*0x20{tick@0,tag@+8,focus@+0x10}

// ── 오브젝트 캠프 가시 틱 (L355~369)
[L355] match tutorial { 0|7|8 => {
[L356]   epic_camp = camp_pos_memo(map, Morgard(4), side=team!=0); [L359] if is_visible_cell(team, x/32000, y/32000) { [L360] epic_camp_last_visible_tick(+0xa0) = tick } ; fallthrough }   // ★0.6.0 +0x80 → +0xa0
         5 => serpen only, _ => skip }
[L365]   if tut ∈ {0,5,7,8}: serpen_camp = camp_pos_memo(map, Serpen(5), ..); [L368] if is_visible_cell(..) { [L369] serpen_camp_last_visible_tick(+0xa8) = tick }   // ★0.6.0 +0x88 → +0xa8

// ── 스폰 콜 채팅 (L374~392) — 서포터만, 10틱마다   // ★0.6.0 chats Vec = +0x2e8 cap / +0x2f0 ptr / +0x2f8 len · 원소 0x18B {tag@0, arg@+8, 0@+0x10}
[L374] if position == Support(4) && tick % 10 == 0 {
[L375]   tps1 = max(tps, 1)
[L376]   remain_tick = moba.map_or(0, |m| m.epic.next_respawn_tick(+0x1b0)).saturating_sub(tick)
[L378]   if tutorial ∉ 1..=6 && moba.map_or(false, |m| m.epic.live_list.len(+0x1a8) == 0) {
[L379]     if moba.epic.next_respawn_tick.saturating_sub(self.epic_spawn_call_tick(+0x90)) > tps*20 && remain_tick <= tps*20 {   // ★0.6.0 +0x70 → +0x90
[L381]       self.epic_spawn_call_tick = tick; [L382] remain_second = remain_tick / tps1
[L383]       self.chats.push(Chat::MorgardPrepare(remain_second, 0))   // tag 0x21
           } }
[L386]   remain_tick = moba.map_or(0, |m| m.serpen.next_respawn_tick(+0x1e0)).saturating_sub(tick)
[L387]   if tutorial ∈ {0,5,7,8} && moba.serpen.live_list.len(+0x1d8) == 0 {
[L388]     if serpen.next_respawn_tick.saturating_sub(self.serpen_spawn_call_tick(+0x98)) > tps*20 && remain_tick <= tps*20 {   // ★0.6.0 +0x78 → +0x98
[L390]       self.serpen_spawn_call_tick = tick; [L391] remain_second = remain_tick / tps1
[L392]       self.chats.push(Chat::SerpenPrepare(remain_second, 0))    // tag 0x18
           } } }

// ── 적 시야 추적 + MIA 콜 (L397~442)   // ★0.6.0 vision: last_visible_ticks +0x598+p*8 · last_checked_ticks +0x5c0 · last_visible_distance +0x5e8 · last_visible_pos +0x520+p*0x10 · last_hp_ratio +0x610 · mia_call_ticks +0x570 · last_call_tick +0x708
[L397] my_champ = cache.player_champion[team][position]; [L399] if None → goto EPILOGUE
[L400] for p in game.iter_player() {                              // vt+0x208 · &[PlayerState] stride 0xab0 (구 2528)
[L401]   pteam = p.info.team(+0xa00) (≥2 → panic); pos = p.info.position(+0xa90); c = cache.player_champion[pteam][pos]
[L403]   if pteam != team {                                        // 적만 갱신
[L404]     if let Some(c) = c {
[L406]       if c.is_visible_from(my_champ) {   // my_champ.team.tag(+0) != 0 (Neutral) ‖ c.visible_state[my_champ.team.idx(+8)](+0x38+idx*0x18).tag == Visible(0)
[L407~411]     last_visible_ticks[pos]=tick; last_checked_ticks[pos]=tick; last_visible_distance[pos]=dist²(c,my_champ)(+0x660/+0x668 abs_diff²합);
               last_visible_pos[pos]=(c.x,c.y); last_hp_ratio[pos]=c.hp(+0x670)*100/c.stat_cached.hp(+0x628) (0 이면 div-by-zero 패닉) }
           } else {                                                // 적 챔프 없음(사망)
[L414~416]   last_visible_ticks[pos]=tick; last_checked_ticks[pos]=tick; last_visible_distance[pos]=10^12
[L418]       home = cache.nexus[pteam](+0x170+pteam*8).map(|n| (n.x,n.y)).unwrap_or(map.nexus_pos[pteam](+0x6d50+pteam*0x10))
[L420]       last_visible_pos[pos] = home } }
       // 아군(pteam==team)도 아래 MIA 검사는 통과(0.5.8 과 동일)
[L425]   if self.last_call_tick(+0x708) + tps < tick {
[L426]     lv = last_visible_ticks[pos]; if lv != 0 {
[L428]       if lv + tps*4 < tick {
[L429]         if last_hp_ratio[pos] > 59 {
[L430]           if last_visible_distance[pos] < 28900000000 (170000²) {   // 코드: (d² >> 8) < 112890625
[L431]             if mia_call_ticks[pos](+0x570) < lv + tps*4 {
[L433]               mia_call_ticks[pos] = tick; [L435] last_call_tick = tick
[L436]               self.chats.push(Chat::Mia(p.position, 0)) } } } } } }   // tag 1 · +4: pos u32 · +8: 0
     }

// ── ★0.6.0 신규 에필로그(f1bf92) ──
EPILOGUE:
       self.+0x138..+0x158 (5 qword) = finish_race(player, data)         // 콜리 v3: fe1ad0(sret 40B, player, data) · v3 소비처(end_check f28320 등)의 캐시 · 본체 미독
[L442] return
```

## changes
- 신규 프롤로그 5.6KB(v6 감사 텔레메트리 · f20f60/f21490 · 결정 영향 없음 추정).
- 신규 에필로그: `self.+0x138..+0x158 = fe1ad0 finish_race(player, data)`.
- 웨이브 우선 게이트: `vt+0xe8(game) ? player.strategy(+0x568).+0x10 : game.strategy(team)(vt+0x108).+0x10` — 팀 전략(Strategy.wav) 분기 신규.
- 오프셋: is_init +0xcc4 · next_respawn +0x668 · objective_discipline +0x3b8/+0x3c1 · allies +0x488 · ally_battle_stop +0x20 · giveup +0x70/+0x78, +0x80/+0x88 · spawn_call +0x90/+0x98 · camp_last_visible +0xa0/+0xa8 · wave line +0xcd3 · last_battle_tick +0x510 · chats +0x2e8/+0x2f0/+0x2f8 · vision +0x520/+0x570/+0x598/+0x5c0/+0x5e8/+0x610/+0x708 · PlayerState +0xa00/+0xa90/+0x568 stride 0xab0 · bb stride 0x5c8(+0x300 tag/+0x308 focus) · cache lead 불변 · camps moba+0x18.
- camp_pos → TLS 메모이즈 1416a7af0.
- 정정: +0xcc7 세팅·+0xc98 정리는 f0f080 pre-update(별도) — 이 함수엔 없음.
- 콜리 v3: `f105d0` sanitize · `16febd0` get_info · `f880e0` find_wave_priority_clear_line · `1416a7af0` memo · `fe1ad0` finish_race(신규) · `f20f60`/`f21490`(감사 종료).

## verified
- Ghidra 0.6.0 f18f90 디컴(`MIG\scr060\w11\f18f90.c` 저장본) 레거시 구간: sanitize f105d0 · +0xcc4 init/+0x668 next_respawn(+0x670/+0x678/+0x680 idx) · +0x3b8/+0x3c1 · 캠프 루프(memo · vt+0x100 · vt+0x40 · 16febd0 +0x100) · allies +0x488/+0x490 10s · ally_battle_stop +0x20..+0x68 6s · +0x80/+0x88·+0x70/+0x78 20s · vt+0xe8/vt+0x108/+0x578 · +0xcd3 · 마스크 0x185/0x1b1/0x1ab · bb +0x20/+0x48/+0x70 · f880e0 · +0x300 tag5/+0x308 · +0xa0/+0xa8 · 서포터 10틱 · +0x1b0/+0x1a8/+0x1e0/+0x1d8 · +0x90/+0x98 · chat 0x21/0x18 · +0x2e8~ · iter_player vt+0x208 stride 0xab0 · +0x598/+0x5c0/+0x5e8/+0x520/+0x610 · nexus +0x170/+0x6d50 · MIA +0x708/+0x570/59/`>>8 < 0x6ba9301` · chat tag 1 · 에필로그 fe1ad0 → +0x138..+0x158.
- 미확인: 프롤로그 감사 블록 세부(RE 요약 전재 · 결정 영향 없음은 추정) · fe1ad0 본체 · vt+0xe8 의 정확한 의미(r22 d 「is_solorank」 채택).

## confidence
A(레거시 본체·에필로그) / C(프롤로그 감사 블록 — 요약만).
