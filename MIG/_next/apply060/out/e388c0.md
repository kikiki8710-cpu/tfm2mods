# e388c0→ed3dd0 SerpenCheckSubPlan::score

## logic_060
```
fn score(&self, version, parameter, rnd, player, data, action, debug) -> i64   // serpen_check.rs:123 · self(move_check) 미사용
// ★0.6.0 exe ed3dd0 = SubPlan::score JT 디스패처(jt 3a83bac · 18 엔트리 · idx 17 ObjContest 신설 = f72620 arm(Hide 와 동일)) — SerpenCheck(idx 11 @ed417d) 본체는 그 안에 인라인 · 형제 EpicCheck(idx 8 @ed3fe3) 도 인라인
// ★0.6.0 SmallActionPlay 태그 +0xb1 · idx = tag<3 ? 6 : tag-3 · Around arm 마스크 0xfdfb 의 0 비트 = idx 2 Around / idx 9 LaneMinionPosition(구 0x1fbf3: 2 Around/3 AroundHide/10 LaneMinionPosition — AroundHide 제거 재번호)
let base = interaction_score(version, rnd, player, data, parameter, action, debug);   // L124 · 콜리 v3: f72620(구 d57540 · 동치)
let add = match action.get_action() {   // L125
    Around{target_id} /*idx 2 Around / 9 LaneMinionPosition · payload+0x8*/ => {
        // L127: 살아있는 첫 세르펜 엔티티
        let game = data.cache.game;   // &dyn AbstractGame (+0x0 data, +0x8 vtable)
        let serpen: Option<&Entity> = game.get_game_mode() /*vtable+0x40 → (tag, &moba)*/
            .as_moba() /*tag≠0(Moba) 이면 unwrap_failed*/ .unwrap()
            .jungle_runner.serpen.live_list /*MobaMode+0x1d0 ptr / +0x1d8 len · Vec<usize>*/
            .get(0) /*len==0 → None*/
            .and_then(|id| game.get_entity_by_id(*id) /*vtable+0x1f0*/);
        // L128
        if let Some(serpen) = serpen {
            if serpen.id /*+0x5c0*/ == target_id && !game.is_visible(player.info.team /*+0xa00*/, target_id) /*vtable+0xf8*/ { 10 } else { 0 }
        } else { 0 }
    }
    _ => 0,   // Attack/Skill/Skill2 포함 전부 0
};
let mut score = base + add;   // L124 합산 · L141

// ★0.6.0 신설: SubPlan::score 디스패처 공통 후처리(모든 arm 공통 · asm ed4207~ed42a3) — Line* 서브플랜 + DefensivePlay 특성 한정 「불리한 교환 거부」
if parameter.line_subplan(+0x1501) == 1 {
    let aggr = player.traits.aggressive_play(+0x49d) != 0; let def = player.traits.defensive_play(+0x49e) == 1;
    if aggr || def {                                                                   // (aggr 이면 f71fc0 는 호출되나 결과는 버림 — 텔레메트리만)
        if let Some((a, b)) = trade_ratio(player.team(+0xa00), player.position(+0xa90), data, parameter, action) {   // 콜리 v3: f71fc0(&out, team, pos, data, param, action) · out: tag@0(1=Some) · a@+8 · b@+0x10
            // (텔레메트리 카운터 145029d38[(aggr?0:1)*3] += 1)
            if a > 0 && !aggr && score >= -9998 && b < 2*a { score = -9999; /* (카운터 += 1) */ }
        }
    }
}
return score;

// ── 콜리 v3 요약: f71fc0 trade_ratio(team, pos, data, param, action) -> Option<(a: i64, b: i64)>  (capstone · Ghidra 디컴 실패) ──
//   jt 3a8fd50(idx = tag<3 ? 6 : tag-3): Trace(idx 10) → tgt_id = payload+0x60 · kind 4 / Attack(11) → payload+8 · kind 6 / Skill(12) 7 / Skill2(13) 8 / Ult(14) 9 / 그 외 → None
//   tgt = game.get_entity_by_id(tgt_id)?(vt 0x1f0) ; me = cache.player_champion[team][pos]? ; tgt.ty(+0x68) != 13 → None ; tgt.TeamType(+0/+8) == me.TeamType → None(아군)
//   tp = param.near_enemies(+0x14d8/+0x14f0 · stride 0xd8).iter().find(|e| e.id(+0x58) == tgt.id)? → None 이면 None
//   eff = kind 7 → me+0x4c8(태그 +0x4f8) · 8 → level(+0x5c8)≥3 ? me+0x500 : 더미 · 9 → level≥5 ? me+0x538 : 더미 · 4/6 → me+0x490(태그 +0x4c0)
//   dmg = eff.tag(+0x30)!=-1 ? estimate_damage(1643790)(eff, ctx, me, 표 3a8e0d8, tgt) : 0
//   a_raw = max(0, param.player.applyed(+0x998) + possible_risk(ff21e0)(&param.player(+0x918), data, 45) + param.player.tower_risk(+0x9b0))
//   if eff.tag != -1 && !in_range(1416432c0)(eff, me) {   // 사거리 밖 → 접근 중 피해로 상향
//       reach = me.stat_buff.range(+0x438) + eff.range(+0x10) + (level-1)*eff.growth(+0x18) + eff.vt[0x108](eff, me, tgt) + eff_speed(me) + eff_speed(tgt)   // eff_speed(e) = e.+0x470!=0 ? (e.+0x470+100)*e.+0x680/100 : e.+0x680
//       a_raw = max(a_raw, max(0, applyed + tower_risk + approach_dmg(f76f20)(cache, ctx, near_enemies, me, tgt.pos, reach))) }
//   b_raw = max(0, tp.risk(+0x80) + dmg + possible_risk(ff21e0)(tp, data, 45) + tp.+0x98)
//   a = a_raw*100 / max(me.hp(+0x670), 1) ; b = b_raw*100 / max(tgt.hp, 1) ; Some((a, b))
//   ⇒ a = 내가 받을 위험 % · b = 상대가 받을 위험 % · b < 2a(내 손해 대비 상대 손해가 2배 미만) 이면 거부

★형제 대조: SerpenCheck 는 Around arm 만(공격은 SerpenHunt 몫). is_visible 인자 순서 (team, id). `serpen.id == target_id` 뒤 `is_visible` 단락.
rnd gen_range 사이트: 0(interaction_score 1회 전달) · version 분기 0 · debug 직접 쓰기 0 · self 읽기 0.
ev1 프로브는 디스패처 단위로 잡아야 한다(본체 인라인).
```

## changes
- SerpenCheck arm 자체: 동치(마스크 0x1fbf3→0xfdfb 재번호 · serpen live_list +0x1d0/+0x1d8 · vt 0x40/0x1f0/0xf8 · +10 가산 동일).
- 디스패처 공통 후처리 신설: `if param.+0x1501 && def && !aggr { if let Some((a,b)) = f71fc0(..) { if a>0 && score>=-9998 && b<2a { score = -9999 } } }`.
- JT 18 엔트리(idx 17 ObjContest = f72620 arm) · arm 콜리 표 = RE 배치 E §4.
- 오프셋: PS +0x930→+0xa00 · 콜리 interaction_score d57540→f72620.

## verified
- capstone ed417d~ed4330: f72620 호출 · `sub al,3; cmovae ecx,6` · `bt 0xfdfb` · Around arm(vt 0x40 → 태그 0 · +0x1d8/+0x1d0 · vt 0x1f0 · +0x5c0 == target · vt 0xf8(team +0xa00, target) · 10/0) · 후처리(`cmp [rsi+0x1501],1` · +0x49d/+0x49e · f71fc0(rcx=&out, rdx=team, r8d=pos, r9=data, [+0x20]=param, [+0x28]=action) · out.tag==1 · `test rcx; setle; or al,bpl` · `cmp rax,-0x270e; jl` · `add rcx,rcx; cmp rdx,rcx; jge` · -9999).
- jt.py 3a83bac 18 엔트리 덤프(idx 11 → ed417d · 8 → ed3fe3 · 7/17 → ed3e31).
- capstone f71fc0 전량(1629B): 위 요약의 분기/오프셋/산식. 콜리 ff21e0(possible_risk 변형) · f76f20(접근 피해) · 1416432c0(사거리 판정) 내부 미대조(RE 불확실 항목 그대로).
- Ghidra ed3dd0 디컴은 JT 미복구(참고 불가).

## confidence
A(arm·후처리) / B(f71fc0 산식 요약 — 콜리 3개 내부 미대조).