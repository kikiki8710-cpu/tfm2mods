# dfb840→dd8e70 BattlePlan::update

## logic_060
```
// sig 동일: fn update(&mut self: BattlePlan(0x228), version, rnd, player, data, positioning_score, team_plan: &TeamPlan, debug)
// ★0.6.0 BattlePlan 필드(w2 §0): +0x00/+0x08 support_target(tag/id · BigPlan 니치) · +0x10..+0x28 region · +0x30/+0x38 well_runaway · +0x60/+0x68 main_goal(tag/focus) · +0x78/+0x80 sub_goal(tag/focus)
//   · +0x88/+0x90/+0x98 chats · +0xa0.. v54_reentry_ticks · +0xe8 prev_die_eval · +0x1c0 dive_join_tick · +0x1c8 start_tick · +0x1d0 ff_rescue_tick · +0x1d8 idle_since · +0x1e0/+0x1e8 idle_anchor
//   · +0x1f4/+0x1f5 v54_prev_eval(oor 2=None / dir) · ★+0x1f6 u16 br(battle.rs 줄번호 · 모든 exit 기록) · +0x1f8 with_dive · ★+0x1f9 team_dive · +0x1fa dive_entered · +0x1ff help_called
//   · +0x200 dive_abandoned · +0x207 dive_tower(i8 · -1 None) · +0x208/+0x209/+0x20a main_objective(tag/phase/3B) · +0x20b die_fall_evals · ★+0x20d engage_stamp(i8) · +0x20f dive_abort_src · +0x213 entry_src · +0x215 exit_src · +0x21e exit_sub
//   · ★+0x150 disengage_flip_tick · ★+0x1b0 rebar_die · ★+0x1b8 rebar_reenter (update_v32 가 매 호출 기록)
// ★0.6.0 태그: BattleSubPlanGoal Trace 0 · Kiting 1 · KitingBack 2 · RunAway 3 · End 4 (구 Protect1/Assassin5/AssassinReady6 제거) · BattlePlanGoal TryKill 0 · Support 1 · Response 2 · Avoid 3
// ★0.6.0 인라인 헬퍼: is_ignored_well_enemy = ee7fd0(version,player,e) · is_ignored_battle_enemy = eeb400(version,player,data,e,with_dive) · is_recent_visible = 인라인(bb[1-team].last_seen(+0x3e8+pos*8)+120 ≥ tick · bb stride 0x5c8) 또는 17d3bc0
//   · engage_pair_range(a,b) = max(fight_kit_range_cached dea110(data,a,b), dea110(data,b,a)) (v>1 고정) · max_range_cached = de9900

fn update(self, version, rnd, player, data, positioning_score, team_plan, debug)
 entry_sub_goal = self.sub_goal(+0x78) ; entry_focus = self.sub_goal.focus(+0x80)   // ★0.6.0 에필로그 REBAR/PLANPATH 용 진입 스냅샷
 champ = cache.player_champion[team][pos]  (+0x1e0+team*0x28+pos*8 · team +0xa00 · pos +0xa90) .unwrap()
 // ── 430~431 main_objective 갱신 (무조건)
 objective = team_plan.objective() 인라인 ★0.6.0 이중모드: if tp.+0x3e4(serpen take_born)==2 { if tp.+0x404(morgard)==2 { tp.+0xcd5 u24 } else { tp.+0x400<<8 /*Morgard{phase}*/ } } else { tp.+0x3e0*0x100 + 1 /*Serpen{phase}*/ }
 self.main_objective(+0x208..+0x20a) = fight_model::v25_scoped_battle_objective(version, objective, player, data, self.current_focus())   // ★0.6.0 eec620 (phase 집합 {1,2}→{1,2,4} · w11 §B7)
 //   current_focus() = (sub_goal.tag < 3 ⇒ Some(+0x80)) .or(support_target +0x00==1 ⇒ +0x08) .or(main_goal.tag < 2 ⇒ Some(+0x68))   // ★0.6.0 재번호(구 focus() 6종 → Trace/Kiting/KitingBack)

 // ── 439~442 교전 정지 워치독 (version>1 고정 참)
 if self.sub_goal != End(4) {
   tick = game.tick()
   enemy_near = iter_champions(enemy_team).any(|e| dist_sq(e, champ) <= 250000²)
   moved = utils::distance(champ.pos, self.idle_anchor(+0x1e0/+0x1e8)) > 8000
   active = champ.ty(+0x68)==Champion(13) && !matches!(champ.action_state(+0x70), Idle(0)|Move(2))
   if active || enemy_near || moved || self.idle_since(+0x1d8) == 0 { self.idle_since = tick; self.idle_anchor = champ.pos }
   else if tick.saturating_sub(self.idle_since) >= ((1200 - 4*min(player.param.input_delay_max(+0x200),100))/50) & 0x3e /*= max(input_delay_max/100*2,2) 동일식*/ {
     self.exit_src(+0x215) = 2; self.br(+0x1f6) = 689; self.sub_goal = End(4); goto EPILOGUE }   // ★0.6.0 br 기록
 }
 // ── 452~469 v54 재진입 계측
 (any_in_range, dir_now, oor) = match self.sub_goal.engage_dir() { Trace(0) => (true, +1, 0),
   KitingBack(2)|RunAway(3) => { found = iter_champions(enemy_team).any(|c| is_recent_visible(c) && !is_ignored_battle_enemy(c, self.with_dive)
        && dist_sq(c, champ) <= (engage_pair_range(champ,c)+30000)²); (false, -1, if found {0} else {1}) },
   _ => (false, 0, 0) }
 if let Some((prev_oor, prev_dir)) = self.v54_prev_eval(+0x1f4/+0x1f5) { if prev_dir == -1 && prev_oor && any_in_range { self.v54_reentry_ticks(+0xa0..).push(game.tick()) } }
 self.v54_prev_eval = Some((oor != 0, dir_now))
 // ── 473~491 적 우물
 if is_enemy_well_danger(version, player, champ.x, champ.y) {
   *(u16*)(+0x1f8) = 0 /*with_dive=0, team_dive=0 ★0.6.0*/; self.dive_tower(+0x207) = -1; self.dive_abandoned(+0x200) = 1; self.dive_abort_src(+0x20f) = 3; self.exit_src = 6; self.br = 730; self.sub_goal = RunAway(3);
   self.well_runaway(+0x30/+0x38) = Some(game.tick() + tps*5); goto EPILOGUE }
 if let Some(wr) = self.well_runaway { if game.tick() < wr { self.exit_src = 6; self.sub_goal = RunAway(3); goto EPILOGUE } else { self.well_runaway = None } }
 // ── 496~505 다이브 통과 abort
 if self.with_dive(+0x1f8) {
   near_towers = cache.iter_towers_without_nexus(enemy_team).filter(|t| dist_sq(t, champ) <= 120000²)   // e7f1d0
   nexus = cache.nexus[enemy_team](+0x170+team*8).unwrap()
   if near_towers.any(|t| t.distance(nexus) > champ.distance(nexus) + 60000) { self.with_dive=0; self.team_dive(+0x1f9)=0; self.dive_abandoned=1; self.dive_abort_src=4 } }
 // ── 509~540 도움 콜
 if !(game.tick().saturating_sub(self.start_tick(+0x1c8)) < 30 || self.help_called(+0x1ff)) {
   near_enemies: bumpalo Vec<&Entity> = iter_champions(enemy_team).filter(|c| is_recent_visible(c) && !is_ignored_well_enemy(c) && dist_sq(c,champ) <= (max_range_cached(data,c,champ)+30000)²).collect_in(pool)   // e0e6e0
   near_wide_allies = iter_champions(team).filter(|c| c.id != champ.id && dist_sq(c,champ) <= 108100000000).count()
   if ctx.debug(+0x3b) { … debug.infos push (무영향) }
   me_die_tick = check_kill_die_tick(version, data, player, champ, &near_enemies, &[], false, false, false, &None)   // ★0.6.0 eda920 신 ABI (0,0,0,None)
   if near_wide_allies != 0 && !(me_die_tick > tps || self.help_called || self.sub_goal == RunAway(3)) { self.help_called = 1; self.chats(+0x88..).push(Chat::BattleHelp(champ.id, 0) /*tag 6*/) }
   drop(near_enemies) }
 // ── 549~556 앵커 (version>1 고정)
 (anchor, anchor_margin) = (champ.pos, 0)
 if let Some(t) = self.current_focus().and_then(|id| game.get_entity_by_id(id) /*vt +0x1f0*/) { anchor_margin = if game.tick() == self.start_tick { t.move_speed(+0x640) * tps } else { 0 }; anchor = t.pos }
 // ── 562~579 v1 오브젝트 규율 · 589~594 should_end_object_finish_kill_priority_battle — ★v2 사장(version<2 전용 · br 831/847)
 // ── 598~643 main_objective 별 종료
 if let Some(mo) = self.main_objective(+0x208 != 0xff) {
   match mo {
     Morgard{phase,..}(tag 0) if phase == 3 => { camp = map.camp_pos(Morgard(4), team==0) /*157b720*/; eff = 200000.sat_sub(anchor_margin);
         if dist_sq(anchor, camp) >= eff² && !is_enemy_side(ctx, anchor, team==0) { (debug "DISC-END") self.exit_src=4; self.exit_sub(+0x21e)=3; self.br=869; self.sub_goal=End(4); goto EPILOGUE } }
     Serpen{phase,..}(tag 1) if phase == 3 => { 동일(camp Serpen(5)) → exit_sub=4; br=891 }
     Defense(2) => { nexus = cache.nexus[team].unwrap(); *(u16*)(+0x1f8)=0 /*with_dive·team_dive*/; self.dive_abandoned=1; self.dive_abort_src=5;   // 무조건
         // ★0.6.0 삭제(v3): 넥서스 거리 End — `if version < 3 { eff=300000.sat_sub(margin); if dist_sq(anchor,nexus) >= eff² { exit 4/5; br 914; End } }` 는 v3 에서 도달 불가 }
     _ => {} } }
 // ── 675~680 region 이탈
 if let Some(region) = self.region(+0x10) { if dist_sq(champ.pos, region.center(+0x18/+0x20)) >= region.range(+0x28)² { self.exit_src=5; self.br=953; self.sub_goal=End(4); goto EPILOGUE } }
 // ── 686~701 battle_recency — ★0.6.0 사실상 사장: 게이트 `!recent_start && !kiting_like && sub_goal.tag > 1` 이 재번호 후 {Kiting 1, Trace 0, KitingBack 2, RunAway 3} 를 전부 제외 ⇒ End(4) 일 때만 발화(이미 End 이면 무의미)
 (in_battle, bt) = simulation::battle_recency(game, player.id)   // 16a88b0
 kiting_like = matches!(self.sub_goal, KitingBack(2)|RunAway(3)) ; recent_start = self.start_tick >= game.tick().saturating_sub(120)
 if in_battle { if bt < tick.sat_sub(120) && !recent_start && !kiting_like && self.sub_goal == End { self.exit_src=3; self.br=976; self.sub_goal=End } }
 else if !recent_start && !kiting_like && self.sub_goal == End { self.exit_src=3; self.br=969; self.sub_goal=End }   // (goto EPILOGUE 는 위 두 경우 동일)
 // ── 705~737 KitingBack/RunAway 종료 판정
 if kiting_like {
   risk_all_zero = position_risk_all_zero_near(version, player, data, positioning_score, PositionEvalPurpose::RunAway(3))   // ffdda0
   has_near_visible_enemies = iter_champions(enemy_team).any(|c| dist_sq(c,champ) <= 150000² && !is_ignored_well_enemy(c) && is_recent_visible(c))
   has_runaway_threat = iter_champions(enemy_team).any(|c| v22_visible_enemy_is_runaway_threat(version, player, data, champ, c))
   if self.sub_goal == RunAway(3) {
     if !has_runaway_threat { self.br=996; self.sub_goal = End(4); goto EPILOGUE }
     safe = iter_champions(enemy_team).enumerate().any(|(p,c)| v22_visible_enemy_is_runaway_threat(..) && bb[enemy_team].big_goal[p].tag(+0x2f8+p*0x20+8)==Battle(5) && focus Some(+0x10==1))   // ★0.6.0 bb 레이아웃
   } else { safe = if risk_all_zero { <!recent_start 만으로 결정> } else { has_near_visible_enemies } }
   if !safe && !recent_start { self.sub_goal = End(4); goto EPILOGUE } }
 self.update_v32(version, rnd, player, data, team_plan /*positioning_score 포함 인자열 동일*/, debug)   // dcce40

 EPILOGUE:   // ★0.6.0 신규 — 모든 return 경로가 여기로 합류(update_v32 뒤 포함)
 if version >= 3 {                                                       // ★0.6.0 (디컴 dd8e70 L1975 `local_80 < 3` 스킵)
   stamp = self.engage_stamp(+0x20d)
   if stamp == 1 && matches!(self.sub_goal, KitingBack(2)|RunAway(3)) { self.disengage_flip_tick(+0x150) = game.tick() }
   if stamp == -1 && self.sub_goal == Trace(0) {
     undying = cache.player_champion[team][pos].map_or(false, |c| c.undying(+0x488))
     if !undying && !last_stand_flags(player, data).nexus_last_stand /*db0160 bit0 · e76aa0 메모*/ {
       el = tick.saturating_sub(self.disengage_flip_tick)
       if el <= tps*4 && self.rebar_die(+0x1b0) <= tps + self.rebar_reenter(+0x1b8) {
         (debug "[REBAR] die= reenter= want={sub_goal} keep={entry_sub_goal} br={+0x1f6}")
         self.sub_goal = entry_sub_goal; self.sub_goal.focus(+0x80) = entry_focus } } }   // 이탈 4초 내 Trace 복귀 억제(진입 시 값으로 되돌림)
   self.engage_stamp = match self.sub_goal { KitingBack(2)|RunAway(3) => -1, Trace(0) => 1, _ => 유지 } }
 if ctx.debug && (self.sub_goal != entry_sub_goal || (sub_goal.tag ∈ {0,1,2} && focus != entry_focus)) { debug "PLANPATH T sub= prev= main= entry= exit= dive= scr= br=" }
```

## changes
- 태그 재번호: sub_goal 8→5(Trace0/Kiting1/KitingBack2/RunAway3/End4) · current_focus 는 `tag<3` 로 판정 · support_target(+0x00/+0x08) 이 BigPlan 니치.
- 430~431: objective 읽기 = TeamPlan 이중모드 인라인(+0x3e4/+0x404 씬 → 없으면 +0xcd5) · v25_scoped_battle_objective 콜리 eec620(phase {1,2,4}).
- 모든 exit 에 `br(+0x1f6) = battle.rs 줄번호` 기록 · 우물/다이브 exit 는 `mov word [+0x1f8],0` 로 with_dive·team_dive 동시 클리어.
- 562~594(v27 규율 · should_end) = v<2 전용 → 사장 · 632~643 Defense 넥서스 거리 End = `version<3` 한정 → 사장(무조건 with_dive/abandoned/abort_src=5 쓰기만 남음).
- 686~701 battle_recency End: 재번호로 조건이 End(4) 에서만 참 → 사실상 사장.
- 531 check_kill_die_tick → eda920 신 ABI `(…,&near_enemies,&[],0,0,0,&None)`.
- ★신규 에필로그(v3): REBAR(engage_stamp +0x20d · disengage_flip_tick +0x150 · rebar_die/reenter +0x1b0/+0x1b8 · last_stand_flags db0160) + PLANPATH 디버그.
- 콜리 v3: e0b730→eec620 · d1fe30 조합→eeb400/ee7fd0 · 1323a00→17d3bc0/인라인 · e0cf10/e0d720→de9900/dea110 · ca19b0→e0e6e0 · d73310→e7f1d0 · eb82d0→eda920 · d8ca70→ffdda0 · 131f300→16a88b0 · df36e0→dcce40 · 신규 db0160/1660a00/157b720/13f0790.
- 오프셋: BattlePlan 0x118→0x228 재배치(위 필드표) · player team/pos +0xa00/+0xa90 · bb stride 0x5c8(big_goal +0x2f8+p*0x20 · last_seen +0x3e8) · nexus +0x170 · tps +0x12f8.

## verified
- Ghidra 0.6.0 dd8e70 디컴(2079줄): 에필로그 `if (local_80 < 3) goto` → `+0x20d` 읽기 · `+0x150 = tick` · `+0x1b0 <= tps + +0x1b8` · `el <= tps*4` · sub_goal/focus 를 진입 스냅샷(local_208/uStack_200) 으로 복원 · stamp 갱신(`tag-2<2 → -1`, `tag==0 → 1`) · db0160 호출(L1975~2048) ✓ · Defense 블록 `*(u16*)(+0x1f8)=0; +0x200=1; +0x20f=5; if version<3 { 300000 … exit 4/5 br 0x392 }`(L1830~1852) ✓ · 우물 exit `+0x207=-1,+0x200=1,+0x20f=3,+0x215=6,+0x1f6=0x2da, sub_goal=3, well_runaway=tick+5tps`(L1960~1969) ✓.
- 확인 못 한 지점: 워치독 문턱 인라인식·v54·도움콜·battle_recency 는 RE w2 §1-2 판정(동치) 을 그대로 옮김(디컴 재대조 생략).

## confidence
A — 본체 동치 판정 + 신규 에필로그가 디컴에서 그대로 확인됨. 잔여 불확실 = update_v32 가 +0x1b0/+0x1b8 을 기록하는 정확한 위치(w2 §2 P10 근거).