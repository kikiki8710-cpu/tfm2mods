# d26900→ec4c20 PassiveLinePlan::v46_stage1

## logic_060
```
fn v46_stage1(player, cache, ctx, champ, front, tower_xy: (u64,u64), my_hp, range_gate, only: Option<&[usize]>, near_enemies: &[&Entity]) -> bumpalo::Vec<(usize,usize,usize)>   // passive_line.rs:663 · ec4c20
// ★0.6.0 시그니처: (team, my_pos) 두 인자 → `player: &PlayerState` 하나(team=+0xa00 · my_pos=+0xa90). version 인자는 양버전 모두 없음. my_tower 는 양버전 모두 (x,y) 좌표로 전달(0.5.8 명세의 `my_tower: &Entity` 는 추상 표기).
// 호출자 ec7180(PassiveLinePlan::update) 실인자: (sret, player, cache, ctx, champ, front, tower.x, tower.y, champ.hp(+0x670), range_gate=false, self.only(+0x38 ptr/len), near_enemies ptr/len)

// 준비 (L667~689)
pool = ctx.pool(+0); tps = ctx.setting(+8).tick_per_second(+0x12f8); my_ms = champ.stat_cached.move_speed(+0x640)
my_range = champ.attack_effect(+0x4c0 != -1).map(|f| f.range(+0x4a0) + f.growth_range(+0x4a8)*(level(+0x5c8)-1) + champ.stat_buff_cached.range(+0x438)).unwrap_or(0)   // L671
pull = my_range + champ.radius() + front.radius()          // L672 · radius() = mult(+0x470)==0 ? radius(+0x680) : radius*(mult+100)/100
cs_lock = max(champ.attack_action(+0x570/+0x578).duration(vt+0x80)() * 100 / max(champ.stat_buff_cached.attack_speed_mult(+0x3fc)+100, 1), 3)   // L673 attack_duration() 인라인
escape_ticks = utils::distance(front, tower_xy).saturating_sub(pull) / max(my_ms,1) + cs_lock   // L674 · 콜리 v3: 1660a00(구 12a07d0)
tower_disable_tick = setting[TABLE_143a83698[ctx.tutorial(+0x38)]]   // L677~680 (First|Bottom → 2v2 · MidBottom → 3v3 · 그 외 기본)   // ★0.6.0 테이블 1433d9e58→143a83698
my_towers: bumpalo Vec<&Entity> = cache.iter_towers_without_nexus(team)   // +0x180+team*8 · +0x190 · +0x1a0 · +0x1b0 · +0x1c0 · +0x1d0 Option 6개 + twin_towers(+0x130+team*0x20 ptr / +0x148 len)
    .filter(|t| game.tick() <= tower_disable_tick && utils::distance(t, front).saturating_sub(pull) <= 150000).collect_in(pool)   // L684~688 closure$1 · 콜리 v3: collect 헬퍼 e110e0(구 ca3e10)
committers = Vec::new_in(pool)                              // L689 · grow 헬퍼 dc07c0

for e in near_enemies {                                     // L690
  if only.is_some_and(|ids| !ids.contains(&e.id)) { continue }   // L691 (8-unroll contains)
  ep = cache.player_by_champion_id(e.id).unwrap()          // L694 · 콜리 v3: d72be0(구 d31bb0) · None 이면 패닉
  e_pos = ep.info.position(+0xa90).as_index()              // L695   // ★0.6.0 +0x9c0→+0xa90
  // ★0.6.0 L696~699 재작성: 특성 플래그 게이트(game_core 1416fbbc0 의 인라인판)
  (diff_bound, die_tick_bound, tower_tick_bound) =
    if ep.trait_49d(+0x49d) != 0      { (0,   67, 32) }                       // ★0.6.0 특성 A(공격형 추정): 차이 게이트 0
    else if ep.trait_49e(+0x49e) != 0 { (240, 67, 32) }                       // ★0.6.0 특성 B(수비형 추정): 차이 게이트 240
    else { a = min(ep.info.parameter.aggressive(+0x230), 100)                   // 구 공식 (aggr 0..100 → 0.5.8 명세의 aggr=10a 표기와 동치)
           ( 80 + (80000 - 800*a)/1000,   // L697 diff_bound 80..160
             45 + 450*a/1000,             // L698 die_tick_bound 45..90
             15 + 350*a/1000 ) }          // L699 tower_tick_bound 15..50

  // 적측 킬 능력 (L704~718): 적 팀(1-team) 챔피언 전원(e 자신 포함, 가시성 무관)
  kill_dps = 0; kill_nuke = 0
  for a in cache.player_champion[+0x1e0][1-team].iter().flatten() {
    if distance_sq(a, e) > 150000² { continue }            // L705 (ult 0x53d1ac101)
    if a.hp(+0x670)*100/max(a.stat_cached.hp(+0x628),1) < 40 { continue }  // L708
    (t,p) = 위치 of a.id in cache.player_champion[0..2][0..5] (unwrap 패닉 가드) ; ap = cache.player_state[+0x230][t][p] (None → 패닉) ; c = &cache.player_champion_cache[+0x280][ap.team(+0xa00)][ap.pos(+0xa90)]   // L711~712 · 팀 stride 0x1090 · 포지션 stride 0x350   // ★0.6.0 0xfa0/0x320→0x1090/0x350(_sec 필드 rel<0x230 불변)
    nuke = if a.can_attack()/*164b370*/ { c.attack[my_pos](+0x0) } else { switch a.ty(+0x68) → attack_cooldown 필드 ≤ tps ? c.attack[my_pos] : 0 (점프테이블 143a83a40 · Ghidra 미복원 · 0.5.8 entity.rs:1748 규칙 인용) }   // L714
    if a.can_skill()/*165d410*/  || a.ty!=Champion(0xd) || a.skill_cooldown(+0xb8)  <= tps { nuke = max(nuke, c.skill[my_pos](+0x28)) }   // L715
    if a.can_skill2()/*164b650*/ || a.ty!=Champion      || a.skill2_cooldown(+0xc0) <= tps { nuke = max(nuke, c.skill2[my_pos](+0x50)) }  // L716
    kill_dps  += c.attack_per_sec[my_pos](+0x190) + c.skill_per_sec[my_pos](+0x1b8) + c.skill2_per_sec[my_pos](+0x1e0)   // L717
    kill_nuke += nuke                                       // L718
  }
  my_die = my_hp.saturating_sub(kill_nuke) * 60 / max(kill_dps,1)   // L720 (틱)

  // 우리측 억지력 (L726~749): 내 팀 챔피언 + 내 타워
  det_dps = 0; det_nuke = 0
  for a in cache.player_champion[team].iter().flatten() {
    if distance_sq(a, e) > 150000² { continue }            // L727
    if !a.is_visible_from(e) { continue }                  // L730 인라인: e.team.tag(+0)==Player(0) 이면 a.visible_state[e.team.idx(+8)](+0x38+idx*0x18).tag == 0(Visible) 필요 · idx<2 bounds
    if is_ignored_well_enemy(version, ep, a) { continue }  // L733 인라인: a.team.tag==0 && a.team.idx == 1-ep.team && a 가 적 우물 사각 안(ep.team==1: (x≤64000 ∧ 800000≤y≤960000) ∨ (x≤160000 ∧ 896000≤y≤960000) · ep.team==0: (800000≤x≤960000 ∧ y≤64000) ∨ (896000≤x≤960000 ∧ y≤160000))
    c = ChampionCache of a (L736~737, 위와 같은 조회 · 패닉 가드)
    nuke = (L739~741: 위와 같은 즉발 규칙, 대상 열 = e_pos)
    det_dps  += c.attack_per_sec[e_pos] + c.skill_per_sec[e_pos] + c.skill2_per_sec[e_pos]   // L742
    det_nuke += nuke                                        // L743
  }
  for t in my_towers {                                      // L745
    if let Some(atk) = t.attack_effect(+0x490, 태그 +0x4c0 != -1) {           // L746
      dmg = atk.expected_damage_target(ctx, t as &dyn AbstractEntity(@anon 143a80f00), e)   // L747 · 콜리 v3: 1643790(구 12857f0)
      cool = max(t.attack_action.cooltime(vt+0x90)(t) * 100 / max(t.attack_speed_mult(+0x3fc)+100, 1), 3)   // attack_cooltime() 인라인
      det_dps  += dmg*tps / cool                             // L748
      det_nuke += dmg                                       // L749
    }
  }
  e_die = e.hp.saturating_sub(det_nuke) * 60 / max(det_dps,1)   // L752

  // 게이트 (L755): 둘 중 하나라도 참이면 커밋터 아님
  if e_die < my_die + diff_bound || my_die >= escape_ticks.saturating_sub(tower_tick_bound) { continue }

  // 커밋 판정 (L762~770)
  if e.stat_cached.move_speed(+0x640) > my_ms {              // L762 — 나보다 빠르면 무조건 커밋터
    push
  } else if my_die < die_tick_bound {                       // L764
    if range_gate {                                         // L767
      e_range = e.attack_effect.map(range 공식).unwrap_or(0)           // L768
      // L770: e 가 CS 중인 나에게 닿는가
      if pull + utils::distance(e, front) > e_range + e.move_speed*cs_lock + e.radius() + champ.radius() { continue }
    }
    push
  } else { continue }
  push: [★0.6.0 부작용: env::var("V46_TRACE") 가 설정돼 있으면 stderr 로 트레이스 출력(FUN_142f31f90/142f33b70) — 판정 무관]
        committers.push((e.id, my_die, kill_dps))          // L775
}
return committers                                          // L777 (sret {ptr, pool, cap, len})
```
// ★v2 사장: 없음(version 인자 없음).

## changes
- L696~699 특성 게이트 신규: `(c,b,a') = ep.+0x49d ? (0,67,32) : ep.+0x49e ? (240,67,32) : 구 공식` (game_core 1416fbbc0 과 동일 산식의 인라인판).
- 시그니처: `(team, my_pos)` → `player`(team=+0xa00 · my_pos=+0xa90 내부 로드). 그 외 인자 순서 동일(tower 는 (x,y)).
- 오프셋: PlayerState team/position +0xa00/+0xa90 · 특성 +0x49d/+0x49e(신규) · aggr +0x230 불변 · ChampionCache 팀 stride 0x1090 · 포지션 stride 0x350(사용 필드 +0/+0x28/+0x50/+0x190/+0x1b8/+0x1e0 불변).
- 콜리 RVA: distance 12a07d0→1660a00 · player_by_champion_id d31bb0→d72be0 · can_attack 128cc90→164b370 · can_skill 129ed50→165d410 · can_skill2 128cf70→164b650 · expected_damage_target 12857f0→1643790 · collect ca3e10→e110e0 · grow ccc9c0→dc07c0 · tutorial 테이블 →143a83698.
- 신규 부작용(판정 무관): push 직전 `V46_TRACE` 환경변수 체크 후 트레이스 출력.

## verified
- Ghidra 0.6.0 `ec4c20` 전문 디컴: 준비부(reach·radius·vt+0x80·1660a00(front, p7, p8)·테이블) · only 8-unroll · d72be0 · `+0x49d==0 → c=0xf0 ; +0x49e==0 → a=min(+0x230,100) c=(80000-800a)/1000+0x50 b=450a/1000+0x2d a'=350a/1000+0xf` / `+0x49d!=0 → c=0` · 적측 루프(0x53d1ac101 · hp%<0x28 · 캐시 인덱스 team*0x212+pos*0x6a qword · 164b370/165d410/164b650 · +0xb8/+0xc0 ≤ tps · +0x32/+0x37/+0x3c qword) · `*60/max(dps,1)` · 아측 루프(visible_state +0x38+idx*0x18 · 우물 사각 상수 64000/160000/800000/896000) · 타워 루프(1643790 · vt+0x90 · max(…,3)) · 게이트 `e_die < c+my_die` / `sat_sub(escape,a') <= my_die` · `+0x640 > my_ms` · `b <= my_die` · range_gate · 1660a00(e, front) · V46_TRACE · push(id, my_die, kill_dps).
- Ghidra 0.6.0 호출부 `ec7180 +0x90bc` capstone: rdx=player · r8=cache · r9=ctx · 스택 champ/front/[tower+0x660,+0x668]/champ.hp/0/self+0x38(16B)/near ptr,len — 시그니처 확정.
- Ghidra 0.5.8 `d26900`(ghidra_beta) 디컴 대조: 인자 (team, my_pos) · 특성 분기 없음 · 나머지 동형(0x9c0/0x930 · 0xfa0/0x320 stride).
- 미확인: +0x49d/+0x49e 가 어느 ChampTrait 인지 · can_attack 실패 시 ty 별 attack_cooldown 점프테이블(양버전 Ghidra 미복원 · IR 규칙 인용).

## confidence
A — 변경 지점·시그니처·오프셋 전부 디컴/호출부 대조. 특성 플래그 정체만 미상.
