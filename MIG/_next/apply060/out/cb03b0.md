# cb03b0→e96780 EpicCheckSubPlan::action_candidates

## logic_060
```
// epic_check.rs:14 · 0.6.0 e96780 · ★0.6.0 인자 9→8: fn(sret, &mut self, version, rnd, player, data, parameter, team_plan) — `debug` 삭제(rs:52~53 로그 제거)
// ★0.6.0 오프셋: self.move_check = self+0 (u8) · PlayerState team +0xa00 · pos +0xa90 · Entity ty +0x68 · action_state +0x70 · level +0x5c8 · skill +0x4c8(casting +0x4f8) · skill2 +0x500(+0x530) · ult +0x538(+0x568) · xy +0x660/+0x668 · hp +0x670 · max_hp +0x628 · id +0x5c0 · stat_buff.range +0x438 · radius +0x680 · mult +0x470
//   TeamPlan v10_armed +0xcc0 · ctx.tutorial +0x38 · ctx.map +0x20 · setting width +0x12b8 · height +0x12c0 · cache.others[team] ptr +0x130+team*0x20 / len +0x148+team*0x20 · bb stride 0x5c8 · last_seen +0x3e8+pos*8
//   SmallActionPlay 0xb8B · 태그 +0xb1: RunAway 3 · AroundPosition 6 · Trace 0xd(구 0xe)
// 콜리 v3: nontarget_windup_perceived de6080 · position_score_at_position d84db0→ff5ec0(version,player,data,cx,cy,0xb) · v25_objective_posture dd26e0→f09860(sret,tp,version,player,data,4) · AroundPosition 내부 new d31d20→dde330(rnd,x,y,60000) · camp_pos memo ffa3e0→16a7af0 · Vec grow/extend ccc7a0/cada80→dc05a0/e920f0 · battle_action eb6100→ed6300 · attack_summon_action ed8550 · 신규 165e110 slot→pos idx · db8970 앵커 fold · REQ 표 143a802c8 · BIAS 표 143a802a0

fn action_candidates(&mut self, version, rnd, player, data, parameter, team_plan) -> Vec<SmallActionPlay> {
  let bump = data.context.pool;  let mut res = Vec::new_in(bump);                        // rs:15
  let team = player.info.team; assert!(team<2);                                             // rs:17
  let champ = data.cache.player_champion[team][player.info.position].unwrap();              // rs:17
  // rs:23~31  (attack_nexus 와 동일 구조 · closure#0)
  let has_non_target_action_range = data.cache.player_champion[1-team].iter().flatten().any(|c|
      nontarget_windup_perceived(version, player, data, c) && c.ty==Champion(13) && {
          let eff = match c.action_state.tag { 4=>skill_effect(casting 1|2 아니면 false, −1 panic), 5=>(level>2 ? skill2_effect : NONE표 143a7e940)…, 6=>(level>4 ? ult_effect : NONE)…, _=>return false };
          Effect::is_in_range(eff, c, champ) /* range+(lv-1)*growth+stat_buff.range+range_adjust(vt+0x108)+c.radius()+champ.radius() 제곱 비교 */ });
  // rs:36~38
  let ps = position_score_at_position(version, player, data, clamp(champ.x/32000,29)*32000+16000, clamp(champ.y/32000,29)*32000+16000, PositionEvalPurpose::Objective /*11*/);   // 셀 중심 6인자(0.5.8 부터 동일 · parameter 미전달)
  if ps.on_trajectory || (has_non_target_action_range || ps.on_periodic_trajectory) {         // rs:38
      res.push(RunAway(SmallActionRunAway::new_with_skill(data, player, 5, true)));           // rs:40  tag 3 (end_delay 5 · with_skill=1 · path_finder None)
      return res;                                                                            // rs:41
  }
  // rs:45~55  v25 오브젝트 자세
  let posture = team_plan.v25_objective_posture(version, player, data, JungleType::Morgard /*4*/);   // rs:45  f09860 sret 88B (+0 tag −1=None · kind u8 @+0x50 · wait_pos @+0x10/+0x18 · near_enemy_count @+0x38 · focus_enemy Some @+0 bit0 / id @+8)
  if let Some(posture) = posture {                                                           // rs:46
      if posture.kind as u8 > 2 {                                                            // rs:47  WaitGroup(3) | SoftDisengage(4)
          if posture.kind == SoftDisengage(4) && posture.near_enemy_count != 0 {              // rs:48
              res.push(RunAway(SmallActionRunAway::new_with_skill(data, player, 5, false)));  // rs:49  tag 3 · with_skill=false
          }
          res.push(AroundPosition(SmallActionAroundPosition::new(rnd, data, posture.wait_pos.0, posture.wait_pos.1, 5)));   // rs:51  tag 6 (radius 60000 · 80000 필드 · delay 5)
          // ★0.6.0 삭제: rs:52~53 `if ctx.debug { debug.infos[champ.id].push("v25 morgard check posture: {:?}") }`
          return res;                                                                        // rs:55
      } else if posture.kind == Screen(2) && posture.focus_enemy.is_some() {                 // rs:58
          let focus_enemy = posture.focus_enemy.unwrap();                                    // rs:59
          res.push(Trace(SmallActionTrace::new_attack_range(data, focus_enemy, 5)));         // rs:60  tag 0xd ★0.6.0 0xe→0xd (margin 15000 · attack_range_only=true · goal=get_entity_by_id(focus_enemy).xy or (0,0))
          // ⚠return 없음 — 아래 rs:65 로 계속
      }
      // Commit(0)/HoldCamp(1) 또는 Screen+focus None → 아래로
  }
  // rs:65~75  사이드 경유 검사 (self.move_check 갱신) — ★0.6.0 재작성(SerpenCheck 와 동일 패턴)
  let mut mc_now = true;
  if !self.move_check {                                                                      // rs:65
      if ((champ.x - champ.y + setting.height) > setting.width) == (team==0) {               // rs:66 is_enemy_side(ctx, team, x, y) — 적 진영이면 즉시 통과
          self.move_check = true;
      } else {
          let stump = map.camp_pos(JungleType::Stump /*2*/, team==0);                       // rs:69
          if champ.dist_sq(stump) < 4_900_000_001 /*70000²+1*/ {                             // rs:71
              if !team_plan.v10_armed /*+0xcc0*/ { self.move_check = true; }                  // ★0.6.0 구 동작(v≥2 에선 항상 armed=1 → 아래 경로만 실행)
              else {                                                                          // ★0.6.0 신규
                  let morgard = map.camp_pos(JungleType::Morgard /*4*/, team==0);
                  let near = player_champion[team].iter().flatten()                           // 자기 포함 · 5슬롯 언롤
                      .filter(|a| a.hp*100/a.max_hp >= 50 /*max_hp==0 → div0 panic*/ && a.dist_sq(morgard) < 48_400_000_001 /*220000²+1*/).count();
                  let need = REQ_143a802c8[ctx.tutorial];                                     // [3,2,1,2,1,3,1,3,3]
                  let anchor = player_champion[team].iter().enumerate().flatten()             // 첫 hp≥40 슬롯을 초기값으로 db8970 fold
                      .filter(|(_,a)| a.hp*100/a.max_hp >= 40)
                      .min_by_key(|(slot,a)| distance(a.xy, morgard)(1660a00) + BIAS_143a802a0[165e110(slot)]);   // BIAS = [60000,0,40000,60000,20000] · 동점=앞
                  let ok = anchor.is_none() ? (near >= need) : (near >= need || anchor.slot == my_position);
                  if ok { self.move_check = true; } else { mc_now = false; }
              }
          } else { mc_now = false; }
      }
  }
  // rs:77~87  모르가드 캠프 접근
  let camp = map.camp_pos(JungleType::Morgard /*4*/, team==0);                               // rs:77
  if champ.dist_sq(camp) > 22_500_000_000 /*150000²*/ {                                      // rs:78  멀다
      if mc_now {                                                                            // rs:79
          res.push(AroundPosition(SmallActionAroundPosition::new(rnd, data, camp.0, camp.1, 5)));           // rs:80  모르가드 캠프로
      } else {
          let c2 = map.camp_pos(JungleType::Stump /*2*/, team==0);                            // rs:83
          res.push(AroundPosition(SmallActionAroundPosition::new(rnd, data, c2.0, c2.1, 5)));               // rs:84  Stump 경유지로
      }
  } else {
      res.push(AroundPosition(SmallActionAroundPosition::new(rnd, data, camp.0, camp.1, 5)));               // rs:87  캠프 근처 대기
  }
  // rs:90~95  적 근접 → 도주 후보 추가
  let enemy_near = player_champion[1-team].iter().flatten().any(|c|                          // rs:90  closure#1
          (game.is_visible(vt+0xf8)(team, c.id) || bb[1-team].last_seen[pos(c)] + 0x78 >= tick)   // is_recent_visible 인라인 · ⚠bb 인덱스 = 1-team
          && c.dist_sq(champ) < 22_500_000_001)                                              // rs:91
      || cache.others[1-team].iter().any(|x| x.dist_sq(champ) < 22_500_000_001);            // rs:92  closure#2 (첫 절 참이면 미평가)
  if enemy_near {
      res.push(RunAway(SmallActionRunAway::new(data, player, 5)));                           // rs:95  tag 3
  }
  // rs:98~101
  if game.is_visible(vt+0xf8)(1-team, champ.id) {                                            // rs:98
      res.extend(battle_action(version, rnd, player, data, 5));                              // rs:99  ed6300
  }
  res.extend(attack_summon_action(player, data));                                            // rs:101 ed8550
  res                                                                                        // rs:103
}
```

## changes
- 인자 9→8(`debug` 삭제) · rs:52~53 디버그 로그 제거.
- rs:65~75 재작성: `!move_check` 일 때 적 진영 → move_check=true / 자기 진영 → Stump 70000 이내면 `!v10_armed ? move_check=true : (Morgard 220000 내 hp≥50 아군 수 ≥ REQ[tutorial] ‖ hp≥40 앵커(dist+BIAS 최소·동점 앞)==나 → move_check=true, 아니면 mc_now=false)` · Stump 밖 → mc_now=false. (0.5.8 은 Stump 70000 이내면 무조건 move_check=true)
- Trace 태그 0xe→0xd. RunAway 3 · AroundPosition 6 은 동일.
- rs:36 position_score 는 0.5.8 부터 셀 중심 6인자(동치 · 명세 표기만 정정).
- 오프셋: TeamPlan v10_armed +0xcc0 · bb stride 0x5c8/last_seen +0x3e8 · others +0x130/+0x148+team*0x20.
- 콜리 v3: ff5ec0 · f09860 · dde330 · 16a7af0 · dc05a0/e920f0 · ed6300 · ed8550 · de6080 · 신규 165e110 · db8970.

## verified
- Ghidra 0.6.0 e96780 디컴 전량 정독: 인자 8(param_8 = team_plan) · 논타겟 사거리 판정(action_state 4/5/6 · NONE 표 143a7e940) · ff5ec0 6인자 셀 중심(clamp 0x1d) · RunAway 필드(0x7d00/0xe2900 · with_skill 1/0 · 태그 3) · f09860(…,4) · kind>2/==4&&+0x38!=0 · AroundPosition(dde330 60000 · 80000 · 태그 6) · Screen(2)&&bit0 → Trace(15000 · 태그 0xd) · is_enemy_side `(x−y+height) > width == (team==0)` · Stump `< 0x124101101` · `+0xcc0==0 → move_check=1` · Morgard 5슬롯 hp%≥50(`<0x32` 제외)·`< 0xb44dd6401` · REQ 143a802c8[tutorial] · 앵커 첫 hp%≥40(`<0x28` 제외) → db8970(BIAS 143a802a0[165e110(slot)] + 1660a00) · `anchor None → near<need → false` / `near<need && anchor.slot != my_pos → false` · 이후 rs:77~ 150000² 분기.
- 디컴 꼬리(rs:90~103)는 Ghidra memcpy 아티팩트로 절단 — RE w10 §2 「그대로」 판정 채택.

## confidence
A — 변경 구역(rs:65~75) 상수·표·분기 전부 디컴 확인. rs:90~103 은 RE 판정 의존(구조 동일).
