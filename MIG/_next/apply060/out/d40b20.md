# d40b20→fafe50 best_jungle_goal

## logic_060
```
fn best_jungle_goal(version, rnd, player, data, team_plan, now_camp: Option<JungleType> /*u8 · 0xff=None*/,
                    allow_invade: bool /* ★0.6.0 신규 7번째 인자 */, debug) -> (JungleType, owner_team: usize)   // ★0.6.0 반환 = (camp AL, owner_team RDX)
// ★0.6.0 콜러 값: passive_plan d3b340/d3c6b9 = `tick >= LPH+0x20d8` · PassiveJungle fa31fe = `tick >= self+0x78` · fa66b6 = true
//   콜러 PassiveJunglePlan 은 (.jungle +0x83, .team +0x60) 에 저장 — 블록 B 만 owner_team = 1−team, 그 외 = team
// ★v2 사장: version<3 은 [807~835] 만(A·게이트·B·C′ 없음)
// 인자 레지스터: rcx version · rdx rnd · r8 player · r9 data · [rsp+0x28] team_plan · [+0x30] now_camp · [+0x38] allow_invade · [+0x40] debug
cache = data.cache(+0) ; game = cache.game(+0 ptr/+8 vt) ; ctx = data.context(+8) ; setting = ctx+8 ; map = ctx+0x20 ; tut = ctx.tutorial(+0x38)
team = player.info.team(+0xa00) (≥2 → panic_bounds_check) ; pos = player.info.position(+0xa90) ; enemy = 1 − team

[807] jungle_camps: [JungleType;4] = [Rhino(0), Mushroom(1), Bee(3), Stump(2)]   // 0x02030100 · 반환 캠프 필터 없음(now_camp 폴백 → Morgard/Serpen 가능 · 0.5.8 주석 유지)

// ── ★0.6.0 블록 A(fafe8e~fb01d3 · dbd4e0 첫 통과 + db7720 fold): 내 진영 「살아 있는」 캠프 중 120000² 안 최근접 ──
[A]   champ = cache.player_champion[team][pos]                         // cache+0x1e0+team*0x28+pos*8
      if champ.is_some() && game.get_game_mode() /*vt+0x40 · tag0=Some(moba)*/ is Some(moba) {
          // ★좌표 = 하드코딩 표(3a77068 X / 3a77098 Y · idx=판별자) · **팀 1 방향으로 저장 · 팀 0 은 (Y[c],X[c]) 스왑**(r22 d 정정 · 팀0 Rhino=(496000,752000))
          X = [752000 /*Rhino0*/, 592000 /*Mushroom1*/, 448000 /*Stump2*/, 800000 /*Bee3*/]
          Y = [496000,            176000,               256000,            351000        ]
          own_xy(c) = team==0 ? (Y[c],X[c]) : (X[c],Y[c])
          best = [Rhino0, Mushroom1, Stump2, Bee3].iter()              // 0x03020100 · ※[807] 과 순서 다름(판별자 순)
              .filter(|c| moba.camps[team*4+c].live_list.len(+0x10) != 0   // moba+0x18 + (team*4+c)*0x30 {live ptr@+8, len@+0x10, next@+0x18}
                       && abs_diff(own_xy(c).0, champ.x(+0x660))² + abs_diff(own_xy(c).1, champ.y(+0x668))² < 14400000001 /* ≤ 120000² */)
              .min_by_key(dist²)                                       // first-wins
          if let Some(c) = best { return (c, team) }
      }

// ── ★0.6.0 게이트(fafee1~fb012e): 오브젝트 창 · skip_race · jng ──
      tps = setting.tps(+0x12f8) ; tick = game.tick() /*vt+0x28*/
      in_set = tut < 9 && (0x1a1 >> tut) & 1                            // {0,5,7,8}
      window = sat(setting.epic_first(+0x8a8) − 30·tps)
      (skip_race, allow) =
        if !(in_set && tick >= window) {                                // 창 이전 ‖ tutorial∉집합
            s = game.strategy(team)                                     // vt+0x108 → Strategy 24B(+0x10 wav · +0x11 foc · +0x12 jng)
            (true, allow_invade && s.jng(+0x12) == 2 /*CounterJungle*/)   // ★이 경로에서만 jng 검사
        } else { match game.get_game_mode() {
            None => (false, allow_invade),
            Some(moba) => {
                if tut ∈ {0,7,8} && moba.camps[8 /*Morgard +0x198*/].live_len == 0
                   && sat(moba.camps[8].next(+0x1b0) − tick) <= 30·tps      { goto LEGACY }   // ★에픽 30s 내 스폰 예정 → 블록 B 전체 스킵
                if moba.camps[9 /*Serpen +0x1c8*/].live_len(+0x1d8) == 0
                   && sat(moba.camps[9].next(+0x1e0) − tick) <= 30·tps     { goto LEGACY }   // ★세르펜 동형(tut∈{0,5,7,8} 전부 도달)
                (false, allow_invade)
            } } }
      if version < 3 || !allow { goto LEGACY }                          // faff92~faffa0

// ── ★0.6.0 블록 B(faffa6~fb0433 · fb088f~fb0a0d): 역정글 후보 ──
      let Some(me) = cache.player_champion[team][pos] else { goto LEGACY }
      s = game.strategy(team)                                           // fb0020 재호출
      (first, second): (u8,u8) = match s.foc(+0x11) {
          0 /*Top*/    => (Stump2, Mushroom1),                          // 0x0102
          1 /*Bottom*/ => (Bee3, Rhino0),                               // 0x0003
          _ /*All*/    => if setting.height(+0x12c0).wrapping_sub(me.y) < me.x { (Bee3, Rhino0) } else { (Stump2, Mushroom1) }   // 봇사이드(x+y>height)
      }
      // 후보 = [first, second].filter(can_invade_camp).min_by_key(dist²) · 동점 first-wins (dbd370: acc_d2 > d2 일 때만 교체)
      best: Option<(u64,u8)> = None
      for c in [first, second] {
          if faf2a0 /*can_invade_camp*/(version, player, data, c) {     // 콜리 v3: 신규 · 등가 Rust = RE r22 d §4(아래 요약)
              (cx,cy) = camp_pos_memo(map, c, side = enemy)             // 1416a7af0 = map.camps[c].pos[side]
              d2 = abs_diff(me.x,cx)² + abs_diff(me.y,cy)²
              if best.map_or(true, |(bd,_)| bd > d2) { best = Some((d2,c)) }
          }
      }
      let Some((_, camp)) = best else { goto LEGACY }
      if skip_race { return (camp, enemy) }                              // fb034d
      if cache.player_champion[enemy][Jungle(1)].is_none() { return (camp, enemy) }   // fb0367
      let Some(me) = cache.player_champion[team][pos] else { goto LEGACY }
      let Some(moba) = game.get_game_mode() else { goto LEGACY }
      ms = max(me.move_speed(+0x640), 1) ; tick = game.tick()
      st = get_camp_state(moba.camps, enemy, camp)                     // 1416e4900 = camps+(enemy*4+camp)*0x30 (camp4→+0x180)
      enemy_ready = st.live_len != 0 ? game.tick() : (st.next == 0 ? u64::MAX : st.next)
      (cx,cy) = camp_pos_memo(map, camp, side = enemy)                  // 14157b720(map, camp, team==1) = memo(side = b^1)
      invade_at = max(sat(tick + free_dist_ticks(data, me.x, me.y, cx, cy, ms) /*fafc10*/), enemy_ready)
      // 내 정글 4캠프 최단 확보 틱(fb0911~ camp0 + dbe860 fold 1..3)
      own_at(c) = { st = get_camp_state(moba.camps, team, c); ready = st.live_len != 0 ? tick : st.next;   // ★next==0 → MAX 매핑 없음(raw)
                    (ox,oy) = own_xy(c); max(sat(tick + free_dist_ticks(data, me.x, me.y, ox, oy, ms)), ready) }
      own_earliest = own_at(0); for c in 1..4 { v = own_at(c); if v <= own_earliest { own_earliest = v } }   // dbe860 CMOVNC(<=)
      if invade_at < own_earliest { return (camp, enemy) }              // fb0a04 `cmp rdi,rax; jnc LEGACY`

LEGACY:   // ── fb0433~fb082a = 0.5.8 [814~835] 동치 + C′ ──
[814-815] not_cleared_camps: bumpalo Vec<JungleType> =
 jungle_camps.into_iter()
 .filter(|c| !is_cleared(*c, player.info.team, _version, _rnd, player, data, team_plan, offset=0, _debug))   // e052c0 collect · 술어 faf060 · offset 리터럴 0
 .collect_in(data.context.pool)

[817] if not_cleared_camps.is_empty() {
[818] return jungle_camps.into_iter()
[819] .min_by_key(|c| game.get_game_mode()                              // vt+0x40 · Moba 아니면 unwrap_failed(143821770)
                        .unwrap()
                        .camps /*moba+0x18 · team∉{0,1} → unreachable "team value must be 0 or 1"*/
                        .get_camp_state(team, *c).next_respawn_tick /*+0x18*/)   // dbe6f0 fold
[821] .unwrap();                                                        // → (camp, team)
 // 동점이면 배열 앞쪽(Rhino→Mushroom→Bee→Stump)
 }

[823] champ = cache.player_champion[team][pos]                          // ★0.6.0 PlayerState +0xa00/+0xa90 (cache +0x1e0 불변)
[824] if champ.is_none() {
[825] camp = match now_camp {
 Some(c) => c,
[829] None => *jungle_camps.choose(rnd).unwrap(),                       // ChaCha 워드 (w & 0x20000000)!=0 재추출 · idx = w>>30
 };
 return (camp, team); // not_cleared 필터를 안 탄다
 }

// ── ★0.6.0 C′(fb04ac~): 팀 전략 foc 로 not_cleared 를 좁힌다 ──
[C′]  if version >= 3 {
          s = game.strategy(team)
          pref = match s.foc(+0x11) { 0 => Some({Mushroom1, Stump2}), 1 => Some({Rhino0, Bee3}), _ => None }   // 2 = 무필터
          if let Some(set) = pref {
              filtered = not_cleared_camps.iter().filter(|c| set.contains(c)).collect_in(pool)
              if !filtered.is_empty() { not_cleared_camps = filtered }   // 비면 전체로 폴백
          }
      }

[832] champ: &Entity = champ.unwrap();
[833] return not_cleared_camps.into_iter()
 .min_by_key(|c| {
[834] (cx, cy) = camp_pos_memo(map, *c, side = team != 0);              // ★0.6.0 TLS 메모이즈 1416a7af0 = 구 camp_pos(c, team==0) 와 동일 매핑
[835] dx = abs_diff(cx, champ.x); dy = abs_diff(cy, champ.y);           // +0x660/+0x668
 dx*dx + dy*dy
 })                                                                     // dc01d0 fold
 .unwrap()  → (camp, team)                                             // 비면 unwrap_failed

// ── 콜리 v3 요약: faf2a0 can_invade_camp(version, player, data, camp) -> bool (0x873 · RE r22 d §4 전문) ──
//   Moba 아니면 false · 적 캠프 ready(생존→tick · next==0→false) · me None→false
//   arrive = tick + free_dist(me→적진영 camp_pos) ; arrive+tps < ready → false ; my_arrive = max(arrive, ready) ; my_clear = ed9a90(data, me, camp, enemy)
//   적 정글러 Some(ej): my_done = my_arrive+my_clear ; ej_arrive = tick+free_dist(ej→camp)
//        my_done ≥ ej_arrive → dps_me/dps_ej = de7750 ; dps_me==0 → false ; dps_ej≠0: ej_hp = (me 팀 Neutral ? ej.hp : ej.visible_state[me.team].tag≠0 ? ej.stat_cached.hp(+0x628) : ej.hp(+0x670))
//                              my_ttk = me.hp*1e5/dps_ej ; ej_ttk = ej_hp*1e5/dps_me ; ej_ttk>my_ttk && (ej_ttk−my_ttk)*3 > my_ttk → false
//        경주: ej_done = max(ej_arrive, ready) + ed9a90(ej) ; my_arrive ≥ ej_done → false
//   적 정글러 None: ps = cache.player_state[enemy][1](+0x230+enemy*0x28+8) Some → 분수 중점(map+0x6d70+enemy*0x20 사각 중점) 에서 base move_speed(Rc +0x580 vt+0x30 → +0x28)
//        start = ps.+0xa98==0 ? tick+ps.+0xaa0 : tick ; ej_arrive = max(start+free_dist, ready) ; margin = [318,216,222,366][camp](3a92f10) ; ej_arrive+margin ≤ my_arrive → false
//   적 챔프 pos∈{0,2,3,4} 중 캠프 150000 이내(≤22_500_000_000) → false
//   version>2: 셀 = min(v/32000, 29) ; me 셀 ≠ camp 셀 → ee2310 격자 경로 is_some() (본체 미독)
//   그 외 true
// 부수효과 없음(0.5.8 과 동일). faf2a0 내부 카운터(LOCK inc 14502a…)는 텔레메트리.
```

## changes
- 시그니처: 7번째 `allow_invade: bool` 추가 · **반환 = (camp, owner_team)** (블록 B → 1−team, 그 외 team).
- 신규 블록 A(v3): 내 진영 살아있는 캠프 120000² 내 최근접 즉시 반환. 좌표 하드코딩 표(팀1 방향 · 팀0 스왑).
- 신규 게이트: `skip_race` = 창(+0x8a8−30tps) 이전 ‖ tut∉{0,5,7,8} → 이때만 `allow &= strategy.jng==2`; 창 이후 Moba 에서 에픽/세르펜 30s 내 스폰 예정이면 블록 B 전체 스킵.
- 신규 블록 B(v3 && allow): strategy.foc → 후보 쌍 → faf2a0 can_invade_camp → 최근접 → skip_race ‖ 적 정글러 없음 → 반환; 아니면 `invade_at < own_earliest`(내 4캠프 최단 확보) 일 때만 반환.
- 신규 C′(v3): strategy.foc 로 not_cleared 필터(비면 폴백).
- camp_pos → TLS 메모이즈 1416a7af0(동일 매핑) · PlayerState +0xa00/+0xa90.
- [807~835] 본체 = 동치.
- 콜리 v3: `faf2a0`(신규 can_invade_camp) · `dbd370`/`dbe860`(fold) · `fafc10` free_dist · `ed9a90` camp_clear_ticks · `de7750` dps · `ee2310` 격자 경로(미독) · `1416e4900` get_camp_state · `14157b720`/`1416a7af0` camp_pos memo · `e052c0`/`faf060`/`dbe6f0`/`dc01d0`(레거시).

## verified
- Ghidra 0.6.0 fafe50 디컴: 인자 순서 · 블록 A 골격(0x03020100 · dbd4e0→db7720) · C′({2,1}/{3,0} · 비면 폴백 · v<3 무필터) · 레거시 본체(0x02030100 · e052c0 · dbe6f0 unwrap 패닉 · choose · dc01d0) · 블록 B 후보 쌍(0x0102/0x0003/height+0x12c0) · faf2a0 1차→2차 · dbd370 · 적 정글러 [enemy][1] · 1416e4900/14157b720/fafc10 · dbe860 비교.
- capstone faff00~fb012e: **skip_race 방향 확정**(r8b=1 은 창 이전 ‖ tut∉집합 경로 · 여기서만 `and r13b`(allow &= jng==2) · 임박 시 `jbe/jmp fb0433` 로 블록 B 스킵) — w11 §B1 「obj_soon 즉시 반환」 은 반대(r22 d 정정과 일치).
- 바이트: 3a77068=[752000,592000,448000,800000] · 3a77098=[496000,176000,256000,351000] · 120000² = `movabs 0x35a4e9001` @dbd6ac.
- dbd370/dbe860 디컴 골격(fold) 확인.
- 미확인(스팟체크 범위 밖 · r22 d 원문 의존): faf2a0 본체 세부 · 좌표표 팀 방향(r22 d 의 dbd650/fb09ad 근거 채택) · ee2310 본체 · 3a92f10 값 의미 · +0xa98/+0xaa0 필드명.

## confidence
B — 게이트·A·C′·레거시·B 골격은 디컴/디스어셈 확인, B 세부(faf2a0 산식·좌표 방향)는 r22 d 원문 전재(스팟체크 미수행).
