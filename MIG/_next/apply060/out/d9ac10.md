# d9ac10→e85f00 steal::should_steal_now

## logic_060
```
fn should_steal_now(version, player, goal_data, team_plan, data) -> StealAction
  // ★0.6.0 TeamPlan 이중모드 술어(objective != Morgard / != Serpen 4곳 치환)
  let not_morgard = tp.serpen_scene_none /*+0x3e4*/ != 2 || (tp.morgard_scene_none /*+0x404*/ == 2 && tp.objective_tag /*+0xcd5*/ != 0);   // ★0.6.0
  let not_serpen  = tp.serpen_scene_none == 2 && !(tp.morgard_scene_none == 2 && tp.objective_tag == 1);                                 // ★0.6.0
  // 의미: 캠프가 신모드(씬 Some)이면 그 캠프의 레거시 스틸 후보는 항상 제외(세르펜) / 항상 허용(에픽) — 비대칭이지만 코드 그대로
  // L286~287
  if player.info.position(+0xa90) != Jungle(1) { return None }
  let Some(my_champ) = cache.player_champion[team(+0xa00)][1] else { return None }     // cache+0x1e0+team*0x28+8
  // L291~299
  in_session_target = team_plan.current_steal_session.map(|s| s.target)     // +0x472 == 2 → None / target = +0x471 (Epic=0 / Serpen=1)   // ★0.6.0 +0x1b9/+0x1ba→+0x471/+0x472
  if my_champ.hp*2 < my_champ.stat_cached.hp { return None }                // L294 hp<50%
  if in_session_target.is_none() && game.tick().saturating_sub(team_plan.last_battle_tick /*+0x510*/) < tps*5 { return None }   // L299   // ★0.6.0 +0x220→+0x510
  // L304~310: 캠프 존에 있을 수 있는 적 수 (5슬롯 count)  — enemy_could_be_at_camp(p) = is_visible(vt+0xf8, team, e.id) ? dist²(e, camp) < 100000²+1
  //           : (last_visible_pos[p] (+0x520+p*0x10) == (0,0) ? false : distance(last_pos, camp) <= sat(tick − last_checked[p] (+0x598+p*8)) * e.move_speed(+0x640) + 100000)   // ★0.6.0 vision +0x230/+0x2a8 → +0x520/+0x598
  epic_zone_short  = !epic_exists(ctx)   || (0..5).filter(|p| enemy_could_be_at_camp(team, cache, team_plan, p, 288000, 288000)).count() < 3   // L329 조건으로 소비
  serpen_zone_short= !serpen_exists(ctx) || (0..5).filter(|p| enemy_could_be_at_camp(team, cache, team_plan, p, 672000, 672000)).count() < 3   // L358
  //   epic_exists = (tutorial−1) >u 5 (태그 ∉ 1..=6) · serpen_exists = tutorial<9 && (0x1a1>>tutorial)&1
  // L315~316
  epic_alive   = epic_exists && mob.epic.live_list.len(+0x1a8) != 0        // moba = get_game_mode()(vt+0x40) tag 0
  serpen_alive = serpen_exists && mob.serpen.live_list.len(+0x1d8) != 0
  // L319~345: 에픽 후보
  epic_action: (StealAction, usize) =
    if in_session_target == Some(Epic) {                                         // L320~321
      if epic_alive && not_morgard { evaluate(Epic, in_session=true) } else { (None, MAX) }   // ★0.6.0 `objective != Morgard(0x41f==0)` → not_morgard
    } else {
      epic_eligible = mob.epic.live_list.get(0) → get_entity_by_id(vt+0x1f0) → is_some_and(|e| e.hp*100 < e.stat_cached.hp*95)   // L323~325
      if epic_alive && not_morgard                                                 // L326~327   // ★0.6.0
         && !obj_spawn.epic_giveup_tick /*+0x70 tag==1, +0x78*/ .is_some_and(|t| t + tps*5 >= tick) && !epic_zone_short   // L328~329   // ★0.6.0 +0x50/+0x58→+0x70/+0x78
         && epic_eligible                                                          // L333
      { evaluate(Epic, in_session=false) } else { (None, MAX) }                    // L334~345
    }
    where evaluate(Epic, s) = evaluate_steal_for_target(player.+0x1e0, team, cache, ctx, my_champ, target=false, epic_entity, goal_data.epic.last_epic_hp(+0x80), epic_enemy_tick(+0x88), last_epic_seen(+0x78), (288000,288000), s)   // 콜리 v3: d9bce0→e87300(skel 동일)
  // L348~370: 세르펜 후보 (대칭)
  serpen_action =
    if in_session_target == Some(Serpen) { if serpen_alive && not_serpen { evaluate(Serpen, true) } else { skip } }   // L350   // ★0.6.0 `objective != Serpen(0x41f==1)` → not_serpen
    else if serpen_alive && not_serpen && !serpen_giveup_tick /*+0x80 tag, +0x88*/ .is_some_and(|t| t+tps*5 >= tick) && !serpen_zone_short && serpen_eligible(hp<95%) { evaluate(Serpen, false) } else { skip }   // L352~362   // ★0.6.0 +0x60/+0x68→+0x80/+0x88
    where evaluate(Serpen, s) = evaluate_steal_for_target(..., target=true, serpen_entity, goal_data.serpen.last_epic_hp(+0xb0), .epic_enemy_tick(+0xb8), .last_epic_seen(+0xc0), (672000,672000), s)
  // L376~390: 합성
  if epic_action.0 == Commit { return epic_action.0 }                     // 에픽 Commit 최우선
  if serpen skipped { return if epic.0==Lurk { Lurk(Epic) } else { None } }
  if serpen_action.0 == Commit { return Commit(Serpen) }
  match (epic.0, serpen.0) {
    (Lurk, Lurk) => if in_session_target.is_none() { Lurk(if epic.1 > serpen.1 { Serpen } else { Epic }) }   // L383: 적 예상 처치 잔여틱 작은 쪽
                    else { Lurk(in_session_target) }                                                          // L380
    (_, Lurk) => Lurk(Serpen),  (Lurk, _) => Lurk(Epic),  _ => None }
```

## changes
- L321·L327 `objective != Morgard` → `not_morgard = 3e4!=2 || (404==2 && cd5!=0)` / L350·L352 `objective != Serpen` → `not_serpen = 3e4==2 && !(404==2 && cd5==1)`.
- TeamPlan 오프셋: steal session `+0x1b9/+0x1ba → +0x471/+0x472` · last_battle_tick `+0x220→+0x510` · vision last_visible_pos `+0x230+p*0x10 → +0x520+p*0x10` · last_checked `+0x2a8+p*8 → +0x598+p*8` · epic giveup `+0x50/+0x58 → +0x70/+0x78` · serpen giveup `+0x60/+0x68 → +0x80/+0x88`.
- PlayerState `+0x930/+0x9c0 → +0xa00/+0xa90`. distance `12a07d0→1660a00`. evaluate `d9bce0→e87300`.
- 그 외(hp<50% · 5초 전투 쿨 · 3명 존 카운트 288000/672000 · +100000 여유 · hp<95% · giveup+5s · 합성 규칙) 동일. version 게이트 없음.

## verified
- Ghidra 0.6.0 `FUN_140e85f00` 전문 디컴 + 0.5.8 `d9ac10` 대조: 4 술어 사이트(에픽 비세션 `(3e4!=2 || (404==2&&cd5!=0)) && +0x70==1` · 에픽 세션 `!alive || (3e4==2 && (404!=2 || cd5==0)) → None` · 세르펜 세션 `!alive || 3e4!=2 || (404==2&&cd5==1) → skip` · 세르펜 비세션 `3e4==2 && (404!=2 || cd5!=1) && +0x80==1`) · 5슬롯 존 카운트(0x46500/0xa4100 · 0x2540be401 · +100000) · `hp*100 < max*0x5f` · e87300 인자(goal +0x80/+0x88/+0x78 · +0xb8/+0xc0/+0xb0) 확인. 0.5.8 도 `player+0x1e0` 를 evaluate 1번 인자로 넘김(동일).
- 미확인: e87300 내부(RE 「skel 동일」).

## confidence
A — 양 버전 전문 디컴 대조, 차이 = 4 술어뿐.
