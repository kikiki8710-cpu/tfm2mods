# e8fd70→f3d660 AgentVerHamster::item_v26

## logic_060
```
fn item_v26(&mut self, rnd, player, context, build_slot, inventory_index) -> Option<usize>   // lib.rs:1149~1170 (self 미사용) · 0.6.0 f3d660 exe ABI = (rnd, player, item_list.ptr, item_list.len, build_slot, inv.tag, inv.val) → bool(AL)+out
  item_list = &context.item_list                                   // IR 는 승격된 &Vec<Box<dyn ItemInfo>>(ptr,len · 원소 0x10 = (data, vtable)) — GameContext 필드 오프셋은 호출자(buy_item/upgrade_item) 소관
  // L1151 — 목표 아이템: 활성 빌드 항목 중 build_slot 번째
  target: usize = *player.info.item_builds /*Vec ptr +0x558 · len +0x560 ★0.6.0(구 +0x4e8/+0x4f0)*/.iter()
        .filter(|&&i| i < item_list.len() && item_list[i].is_active())   // i>=len 이면 패닉 없이 술어 거짓(get 의미) · is_active = vtable+0x50
        .nth(build_slot)?                                            // advance_by(build_slot: 술어 참일 때만 accum-=1) → next(). 소진 → None
  target_item = &item_list[target]
  // L1153
  next_item: usize = if let Some(inv) = inventory_index {
      // L1154
      current = player.info.items /*Vec ptr +0x510 · len +0x518 ★0.6.0(구 +0x4a0/+0x4a8) · 원소 0x10 Box<dyn>*/.get(inv)?   // inv >= items.len → None(패닉 아님)
      // L1155
      random_item_next_toward_target(item_list, current.key(), target_item.key(), rnd)?   // key = vtable+0x58 → &String → &str · key→index 조회 = TLS 캐시 맵 16a8100 ★0.6.0 RVA(구 105fae0 · 동치) 2회(둘 다 Some 이어야 함) → e56660 ★0.6.0 RVA(구 game_core item.rs:353) → Option
  } else {
      // L1157
      path: Vec<usize> = random_item_build_path(item_list, target, rnd)?   // 콜리 e56130 ★0.6.0 RVA(game_core item.rs:320) · None 니치 cap==-1
      // L1158
      first = *path.first()?                                         // len==0 → drop(path), None
      // L1159
      drop(path); first
  }
  // L1161 ★0.6.0: active_inventory_item_indices(...).len() 대신 카운트 인라인 · 임계 3→4
  active_cnt = player.info.items.iter().filter(|inv| item_list.iter().any(|li| li.is_active() /*vt+0x50*/ && li.key().len()==inv.key().len() && memcmp(li.key(), inv.key())==0)).count()   // items.len==0 이면 0 (None 경로에선 카운트 루프 자체를 건너뜀)
  if active_cnt >= 4 /*★0.6.0 `cmp rax,3; jbe` — 구 len>=3*/ && item_list[next_item].tier() == 0 { return None }   // tier = vtable+0x70 · 색인은 panic_bounds_check(item_list.len)
  // L1165
  if player.info.gold /*+0xa68 ★0.6.0(구 +0x998)*/ >= item_list[next_item].price() { Some(next_item) } else { None }   // price = vtable+0x68 · uge

★rnd 소비: 본문 직접 gen_range 0회. 호출당 정확히 하나 — inventory_index Some → random_item_next_toward_target 1회 / None → random_item_build_path 1회 (내부 gen_range 수는 game_core 경계 · 미명세).
★None 이 되는 경로: 빌드 소진 · inv bounds · key→index 실패(2곳) · next_toward None · build_path None · path 빈 · 4개+ && tier0 · gold 부족.
```

## changes
- L1161: 활성 인벤 아이템 임계 `len() > 2`(3개 이상) → `count > 3`(4개 이상) — 슬롯 3→4 대응. 헬퍼(lib.rs:542 active_inventory_item_indices)가 Vec 수집 없이 카운트 루프로 인라인(key 길이+memcmp 동일).
- item_builds +0x4e8/+0x4f0 → +0x558/+0x560 · items +0x4a0/+0x4a8 → +0x510/+0x518 · gold +0x998 → +0xa68.
- 콜리: random_item_build_path e56130 · random_item_next_toward_target e56660 · key→index TLS 캐시 16a8100.
- v2 사장 게이트 없음.

## verified
- Ghidra 0.6.0 f3d660 디컴 전체 확인: +0x558/+0x560 nth 루프(vt+0x50) · inv Some: +0x518 bounds → vt+0x58 key×2 → 16a8100×2(==1 검사) → e56660(==1) / None: e56130 → cap==-1 → len==0 → 첫 원소 · 카운트 루프(vt+0x50 && len== && memcmp) · `3 < count` → vt+0x70==0 → false · vt+0x68 price <= +0xa68.
- 미확인: 16a8100 이 0.5.8 105fae0 과 동치라는 판단은 RE 인용(본 세션 미대조).

## confidence
A — 변경 한 줄(임계)과 오프셋 전부 디컴 확인.