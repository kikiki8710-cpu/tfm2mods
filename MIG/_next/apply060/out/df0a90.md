# df0a90→dcb890 SerpenHuntAndPokePlan::is_end

## logic_060
```
fn is_end(&self, version, _rnd, player, data, team_plan, _debug) -> bool   // serpen/hunt_and_poke.rs:162 · dcb890
// ★0.6.0 L163 재작성: 구 `objective.tag != Serpen → true` → 씬 모드 게이트
//   tp+0x3e4 = v4_serpen 씬 take_born(2 = None = 레거시 objective 사용) · tp+0x404 = v5_morgard 동형 · tp+0xcd5 = objective(1=Serpen) · tp+0xcd6 = phase
if team_plan.v4_serpen.take_born(+0x3e4) == 2 {                                   // ★0.6.0 레거시 objective 모드
    if team_plan.v5_morgard.take_born(+0x404) != 2 { return true; }                // ★0.6.0 (morgard 씬이 살아있으면 종료)
    if team_plan.objective.tag(+0xcd5) != MainObjective::Serpen(1) { return true; }   // 구 L163 상당   // ★0.6.0 +0x41f→+0xcd5
}
// L168
let camp_pos = ctx.map.camp_pos(JungleType::Serpen(5), is_red = player.team(+0xa00) != 0);   // 콜리 v3: 16a7af0(구 ffa3e0) · 인자 bool 은 `team != 0`   // ★0.6.0 +0x930→+0xa00
// L169  take_setup_like 인라인 — ★0.6.0 재작성
let setup_like = if team_plan.v4_serpen.take_born(+0x3e4) == 2 {
    team_plan.v5_morgard.take_born(+0x404) == 2 && team_plan.objective.tag(+0xcd5) == 1 && team_plan.objective.phase(+0xcd6) == ObjectPhase::Setup(1)   // 구 `objective==Serpen && phase==Setup`   // ★0.6.0 +0x420→+0xcd6
} else {
    team_plan.v4_serpen.phase(+0x3e0) == 1                                          // ★0.6.0 신규: 씬 모드일 때 씬 phase==1
};
if setup_like {
    // L170
    if team_plan.v24_objective_setup_should_release_to_passive(version, player, data, JungleType::Serpen(5)) { return true; }   // 콜리 v3: f0e390(구 dd7250)
    // L174  vtable+0x100 is_visible_cell(game, team, cx, cy)
    if game.is_visible_cell(player.team, camp_pos.x / 32000, camp_pos.y / 32000) {
        // L175~176  enemy_team = 1 - player.team (bounds<2)
        let nearest_to_camp_enemy = cache.iter_champions(enemy_team)                // player_champion[+0x1e0][enemy_team][0..5] Some 만
            .filter(|e| blackboard[enemy_team](stride 0x5c8).is_recent_visible(game, ctx, player, e))   // closure#0 — 인라인: game.is_visible(vt+0xf8)(team, e.id) || (p = game.get_player(vt+0x150)(e.id); p.is_some() && bb.last_seen(+0x3e8)[p.position(+0xa90)] + 0x78 >= game.tick(vt+0x28))   // 콜리 v3: 원소 2~4 는 헬퍼 17d3bc0(구 1323a00)   // ★0.6.0 bb 0x2e8→0x5c8 · last_seen +0x1e0→+0x3e8
            .min_by_key(|e| dist_sq(e, camp_pos));                                          // closure s_0 · fold 헬퍼 dbb440 (동점=앞)
        // L178
        let Some(e) = nearest_to_camp_enemy else { return true; };
        // L179
        if dist_sq(e, camp_pos) > 22500000000 { return true; }                            // 150000²
    }
}
// L189  vtable+0x40 get_game_mode → (tag,&moba) · tag != 0(Moba) 이면 unwrap 패닉
let m = game.get_game_mode().as_moba().unwrap();
let serpen = m.jungle_runner.serpen.live_list(ptr +0x1d0 / len +0x1d8).first().and_then(|id| game.get_entity_by_id(*id));   // vtable+0x1f0
// L190
if serpen.is_some() { return false; }
// L191
let m = game.get_game_mode().as_moba().unwrap();
return m.jungle_runner.serpen.next_respawn_tick(+0x1e0).saturating_sub(game.tick()) > tps(+0x12f8) * 15;   // vtable+0x28 tick
```
// ★v2 사장: 없음(version 은 f0e390 에 전달만).

## changes
- L163: `objective.tag(+0x41f) != Serpen(1) → true` → `if tp.3e4==2 { if tp.404!=2 → true; if tp.cd5!=1 → true }` (씬 모드(3e4!=2)면 objective 검사 없이 통과).
- L169 setup_like: `objective==Serpen && phase(+0x420)==Setup(1)` → `(tp.3e4==2) ? (tp.404==2 && tp.cd5==1 && tp.cd6==1) : (tp.3e0==1)`.
- 이하(L170~191) 동치. 오프셋: TeamPlan objective +0x41f/+0x420→+0xcd5/+0xcd6 · 신규 +0x3e0/+0x3e4/+0x404 · PlayerState team +0xa00 · position +0xa90 · bb stride 0x5c8 · last_seen +0x3e8.
- 콜리 RVA: camp_pos ffa3e0→16a7af0 · v24_release dd7250→f0e390 · is_recent_visible 1323a00→17d3bc0 · min fold→dbb440.
- 필드 의미(+0x3e4 take_born 니치 2=None · +0x3e0 phase)는 spec_patch §A TeamPlan 행(r21 w7 Debug fmt) 기준 — 배치 A RE 는 「추정」으로 표기했었음.

## verified
- Ghidra 0.6.0 `dcb890` 전문 디컴: 진입 `+0x3e4==2 → (+0x404!=2 → true) · (+0xcd5!=1 → true)` · camp_pos 16a7af0(5, team!=0) · setup_like 분기(`+0x3e4==2 ? (+0x404==2 && +0xcd5==1 → cVar=+0xcd6) : cVar=+0x3e0` → `!=1 → 꼬리`) · f0e390(…,5) · vt+0x100 · 1-team bounds · is_recent_visible 인라인(vt+0xf8 / vt+0x150 → bb+0x3e8+pos*8 + 0x78 >= vt+0x28) · 17d3bc0 · dbb440 · 0x53d1ac101 · vt+0x40 · +0x1d8/+0x1d0 · vt+0x1f0 · +0x1e0 · tps*0xf.
- Ghidra 0.5.8 `df0a90`(ghidra_beta) 디컴 대조: 진입 `+0x41f!=1 → true` · setup `+0x41f==1 && +0x420==1` · 나머지 동일 구조.
- 미확인: 17d3bc0(is_recent_visible 헬퍼)·dbb440 내부 미디컴(인라인판과 동형 가정) · +0x3e0/+0x3e4/+0x404 의 런타임 값(실측 부기: ObjContest 씬 실발생 → 씬 모드 경로가 실제로 돈다).

## confidence
A — 변경 2건 양버전 디컴 대조 · 꼬리 동치 확인.
