[package]
name = "sylas_hijack"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

# mod-api 는 SDK 빌드 스크립트(build_mod.bat, sdk_0414_new)가 자동 주입 — 여기 적지 말 것.
[dependencies]

[workspace]
